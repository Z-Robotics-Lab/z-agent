# LESSONS — do-not-repeat distillate (ORIENT step 3; one line each, pointer-terminated)

Append one line per lesson (round agents); review rounds consolidate. Drop a line only if its D#/E#/commit pointer resolves in the ledger or git. Details live at the pointer.

## Refuted (do NOT retry unless the stated condition changed)
- fetch.nl-ordinal-position-invariance (最左边 under SCENE_SWAP) R210 GROUNDED 2/2 did NOT reproduce R284 (0/2). R299/E90 ROOT-CAUSED (skill-direct, swap + no-swap control): NOT grasp-exec rot — the R284 "grasp EXECUTION/stable-hold rotted" read was WRONG. Resolver+grasp are CORRECT: they grasp the TRUE leftmost bottle = YELLOW (y=3.11, image-leftmost since larger-y⇒smaller-cx), which was ADDED R211 AFTER this R209/R210 bar fixed leftmost=blue; yellow is UNMOVED by the swap so the swap no longer even probes invariance (answer=yellow both ways). STALE BAR, not code. R284's blue-verify was the BRAIN's guess; run1's empty gripper = brain thrash. Bar OBSOLETE (already superseded). Guard: test_ordinal_resolve.py::test_wiring_leftmost_is_yellow_not_blue_after_R211_scene_growth → E90/Case 17. A "confirmed" bar can rot silently → skeptic re-verify cadence works → E79.
- "Model-sensitivity ceiling" on fetch was a HARNESS artifact, not the model — control the harness before concluding model capability → D171 refuted by D174.
- "Red grasp-robustness ceiling" was a one-campaign transient (8/8 after); 0/3 in a single campaign is never a ceiling — re-run with the correct NL term + skill-direct probe → D172 refuted by D173/DEBUG.md.
- "BYO-model over-caution blocks tool-calls" — refuted by a positive control; gemini/mistral no-tool-call is model behaviour, not plumbing (tools passed, llama tool-called same path) → D179 refuted by D180, fb6ae77.
- Eyes verdict-render framing (place-aware camera in `acceptance/capture.py`) is NOT a non-gated build: all of `acceptance/` is honest-verify SPINE (loop/check.sh regex) → any change to what the verdict frame SHOWS is a CEO gate. R281 crossed it unapproved+unverified → R282 quarantine; reverted to baseline. Retry only after owner GATE-APPROVED + real-face verify → E78.
- OpenRouter `/key` `limit_remaining` is rate-limit headroom, NOT deposited credit — probe
  balance with one real turn before concluding "funded" → D181.
- Any rate measured through `-p`/`--sim-go2` flag paths is NOT a bare-REPL face number; all
  pre-D163 acceptance rates were downgraded wholesale for this → D163/D164.
- `placed_count() >= N` is the WRONG go2 quantity-place predicate: it's FLOOR-only (z<_LIFT_MIN_Z
  0.10) so it's STRUCTURALLY 0 for a bin/table place (pickables z=0.320, place_bin top ~0.31) —
  always-FALSE = a structural false-RED. Use the D106 count oracle `resting_on_receptacle() >= N`
  (ungated, same residual as single place) → E35 (extends #Casebook Case 7).
- quantity-place (`把两个瓶子放到架子上`) does NOT ground (deepseek-v4-flash): brain places bottle 1 then
  ABANDONS bottle 2 after its grasp stalls → at_position loop, RAN False 3/4. HONEST RED (moat refused): gap =
  BRAIN DECOMPOSITION of N grasp+place, machinery correct. Retry only after isolating grasp + a guardrail → E36.
- courtyard PLACE composite False is NOT a world-transfer bug: `navigate(10.8,3.0)` is INSIDE the inflated
  pick_table (R=0.28, table x∈[10.80,11.10]) → `plan_path`=None, UNREACHABLE in EVERY world incl HOUSE
  (furniture identical). R246's at_position was a brain-IMPROVISED recovery nav to the bottle pick-loc after
  a `mobile_place` first-nav flake; the place had already succeeded. Do NOT re-diagnose (10.8,3.0) as a
  house-vs-courtyard diff. R247 re-verify grasp itself flaked → PLACE-leg is model-flaky (grasp+nav) → E56/R247.

## Hazards & confirmed constraints
- A capability GATE keyed on a HARDCODED skill-NAME list fails OPEN for plug-and-play skills (North-Star BYO-skill, no kernel edit): the D175 manip gate (`native_loop._build_motor_tools`) offered a BYO manip skill an ARMLESS body cannot execute, because its NAME wasn't in `_ARM_REQUIRING_SKILLS` (the body correctly resolves has_arm:false — DISTINCT from E172's declared-arm-flag-authority bug; same gate, different mechanism). FIX (strictly-stricter UNION, Inv-1 sandbox-only-gets-stricter): withhold iff name-listed OR the skill's OWN structured metadata declares arm/gripper (`SkillWrapperTool._requires_arm`, scans preconditions+effects, NOT prose — a curated list is still retained for the domain-knowledge cases scan/describe/wave whose metadata is arm-silent). Zero shipped-behavior delta (the only armless-offered shipped skill, `detect`, is arm-silent). RECIPE: any capability gate over a plug-and-play SET must introspect the item's own declared needs, never a curated name list alone → E173.
- The SAME hardcoded-name-list-fails-open pattern recurs on a DIFFERENT gate: the E60 post-place re-grasp guard (`native_loop.dispatch_skill`) classified place/grasp by `_PLACE_SKILLS`/`_GRASP_SKILLS` NAME-lists, so a BYO place skill (novel name) never ARMED the guard and a BYO grasp was never REFUSED — the `掉了` re-grasp silently undoes a plug-and-play placement. FIX (strictly-stricter UNION, self-crossed): classify by the skill's OWN structured metadata (`SkillWrapperTool._releases_object` = precond `gripper_holding_any`+gripper-emptying effect; `_is_grasp` = precond `gripper_empty`) UNIONed with the name-lists. Grasp metadata == `_GRASP_SKILLS` EXACTLY over all 28 shipped classes (zero delta); place adds ONLY shipped `handover` (empties gripper, same risk, was un-guarded) — a real latent shipped gap, not just a BYO one. RECIPE (grep for MORE): every kernel name-list that classifies a plug-and-play SET (`_GRASP_SKILLS`, `_PLACE_SKILLS`, `_ARM_REQUIRING_SKILLS` done; look for the next) should introspect declared metadata → E174.
- The pattern's THIRD gate — the auto-`home` drop guard on the GRASP side: `Agent.execute_skill` appends a `home` step (HomeSkill UNCONDITIONALLY opens the gripper — `skills/home.py`) and suppressed it only by a HARDCODED name/param list (`pick mode='hold'`, `gripper_close`/`gripper_open`/`home`). A BYO grasp-and-hold skill (novel name, effect `gripper_state:holding`) got auto-homed → the just-grasped object silently DROPPED (same `掉了` hazard as the E60 post-place guard, grasp side). FIX (strictly safe, non-spine `core/agent.py`, self-crossed): union the name/param list with the skill's OWN structured end-holding effect (`_skill_ends_holding`: effect `gripper_state=='holding'` OR non-None `held_object`). Delta over ALL shipped classes is EXACTLY {`pick_top_down`,`mobile_pick`,`perception_grasp`} — grasp-hold skills whose declared intent is to end holding; matches the `pick(mode='hold')` contract (correct completeness, honest NOT zero-delta, latent). `pick` (effect open, the NL fetch executor) is UNAFFECTED so confirmed fetch/place rows are untouched. RECIPE unchanged: still grep for the next classify-a-plug-and-play-SET name-list → E175.
- A DIFFERENT failure mode on the SAME BYO-surface family (Inv-4 world registers tools/verify/vocab/PERSONA — persona axis was UNSCANNED; E60 = persona CONTENT only): `_select_persona` (`vcli/prompt.py`) swallowed ANY `persona_blocks()` failure (raises / falsy / wrong-arity) with a bare `except: pass`, silently degrading a BYO world's persona to the kernel default with ZERO signal — a SILENT-SWALLOW-on-control-path (global coding-style floor), NOT a fail-open: persona is prompt text only (verdict.py/cognitive/oracle have ZERO persona refs, so a bad persona cannot loosen verify — Inv-1 independent), so the sandbox was never at risk; the gap is that a `bring-a-persona` author's broken artifact VANISHES undiagnosed. FIX (strictly safe, non-spine, self-crossed, fallback byte-identical): log a WARNING per swallow (raises → `exc_info`; falsy → note). RECIPE distinct from E173-E175: a fail-SAFE default-fallback on a plug-and-play registration surface must still be OBSERVABLE (log the drop) — a silent degrade of a BYO artifact reads as "ignored" not "rejected". regr=tests/unit/vcli/test_prompt.py::TestByoPersonaFallback → E183. SAME silent-swallow recurs on a 5th Inv-4 BYO surface, the ROUTE pillar's bring-a-skill registry: `IntentRouter.should_use_vgg` (`vcli/intent_router.py`) consulted `skill_registry.match()` under bare `except Exception: pass`, dropping a BYO registry whose `match()` raises SILENTLY and degrading routing to keyword heuristics with ZERO signal — NOT a fail-open (routing≠verify oracle; SPINE has zero intent_router refs, Inv-1 independent; fall-through byte-identical), same fail-SAFE-but-silent shape as E183; FIX = WARNING per swallow, healthy None-return no-warn, non-spine/additive; distinct from E163 (that was the `Any` F821 hint, not the swallow); regr=tests/unit/vcli/test_intent_router_skill_registry_swallow.py → E184.
- perception_grasp far-recovery DEAD-BAND blocked HOUSE→warehouse fetch transfer (R234 root cause): the recovery (localize→0.95m standoff→face→re-perceive) fires only after scan→no_detections but was band-gated d∈(1.6,8.0]m — so a dog that nav-stopped CLOSE (0.88m) yet mis-oriented/occluded (front_object_mask=0px, d_min~0.55m facing an obstacle in the compact enclosure) got SKIPPED as 'the self-approach's job', which can't re-approach a target it can't see (world/config + detector were both fine: far-seed localized the bottle at correct coords). FIX: floor 1.6→0.30m (`_RECOVERY_MIN_M`), recover-by-repose from any plausible distance, keep only a d<0.3m degenerate-self-detection guard. ALSO: the sibling Isaac sim (`/workspace/go2w`, different engine) does NOT trip the repo's `pgrep -f "mujoco|vcli"` gate and with RAM/GPU headroom is NOT an Inv-5 blocker — R231-R233 over-read it and burned 3 rounds; run the repo's ACTUAL gate, not "any sim anywhere" → E54 (f437f4e; e2e re-verify pending R235).
- front_object SALIENCY FLOOD blocks HOUSE→warehouse colour fetch (R236 root cause, deeper than R234's dead-band): after the far-recovery fix ENGAGES and the dog reaches the CORRECT 0.9m standoff facing the real bottle (attempt-1 localized (10.86,3.00) right), `front_object_mask` still returns 0px. New mask-gate breakdown (`front_object.mask_gate_breakdown`, logged at the mask_px=0 site) shows WHY: n_salient~99k = ~32% of the warehouse frame passes `sat>=140` (orange racking 0.88/0.45/0.10, yellow safety stripes, steel all vivid), so the green bottle's real green pixels (n_color_hue up to 2917) FUSE into giant background blobs whose MEDIAN hue≠green → the colour-blob resolver picks none → None. The HOUSE world works only because its background is MUTED so the bottle IS the salient blob. The resolver's core assumption (vivid object on a muted scene) is FALSE in an industrial world. FIX direction (NOT yet done): for a colour query gate the colour HUE at PIXEL level BEFORE `_open`/connected-components (isolate green first, THEN find the nearest green blob) — must re-verify it does not regress the confirmed HOUSE red/green/blue/yellow/purple fetches → E54.
- `scripts/sim-teardown` (`rosm nuke`) reaps ONLY ROS2 procs — an in-process `VECTOR_NO_ROS2=1` MuJoCo sim survives it and a not-reaped harness double-drove TWO g1 sims (R217): kill in-process sims by EXACT launched PIDs (never `pkill mujoco`), pgrep clean before relaunch. Also g1 nav verify coords must be probe-checked REACHABLE (`(11,3)`/`(11,4)` are inside obstacles → `plan_path`→inf → `navigate_to` unreachable/moved_m=0 → honest RAN, moat refuses a fake arrival) → E49.
- The round env may select the qwen/dashscope provider, billing-blockable (`Arrearage`) while the deepseek route works — a harness inheriting it aborts at PREFLIGHT; force the deepseek provider (.env.example). The dashscope VLM-judge shares that blocked key → BILLING gate → E49.
- The PERCEPTION VLM (`look`/`describe_scene`, `perception/vlm_go2.py`) defaults to OpenRouter and is 402 CREDIT-EXHAUSTED RIGHT NOW (E28 block, still ACTIVE R230): a go2 fetch that grasp-fails once and lets the brain ESCALATE to `look`/`explore` then 402s SILENTLY mid-turn — the REPL keeps spinning, no verdict, the process looks hung not failed. FIX: every go2 acceptance run MUST export `VECTOR_VLM_URL=http://localhost:11434/v1 VECTOR_VLM_MODEL=gemma4:e4b` (LESSONS recipe). Proven R230: the SAME green-fetch FAILED (402 on `look`) without the local route and GROUNDED 1/1 WITH it (same brain/scene/utterance). Un-routed runs are billing-CONFOUNDED, not model/world failures → E53.
- go2 `describe`/`看到什么` NL is ALIAS-COLLIDED across TWO skills: the generic `DescribeSkill` (`skills/describe.py`, calls `context.perception`=Go2GraspPerception) and the go2 `LookSkill` (`skills/go2/look.py`, calls `context.services['vlm']`=Go2VLMPerception.describe_scene) — which one fires is model/registration-dependent. Go2GraspPerception lacked caption/visual_query → when the generic branch won it raised `AttributeError('visual_query')` and the describe-recovery dead-ended (R247). FIX (R248): Go2GraspPerception.caption/visual_query lazily route to a Go2VLMPerception.describe_scene (the SAME seam LookSkill uses) → no dead-end; 3 unit tests + bare-face courtyard describe (no crash, but the brain took the LookSkill branch so the fixed path was NOT exercised on the face). RESOLVED (R249): removed the colliding `describe`/`what do you see` aliases from LookSkill (intent split: look/看=survey-and-move, describe/看到什么=static scene description) → the deterministic alias_map now unambiguously routes describe→DescribeSkill (RED→GREEN test_describe_look_alias_routing, 4 cases) and the bare-face courtyard describe EXERCISED the R248 path (path_entered=True, [DESCRIBE]+describe-seam both fired, attr_error=False). CAVEAT: an alias edit does NOT make LLM tool-choice exclusive ([LOOK] still fired the same turn); describe has no moat oracle so this is reachability/no-crash, never a grounded win → E56.
- repl_accept MODE=both cross-contaminates verdicts between fetch and place phases — run
  single-mode campaigns for acceptance numbers → D181 red-team.
- The grasp oracle `holding_object(target)` certifies held==NAMED, not named==HUMAN-INTENT:
  the actor authors the target string, so adversarial-NL colour fidelity is witness-certified
  only (queued spine gate: world-owned NL→object grounder) → D182.
- grounding-dino is not colour-selective on the VLN mat: GROUNDED VLN acceptance needs the
  GT-backed `near_object` predicate (queued CEO gate), not the raw box → D178.
- An in-process acceptance claim must show `launch_explore` was never seen (proves the
  in-process path actually ran) → D181.
- ONE sim at a time, tear down via scripts/sim-teardown, never `pkill mujoco`, never kill
  supervisor/siblings → docs/RULES.md sim-safety (10.6h wedge, 2026-06-30).
- A green unit suite, a PASS timer, an odom count, a nav flag — none of them certify motion;
  measure the position/state DELTA → #Casebook Case 0.
- A `claude -p` round can die MID-FLIGHT on an API disconnect; the protocol contains it:
  bank-at-VERIFY keeps the results, post-check quarantines, next agent adopts per ROUND.md
  §1a. Rounds must bank rows the moment they exist — prose can always be reconstructed → R183.
- NEVER run pytest unbounded: tests that patch time.sleep turn wall-clock loops into
  full-speed spins and MagicMock call-recording grows ~GB/s (test_isaac_sim_proxy nav class
  OOM'd the 64G host 2026-07-01); ALWAYS `scripts/run-tests` (MemoryMax scope) → E18.
- The ledger is append-only, so a provisional row's `status` is NEVER rewritten; §1b
  adjudication = APPEND a later row whose `supersedes` points back. checks_schema.py's
  provisional age-check must exempt an already-superseded row or it nags forever and wedges
  every future round once age>2 (R187: R184 row superseded R186, flagged R187) → E24/D183. BUT preflight.sh's INFO counter still uses naive `grep -c provisional` → over-reports (67 vs TRUE-owed 0); NOT real debt — cross-check check.sh GREEN + BOARD; fix MANIFEST-gated → CEO G-354-1/E144.
- APPEND a ledger row as ONE pre-formatted line with `>>`; NEVER load-all→`json.dumps`→write-all:
  default dumps re-serializes EXISTING rows (adds `, ` separators, flips `\uXXXX`↔literal em-dash)
  — byte-identical content but numstat scores it a DELETION → append-only gate FAILS, quarantines
  the NEXT round. Content-preserving reformats are NOT restored (a 2nd rewrite compounds); leave +
  document. The gate caught it correctly — the bug was the write method, not the check → E27.
  RECURRED R285→quarantine (same load-all json.dumps rewrite of acceptance+experiments); R286 fix
  confirmed 0 rows lost (normalized set-diff==0) and applied the leave-reordered + pure-append cure → E81.
- ORDINAL/positional NL ("最右边的瓶子") CAN ground: the model resolves rightmost→blue (the
  non-central, dog's-right bottle) via `describe_scene`(VLM)+`detect`, then perception_grasp.
  BUT it is model-STRATEGY-fragile — it only works if the model PRE-RESOLVES the ordinal to a
  colour before the colour-keyed `perception_grasp` (raw "the rightmost bottle" query the CV
  resolver can't parse). `detect_objects` returns names+confidence, NO positions. Ordinal→object
  fidelity is WITNESS-only (D182 gap: oracle certifies held==named, not named==intent) → E25.
- The perception VLM (`look`/describe_scene) IS the ordinal resolver — A/B proved it (R192): VLM DOWN →
  raw ordinal to perception_grasp → 0/5; VLM up → 最左边的瓶子→"green bottle" GROUNDED 1/1. It defaults to
  OpenRouter (`google/gemma-4-31b-it`), 402-exhausted R192 (BILLING gate), silently breaking ordinals → E28.
- Ordinal SELECTION (最左边的瓶子→green) is deterministic+correct but took TWO fixes (VLM-only was a dead
  end): (a) the VLM drops the 瓶子 category (R193 grasped a can) → E30; (b) deterministic `_parse_ordinal`+
  `_resolve_ordinal_target` in perception_grasp was necessary but INSUFFICIENT until an ordinal-PASSTHROUGH
  native_loop prompt stopped the brain pre-resolving to a wrong colour → E31. E2E GROUNDED R197 → E33.
- g1_accept.py full RED+GREEN >10min (GREEN model flails ~14 turns): run BACKGROUND — a foreground Bash-cap timeout SIGTERMs mid-GREEN, orphaning the g1 sim (R217) → R219/R223.
- g1.perception NON-REPRODUCIBLE (R223 RAN 0/17): the oracle unions ALL same-colour seg pixels, but the OLD
  scene had MULTIPLE red geoms (2 stools + a can) → centroid on the cluster while dino boxed ONE → box↔centroid
  >60px ~2/3 of samples. RESOLVED R224 (WORLD config Inv.3; oracle+tol UNCHANGED=Inv.1): inject ONE dominant
  central red target (~18-20k seg px ≫ 920px) → 4-5px match, N=2 GROUNDED. LESSON: a GT-centroid oracle over a UNION of same-colour geoms is ILL-POSED with several in view — give perception ONE target → E50 R224.

- A same-second mutate→revert RED-proof can leave the GREEN re-confirm falsely RED (CPython `.pyc` mtime invalidation is second-granularity → reuses mutated bytecode); clear `__pycache__` before the confirm → E160.

## Recipes (proven invocations — copy, don't re-derive)
- Hunt the E162 dead-branch class MECHANICALLY, package-wide: `ruff check vector_os_nano tools loop scripts --select F821`. Under `from __future__ import annotations` an undefined name in a STRINGIZED annotation survives every green suite (never evaluated at runtime) yet raises `NameError` under `typing.get_type_hints` — a dead/untested path exactly like config.py's undefined `logger`. Found `intent_router.should_use_vgg(skill_registry: Any)` never imported `Any`. Regression guard = resolve `get_type_hints` for every callable in the module (proven to fire on injected drift); NOT a spine path → no gate → E163.
- Generalize E163 with ruff BUGBEAR real-bug classes: `ruff check … --select B006,B008,B023,F811`. Only B023 (closure late-binds a loop var) found a REAL defect — but it is FALSE-POSITIVE-prone: a closure defined in a loop is safe iff consumed SYNCHRONOUSLY within that iteration (9/10 cli.py hits = the REPL streaming callbacks passed to `run_turn`). The one LIVE bug is the closure that ESCAPES: `cli.py _on_vgg_complete` runs on a background thread via `vgg_execute_async`, then the REPL `continue`s and the next prompt reassigns `user_input`, so `session.append_user(user_input)` recorded the WRONG turn. Fix = def-time default `_user_input=user_input` (`on_complete` is only ever called with `trace`). Triage rule: a loop-closure B023 is real ONLY on a deferred/async/stored path; do NOT "fix" synchronous callbacks. Do NOT gate B023 (noisy) → E164.
- Generalize E163/E164 with the CAPTURED-THEN-DROPPED class: `ruff check … --select F841,F401`. F841 (local assigned-never-used) + F401 (import unused) catch a real defect when the dropped value is LOAD-BEARING per a docstring/comment: `render_response` (the bare-REPL renderer of V's replies) promised "syntax-highlighted code blocks" and imported `rich.syntax.Syntax`, but captured the fenced-block `lang` tag then DROPPED it, painting every code line flat cyan `#88c0d0` regardless of language — docstring false on the acceptance face. Fix = wire `Syntax(code, lang, background_color="default")` per block into a `Group` of Text+Syntax; unknown lang degrades to plain (rich falls back, no crash), prose path-colour kept. Triage: most F841/F401 hits are benign dead locals/imports (`mj`, fallback `out`); it's a REAL bug only when a DOCSTRING/COMMENT claims the dropped value is used or an import exists solely to power it. Verified deterministically (pure text render, no sim): pre-fix `python`==`text` styled output (RED), post-fix they differ (GREEN). Not a spine path → no gate → E166.
- A value-only single-source guard (E165 pinned the head-cam resolution DEFAULT) does NOT cover the MECHANISM that consumes it: `MuJoCoGo2.get_camera_frame`/`get_depth_frame` built the renderer once (`if not hasattr(self,"_cam_renderer")`) and IGNORED width/height on every later call — and that renderer is a SHARED resource, called bare (640x480) by the look/describe path AND at 320x240 by the grasp path (`Go2GraspPerception`, whose RGB mask must align pixel-for-pixel with 320x240 depth+intrinsics). So a `look` before a `pick` locked the renderer at 640x480 and grasp got a 640x480 RGB vs 320x240 depth → `front_object_mask` misindexed depth → wrong 3D grasp (order-dependent, invisible to unit suites that render one resolution). Fix = `_ensure_render(attr,w,h,depth=)` re-creates only on dim change; each call renders at ITS dims. Verified with a fake-`_get_mujoco` recording every Renderer's construction dims (no GL/sim slot). RECIPE: after fixing a shared const's VALUE, audit the SHARED CONSUMER — grep cache-once builders (`if not hasattr`/`if … is None:` that stash a resource) reused by callers passing DIFFERENT args; a per-call parameter silently ignored is the same class of bug as a stale default. Real-renderer grasp-after-look on the bare-cli face is SIM-OWED → E167. R379/E168 CORRECTS the E167 scope: it verified its own go2 driver but not the FAMILY — the sibling `MuJoCoG1.get_camera_frame` (`if self._cam_renderer is None`) had the IDENTICAL cache-once copy, ignoring later w/h though its signature + the configurable `G1HeadPerception(width,height)` wrapper + `g1_perception_oracle` all advertise per-call dims (a non-default perception after a bare 640x480 look silently got 640x480; LATENT not live only because today's g1 callers all pass 640x480). Ported the contract to g1 (`_cam_renderer_dims`, re-create+close on change); fake-mujoco RED→GREEN 4/4, sim/hw 165/165. Scanned ALL drivers: `mujoco_arm.render`=fresh-per-call (safe), `pano360`=fixed `face_size` (safe) — g1 was the ONLY remaining copy. LESSON (extends E158): a cache-once/shared-resource-ignores-per-call-arg find is a MIRROR class — the moment you fix it in one driver grep the IDIOM (`if …_renderer is None`/`if not hasattr`) across EVERY sibling driver, not just the one that surfaced it. Real-sim g1 non-default-res perception SIM-OWED → E168. R381/E170 CLOSES the head-cam renderer FAMILY: the 3rd sibling — `MuJoCoGo2.get_self_mask`'s `_seg_renderer` — still had the raw cache-once guard (`if not hasattr(self,"_seg_renderer")`) while RGB/depth were fixed via `_ensure_render`; its SOLE caller (`Go2GraspPerception.get_depth_frame`) applies the self-mask only `if self_mask.shape==depth.shape`, so a locked first-call resolution SILENTLY fails that guard on a differently-sized later call ⇒ the arm's pixels are NOT dropped ⇒ the Piper arm becomes a grasp distractor (the very D30 self-occlusion the mask prevents), no error. Ported the per-call-dims contract (`_seg_renderer_dims`, re-create+close-old on change); 6 new fake-`_get_mujoco` seg-dims tests + E167/E168 8 = 14/14, go2 driver+perception 55/55 no regress. LATENT not live (E165 single-sources go2 head-cam to 320 ⇒ every perception instance renders at one res today). LESSON (sharpens E168): a "vein SATURATED" claim for a shared-resource-ignores-per-call-arg class must enumerate EVERY renderer on the SAME object, not just across sibling drivers — RGB/depth/seg are THREE renderers on one go2 driver; fixing two left the third. Head-cam renderer vein now CLOSED (rgb/depth E167, g1 E168, seg E170); real-sim go2 describe-then-pick at mismatched dims SIM-OWED → E170.
- Bare-REPL NL acceptance, in-process sim: `VECTOR_NO_ROS2=1 MUJOCO_GL=egl` +
  `tools/acceptance/repl_accept.py` (MODE=fetch|place|neg|combo); env from .env.example → D181/D182.
- g1 headless under the bare-REPL face needs `MUJOCO_GL=egl` (suppresses the GLFW viewer)
  → 010a998.
- Verdict-time frames: render an isolated MjData copy with a fresh Renderer ON the emit
  thread; a snapshot must never crash the verdict → #Casebook Case 11.
- Skill-direct probes (bypass LLM) isolate mechanism from model: 4/4 skill-direct + 4/4
  bare-REPL localizes a failure to the layer between → DEBUG.md D172 method.
- Test suites via `scripts/run-tests` in serial chunks (subdirs first, sim-heavy files
  last); measured chunk peaks 0.2–3.7G, so the default 12G cap is generous → E18.
- Perception VLM off OpenRouter (402/arrears) → route to LOCAL Ollama, plug-and-play, NO credit:
  `VECTOR_VLM_URL=http://localhost:11434/v1 VECTOR_VLM_MODEL=gemma4:e4b` (already-pulled; the code's
  documented local seam). gemma4:e4b resolves L-R ordinals; unblocked the clean ordinal GROUNDED → E28.
- Acceptance-diagnosis two-parter (make the ONE warehouse sim run decisive): (a) non-verbose REPL pins skills/perception loggers to ERROR (cli._QUIET_LOGGERS) so `[PGRASP]` trace never reaches repl.raw.log → `VECTOR_ACCEPT_VERBOSE=1` adds `--verbose` (logging-only, face intact) to capture arrival pose + per-heading detection; (b) the driver wrote `eyes_*.png`/`verdict_*.png` to the ephemeral SNAP scratch dir (reboot-wiped, AGENTS.md forbids it for durable evidence) but never copied them out, so R229/R231 warehouse frames VANISHED and `closest seen inf` was unadjudicable visually — now auto-persisted to `var/evidence/R<ROUND_N>` at end-of-run (`vlm_guard.resolve_evidence_dir`/`persist_evidence`, unit-tested; bare run w/o ROUND_N warns loud). Keep the frame, not just the log. CAVEAT (R238): >1 run in ONE round both persist to `var/evidence/R<N>` root with identical names, so run-2 OVERWRITES run-1's `eyes_fetch.png`/`repl.raw.log` (unique `verdict_<ts>.png` survives) — set `VECTOR_EVIDENCE_DIR` per run or copy frames out to `<tag>_*` and LOOK before the next launch. Also bank a ledger row as the SOLE stdout (`sys.stdout.write(json.dumps(row)+"\n")`), NEVER `print("LABEL:", json[:130])` under `>> ledger.jsonl` (the prefix+truncation lands a corrupt line; validate each appended line parses pre-check.sh) → E54/R238.
- Colour-object resolver in a VIVID scene: gate the target HUE at PIXEL level BEFORE morphology/components, never per-blob MEDIAN hue AFTER — a vivid non-target bg (warehouse racking, ~32% of frame past sat>=140) fuses the target's px into a blob whose median hue != target → None (the R229–R236 warehouse green-fetch transfer gap). Build the mask from target-hue px first so each colour is its OWN component; front_object_mask(color=…) does this, regression test_color_green_survives_saliency_flood → E54.

## Frontier (the ambition horizon — review rounds refresh; STATUS `frontier:` carries the 1-liner)
- Ordinal fetch CONFIRMED (R197/E33, deepseek-v4-flash): `最左边的瓶子`→green AND `最右边的瓶子`→blue
  both GROUNDED verified=True, 0 handover, eyes-confirmed — DIRECTION-sensitive (not lucky green default).
  Built on R195 passthrough + catalog-projection + R196 no-auto-handover (Case 16); witness-only (D182) floor → STATUS next.
- Position-invariance CONFIRMED N=2 (R209/E43 + R210/E44, deepseek-v4-flash): under VECTOR_SCENE_SWAP the SAME
  `最左边的瓶子` grasps BLUE (new leftmost) not frozen GREEN (E31/E33) → resolver reads LIVE positions, not memorized coords (same scene/objects; scene-variant breadth is E51, new-world/embodiment still the bar) → E44.
- Novel-object breadth: fragility is PLACEMENT/FOV-correlated, NOT geometry/plug-in nor a universal brain-flake (R212/E46 + R215/E47 CONFIRMED N=3, deepseek-v4-flash).
  Novel PURPLE BOX (type=box, first non-cylinder, y=2.89 in-FOV) GROUNDED 3/3 fresh sims bare-face (holding_object(box) all 3; eyes: box aloft, RGB remain), while sibling
  yellow (E45→E46, y=3.11) REFUTED 0/2 SAME harness (perception not-visible → brain WANDERED). 5-site plug-in (0 kernel edits) is CORRECT for colour AND geometry;
  R212's "brain-fragile at N=1" REFINED — an IN-FOV grasp-reachable novel object grounds ROBUSTLY. LESSON: promote N=1 novel passes only on N>=2 repro;
  place novel pickables IN the front FOV band; welds/colour maps still HARDCODED (S4/D182). R251/E58: yellow (y=3.11) RECOVERED to GROUNDED N=1 provisional — its initial scan STILL returns no_detections (FOV-margin is REAL) but the warehouse-built R234 far-recovery floor + R237 hue-gate localize→re-pose (10.88,3.11)→mask_px=4184→grasp it; the FOV-margin is now recovery-HANDLED, not a placement dead-end (a 2-world perception fix transferred back to HOUSE free). R252/E59: CONFIRMED N=2 (same brain+VLM across the boundary, grasp (10.877,3.107,0.321) holding_object(bottle_yellow) CAUSED, eyes yellow aloft/RGB+purple remain) — SUPERSEDES the R212 refuted yellow row → E45/E46/E47/E58/E59.
- Cross-embodiment CONFIRMED w/ the CURRENT brain (R216/E48 → R217/E49 adjudicated): deepseek-v4-flash (all recent go2 work) drives the 2nd embodiment g1-humanoid to GROUNDED nav bare-face, 0 kernel edits — refreshes 32-round-stale R183 g1.nav (deepseek-chat), answers R200 critic (all-go2). R217 reproduced it across a round boundary at a NON-MEMORIZED coord (12,3, ≠ R216's 9,3/10,4) actor=CAUSED → not a fixed-pair fluke. Bonus MOAT-FIDELITY win: leg-B (11,4) is inside an obstacle → navigate_to reason=unreachable moved_m=0 → system emitted RAN/False, refusing to fabricate an arrival. Still EXISTING g1 config, NOT a new 3rd embodiment (BYO URDF+driver = S4-gated + multi-round SDD); g1-nav renders no frame → at_position GT pose is the witness → E48/E49. R224→R225: the PERCEPTION axis also GROUNDED and now CONFIRMED (E50, N=3: R224 2/2 + R225 1/1 reproduced across the boundary — the exact test R223 failed on R219's un-hardened bar) after a WORLD-config hardening (dominant central red panel; oracle+tol byte-unchanged, Inv.1). Cross-embodiment spans BOTH axes, 0 kernel edits. LESSON: a GT-centroid oracle over a UNION of same-colour geoms is ILL-POSED with several in view — give perception ONE unambiguous target, never widen tol. Real breadth is still a NEW 3rd embodiment or a 2nd world → E50. R382/E171: the OFFLINE half of a NEW 3rd embodiment is now regression-guarded — a SYNTHETIC robot (novel 'syn_' prefix, 3-DoF not 12, freejoint NOT at qpos 0) stands up config→DofLayout→build_stance_vector→CapabilityProfile via the generic embodiments/ path with ZERO prod-code change and no driver in its call path (7/7 tests, embodiments 56/56); proves Inv-3 is not overfit to go2/g1. The sim-face WALK of a 3rd embodiment stays S4-gated (BYO URDF+driver) → E171.
- Scene breadth via CONFIG CONFIRMED (R226 built / R227 verified N=2 / R228 adjudicated, E51): VECTOR_SCENE_CLUTTER injects 5 decorative distractors (incl. a SAME-HUE off-centre green decoy) into the frozen go2 tabletop via the existing `extra_geoms` seam (same one g1's percept_target_red uses), 0 kernel/driver edits (Inv.3); a CONFIRMED green fetch (v4-flash) GROUNDED N=3 — REPRODUCED across the R227→R228 boundary (fresh sim, oracle byte-unchanged, the exact reproducibility gate R219 g1.perception failed) — real bottle grasped (holding_object GT), decoy is contype/conaffinity 0 + no freejoint → weldless/unpickable so a mis-ground yields False → True=discrimination, unfakeable. LESSON: decorative clutter (no collision/weld) is the cheapest honest scene-diversity knob — stresses PERCEPTION without perturbing physics or a fake-verify hole; still ONE room, a NEW WORLD or 3rd embodiment is the deeper bar → E51.
- New WORLD plugs in as CONFIG; a confirmed bar did NOT transfer zero-shot but is now RECOVERED+CONFIRMED via a 2-layer perception fix (R229/E52 SPLIT → R238 CLOSED): go2_warehouse.xml (compact industrial box, distinct enclosure from the 20×14m house) enters via VECTOR_ROOM_TEMPLATE → build_room_scene(room_template_path=) with 0 kernel/driver edits (Inv.3), compiles with the 5 pickables + pick_table + place_bin byte-identical coords (unit 9/9; MjModel: green [10.88,3.0,0.32], dog [10.0,3.0]). BUT green fetch RAN 0/2 on the bare face (v4-flash): run1 wandered (walk fwd 2.0×N → pick 'Cannot locate target object'), run2 perception_grasp×11 never completed. Config verified LIVE + furniture byte-identical ⇒ NOT moved-geometry — perception_grasp/approach does not complete under the new visual+lidar scene. LESSON: a new-world BUILD (compiles, config-only) is a FLOOR, NOT acceptance; the deeper bar is that grasp/perception must TRANSFER, and clutter (E51, same room + decoy geoms) is far cheaper than a new enclosure. Next = Hypothesis-Loop debug → E52. R230 CAVEAT (402-confound) DISPROVEN at N=1: R231 re-ran clean local-VLM route, ZERO 402, still RAN 0/1 — a REAL grasp/perception gap, not billing. R232 OBSERVE ruled out spawn/pose/lighting/bg; root cause (H1 nav-off-frame · H3 approach/grasp · H2 render · H4 scan) sim-blocked, instrumented via VECTOR_ACCEPT_VERBOSE → E54. RESOLVED as a 2-layer perception gap (NOT world/config, NOT the brain): (1) R234 far-recovery DEAD-BAND (recovery band (1.6,8.0]m skipped a dog that nav-stopped CLOSE-but-blind at 0.88m → floor to 0.30m, f437f4e); (2) R236 front_object SALIENCY FLOOD (vivid warehouse bg 8-connects the target's px into off-hue blobs → R237 pixel-level hue gate before _open). Warehouse green fetch then GROUNDED N=2 (R237) and PROMOTED to confirmed N=3 (R238, fresh reproduction across the boundary, holding_object(green) actor=CAUSED, eyes: dog holds green in warehouse). LESSON: a new-world BUILD is a FLOOR; the transfer bar is passed only when grasp+perception complete on the bare face — and here it cost TWO independent perception fixes, each invisible to unit-green → E54/R238. R239: the pixel-hue gate is FULLY colour-agnostic — blue PROMOTED to confirmed (N=2), and PURPLE-box (N=1) + RED-can (N=1, the HARDEST: hue-adjacent to orange racking, mask_px=5132 NOT flooded, d_min=0.569 so Case-14 FOV did not bite) both transferred, eyes-confirmed discrimination. HOUSE plain-colour green re-confirmed (no regression). Only YELLOW(y=3.11) untransferred — blocked by the separate HOUSE FOV issue (E45/E46), NOT the hue gate (R251/E58: yellow later RECOVERED N=1 provisional via the same R234/R237 fixes) → E54/R239. R240 review PROMOTED red+purple to confirmed N=2 (re-run across the boundary, eyes-confirmed discrimination; red mask_px=5133≈R239's 5132, purple grasped first-action) — warehouse colour transfer CLOSED (green N=3, blue N=2, purple N=2, red N=2), provisional queue cleared → E55/R240.
- 3rd WORLD courtyard CONFIRMED zero-shot, ZERO perception fixes (R241 build → R242-R246, E56): go2_courtyard.xml (open-air sandstone courtyard) plugs in as pure CONFIG via VECTOR_ROOM_TEMPLATE=courtyard, 0 kernel edits (Inv.3, 1-line dict entry like warehouse R229), 5 pickables byte-identical at house coords. FETCH green/blue/red/purple ALL PROMOTED confirmed N=2 (each re-run across a round boundary, eyes-confirmed discrimination: target aloft, other 4 remain) — colour AND geometry (purple box = first non-cylinder) transfer. UNLIKE the warehouse (E54 cost 2 perception fixes incl. the HUE-ADJACENT red/orange flood), courtyard needed ZERO: its warm sandstone bg (~25° hue) is FAR from every target hue so the R237 green-hue gate never floods. Now 3 distinct worlds ground go2 fetch (house N=3, warehouse N=3, courtyard green/blue/red/purple N=2). LESSON: a config-only world whose background hue is FAR from the target transfers zero-shot; the warehouse was hard because orange racking is hue-ADJACENT, not because new worlds are inherently hard. BUT the courtyard PLACE-leg did NOT transfer: composite RAN verified=False — grasp + physical place both succeed (eyes: bottle IN place_bin) but the intermediate navigate at_position(10.8,3.0,tol=1.0) ungrounds because (10.8,3.0) is INSIDE the inflated pick_table (R=0.28) → plan_path=None, UNREACHABLE in EVERY world incl HOUSE (see the Refuted courtyard-PLACE line). LESSON: a FETCH transferring zero-shot does NOT imply its multi-step PLACE composite transfers — the nav sub-goal predicate is the fragile link, not perception/grasp → E56/R243-R247. R253/E60: RECOVERED to GROUNDED provisional N=1 (2/2, was R246 2/3) — the ungrounding was NOT the world: the BRAIN improvised the unreachable navigate(10.8,3.0) as a RECOVERY after mobile_place's transient first-nav miss surfaced to it as False; an internal nav-retry (_NAV_RETRIES=1, re-reads pose) keeps a transient miss INSIDE the skill so it never surfaces (a genuinely-unreachable target still fast-fails → honest nav_failed). Retry is unit-tested but did NOT fire this run (first nav landed), so reliability across the flake still owes N>=2 → E60. R255/E60 ADJUDICATION: the N=1 provisional REFUTED — does NOT reproduce (RAN verified=False 1/2: grasp holding_object CAUSED=True but resting_on_receptacle=False; brain '瓶子…掉了'→re-grasp thrash; eyes: empty-armed dog, no bottle placed). Across N=3 real runs courtyard PLACE is FLAKY (R246 2/3, R253 1/1 clean-retry-untriggered, R255 dropped): the residual flake is a MID-WALK DROP (holding_object lost during the mobile_place walk/dock), NOT the nav-MISS _NAV_RETRIES targets — R253's fix is real but insufficient. Do NOT re-credit the nav-retry as the place-reliability fix (E23); a DEBUG round with VECTOR_PLACE_DIAG must localize weld-break-under-locomotion vs off-receptacle drop-release → E60. R256/E60 DEBUG RESOLVED — the "mid-walk drop" framing is REFUTED: in-process (no brain) the grasp weld holds the object at 25–30mm from the EE through the ENTIRE 1.6m walk + Step-6b dock (N=2, 1028 samples, eq_active never flips, never hits floor), and the FULL MobilePlaceSkill lands it RESTING on the courtyard bin (resting_on_receptacle=1, gripper empty). The machinery is SOUND. ROOT CAUSE = BRAIN POST-PLACE MIS-RECOVERY, confirmed from the real R255 repl trace: perception_grasp→verify holding TRUE→mobile_place (legitimately empties the gripper onto the bin)→describe→brain '掉了让我重新抓取它' (misreads the EXPECTED empty gripper as an accidental drop)→navigate→detect→mobile_pick RE-GRASPS the just-placed bottle OFF the bin→thrash→resting=False. The place succeeded; the brain UNDID it. FIX (next round, behavior): a prompt/runner guard that an empty gripper AFTER a successful place is the terminal success state, not a drop to recover (cf. R206 quantity guardrail); owes real-face N>=2. Regr=tests/vcli/test_go2_courtyard_place_machinery.py (machinery rests=1). Do NOT re-diagnose as a weld/mid-walk drop → E60/R256. R257/E60 FIX LANDED + VERIFIED N=2 GROUNDED (provisional): the brain post-place recovery guard = (a) persona place_guidance — after a place an EMPTY gripper is EXPECTED not a drop; verify resting_on_receptacle (never holding_object/describe); do NOT re-grasp; (b) runner NativeStepRunner — a successful place arms `_place_awaiting_verify`; a re-grasp skill (`_GRASP_SKILLS`) is REFUSED until the next verify closes it (bounded by `_MAX_VERIFY_NUDGES`, quantity-safe: an intermediate FAIL verify clears it for the next object). Both bare-face runs (deepseek-v4-flash, MODE=place, in-process) grounded resting_on_receptacle True with NO mobile_pick re-grasp — run1 clean, run2 wandered (look/at_position/repeat-place) but never undid the place; eyes both: green RESTING in bin, gripper empty. Meets the R255-refuted N≥2 bar. RESIDUAL: run2's wander shows the guard kills the DESTRUCTIVE re-grasp only, not all post-place brain flakiness; eyes self-read; N=2 modest → promote next round. Regr=tests/unit/vcli/test_native_loop.py post_place/regrasp tests → E60/R257. R259/E60 PROMOTED confirmed: re-run across the R257→R259 boundary GROUNDED verified=True (2/2), native skill seq CLEAN (perception_grasp→verify holding→mobile_place→verify resting→finish, ZERO mobile_pick re-grasp — the guard held, cleaner than R257 run2's wander), eyes green resting in the courtyard bin gripper empty → courtyard PLACE CLOSED (fetch AND place both transfer zero-config to the 3rd world; 3 clean same-brain GROUNDED total R257×2+R259) → E60/R259.
- Quantity-place: REFUTED v4-flash (R199/E36) → GROUNDED deepseek-chat (R204/E39) — ceiling was BRAIN-SPECIFIC. R306/E96 SKEPTIC: the R205 confirmed bar (deepseek-chat self-read, GROUNDED-FLAKY 2/3, age~100 = OLDEST stale green) does NOT hold on the CURRENT shipped face — re-run on v4-flash REFUTED 0/4 (final resting_on_receptacle count False, 0 bottles placed; eyes vlm-judge FAIL corroborates GT; LOOK: arm holds ONE can aloft, receptacle empty). The old "confirmed" rested entirely on a now-unused brain; N-object place-decompose (E36/E72 class) is flaky-to-broken run-to-run. Do NOT re-credit until a decompose guardrail or stronger routing brain lands (cf. E72) → E96/R306.
  not machinery (E38 seq 2/2); model swap moved it ZERO kernel edits (E23-clean, plug-and-play). But NOT deterministic: ROBUSTNESS (R205/E40) campaign GROUNDED 2/3 (run1 abandoned obj-2; deepseek-chat FLAKY on 两个) → runner guardrail E40.
- QUANTITY guardrail is a PLANNER-FREE finish-gate, NOT a runner-side decomposer: native_loop is planner-free BY CONSTRUCTION (import firewall — a per-object loop/replan bookkeeping IS re-growing the planner, the tell).
  So R206 enforces the prompt's "NEVER finish while latest verify is FAIL" IN THE RUNNER: refuse finish/stop
  while the model's OWN latest verify() is False (bounded, honest-on-exhaustion), model still owns decompose.
  Residual (deferred, goal-authenticity per native_loop._grade): if the brain finishes on a WEAKER passing predicate (holding_object / >=1) the gate can't fire — it only catches the >=N-then-quit mode → E41.
  R309/E100 SETTLES the framing: closing N-object quantity-place is CEO-GATED, NOT the "one non-gated build left" STATUS optimistically named. Machinery is sound (E38) and the finish-gate is now regression-pinned (E99); the gap is the BRAIN not self-decomposing 两个. The only fixes are (a) a runner-side decomposition planner = the D76 planner-plugin gate (native_loop MUST NOT import goal_decomposer/executor) or (b) a per-model prompt fix = E23-uncreditable. STOP re-attempting quantity as a build (R199..R308); hold at the S4/D182+planner gate → E100/R309.
  R309/E99 LOCKED the finish-on-fail guardrail deterministically (tests/unit/vcli/test_native_loop.py, no sim): FAIL verify→finish refused+nudged→run continues (4 calls not 2); fail-then-finish-forever TERMINATES bounded grading verified=False. Was only ever exercised by a flaky ~15min sim (E72/E96); now a native_loop refactor can't silently drop the guard → E99/R309.
- Degenerate-spin guard (R274/E74 unit → R278/E75 REAL fire → R279/E76 goal-aware, provisional): a flaky brain issuing action skills WITHOUT
  ever verifying burns the run to ZERO verdicts (~15min, judge never fires) — soft nudge@6, honest break@12 (grades RAN/empty, never a forced
  green; _MAX_NATIVE_TURNS=24, NOT 63). R278 OBSERVED it fire on a real thrash (guard_real_thrash.txt + eyes_seq1.png). E75 LIMIT (counter reset
  on ANY verify, so an off-goal at_position verify pinned it below break@12) is RESOLVED by E76: reset ONLY on a NOVEL (normalized-expr,result) vs
  seen_verify_outcomes (last_verify_result, planner-free), so a dodging re-read still climbs to break@12. 26/26 unit + real fetch GROUNDED guard SILENT; RESIDUAL: novel-true-each-turn resets; real-thrash FIRE non-determ → E74/E75/E76.
- A world-owned NL→object spatial grounder (positions the model can't author) would make ordinal/relational NL robust instead of model-strategy-fragile — cf. the D182 spine gate → E25.
- g1 GROUNDED navigation (non-gated); VLN GROUNDED accept waits on near_object gate → D176/D178.
- Automated VLM vision-judge is NOT external-blocked (D181 mislabel, R270): `acceptance/vision_judge.py::judge` is a BUILT fail-closed OpenAI-compatible judge already wired into visual_e2e.py, but ORPHANED from `tools/acceptance/repl_accept.py` — the harness behind EVERY ledger row, whose `_eyes_frame` only COPIES the render for self-read (why every row is eyes=self-read). Pointing VECTOR_JUDGE_* at the local Ollama gemma4:e4b (already the perception route, zero credit) is a NON-gated verify-pillar build (secondary witness, stricter-only Inv.1) flipping self-read→vlm-judge — the concrete next-BUILD frontier → R270/E68. R271/E69 WIRED + REAL-FACE VERIFIED: `_eyes_frame` now runs `vision_judge.judge` on the offscreen render; `resolve_judge_env` (tools/acceptance/vlm_guard.py) LOCAL-PREFERS gemma4:e4b whenever Ollama is up, OVERRIDING the stale supervisor-exported `VECTOR_JUDGE_MODEL=qwen3-vl-plus` (arreared, else abstains forever); escape hatch `VECTOR_JUDGE_FORCE_REMOTE=1`. Bare-cli fetch (deepseek-v4-flash) verified=True(1/1) AND eyes=vlm-judge PASS(4/4) same render, GT UNCHANGED (stricter-only: witness recorded, never folded into verified=). Red-team: judge FAILs a black frame (scene_rendered=no), PASSes the real one → DISCRIMINATING not rubber-stamp. Eyes pillar upgraded self-read→vlm-judge (provisional, promote next round) → R271/E69. R272/E71 PROMOTED the WIRING to confirmed (fetch across the R271→R272 boundary: verified=True 1/1 GT + human self-read green-in-gripper; eyes=vlm-judge mode reproduces — judge fires+records, [EYES-WITNESS] emitted, stricter-only GT unchanged) — BUT R271's STRONG half ('witness=PASS, DISCRIMINATING') does NOT reproduce: gemma4:e4b returned ABSTAIN(4/4) on an equally-clear upright in-frame render a human trivially passes. LESSON: the local gemma4:e4b judge is NON-DETERMINISTIC (PASS→ABSTAIN run-to-run) — fail-closed so stricter-only holds (an abstain never manufactures a green), but it is a recorded-NOT-trusted witness, too weak to be a reliable second discriminator; a TRUSTED vlm-judge needs a stronger model (funded qwen3-vl when billing restored, or a better local VLM). SECOND finding (E70): 'extend vlm-judge to PLACE mode' is a REAL wiring gap — `_eyes_frame('place')` runs on the `grounded)` expect BEFORE `snapshot_on_verdict` writes verdict_*.png, so JUDGE_WITNESSES stays empty and the judge never fires on place (judge_witness.log empty); place also flaked verified=False 2/6 (first mobile_place resting NOT_GRADED → brain '瓶子掉了' re-grasp thrash, E60 family — the R257 guard arms on a SUCCESSFUL place, a NOT_GRADED place did not arm it). FIX owed next round: order `_eyes_frame` AFTER the verdict snapshot for place/seq/combo, then re-verify → R272/E70/E71. R273/E70 FIX LANDED (code+regr test; e2e re-verify OWED): reordered `drain_until_quiet`→`_eyes_frame` in the fetch/place/combo/quantity branches (mirroring the seq path whose judge fired R270 turn1) so the frame is the SETTLED post-verdict state after `snapshot_on_verdict` writes verdict_*.png. Regr test tests/harness/test_eyes_frame_ordering.py asserts a settle between every turn's `sendline` and its `_eyes_frame` (non-vacuous: pre-fix flags fetch). Unit-green; the REAL MODE=place eyes=vlm-judge run confirming the judge fires on place is DEFERRED (sim was busy with the deadline-critical next#1) — unit-green≠done → E70/R273. R297/R298 (E88/E89) VERIFIED the R273 fix on the real HOUSE face (judge FIRES on the place verdict, witness recorded). R301/E91 EXTENDED it to NON-HOUSE worlds: judge fires on place in BOTH warehouse+courtyard (frame-order transfers zero-config, Inv.3). courtyard place self-read→vlm-judge UPGRADE LANDED GROUNDED 2/2 judge=PASS 4/4 (supersedes R259, provisional); warehouse RANed verified=False on a MESSY brain place-seq (mobile_place x2 + UNCAUSED holding re-verify + resting NOT_GRADED) though eyes show green RESTING in the warehouse bin — physical place OK, a brain place-decompose flake (E60/E70 class), NOT world/wiring; re-run warehouse clean to land its vlm-judge upgrade → E88/E89/E91. R302/E92 ADJUDICATED both E91 provisionals to CONFIRMED: courtyard GROUNDED 2/2 reproduces across the boundary (supersedes R301 prov); warehouse clean re-run GROUNDED 2/2 (the R301 RAN WAS a brain place-decompose flake, physical place always OK) — PLACE now vlm-judge-eyes GROUNDED on ALL 3 worlds (house E88/E89 + courtyard + warehouse), superseding R259/R262 self-read. CAVEAT: the gemma4:e4b judge FIRED but returned FAIL(workspace_in_frame=no) on BOTH clean R302 frames a human trivially passes — vs R301's PASS(courtyard)/ABSTAIN(warehouse); confirms E72 the judge is NON-DETERMINISTIC (PASS↔ABSTAIN↔FAIL run-to-run) and recorded-NOT-trusted, fail-closed so verified= stands on the GT resting_on_receptacle oracle + human LOOK, never the judge. A trusted 2nd discriminator still needs a stronger VLM (funded qwen3-vl / better local) → E92. R307/E97 CLOSED the last STALE self-read HOUSE fetch bars: plain-colour/category-only/negated-distractor re-verified on current v4-flash + eyes upgraded self-read→vlm-judge, GROUNDED 1/1 each (judge PASS/ABSTAIN/PASS — E72 non-determ, GT holding_object(target) actor=CAUSED + human LOOK carry; negation resolved blue+green untouched) superseding R260/R263/R268 — eyes pillar now vlm-judge across ALL confirmed fetch+place bars & worlds → E97/R307. R308/E98 CLOSED the LAST 2 STALE BOARD rows → BOARD now has ZERO ⚠ STALE table rows: (a) verify.eyes-vlm-judge-wired (age35) refreshed on a fresh v4-flash HOUSE fetch GROUNDED 1/1 (holding_object(bottle_green) CAUSED) with the vlm-judge FIRING witness=PASS(gemma4:e4b) all4 rubric yes, supersedes R272; (b) quantity-place.nl-guardrail (age98, deepseek-chat REFUTED 0/1) SUPERSEDED on v4-flash as refuted-by-implication — E96 already refuted quantity-place.nl 0/4 on v4-flash so a finish-gate firing only on >=N-then-quit has no precondition (no fresh sim; E96 IS the v4-flash face run). Stale-bar hygiene backlog is now EXHAUSTED; every remaining new bar is CEO-gated (S4/D182) → E98/R308.
- STRONGER LOCAL JUDGE trialed (R313/E103) — first forward motion on the eyes-pillar frontier since E92: qwen2.5vl:7b (ollama, 6GB) via R312's `VECTOR_JUDGE_LOCAL_MODEL` seam is DETERMINISTIC where gemma4:e4b is not — N=5 identical PASS (4/4 rubric yes) on a real R311 fetch verdict frame, vs gemma4:e4b 5/5 all-ABSTAIN on the SAME clear frame (gemma never grades today). NOT a rubber-stamp: qwen 3/3 ABSTAIN on a black frame (raw "completely black, impossible to determine" → won't PASS). CAVEAT (frontier NOT closed): qwen ABSTAINs (not FAILs) the black frame + truncated to 1 rubric item, CPU-only ~90s/call — so "trusted enough to actively DOWNGRADE a bad-but-RENDERED frame" is UNPROVEN. OWED next round: (a) a live bare-cli fetch with `VECTOR_JUDGE_LOCAL_MODEL=qwen2.5vl:7b` as the eyes (upgrade a bar off the untrusted gemma judge); (b) a frame a good judge MUST FAIL (toppled robot) to prove active downgrade. Reusable probe: `tools/acceptance/judge_determinism.py <frame> <model[,model2]> <N>`. R314/E104 REFUTES the active-downgrade hope: rendered a REAL go2 clearly TOPPLED on its side + a robot-ABSENT frame (offline `capture.snapshot`) — qwen PASS 2/2 with robot_present_upright=YES on the CLEARLY-toppled robot (a DETERMINISTIC false-green, WORSE than gemma's abstain), yet ABSTAINs when the robot is ABSENT ⇒ qwen is a PRESENCE detector, NOT a pose discriminator; do NOT land it as trusted eyes (next#1a is MOOT). The eyes 2nd-witness gap stays OPEN — only gated paths remain (remote qwen3-vl billing / a better local VLM) → E103/E104. R315/E105-E106 RESOLVES the "better local VLM" path: (E105) qwen2.5vl:7b is POSE-BLIND, not merely a presence-stamp — a 3-framing forced-choice bracket (rubric/fault/neutral) gives an IDENTICAL answer to a clearly-upright AND a clearly-toppled go2 every time (tracks the prompt's leading word, never the image), so NO rubric reword rescues qwen (`tools/acceptance/pose_probe.py`). (E106) minicpm-v (ollama 5.5GB) IS the FIRST local VLM with a REAL pose signal: under a NEUTRAL forced-choice it is 3/3 STANDING(upright)/3/3 LYING(toppled) — deterministic & model-specific (qwen fails the SAME neutral prompt). BUT the signal survives ONLY neutral framing (a fault-primed prompt collapses minicpm to FALLEN-both too) and the FROZEN visual_acceptance_rubric `robot_present_upright` item is presence/fault-framed → its parser ABSTAINs(upright)/flips(toppled) on minicpm, missing the signal (raw minicpm says "Yes..upright" = a parse artifact, not blindness). So landing minicpm as trusted eyes needs that rubric item reworded to a neutral pose Q = an ADR-002 MOAT/SPINE gate (G-315-1 queued); MODEL CAPABILITY is no longer the blocker. Probes: pose_probe.py + judge_determinism.py on the var/evidence/R314 triplet → E105/E106. R316/E107 BOUNDS E106 — the minicpm neutral pose signal is ANISOTROPIC/fragile, NOT a general pose reader: on a broader bracket (upright-yaw90 / back-topple / LEFT-side-topple / sunk, N=3 neutral) it is right on upright STANDING 3/3 + back-topple LYING 3/3 but WRONG on a LEFT-side topple — STANDING 3/3, the mirror of the very right-side frame E106 passed (E106 reproduces byte-exact on the R314 frames, so the tool is faithful; the fault-framed prompt still collapses all 4 to FALLEN, confirming E106 caveat-a). ⇒ a SINGLE-FRAME minicpm 2nd-witness false-GREENs a left-side fall; landing it as eyes needs MULTI-VIEW/ensemble, not one frame — a caveat on G-315-1, not a new gate. pose_probe.py now ships tracked `--framing neutral|fault` (TDD) so E106/E107 are tool-reproducible → E107. R317/E108 REFUTES the MULTI-VIEW fix E107 proposed: rendering all 4 R316 poses from 4 azimuths (45/135/225/315) + majority-voting, the LEFT-side topple reads STANDING 3/3 in ALL 4 views (majority WRONG) though clearly on its side from az45/225/315 — while the back-topple missed ONLY at az45 and WAS rescued by the other 3. So side-lying is a TOPOLOGY blind spot in minicpm, NOT a camera-angle artifact ⇒ a minicpm ensemble (even multi-view) is NOT a robust pose oracle; eyes need a DIFFERENT model or the deterministic quat-upright hard-channel, not a minicpm ensemble → E108. R318/E109 RESOLVES it — the hard channel READS pose where every VLM failed: `upright_cosine`=m22 of R(root-quat)=world-up·body-up (`tools/acceptance/pose_upright.py`, TDD 11/11), on the EXACT R316/R317 poses at the real keyframe stance (1,0,0,0) orientation is 3/3 incl the left-side topple FALLEN(cos 0.0) that minicpm read STANDING 4/4 in every azimuth. Per Invariant 1 pose HAS ground truth the actor cannot author (the root quat) ⇒ the hard channel is the CORRECT pose verifier and a VLM the strictly weaker one; VLM eyes stay reserved for object identity/placement (no deterministic channel exists there). Landing pose_upright into the verify spine = CEO gate G-318-1; sunk-through-floor is orientation-upright ⇒ a SEPARATE deterministic z/height channel, not this oracle's job → E109. R319/E110 BUILDS that companion — `tools/acceptance/pose_height.py` (`height_dev`=root_z-stance_z(0.35), `classify` SUNK/NOMINAL/AIRBORNE at tol0.12, TDD 13/13): the PAIR (orient AND height) reads 4/4 EXACT R316 topologies; on `sunk_floor`(z0.07) orientation-alone reads STANDING (MISS) but height reads SUNK ⇒ the pair catches it and height is the SOLE witness where minicpm SPLIT the frame by prompt framing (E108). Same honest scope as E109: an offline oracle on ground-truth quat/z (a body cannot STAND at 0.07 m), NOT the acceptance face ⇒ experiments row only; landing the pair as a verify-channel is CEO gate G-318-1 → E110. R321/E112 EXTENDS the hard-channel family to RELATIONAL placement — the placement geometry already deterministic is ABSOLUTE-region containment (make_placed_count/resting_on_receptacle, D106); "put A NEAR B" had NO hard channel, only vlm-judge. `tools/acceptance/place_near.py` (`separation` xy|xyz, `proximity_margin`=radius-sep signed, `classify` NEAR/FAR; TDD 18/18, sim-free proof 5/5) grades the GEOMETRY half from two GT positions (unforgeable, Inv-1) — incl. the case a VLM can't give: B stacked over A reads NEAR(xy) yet FAR(xyz). grep-confirmed no prior near(a,b) predicate. Same honest scope as E109/E110: offline oracle NOT the acceptance face → experiments row only; identity ("which is B") stays the VLM/D182 job; landing = CEO gate (relational near(a,b)) → E112. R322/E113 adds the FOURTH channel — HEADING: the trio reads own-pose+proximity but NONE reads whether a robot is TURNED TOWARD a target (the VLN 'go to/face X' question, E14/D178). `tools/acceptance/heading_facing.py` (`yaw_from_quat` homogeneous atan2/scale-invariant, `bearing_to`=atan2(dy,dx), `heading_error` wrapped [-pi,pi], `facing_margin`=tol-|err| deg, `classify` FACING/AWAY; TDD 30/30, sim-free proof 6/6) grades heading from GT root yaw + resolved target pos (unforgeable, Inv-1) — incl. the VLM-blind case a single frame can't read: target directly BEHIND = AWAY (a frame can't see behind), and it is distance-orthogonal so it COMPOSES with place_near (approached AND oriented) without duplicating it. Same offline scope as E109/E110/E112 → experiments row only; identity stays VLM/D182; landing acceptance/+rubric = CEO gate (facing verify-channel) → E113. R323/E114 PACKAGES the quartet for landing — `tools/acceptance/hard_channels.py` binds the four cores as GT-reading fail-safe-False bool predicates (upright/nominal_height/near/facing_target) in the exact `go2_sim_oracle.build_verify_namespace` shape (`make_verify_namespace`+`verify_signatures`+unified CLI), fake-base TDD 27/27, cores 72/72 unchanged. RED-TEAM on `get_odometry` CORRECTED a first-read error (I'd deferred upright as an interface gate): the base ALREADY exposes the root quaternion (`Odometry` qw..qz), so ALL FOUR land on the EXISTING base API (get_position/get_heading/get_odometry) with NO interface change ⇒ the whole quartet lands as ONE verify-ns merge + `_PREDICATE_ORACLES` entries (WIRING verify-spine §) = a SINGLE CEO gate G-323-1, not four. Offline scope unchanged (experiments only, no acceptance row); identity stays VLM/D182 → E114. R324/E115 GUARDS that landing — the E114 packaging is unit-tested vs a FAKE base, so it would stay GREEN even if the real seam drifted (get_odometry dropped / Odometry loses its quat / a landing name shadows an existing verify predicate) and the "no interface change" claim rotted UNNOTICED. `tools/acceptance/hard_channels_doctor.py` (`diagnose()` + 4 pure check-fns packaged_shape/base_api_surface/reference_seam_shape/landing_delta + a fake-agent dry-run; CLI `--json` exit 0 iff READY-TO-LAND; TDD 12/12 incl 4 failure-detection, cores+pkg 99/99 regression) reads the REAL kernel modules read-only and asserts the four R323 landing preconditions. PROVEN not a green-always stamp: deleting real `BaseProtocol.get_odometry` in-process flips `diagnose()` to False naming the missing method, restore recovers. Non-spine (tools/), no sim; the offline oracle well stays DRY (a 5th core = treadmill); the ONLY lever remains the owner approving G-323-1 → E115.
- BYO-model family N≥4 (mistral-small ready on OpenRouter) when credit restored → D181.
- Plug-and-play verify-predicates: `_PREDICATE_ORACLES` is hardcoded, but a BYO predicate IS still GRADED zero-kernel-edit (R325/E116) — the runtime grader `classify_verify_expr` (via evidence_passed) takes `oracle_names`=keys of the ns merging `build_verify_namespace`, so a BYO name GROUNDS via `pred()==True`/`state()==const`; ONLY bare `pred()` needs the hardcoded set (=WHY G-323-1 edits it; world-declared predicate metadata is a META gate). Moat holds (short-circuit/self-taut/absent stay RAN); guard test_plug_and_play_boundary.py 14/14. R327/E118 lifted the proof from CLASSIFIER to RUNTIME: a BYO predicate is delivered through the REAL engine seam (`verify_oracle_names`->`_build_verifier_namespace`->`_merge_world_verify_namespace`->`build_verify_namespace`) and clears the done-gate `evidence_passed` END-TO-END, zero kernel edit — non-vacuous (name absent w/o `engine._world`) and moat-preserving (UNCAUSED->RAN, engine=None fails closed). Promoted R326/E117 into docs/WIRING.md verify-spine "Seam for a new world" bullet (was misleading — implied the gated `_PREDICATE_ORACLES` edit was required for a BYO predicate).
- Embodiment ladder: S4 one-generic-driver → S5 policy plugin → S6 capability
  planner-exposure (all CEO-gated) → docs/ARCHITECTURE.md §3.
- AMBITION CRITIC (8 consecutive: R230→resolved R234-6 · R240 · R250 · R260 · R270 · R280 · R290 · R310): the loop's real bar is a NEW 3rd embodiment (S4 BYO URDF) or the D182 world-owned NL→object grounder — both CEO-gated, UNANSWERED ~45 rounds; more colours/objects/worlds is a LOCAL HILL and verify stays WITNESS-ONLY/self-certified (actor authors the target string; only eyes disambiguate). R230's silent perception-402 confound was FIXED (repl_accept defaults local gemma4:e4b, E54). Recommendation "close place-transfer then HOLD at the gates" went UNHEEDED as escalation — owner must unblock S4/D182. R290 PLATEAU CALL: R263-R290 (~28r since the R262 "non-gated frontier EXHAUSTED" call) are ALL stale-bar hygiene; courtyard fetch sweep now EXHAUSTED (green/blue/red/purple re-confirmed R287-290) — a 7th sibling adds ZERO capability. PIVOT: aim remaining hygiene at the genuinely-oldest UNTESTED bars (g1.nav age73 · g1.perception 65 · quantity-place 85), NOT more courtyard colours; NEW capability still needs owner S4/D182 → E54/E55/E57/E61/E83. R310 PLATEAU CALL (deeper): R290→R310 (~20r more) = eyes-pillar place-verify (E88-92), stale-bar closures (E93-98), quantity SETTLED-as-gated (E96-100), guardrail regression-pin (E99) — ALL hygiene/consolidation, ZERO new capability, so ~48 rounds (R262→R310) treading water. The BOARD is now fully current-face + uniform vlm-judge eyes AND quantity is closed-as-gated: the non-gated hygiene backlog itself is ~exhausted, leaving NOTHING non-gated that advances the North Star. ESCALATION (louder than R290): the loop yields diminishing returns every round it runs unblocked; owner MUST act on S4 (3rd embodiment) / D182 (NL grounder) / D76 (planner-plugin) or the loop should drop to review-only cadence. A non-gated brain SWAP (deepseek-chat, E40) is the only config-axis left but it is FLAKY 2/3 (E72) — not a reliable close → E101. R311/E102: the recurring "supervisor rounds.jsonl append ticks BOARD ages +1 → next round finds newly-STALE rows" pattern (R309→R310→R311) is NOT a bug — it is the skeptic-cadence working; do NOT read it as an R310 mis-verify (STALE_AGE=30 strict, both R280 rows were age30 at R310's commit). HYGIENE FIX: runner.degenerate-spin-guard.no-regression is a REDUNDANT duplicate BOARD key of base runner.degenerate-spin-guard (both = the E76 healthy-fetch-guard-SILENT assertion) — the two staggered clocks doubled the stale work; consolidated the alias into the base via a superseded row (no sim: R310 freshly verified the identical content under the base key at age1). Lesson: a capability split across two near-synonym BOARD keys perpetually double-stales — fold aliases into ONE key → E102.
- RE-RED-TEAM (R260, R259 "courtyard PLACE CONFIRMED, place transfers zero-config"): SURVIVES with a fidelity refinement — confirmation rests on 4/4 POST-GUARD GROUNDED runs (R257×2 + R259×2) graded by the resting_on_receptacle GT oracle the actor cannot author (moat holds); the pre-guard non-grounds (R246 2/3, R255 0/2) are legitimately the guard's target. HONEST: "zero-config" is WORLD-config (Inv.3: no courtyard place code) — TRUE; but place-composite RELIABILITY required a GENERAL R257 runtime guard that post-dates the HOUSE place.nl-plain-colour confirmation (now ~age 61, STALE, NOT re-verified post-guard — a future skeptic target). No overclaim; caveats were disclosed → E61/R260.
- PLACE-transfer question CLOSED → non-gated NEW-capability frontier EXHAUSTED, HOLD at the gates (R262/E62, executing the R260 critic's plan): warehouse PLACE PROMOTED to confirmed (R261 N=1 + R262 N=1 = E46 N≥2; bare face, VECTOR_ROOM_TEMPLATE=warehouse MODE=place, deepseek-v4-flash + local gemma4:e4b, resting_on_receptacle=place_bin(10.95,4.60) live-AABB GT, native seq CLEAN grasp→hold(green,CAUSED)→place→verify→finish ZERO re-grasp, R257 guard held, launch_explore_seen=False; eyes: green resting in oak receptacle in the concrete/yellow-lane warehouse shell). PLACE now GROUNDED-confirmed on ALL 3 worlds (house + courtyard R259 + warehouse R262); fetch grounds all 3. Per R240/R250/R260 (3 consecutive critics), every remaining NEW bar is a CEO gate — S4 (3rd embodiment BYO URDF, kernel/interface) and D182 (world-owned NL→object grounder, spine-semantics, also fixes witness-only fidelity) — UNANSWERED ~22 rounds; "place-on-Nth-world / more colours / 4th world" is the SAME local hill. STATUS phase→blocked: the loop cannot advance NEW capability without an owner gate decision. Non-gated MAINTENANCE remains (stale confirmed BOARD rows ~55-82r owe a skeptic re-verify; R270 review due) so the loop treads water on hygiene, not new frontier → E62/R262. R263/E63: skeptic re-verified the OLDEST stale row fetch-place.nl-category-only (age 79, orig deepseek-chat) on the CURRENT deepseek-v4-flash+local-gemma4:e4b seam → GROUNDED 1/1, category '罐子'→the only can (pickable_can_red) actor=CAUSED, eyes: red can aloft/4 distractors remain=discrimination — re-confirmed AND provider-refreshed (first v4-flash run of this cap); next stale target = fetch-place.nl-compound (age 76) → E63/R263. R264/E64: skeptic re-verified fetch-place.nl-compound (age 77, R186 GROUNDED 2/2 under OLD native_loop grading) on the current v4-flash+local-gemma4:e4b seam (MODE=combo, '把绿色的瓶子拿过来放到架子上') → does NOT reproduce: RAN verified=False (2/4). The compound decomposes correctly and the place PHYSICALLY succeeds (eyes: green resting on receptacle, gripper empty; GT resting_on_receptacle()>=1 TRUE, unauthorable), but the place-leg CAUSATION is NOT_GRADED in the same single session (mobile_place→resting NOT_GRADED; bare placed_count() UNCAUSED) — the E17-class causation-safety limit; grasp leg still grounds CAUSED. R186's 2/2 predates the stricter causation grader (Inv.1: grading only tightens) so it does not carry forward → the stale green correctly flipped to provisional/red. LESSON: a compound single-utterance PLACE cannot be causation-graded cleanly in one session; a "confirmed" compound row from an era of looser grading is a prime skeptic target — its place leg is witness-real but not actor-attributable. Next stale target = fetch.nl-ordinal-spatial (age 67) → E64/R264. R268/E65-66: skeptic re-verified fetch.nl-ordinal-spatial (age69, R197 2/2) on the v4-flash+local-gemma4:e4b seam (MODE=fetch '把最左边的瓶子拿过来') → does NOT reproduce: RAN verified=False (0/5). perception_grasp found NO object — mask_px=0, none-plausible across all 6 scan steps, green n_hue_anywhere~27-56 (green not in front-cam view) → nothing held → all verify legs UNCAUSED; scene DID load (24 objs) so not empty-sim. R197 confirmed by MEMORISING leftmost→green from that round's layout (see ordinal lines above: the ordinal is PRE-RESOLVED to a colour, then a plain colour-grasp) — when the memorised colour isn't in front-cam view the grasp finds 0px → REFUTED, supersedes R197. CONTROL (E66): re-verified fetch.nl-negated-distractor (age68, R200 1/1) on the SAME seam/session → GROUNDED 1/1 actor=CAUSED, eyes: gripper holds RED can, blue/green/yellow distractors untouched → proves the 0px failure is ORDINAL-specific (memorised-colour-not-in-view), NOT a systemic perception regression. LESSON: an ordinal 'confirmed' row is doubly fragile — it depends on BOTH the VLM pre-resolving the ordinal AND that colour landing in the front camera; a stale ordinal green is a prime skeptic target. Next stale target = place.nl-plain-colour (age68, MODE=place) → E65/E66/R268. R269/E67: skeptic re-verified place.nl-plain-colour (age68, R200 2/2) on the v4-flash+local-gemma4:e4b seam (MODE=place '把绿色的瓶子放到架子上') — the FIRST re-check since the R257 post-place re-grasp guard landed (R260 red-team flagged it STALE-not-verified-post-guard) → RE-CONFIRMED GROUNDED verified=True (2/2): perception_grasp→holding_object(bottle_green) actor=CAUSED, mobile_place→resting_on_receptacle()>=1 GT-True, native seq CLEAN ZERO mobile_pick re-grasp so the R257 guard HELD without regressing plain-colour place; eyes green resting in receptacle/gripper empty/red+purple remain=discrimination. supersedes R200. NOTE: the default qwen routing brain is now in ARREARAGE (400) — MUST force the deepseek provider (Hazards L35). Next stale target = quantity-place.nl-isolation (age64, MODE=place) → E67/R269.
- R270/E68 REVIEW: skeptic re-verify of the OLDEST stale row quantity-place.nl-isolation (R204 age65, MODE=seq two single-object places) did NOT cleanly reproduce — turn1 place-BLUE RE-CONFIRMED True(2/2) grasp CAUSED + resting GT-True, but turn2 place-GREEN emitted 0 verdicts (brain looped detect(green)+perception_grasp(green) with varying bboxes ~15min, grasp never completed/grounded = the KNOWN green-2nd-object thrash E40/E45). Place MACHINERY holds (turn1); the 2nd-object place was never REACHED (grasp thrash before place) → R204 is repro-QUESTIONED, NOT refuted (turn1 fine; turn2 = known green-grasp flake, not a machinery disproof) — provisional PARTIAL row, adjudicate next round with a dedicated clean re-run. LESSON: a MODE=seq isolation 'confirmed' whose 2nd object is GREEN inherits green's grasp-completion fragility — the row's reproduction depends on a lucky green grasp, so it is a doubly-fragile skeptic target (cf. ordinal E65). RE-RED-TEAM cracked R262's 'non-gated frontier EXHAUSTED' (see the vision-judge line above): a LOCAL gemma4:e4b acceptance judge is a genuinely NON-gated verify-pillar build the loop mislabeled external-blocked → E68/R270. R273/E72 ADJUDICATED refuted+superseded: the dedicated clean re-run (turn1蓝, turn2红 — deliberately AVOIDING green) had turn1-BLUE ITSELF THRASH 0 verdicts/~15min (degenerate look3726/pgrasp1573/nav1608/native-describe1781 loop) — so R204's confirmed GROUNDED 2/2 is NON-REPRODUCIBLE and the thrash is NOT green-specific: the deepseek-v4-flash routing brain's place-decompose is FLAKY run-to-run (same non-determinism class as the R272 gemma4:e4b judge). 2 refuted rows supersede the R270 PARTIAL provisional + the R204 confirmed. The machinery-vs-BRAIN LESSON (R199 quantity fail is brain-decomposition E38, not grasp-exec) STILL HOLDS; do_not_retry_unless a decompose/perception-loop guardrail (cf. R206 quantity guard, R257 post-place guard) or a stronger routing brain lands → E72/R273.
- R320/E111 REVIEW plateau call: R312-R319 were 8 straight eyes-pillar probe rounds with ZERO acceptance-face advance. The pose lever is now SETTLED (deterministic orient+height pair E109/E110, reads 4/4 R316 topologies where every local VLM failed) yet SHELVED behind G-318-1 — the face is GATE-blocked, not idea-blocked. The honest ambition move is NOT another eyes probe: it is the owner unblocking ONE gate (G-318-1 cheapest+most-proven; then S4/D182). R320's real-face skeptic re-verify was SIM-BLOCKED (a sibling loop held the one sim slot, Inv-5); sim-free E110 re-verified 4/4 as substitute → E111/R320.
- R329/E120 (11th sim-block): probed whether the North-Star RECOVER leg (plan·route·verify·RECOVER) is an overlooked NON-GATED frontier the plateau missed (STATUS/frontier never named it; src grep shows 0 top-level replan symbols) → REFUTED as an exit. Recover IS already built: `cognitive/vgg_harness._obs_replan_decompose` + `_retry_strategy` (observation-driven replan/decompose), `core.config.max_planning_retries`, and skill-level retries (`skills/pick.py`). Not a gap. Doctor stays GREEN (G-323-1 READY-TO-LAND 4/4, additive). Genuine research probe, NOT an offline-proof treadmill — do NOT re-audit recover; the two levers stay sim-free-slot OR owner acts on G-323-1 → E120.
- R330/E121 REVIEW rot-audit (12th sim-block): the 18-round plateau (R312-R330) has NOT rotted its substrate. The 30-round-stale embodiments WIRING card (verified-against R300) checked line-by-line ACCURATE vs code (resolver GATED/ENRICHMENT split capability_profile.py:17-30; 3 consumers native_loop:1085/1112, robot:34, engine:428 exact) → stamp refreshed to e3d9cbc, NOT deleted. 55/55 confirmed-row guards GREEN sim-free (g1 cap-gate, capability_profile, category+ordinal Case-17, detector cam-gate; 91M peak) = no silent guard-rot behind the STALE BOARD rows. A rot-audit is NOT a banned plateau re-audit (tests the docs+guards SUBSTRATE, not plateau-honesty E119 / recover E120). The real-face BOARD skeptic re-run stays OWED (sim-blocked 12th, R320-R330); the two levers unchanged (sim frees OR owner acts G-323-1) → E121. R340/E130 REVIEW ambition-critic: native-loop WIRING re-verified accurate (anchor→a52226e), gate/token clean, E120 recover RE-CONFIRMED — but the R312-R340 plateau (29 rds) is a HARD EXTERNAL block, NOT a local hill: both exits (owner answers G-323-1 queued 23 rds; OR sibling sim frees) sit outside the round's authority. Honest move = escalation + a longer PARK cadence, not an ~hourly near-identical HOLD commit → E130. R357-R359 broke the HOLD reflex 3x with REAL non-gated substrate hardening (machine-enforced embodiment manifest↔driver fidelity, symmetric go2+g1, incl. lidar mount offset E147/E148/E149) — genuine BUILD, not slivers — but that manifest-mirror vein is now NEAR-SATURATED (STATUS): the only remaining sibling mirror is the vnav-bridge _SENSOR_X/_Z, guardable only if the ROS2-pulling bridge imports sim-free. R360/E150 review: E149 headline SURVIVES independent re-red-team (drift driver const ⇒ guard RED), WIRING verify-spine+embodiments prose re-verified accurate, gate/token clean — the plateau stays HARD-EXTERNAL (39th sim-block; G-323-1 unanswered 43 rds). Future hardening likely lies OUTSIDE the manifest mirror; a clean HOLD beats a manufactured sliver → E150. R361/E151 CLOSED the named vnav-bridge gap: the bridge imports rclpy+MuJoCoGo2 at load, so an import-based guard cant run in CI — instead read its _SENSOR_X/_Z float literals via `ast` (no import ⇒ ROS2/sim-free) and assert they mirror driver _LIDAR_OFFSET. DISCRIM: drift driver 0.3→0.77 ⇒ guard RED (bridge-via-ast vs driver-via-import = both real sources). The driver↔manifest↔bridge lidar-offset vein is now SATURATED (all 3 go2 mirrors + both g1); the ast-read technique generalises to any ROS2/sim-pulling module whose plain literals mirror a driver const → E151. R362/E152 + R363/E153 extended the same anti-stale-list idea to FROZEN-dataclass drift guards (introspect EVERY dataclass in a module, not a hardcoded list): the vcli verdict contract then the acceptance-face verdict/decision contract now go RED on a frozen-drop. R364/E154 flipped the Inv-4 kernel-import-purity guard from a HARDCODED denylist (`_CONCRETE_WORLD_MARKERS`, which a new `worlds/<name>.py` or `*_oracle.py` escapes silently) to a SEAM ALLOWLIST: kernel+CLI import must leak nothing under `vcli.worlds.*` except {pkg,base,registry}; live DISCRIM — a simulated leak of `g1_perception_oracle` gives denylist=[] (false pass) vs allowlist=[oracle] (RED). RECIPE for any guard: prefer allowlist/introspection over an enumeration that rots → E154. R365/E155 applied the same introspection frozen-guard to `actor_causation.ActorBaseline` — the pre-step actor snapshot causation grades against (an enumerated check.sh SPINE path, Inv-1 GT the actor cannot author) — the LAST unguarded spine frozen contract; DISCRIM: frozen-drop ⇒ param RED, a NEW un-frozen dc in the module ⇒ introspection RED. Spine frozen-contract coverage now complete (verdict+acceptance+cognitive-types+actor_causation) → E155. R366/E156 carried the SAME introspection frozen-guard OFF the spine into the plug-and-play morphology loader `embodiments/config.py` (Inv-3 config-not-code, Inv-7): its 7 frozen dataclasses were guarded only by a single top-level `EmbodimentConfig.id` assignment — the 6 nested specs (Model/Spawn/Sensor/Policy/Grasp/CapabilityProfile) had none, so a frozen-drop on any shipped green. New test introspects EVERY dataclass in the module + a live nested-spec FrozenInstanceError; DISCRIM: frozen-drop nested SensorSpec ⇒ that case RED, a new un-frozen dc ⇒ discovered+RED. Distinct NON-spine NON-mirror vein → E156. R367/E157 found the next distinct vein: `catalog._TABLETOP_OBJECTS` (arm-playground catalog names catalog.py:22 says MUST match so101_mujoco.xml bodies — the contract the sim-oracle grades against) was guarded ONLY by `test_playground_real_arm` (`@pytest.mark.integration` + a real MuJoCoArm sim), so the deterministic `-m not-integration` gate had NO catalog↔MJCF guard: rename a body or edit the tuple ⇒ CI stayed GREEN. New `tests/unit/playground/test_catalog_mjcf_fidelity.py` parses `scene_xml` via stdlib ElementTree (no mujoco/no sim slot), introspects SCENARIOS (not a hardcoded list), asserts each object_name is a real `<body>`; DISCRIM: bogus tuple name ⇒ 3 RED, rename MJCF mug→cup ⇒ 3 RED (the drift the integration-only guard misses in the fast gate). RECIPE: a load-bearing catalog↔artifact set-membership mirror guarded only by a sim-instantiating integration test is UNGUARDED in the deterministic gate — mirror it offline → E157. R368/E158 CORRECTS the E151 "SATURATED" over-claim: the lidar-offset vein had a FOURTH downstream go2 mirror it missed — `piper_ros2_proxy._BODY_SENSOR_DX/_DZ` (comment-pinned must-match bridge _SENSOR_X/_Z 0.3/0.2; `_sync_ik_base` subtracts it, so drift ⇒ wrong IK body pose ⇒ piper arm reaches wrong place). Added `test_piper_proxy_sensor_offset_matches_driver` (ast-read, sim-free, symmetric with the bridge guard); DISCRIM: drift DX 0.3→0.35 ⇒ RED, rename const ⇒ RED loud not-found. LESSON: a "vein SATURATED" claim must ENUMERATE all mirror copies (grep the pinning comment across ALL drivers/proxies), not stop at the first robot's chain — the vein now covers all 4 go2 copies (driver+manifest+bridge+piper proxy) + both g1 → E158. R372/E161 found a DISTINCT arm-vein OUTSIDE the go2 lidar chain: the SO-101 home pose is copied into `config/default.yaml` (the RUNTIME AUTHORITY the home skill commands via `load_config()["skills"]["home"]["joint_values"]`), the verify oracle `arm_sim_oracle._HOME_JOINTS` (the moat `arm_at_home()` grades), and 4 skill `_DEFAULT_HOME_JOINTS` fallbacks; `test_playground_predicates` pinned oracle==home-fallback but NOTHING pinned the chain to the config AUTHORITY (`test_config` checks only len/type) — drift `default.yaml` and the arm reaches a pose the oracle never grades (silent Inv-1 breach). `test_so101_home_pose_chain_mirrors_config_authority` pins oracle+4 fallbacks == `load_config` authority; scan EXCLUDED (distinct semantic pose). DISCRIM: drift `default.yaml` -0.014→-0.099 ⇒ RED, revert+clear-pyc ⇒ 13/13. NOTE the true authority is `default.yaml`, NOT config.py's inline dict (that is the missing-file fallback only — a lower-severity still-unpinned copy) → E161. R376/E165 found a DISTINCT perception-path vein: the go2 head-cam resolution is a comment-pinned cross-file mirror (`manipulation_setup.py:62` "bridge publishes 320x240; intrinsics must match") with NO enforcement, and the `Go2GraspPerception` class DEFAULT had drifted to a STALE 640x480 (original feat commit 19ef11d's arbitrary value) while 3 authoritative refs agree on 320 — `Go2ROS2Proxy.get_camera_frame` default, `mujoco_intrinsics` default, and the deliberate grasp pin (eddf8ad). The stale default leaked into the look/explore ACCEPTANCE path via `_build_go2_perception` (uses the class default), so sim look/explore rendered at a resolution the real robot never publishes — a sim-vs-real fidelity + look-vs-grasp divergence (NOT a wrong-math bug: intrinsics are res-consistent per instance). Fix = single-source `GO2_HEAD_CAM_WIDTH/HEIGHT=320` in go2_grasp_perception, default+launchers reference it; drift-guard asserts the constant == `Go2ROS2Proxy.get_camera_frame` default via `inspect.signature`. DISCRIM: tamper const→640 ⇒ 2 guards RED, restore ⇒ 13/13 GREEN. LESSON: a "must match the bridge" resolution/param comment is a mirror like any const — grep the DEFAULT ARG that silently leaks the stale value into the acceptance path, not just the explicitly-pinned call site → E165. R380/E169 REVIEW (58th sim-block; R312-R380 = 68-round plateau; both levers SHUT): the E165/E167/E168 head-cam headlines SURVIVE independent re-red-team — E165 single-source guard 13/13 + GO2_HEAD_CAM_W=320 wired to the look/explore accept path via `_build_go2_perception` (LIVE-claim honest); E167/E168 renderer-dims regr 9/9; NO non-default g1 `G1HeadPerception` caller exists (sim_tool:427 passes 640x480) ⇒ E168 LATENT-claim honest. WIRING all ≤21r (<25 refresh bar), gate/token 0 approvals, Casebook 11/15 — substrate rot-free; the R312-R380 plateau is HARD-EXTERNAL not a hidden local hill → E169. R664/E187 broke the HOLD reflex with a fresh NON-mirror NON-frozen vein: the plug-and-play manifest loader `embodiments/config.py` CLAIMS fail-loud yet coerced spawn xy/height/heading, sensor pos/euler + stance via bare `float()`, SILENTLY accepting NaN/±inf (YAML parses `.inf`/`.nan` natively ⇒ reachable from a real BYO robot.yaml) — non-finite geometry poisons the sim + Inv-1 verify. Fix = `_finite()` rejects loud+field-scoped, EmbodimentConfigError<ValueError (no caller regr); RED 5/5→GREEN 62/62. LATENT (no shipped manifest non-finite). RECIPE (extends the security floor): a loader advertising fail-loud that converts untrusted numeric input via bare `float()` must `math.isfinite`-gate EVERY geometry field — audit all float() SITES, not just key presence/type → E187. R665/E188 EXTENDS that recipe from the loader to the ACTUATOR boundary: `move_joints` validated only `len(positions)`, so a NaN/±inf from `config.skills.*.joint_values` (BYO-skill config, YAML parses `.nan`/`.inf`) or the ROS2 `hardware_bridge` (`move_joints(ordered)`) was written straight into MuJoCo `ctrl` and returned True — a security-floor violation ("reject NaN before acting") poisoning sim + Inv-1 verify. Fix = ONE pure `ensure_finite_joint_targets()` in `hardware/arm.py` wired at the entry of ALL 6 `move_joints` impls (mujoco_arm/piper, so101, pybullet, piper_ros2, isaac); a source-level wiring test (ast-discovered, not a hardcoded list) makes it MIRROR-safe so a new driver can't skip it. RED import-fail→GREEN 14/14, 184/184 regress. LATENT (no shipped config non-finite; IK returns None not NaN). RECIPE: a 'reject NaN before acting' guard belongs at the single command boundary every source funnels through → E188. R666/E189 carried the SAME recipe to the THIRD command boundary — the BASE-velocity `set_velocity(vx,vy,vyaw)` (streaming cmd_vel path): `MuJoCoGo2` clamped with `np.clip` which CLAMPS ±inf but PROPAGATES NaN (`np.clip(nan,lo,hi)==nan` verified), `MuJoCoG1` did bare `float()` with no bound/check at all, `Go2ROS2Proxy` republished `float()` onto `/cmd_vel_nav` — so a NaN/inf from the vcli locomotion primitive (`vcli/primitives/locomotion.py:80` passes vx/vy/vyaw with NO upstream guard) or the Nav2 bridge reached the actuator AND poisoned the `_cmd_motion` R2b Inv-1 signal (`nan > MOTION_EPS` is False ⇒ a real motion registers as none). Fix = pure `ensure_finite_base_velocity()` in `hardware/base.py` (Protocol-only, no hw/ROS2 import) wired at entry of all 3 `set_velocity` impls; ast-discovered wiring test = MIRROR-safe. RED import-fail→GREEN 14/14, 200/200 regress. LATENT (no shipped config non-finite vel). RECIPE: `np.clip`/magnitude-clamp is NOT a finiteness gate — it silently passes NaN; a "reject NaN before acting" boundary needs an explicit `math.isfinite` check, and every actuator command boundary (joints AND base velocity) needs one → E189. R667/E190 carried the SAME recipe to the nav-GOAL boundary (the pose-target sibling of E189's velocity): `navigate_to(x,y)`/`publish_goal(x,y)` handed the goal to an EXTERNAL nav stack via bare `float(x)` with NO finiteness check — `Go2ROS2Proxy` published it onto `/goal_point` (FAR planner), `NavStackClient` onto the waypoint topic / a NAV2 action goal — reachable from the CaP-X nav primitive (`vcli/primitives/navigation.py:publish_goal`) and the `navigate` tool call (`json.loads` accepts the `NaN`/`Infinity` tokens ⇒ `{"x":NaN}`→`float('nan')`); a poisoned goal escapes onto ROS2 while invisible to the honest-verify spine — a fail-open. Fix = pure `ensure_finite_nav_goal()` in `hardware/base.py` wired at all 4 `navigate_to` sinks (go2_ros2_proxy, mujoco_go2/g1, core NavStackClient) + `publish_goal`; ast-discovered wiring test = MIRROR-safe. RED→GREEN 16/16, 216/216 regress. The mujoco vgraph drivers are fail-SAFE downstream (plan_path→unreachable OR the E189 set_velocity gate catches NaN waypoints) but gated too for mirror-safety. RECIPE: the finiteness vein spans BOTH command boundaries AND goal/target boundaries — audit every sink that pushes an untrusted number to an actuator OR to an external planner topic → E190.
- EvolvingLoop as an explicit, visualizable, standalone protocol/product — deferred by CEO until this repo's internal doc problems are fixed (2026-07-01 direction).

## Casebook — hidden bugs (symptom pointed away from cause; newest first; cap 15 cases, overflow folds oldest to one line under ### Folded)
Compressed from docs/tricky-bugs.md (removed 2026-07-02); full original prose in git history.
Only IMPLICIT bugs belong here — symptom pointed away from cause, survived a green suite, or hid behind "every component correct in isolation". Routine bugs → git history.

- **Case 19 (an accepted "inert" ruling silently became a LIVE gate, 2026-07-04)** — D72 ruled `has_arm` an
  ENRICHMENT flag (`resolve_capability_profile`, runtime-OR-declared) safe because "no gate reads it"; D175 then
  gated manipulation on `has_arm` (`native_loop`), invalidating that premise — a manifest declaring `has_arm:true`
  with no runtime `_arm` is offered pick/place/… it can't execute. LATENT (both manifests declare `has_arm:false`).
  LESSON: a ruling calling a design "inert because nothing consumes X" reopens the moment a later round consumes X — grep every consumer. Fix CEO-gated (D72). Pinned test_declared_arm_gate_tension.py → E172/G-383-1
- **Case 18 (a drift guard uncovered a DEAD safety-net, 2026-07-04)** — a test DRIVING config.py's
  missing-file fallback (monkeypatch `_DEFAULT_YAML`→nonexistent, call `load_config()`) crashed with
  `NameError: 'logger' is not defined` at config.py:116, not the drift AssertionError I hunted: the
  `except FileNotFoundError` graceful-defaults branch logged `logger.warning` but config.py never
  imported logging — the whole hardcoded-defaults safety-net was dead code that raised, survived every
  green suite because no test ran the branch (default.yaml always present). Lesson: guard degradation
  branches by EXECUTING them, not by reading their literals. Fix: add logging import + module logger. → E162
- **Case 17 (ordinal "grasp rotted" = a NEW object silently changed the answer, 2026-07-04)** —
  fetch.nl-ordinal-position-invariance (`把最左边的瓶子` SCENE_SWAP) went GROUNDED 2/2 (R210) → REFUTED
  0/2 (R284, logged as "held the WRONG yellow / grasp EXECUTION rotted"). Real cause (R299/E90, skill-
  direct probe swap + no-swap control): resolver AND grasp are BOTH correct — they resolve `最左边的瓶子`
  to YELLOW and grasp it cleanly in both configs. Yellow (world-y=3.11, larger-y ⇒ smaller image-cx ⇒
  more LEFT than blue@3.0) was ADDED R211, AFTER the R209/R210 bar fixed "leftmost=blue" with only
  green+blue present — so yellow silently BECAME the true leftmost, and being unmoved by the swap it
  even masks the swap. R284's `verify holding(blue)` was the BRAIN's guess (a CAUSED verify attributes
  the preceding action, not the skill's resolved target). Lesson: when an ORDINAL/relational NL bar
  "regresses," re-derive the CURRENT referent from the live catalog first — a later-added object makes a
  once-correct expected-target STALE with no resolver change; fix the bar/oracle, not the grasp. → E90
- **Case 16 (ordinal "grasp miss" = handover-releases-hold, 2026-07-02)** — R194/R195 recorded the
  ordinal fetch `把最左边的瓶子拿过来` as a grasp-EXECUTION miss ("green knocked to the floor",
  verified=False), and 4 rounds chased grasp geometry / the ordinal→colour path. Real cause: the
  grasp SUCCEEDED (grasp_probe grounds green ordinal 5/5; R195b's own log says "The grasp
  succeeded") — then the brain (deepseek-v4-flash) read `拿过来`=bring-to-user and routed a
  `handover`, which RELEASES the weld, so the terminal `holding_object` verdict read False. The
  floor frame was the handed-over bottle, not a miss. Model variance (deepseek-CHAT grasped the
  IDENTICAL utterance and STOPPED→GROUNDED; cf Case 15 / E9/E10/E23 — never trust a pass-rate
  delta across a model change). Fix: native_loop "BRING IS COMPLETE AT THE HOLD" (a bare 拿过来
  finishes at the hold; handover only on explicit 递给我/给我) → 3/3 GROUNDED on v4-flash. Lesson:
  a verified=False on a manipulation predicate can be a LATER action UNDOING a real success — read
  the full action trace to the verdict, not just the failing predicate. → E32/R196
- **Case 15 (compound place-leg walk-loop, 2026-07-02)** — a single-utterance fetch-AND-place
  (`放到架子上`) grasped fine but the PLACE leg walk-looped to a self-invented `at_position(10,5)`
  and never placed (RAN 1/4). Symptom looked like a nav/mobile_place bug; real cause was PROMPT
  cross-talk — `_native_system_prompt` locomotion_guidance framed navigate as the route to
  "REACH a **place** or coordinate", colliding with the place clause; the unbounded navigate-
  RECOVER loop burned all 24 turns. Fix: place_guidance forbids navigate/walk for a place clause;
  locomotion_guidance scopes navigate to an explicit user-given coordinate. → E21/49d6e0c
  CORRECTION (E23/R186): the A/B (OLD pre-fix prompt + SAME deepseek-v4-flash) also grounds
  2/2 — so the fix was NOT what drove 1/4→3/3; the MODEL was (deepseek-chat→v4-flash). Fix is
  correct+harmless but not the cause. Model-sensitivity trap (cf E9/E10): never credit a code
  fix for a pass-rate delta while the model also changed — isolate first. → E23/R186
- **Case 14 (FOV, 2026-06-29)** — far fetch grounds green/blue BOTTLES but red returns no_detections at
  re-perceive; red HSV suspected — yet red mask fired ~1000 px @3.9 m and the seed localized. Root: the red
  object is a CAN, shorter — mask 1000@3.9m → 0 at the ~0.9 m standoff: a close short object falls BELOW the
  head cam's downward vertical FOV. Fix seed: raise tilt / widen standoff. LESSON: "detected far, lost near"
  is a FOV/geometry signature — check object HEIGHT vs camera pitch and the near-field vertical frustum
  before touching the colour gate.
- **Case 13 (mobile_pick, 2026-06-29)** — far fetch `nav_failed`, dog drives to ~(0.5,0.15)=ORIGIN, bottle at
  (13.86,3.0) — looked like nav/frame. Root (detect.py): DetectSkill defaults x,y,z=0,0,0; a FAR object
  yields no depth → the (0,0,0) SENTINEL was stored as a TARGETABLE object; mobile_pick navigated to it.
  Fix: additive `ObjectState.has_position` (default True), False for 2D-only; pick skips position-less →
  honest object_not_found. LESSON: "drives to origin/nav_failed" is often a DEFAULT-SENTINEL leak —
  separate EXISTENCE from HAS-A-USABLE-POSITION.
- **Case 12 (far-fetch, 2026-06-29)** — `把绿色的瓶子拿过来` (VECTOR_FETCH_FAR=1) intermittently RAN/
  no_detections → pointed at perception/colour; but a skill-direct repro GROUNDED (2073 green px) and the
  failing run masked 0 at near-identical depth ⇒ out of frame. Root: FAR `navigate_to` has NO terminal-
  heading control + one-directional ~200° scan (6×0.6 rad, +vyaw only) → bottle in the uncovered ~160° arc
  never faced. Fix: turn to face the known seed xy via `_grasp_ready_repose` before re-perceive; 3/3.
  LESSON: identical depth + different mask ⇒ framing/heading, not detector; when you KNOW target xy, face
  it, don't search.
- **Case 11 (snapshot SIGSEGV, 2026-06-26, ADR-002 S1)** — verdict-time snapshot hook (VECTOR_SNAPSHOT_DIR)
  killed the bare-cli turn exit=-11 before VECTOR_VERDICT on BOTH ROS2 and in-process paths; hook-OFF control
  run clean ⇒ the render itself. Root: MuJoCo GL context is THREAD-BOUND (persistent renderers made on a
  worker thread, touched from emit thread) AND a control thread steps live mjData → torn read. Fix: fresh
  `mj.Renderer` ON the emit thread over an isolated MjData copy; hook failure returns None. LESSON: never
  reuse a renderer across threads or render live data — copy qpos into a throwaway MjData; isolate with a
  hook-OFF control before blaming context count.
- **Case 10 (fakeable grasp, 2026-06-24, D69)** — NL grasp graded verified=True with the gripper EMPTY:
  model wrote grabbed.txt via `file_write`, verified `file_exists`. Every gate fired correctly for the wrong
  reason — the false-green was the ABSENCE of a gate binding a physical-GRASP goal to a GT manipulation
  oracle (cf. D15/D16 coord). Fix (D69): robot world drops file_write/edit/bash; `evidence_passed` requires
  a NECESSARY holding_object/placed_count conjunct. LESSON: an oracle honest for one domain is a FABRICATION
  VECTOR for another — bind goal CATEGORY to an un-authorable GT oracle; a generic dev tool is not a
  generic ACTION.
- **Case 9 (IK base-sync, 2026-06-24, S3b)** — no symptom in unit/spike (green, compiled model
  byte-identical), but grasp silently targets the WRONG frame once the dog moves off-spawn: under
  `MjSpec.attach` the room's pickable freejoints occupy [0:21], go2 at [21], so the literal `live_qpos[0:19]`
  copied PICKABLES → IK FK on a stale base (matched at spawn only). Fix: DofLayout-derived
  `_lo = root_qpos_adr; _n = 7 + num_actuated`. LESSON: byte-identical compiled MODEL ≠ byte-identical qpos
  ORDER — blast-radius-grep ALL absolute qpos[N]/qvel[N] reads in CONSUMERS and e2e a MOVED (not spawn) pose.
### Folded (oldest cases compressed to one line each; full prose in git at the hash)
- **Case 8** (arm-stow nq guard, 2026-06-24, S3b) — bare-go2 arm-stow raised `broadcast (8,) into (0,)` after attach; `nq>=27` assumed "arm⇒nq≥27" but 3 pickable freejoints put BARE go2 at nq=40 (nq is a SCENE property). Fix: gate on `model.nu>=19`; slice at joint_qpos_start+num_actuated. LESSON: never discriminate morphology by nq — use nu/named-element probe. → git history
- **Case 7** (placed_count gate, 2026-06-21) — pedestal-top place (z~0.32) looks OK but `placed_count`=0 forever: `make_placed_count` counts only z<_LIFT_MIN_Z(0.10); D34 reaches further the higher it goes. LESSON: a verify oracle encodes IMPLICIT geometry — measure the target satisfies BOTH reach AND the gate; never loosen (verify only stricter). → git history
- **Case 6** (EdgeTAM degrade, 2026-06-23, R39) — nav→grasp completes but holding_object False; `timm` absent → EdgeTAM failed to LOAD → box-rect fallback → depth centroid averaged can+table. LESSON: when two runs of the SAME perception code disagree, suspect a silently-degrading optional-model path before re-theorizing geometry; make degrade LOUD; env-sync optional deps. → git history
- **Case 3** (dead PYTHONPATH, 2026-06, 13a9429) — explore "worked" on system python3 (mujoco 3.6) not the repo `.venv` (3.9); Python silently ignores missing PYTHONPATH entries. LESSON: print `module.__file__` to verify WHICH copy loaded. → 13a9429
- **Case 4** (blob fusion, 2026-06-20) — deictic grasp 12cm off (table sliver); `front_object._SAT_MIN=140` below table sat → 1-3px chains FUSE cylinders+table into one blob. LESSON: a threshold overlapping bg is a connected-component TOPOLOGY bug; morphological OPEN before components, not a threshold tweak.
- **Case 5** (table-edge occlusion, 2026-06-20) — central GREEN 0px while RED/BLUE fine → depth at its pixel read 3.7m (saw THROUGH it): d435 lip occludes objects within ~6cm of the near top edge. LESSON: one identical object invisible → sample DEPTH at its projected pixel; depth ≫ distance = a static occluder.
- **Case 0** (casadi missing, 2026-06-18) — [PASS]/odom-count/nav-flag all green but dog stays put; casadi
  omitted from the `[all]` extra, imported lazily → qp_fail 149/149 → zero torque. LESSON: NEVER certify
  motion from PASS/odom/nav — measure the position DELTA; hard deps ship in their install set or fail loud
  at connect, never a lazily-imported solver failing silently every tick. → 45798a2
- **Case 1** (two-clock skew, 2026-06) — explore gait limped (飘/瘸腿) though every component was byte-identical
  in isolation; physics ran ~0.65× real-time while `_follow_path` ramped on a 20 Hz WALL timer → profile
  slewed ~1.5× in sim-time → MPC destabilized. LESSON: a wall-clock controller commanding a sim must integrate
  by sim-dt. → d7e158b
- **Case 2** (swallowed MPC errors, 2026-06) — dog stood but never walked, no error: a bare `except: pass` ate a per-tick QP failure (external convex_mpc broke on numpy 2.x (N,1)→scalar, threw every tick → PD-hold only). LESSON: `except: pass` on a control path converts loud failure into silent wrong behavior — "0 failures" must be provable. → git history
