# Architecture Decisions (consolidated)

One line of history per decision — dot-points only. Full original prose is in git history
(`git log --all -- docs/architecture-decisions/`). Append a new entry at the bottom; never
rewrite an accepted one (record corrections as a new dated line). This ledger was compacted
ONCE on 2026-06-23 with CEO approval (entries condensed, every D-number preserved); the full
original prose of every entry lives in git history.

---

## D3 — Hardware Abstraction Layer · Accepted 2026-03-25 (live from v2.0)
- Hardware-agnostic: SO-101 and Go2 are interchangeable adapters into one Agent/Skill/LLM pipeline.
- Formalized `BaseProtocol` + `PerceptionProtocol` (alongside `ArmProtocol`/`GripperProtocol`); dict-based `SkillContext`. Rejected ad-hoc `base: Any` duck-typing (brittle, blocks capability queries).

## D4 — Web Visualization replaces RViz · Accepted (revised) 2026-04-09
- ROS2 web viz via **Foxglove Studio** (revised away from a self-built Three.js viewer).

## D5 — Isaac Sim Integration · Superseded / Paused
- Docker'd Isaac as a high-fidelity sim backend — **paused/archived** during the v2.4 perception pivot.
- **MuJoCo remains the primary sim** for dev + CI; the in-tree Isaac proxy is an interface/topic contract only (no working Isaac SDK code).

## D6 — Agent Kernel / World-Plugin · Accepted 2026-06-05
- Engine + VGG + tools + backends + session + permissions = a **model-/skill-/hardware-agnostic orchestration KERNEL**; each deployment target = a **world plugin**.
- A world registers exactly 4 things: tools, verify/primitive namespace, decompose vocabulary, persona. **The kernel never imports a world.** A default `dev`/`code` world ships in-kernel (a means, not the product); the robot is an optional world plugin. Migration, not rewrite.

## D7 — Open-Loop Compiler → Grounded Closed-Loop Controller · Accepted 2026-06-06 (staged)
- Re-architect VGG from a blind one-shot compiler into a grounded closed-loop controller: per-run `Blackboard`; `GoalVerifier` returns `(bool, raw_value)`; `${step.output.path}` binding (no f-strings over data); control-flow IR (`foreach`/`until`/`if`) + observation-driven mid-tree replan; single-source action space from the skill registry (kill the Go2 split-brain vocab); typed perception facts + `ObjectMemory`; unify `VectorEngine` + `VGGHarness` into one controller.
- NOTE: **D9 supersedes the bespoke-planner direction** — the MODEL, not hand-written decompose/replan code, owns plan/route/recover (D7's closed-loop grounding/observation ideas still hold; its hand-authored planner does not).

## D8 — Playground as a Parallel Track via Seam-as-Contract · Accepted 2026-06-06
- Playground developed as a **separate parallel track**; the kernel/world **seam is the versioned public contract**. Extend it with a verified-loop observation surface (GoalTree + per-step StepRecord + replan notes); v1 = preset scenes entered via NL; deterministic sim-oracle verify + VLM perception.
- NOTE: `feat/playground-vln` was later **abandoned** (never merged); `master` is the base (2026-06-18 reframe).

## D9 — Native Model-Driven Tool-Use Producer IS the Orchestration Design · Accepted (CEO ruling) 2026-06-19
- **CEO ruling: the native model-driven tool-use producer is the correct design; the legacy hardcoded planner (`should_use_vgg`→`vgg_decompose`→direct skill exec) is WRONG and is being strangled. Use+optimize the native producer; NEVER retreat to legacy for any capability.** Routing/decompose/recover belong to the MODEL (the North Star), not bespoke keyword code.
- Cutover (done in the REPL): bare `vector-cli` + NL runs native by default — `cli.run_turn_unified` native-first, falls back to legacy ONLY on a zero-action turn; `VECTOR_REPL_NATIVE=0` forces legacy. Mechanism `vcli/native_loop.py run_turn_native`: the MODEL drives a ReAct loop (world skills + registry tools + synthetic `verify`/`finish`), assembles an `ExecutionTrace` the honest verify spine grades BYTE-UNCHANGED; never computes `verified` itself (moat only ever STRICTER, rule 5). Merge-to-master = separate CEO gate.
- Known native improvements to optimize (NOT reasons to retreat): (#1) nav avoidance — open-loop `walk_forward` walks into obstacles → route go-to-place through the nav stack (→D14); (#2) latency — native is synchronous, several LLM round-trips → async + fewer round-trips (→D22-D23, D42).

## D10 — Recover-by-retry is FAIL-CLOSED by design · Accepted 2026-06-20
- The RECOVER pillar is honest BY CONSTRUCTION (proven on real go2 sim via PTY): a fake "done" (finish while latest verify FAILs) grades RAN/verified False; a never-landing recovery TERMINATES (backend-exhaust, max_turns never hit); a GENUINE in-turn recovery records `[RAN, GROUNDED]` and grades **verified False** — the all-GROUNDED gate fails CLOSED on the recorded FAIL row.
- CONSEQUENCE: a deterministic recover-to-verified-True is UNREACHABLE without a per-sub-goal latest-verify gate (LOOSER, forbidden by rule 5). Recover-to-verified-True is shown only on the LIVE path where the model verifies once after recovering. Goal-authenticity of the verify constant stays the deferred residual.

## D11 — Two moat false-greens closed stricter-only (2nd adversarial review) · Accepted 2026-06-20
- 2nd adversarial moat review found TWO independent false-greens grading a FALSE turn `verified=True`; both fixed STRICTER-only (rule 5), proven flipped to RAN on real go2 sim:
  - (A) **Compare-membership short-circuit**: `True in (oracle, True)` classed GROUNDED via the constant member. FIX: membership (`in`/`not in`) is grounded only if the CONTAINER is oracle-derived; a constant-literal container → RAN.
  - (B) **facing/visited cross-channel causation**: a forward walk laundered a satisfied-at-baseline `facing` no-op to CAUSED. FIX: per-channel — `facing` keys on heading (dyaw), `at_position`/`visited` on planar (dxy).
- REMAINING (open): a minor `visited(room)` intra-room residual; the goal-authenticity residual (a trivially-true verify over the actor's own resulting state) — needs the parsed task goal as structured data.

## D12 — Goal-authenticity for coordinate goals · Accepted 2026-06-20
- Downgrades an otherwise-GROUNDED `at_position` step to RAN when the verify CONSTANT does not match the user's PARSED coordinate goal (within tolerance) — so a model can't walk to the WRONG place and self-certify its own landing. INTRA-PACKAGE (vcli/cognitive), STRICTER-only (rule 5), no CEO gate; the NL goal co-travels with the verify in one ExecutionTrace.
- Mechanism: new pure `vcli/cognitive/coord_goal.py` (`parse_goal_coord`, `at_position_const`, `coord_goal_mismatch`); `classify_step_evidence` gains a `goal_text` kw-default (rule 6 additive). FAIL-OPEN when either side isn't a parseable coordinate. Honest by construction — the target is parsed from the USER's words, never actor-authored. Proven on real go2 sim.
- REMAINING (open): wrong-predicate-type (verify a different predicate); non-coordinate / object / room goal authenticity.

## D13 — Callable-container / reduction short-circuit closed (3rd moat review) · Accepted 2026-06-20
- 3rd review found D12's fix incomplete (rejected only LITERAL containers); a builtin container constructor (an `ast.Call`) bypassed it — `True in tuple((at_position(X,Y), True))`, `len(tuple((...,True)))==2` graded GROUNDED while the robot was nowhere near. FIX (stricter-only): new `_truth_bearing_oracle` — an oracle counts toward GROUNDED only when its VALUE reaches the operand (direct call, index/arithmetic over one, or a reduction whose arg is truth-bearing); an oracle buried in a literal/constructed container is inert. Proven RAN on real go2 sim.
- LESSON: fixing one member of a short-circuit family doesn't close it; the robust fix is a semantic "does the oracle's value reach the truth" predicate, not a node-shape blocklist.
- REMAINING (open): wrong-predicate-type on the default path; legacy-producer goal-poisoning; multi-coordinate phrasing; shape-trivial `len(get_position())==3` xfail. Moat NOT yet dry.

## D14 — Native navigation routes through the avoidance planner, not blind walk (D9 #1) · Accepted 2026-06-20
- Native gains a coordinate `navigate(x, y)` motor tool (go2/base worlds, `native_loop._NativeBaseNavigateTool`) driving `publish_goal` → FAR/local planner/lidar — the obstacle-AVOIDING route `walk_forward` lacks; system prompt steers REACH-a-place to `navigate`, `walk` only for an explicit short step.
- **Honesty invariant**: `navigate`'s cmd_vel runs on the bridge thread, GATED OUT of actor-causation (never sets `_skill_ctrl_tid`), so a `navigate` step's `at_position` verify grades **UNCAUSED → RAN** — honest "ran, can't prove the actor caused it" until causation is extended to cmd_vel. Spine BYTE-UNCHANGED; producer-side routing + prompt only.

## D15 — Wrong-predicate-type closed: a coordinate goal must be VERIFIED with a matching at_position (turn-level gate) · Accepted 2026-06-20
- Closes the wrong-predicate residual: a coordinate goal could pass verified ENTIRELY with a non-`at_position` predicate (`facing`, `len(get_position())==3`) per-step-honest but never asserting the coordinate. FIX (stricter-only, intra-package): `evidence_passed` gains a turn-gate (runs only after all-GROUNDED, can only REJECT) scoped to a go2-coordinate world — require ≥1 GROUNDED step whose verify is a bounded-tol literal `at_position` matching the commanded coord; coordinate-less go2 goals fail OPEN.
- Bonus hardening to `at_position_const`: read kwarg form `at_position(x=,y=)`; reject TOL-INFLATION (`tol`>2.0m) → fail CLOSED. Proven on real go2 sim. REMAINING (open): shape-trivial xfail; legacy goal-poisoning; object/room authenticity.

## D16 — Boolean-necessity closed: a goal's matching at_position must be a NECESSARY conjunct, not merely PRESENT (4th moat review) · Accepted 2026-06-20
- 4th review: the gates checked only that a goal-matching `at_position` was PRESENT, not NECESSARY — so a walk to the WRONG place + a verify with `at_position(goal)` buried under a disjunction/negation/threshold graded True (10 variants reproduced). FIX (stricter-only): `_necessary_at_position_consts` — only `at_position` calls reachable through `BoolOp(And)` ONLY genuinely gate the verdict; an `or`/`not`/arithmetic/IfExp sibling is excluded. Proven RAN on real go2 sim.
- LESSON: PRESENT ≠ NECESSARY; honest test is boolean necessity (reachable through AND), not lexical presence. 4th consecutive structural false-green → moat still NOT dry. This fix was written by a crashed prior round, RE-VERIFIED from scratch and LANDED (don't trust a dead round's claim). REMAINING (open): shape-trivial xfail; multi-coord parse-asymmetry; legacy goal-poisoning; object/room authenticity.

## D17 — Perception-driven Go2+Piper grasp as a native @skill · Accepted 2026-06-20
- The honest manipulation route: LOCALIZE the target by PERCEIVING it (real depth+mask → pointcloud centroid → IK → grasp), never reading sim GT. `perception/grasp_point.py grasp_point_from_rgbd` (PURE; FAILS LOUD with None on <4 depth points, never a fabricated/GT-fallback point; depth in METRES); `perception/go2_grasp_perception.py` + `skills/perception_grasp.py` (@skill), `verify_hint=holding_object('<object>')`. Spine BYTE-UNCHANGED, no CEO gate. Partial real-verify: green/blue grasp points ~4cm from GT (cam→world correct); red miss was a mask-quality artifact (color threshold caught background).
- BLOCKED (env, not code): timm/EdgeTAM, GPU contention, VLM incompat. NEXT: full bare-cli acceptance; the GROUNDED grading-binding (weld + Piper-proxy oracle methods) is a Piper-interface CEO item.

## D18 — Grasp R2: foreground depth-cluster gating + VLM-on-sim-render finding · 2026-06-20
- Hardened grasp geometry: `select_nearest_cluster` keeps the nearest coherent depth band before the centroid → fixes the red-can mask-leak; all 3 grasp-viable. VLM finding (design risk confirmed): a compat shim makes moondream2 LOAD but it boxes the BACKGROUND on the low-fidelity sim render — VLM stage is render-fidelity-limited, not code-limited; moondream3 OOMs at 16GB. Spine byte-unchanged.

## D19 — Grasp R3: deictic front-object resolver ("抓前面的东西" without a VLM) · 2026-06-20
- "抓前面的东西" is DEICTIC → a salient-object + depth resolver (`perception/front_object.py`), not a VLM: vivid-saturation saliency gated to central forward FOV + a 2m workspace, picks the nearest central blob; FAILS LOUD (None). Real-verify: resolves the center-front green cylinder ~7cm from GT from real sensor data. VLM/EdgeTAM remain the pluggable upgrade (network/render-gated). Spine byte-unchanged, no gate.

## D20 — Grasp R4: perception milestone reached; remaining work hits gate + parked-nav + motion-feasibility · 2026-06-20
- MILESTONE: the stated gap (pick is world-model + top-down, not perception-driven) is CLOSED + real-verified over R1-R3; never a GT pose. Full GROUNDED bare-cli grasp blocked by THREE things: (1) NAVIGATION-APPROACH (PARKED by CEO — dog must drive ~0.5m); (2) GRASP-MOTION FEASIBILITY (new finding, non-gated next round); (3) GROUNDED GRADING-BINDING (CEO GATE — weld + a new bridge→proxy object-state topic). Surfaced to Yusen; loop stays alive on (2).

## D21 — Grasp R5: motion-feasibility CLEARED — Piper top-down-grasps all table objects once approached ~0.5m · 2026-06-20
- IK sweep: the Piper top-down-IK reaches all 3 objects once the dog is at dog_x ≥ 10.5; unreachable only at spawn distance. No arm-mount/table redesign needed. Corrects R4's "0.5m-nearer also unreachable" (that moved the target, not the arm base). The full grasp now reduces to TWO Yusen-gated items: (1) APPROACH (a scripted walk-forward suffices, not FAR); (2) GROUNDED grading-binding. Both gated — don't cross autonomously.

## D22 — Native latency (D9 #2): diagnosed = LLM round-trips, not setup · 2026-06-20
- DIAGNOSED: per-turn setup is ~0.04ms (negligible); latency is the SYNCHRONOUS LLM round-trips. Lever = FEWER round-trips. The runner already dispatches multiple tool calls per turn, so it's a PROMPT concern; prompt the model to emit (action, verify) as TWO calls in ONE response (keep `finish` for next response so a FAILed verify can recover — D10 preserved). Verified offline only; NOT yet verified on the live model. Spine byte-unchanged.

## D23 — Native latency R7: live REPL progress feedback (real-verified) + batching prompt reverted · 2026-06-20
- SHIPPED + real-LLM verified: per-step progress feedback (`on_progress` streams model text tail + each tool dispatch to the REPL spinner) — the frozen "load" wait now shows activity; best-effort, trace/spine byte-unchanged. REVERTED the D22 batching PROMPT (the real LLM didn't batch with or without it) but KEPT the runner's batching SUPPORT. FINDING (pre-existing): a weak model SKIPS verify on easy tasks → empty trace (→D24).

## D24 — Native verify-compliance: force a model-authored verify before finish · 2026-06-20
- Closes D23: when the model finishes/stops with an UNVERIFIED action, the runner re-prompts for a verify (bounded by `_MAX_VERIFY_NUDGES`) — the runner NEVER authors the predicate (verify stays model-authored, graded by the unchanged spine); a stubborn model still TERMINATES bounded with an empty/RAN trace (never a false green). Real-LLM verified (the D23 steps=0 turn is now steps=1). Spine byte-unchanged.

## D25 — Native RECOVER pillar real-verified on the live model; D10 confirmed · 2026-06-20
- Real-LLM test of pillar #4: with a flaky skill the model RECOVERED (re-acted until a verify PASSed, didn't give up). D10 CONFIRMED on the real model: the recorded intermediate FAIL rows make the turn grade verified=False even though the final state is correct (rule 5 — a later PASS overwriting a recorded FAIL would be LOOSER). Shipped a fast scripted recover regression guard. All 4 North-Star pillars now exercised honest on the live model. Spine byte-unchanged.

## D26 — M1 milestone review (R10): clean, honest, green; grasp gated · 2026-06-20
- Review after D17-D25: verify spine vcli/cognitive/ BYTE-UNCHANGED the whole session (empty diff); tests green (only the documented pre-existing deepseek env reds); docs hygiene OK. GRASP blocked on Yusen's two R4 decisions (approach-method + grading-binding); native producer's 4 pillars real-verified honest. Buildable-without-Yusen surface near-exhausted; one valuable non-gated test left = PLAN/ROUTE on the real model (→D27).

## D27 — Native PLAN/ROUTE real-verified; CORRECTION: the grasp approach is NON-gated · 2026-06-20
- PLAN/ROUTE pillar real-verified (a 2-step dev task decomposed+routed+verified per-subgoal, both GROUNDED). CORRECTION: the grasp APPROACH-method is NOT a CEO gate — a scripted open-loop walk uses the existing `walk` primitive (not FAR, no new interface/dep); R4 over-escalated by bundling it. So the non-gated surface is NOT exhausted — the full grasp MOTION is buildable now. The ONLY real gate left is the GROUNDED grading-binding (weld + a new bridge→proxy object-state topic).

## D28 — Grasp R12: scripted approach + full motion WIRED + approach real-verified; grasp not yet completing · 2026-06-20
- Built the non-gated approach: perceive afar (accurate) → if out of reach, drive base FORWARD to standoff (position-feedback open-loop walk, NOT FAR) → grasp the afar-perceived point (the world coord is motion-invariant; close-up re-perceive looks OVER the table). APPROACH real-verified (dog drives forward). Grasp does NOT complete (`ik_unreachable`): compounding margins (run-variable perception, approach undershoot, marginal reach). ~80%; honest. Spine byte-unchanged.

## D29 — Grasp R13: ROOT CAUSE = robot self-occlusion (aims at its OWN gripper); not working · 2026-06-20
- Per-color geometry is SOLID (~2cm). Failure is PERCEPTION SELECTION: the Piper gripper / yellow EE site sits in the head-cam FOV at table level → `front_object_mask` picks IT (grasp ~12cm off — the robot localizing its own gripper). Caught a FALSE-POSITIVE (skill success=True is the gripper's commanded-closed heuristic, NO weld — not believed). Partial stow mitigation insufficient with the gripper connected. ~70%; spine byte-unchanged.

## D30 — Grasp R14: self-occlusion confirmed; simple hides RULED OUT; robust fix = segmentation self-filter · 2026-06-20
- Confirmed the connected arm occludes the cylinders. RULED OUT (each verified no-op): hiding viz SITES, hiding the arm by GEOM GROUP (post-`MjSpec.attach` the Piper shares group 0 with the cylinders), a stow pose (droops with the gripper). ROBUST FIX (R15): SEGMENTATION self-filter — render per-pixel geom-id, mask the robot's own "piper*" geoms, exclude before front_object. Grasp still doesn't work; thorough diagnosis, no false claims. ~70%.

## D31 — Grasp R15: seg self-filter WIP; perception last-mile HARD-BLOCKED; escalated to deep-debug · 2026-06-20
- Implemented the seg self-filter but it UNDER-REPORTS (6-21px); failure persists. 3-round hard-block with contradictory signals (the 116px distractor is NOT geoms/sites/cam-pose/visible-occlusion; the only delta is `MuJoCoPiper.connect()`). Root cause not cleanly isolated — anchored on occlusion. ESCALATED to a vr-lead deep-debug (Debug Protocol). Geometry/approach/native pillars proven + unchanged; spine byte-unchanged.

## D32 — Grasp R15: perception last-mile RESOLVED — saturation-bridge blob fusion (NOT self-occlusion) · 2026-06-20
- ROOT CAUSE (deep-debug): `_SAT_MIN=140` sits BELOW the brown table's saturation → the table passes the saliency mask in thin chains that 8-connectivity FUSES table+cylinders into one blob; the green stops being its own component and the most-central fallback grabs a table sliver. The arm-connect correlation was a red herring; D29-D31's self-occlusion theory was WRONG (see tricky-bugs Case 4). FIX: morphological OPENING before connected components (severs sub-kernel bridges, threshold-independent). Real-verified live go2+piper: green @ **2.3cm** from GT (was 12.2cm). The seg self-filter (D31) is HARMLESS and stays but was not the fix. STILL GATED (CEO): grading-binding. Spine byte-unchanged.

## D33 — Grasp R17: GROUNDED grading-binding BUILT (CEO-approved); completion blocked on tight geometry · 2026-06-20
- Yusen APPROVED the grading-binding. Built (mirroring the SO-101 weld): scene weld eqs via the `go2_room.xml` GRASP_WELDS placeholder (GOTCHA: edit the TEMPLATE — the generated `scene_room_piper.xml` is overwritten on every connect); `MuJoCoPiperGripper._try_grasp`/`weld_is_active`/weld-backed `is_holding`; `MuJoCoPiper.get_object_positions`; heading-corrected approach. Welds load + fire when the EE reaches. NOT yet robust: geometry is TIGHT (Piper reach ~0.22m fwd, dog table-limited ~10.6, cylinders at the reach edge). Escalated completion to a vr-lead deep-iterate; never trust skill.success. Spine byte-unchanged.

## D34 — Grasp R18: RELIABLY GROUNDED (3/3 live) — reach-matched geometry + lateral/seat approach + post-weld lift · 2026-06-20
- vr-lead closed the completion blocker: the full pipeline grades GROUNDED on live go2+piper (green lifts ~24cm, `holding_object` True via the real oracle). ROOT CAUSE of D33 tightness (measured): the Piper top-down reach GROWS with target HEIGHT (~0.22m fwd at z~0.25 but ~0.49m at z~0.32). Honest fix = GEOMETRY (objects on a tall pedestal z=0.32, set 8cm back from the table's near top edge which occludes the d435; blue/red pushed wide so green wins the resolver). Three skill fixes in `skills/perception_grasp.py` (full-jam seat, lateral vy tracking, shallow pre-grasp hover + post-weld shoulder-up LIFT) run ONLY after a real weld. Honest by construction; no GT-as-grasp-point (see tricky-bugs Case 5). Spine BYTE-UNCHANGED.

## D35 — Grasp R18 RED-TEAM: GROUNDED grasp WORKS (4/5), reliability ~80% (not 3/3) · 2026-06-20
- Independently red-teamed D34 on the live sim: 4/5 runs FULLY GROUNDED (third-person render visually confirms green held aloft), 1/5 failed → honest reliability ~80%, NOT the vr-lead's claimed 3/3 (red-team corrected an optimistic claim). The MILESTONE: the GOAL's manipulation route works end-to-end + real-verified + visually confirmed; the geometry fix is a real kinematic fact, not gaming. Spine byte-unchanged. REMAINING (not blockers): bare-cli acceptance (project's NON-NEGOTIABLE surface, not yet through bare vector-cli); reliability hardening.

## D36 — Grasp R19: bridge-path grasp (toward bare-cli acceptance) — un-gate + weld-on-close + /piper/object_state · 2026-06-20
- The in-process weld is NOT on the bare-cli path (bare-cli launches `go2_vnav_bridge.py` subprocess, agent drives via ROS2 proxies). Three gaps closed: (1) manipulation un-gated (registered whenever `with_arm=True`); (2) bridge welds the nearest pickable on a CLOSE cmd; (3) additive `/piper/object_state` topic (Float64MultiArray on an additive topic — no new msg TYPE, within the approved grading-binding envelope) so the proxies + oracle + actor_causation read REAL physics over ROS2. RISK: bridge free-cam image vs d435-pose projection mismatch (if grasp fails, publish from the named d435 cameras). Spine byte-unchanged; real-verify in progress.

## D37 — Grasp R21: bare-cli bridge path — RED-TEAM found a WORLD-ROUTING blocker (not a grasp failure) · 2026-06-21
- Drove the bare-cli acceptance: VERDICT evidence=FAILED, n_steps=0, oracle_names = the DEV world's (file_exists…), NOT arm-sim's (holding_object). DIAGNOSIS: the verify namespace did NOT switch to arm-sim — a WORLD/verify-namespace ROUTING issue on the bare-cli path (kernel/world seam), NOT a grasp/grading-binding failure (those all work in-process). Surfaced an investment decision to Yusen. Spine byte-unchanged.

## D38 — Grasp R22: bare-cli WORLD-ROUTING SOLVED (two-turn REPL); grasp RANs honestly, doesn't HOLD yet · 2026-06-21
- Committed the vr-lead's IK sensor-offset fix + reach-to-stall. KEY FINDING (corrects D37): D37's "wrong verify world" was a `-p` SINGLE-TURN artifact. The TWO-TURN REPL (切换到 go2 带机械臂 → 抓前面的东西) CORRECTLY switched to arm-sim → verdict **RAN verified=False** — world-routing SOLVED, the grasp RUNS and the spine grades it HONESTLY on the bridge path. The grasp does NOT physically HOLD yet (cause not yet visible). Spine byte-unchanged.

## D39 — Grasp R23: BARE-CLI GROUNDED ACHIEVED — the GOAL is COMPLETE · 2026-06-21
- The full Go2+Piper perception-driven grasp grades GROUNDED through bare vector-cli + NL, confirmed twice: an instrumented bridge probe (perception 2cm → bridge weld fires → is_holding via /piper/object_state → green lifts +23cm → holding_object True via the real oracle) AND the literal two-turn cli.main REPL (切换到 go2 带机械臂 → 抓前面的东西 → verdict **GROUNDED verified=True**). D38's RAN was the ~80% reliability variance; the verdict is HONEST (GROUNDED on a hold, RAN on a miss). Residual: reliability ~80%; VLM+EdgeTAM the pluggable upgrade. Spine byte-unchanged.

## D40 — Grasp R24: Reliability 80%→100% claim (15/15) — post-approach IK nudge · 2026-06-21
- Root cause of the ~20% MISS: the approach stall fires short (false stall) when lateral correction competes with forward advance → IK FAIL on pre-grasp. Fix: `_POST_APPROACH_NUDGE_N` pure-forward presses, IK-gated (exits as soon as IK succeeds), ~20 lines in `skills/perception_grasp.py`. Claimed 15/15 (corrected by D41). Spine byte-unchanged.

## D41 — Grasp R25 RED-TEAM: reliability nudge is honest but ~80% (NOT 100%) · 2026-06-21
- Independently red-teamed D40: 8/10 GROUNDED (~80%), NOT 100% — the pure-vx nudge can't fix a MISS caused by lateral y-drift. The nudge is honest + harmless (kept). 3rd inflated sub-agent reliability claim the red-team caught this session — ALWAYS reproduce a reliability headline independently. The grasp grades HONESTLY regardless. DECISION: ~80% is acceptable for a complete-goal capability; ≥90% is diminishing-returns polish (log y-drift nudge as future). Spine byte-unchanged.

## D42 — R26: D9 #2 native latency PROFILED — low-value (producer ~ms; sim/LLM-dominated) · 2026-06-21
- Profiled: the producer loop is ALREADY ~milliseconds. Robot-turn wall-time is SIM-dominated (physical motion) + LLM-network, NOT producer-bound; single-robot actions are physical+sequential and the sim serializes anyway → "sync→async" on the producer has no measurable win. D9 #2 as a producer-async refactor is premature optimization touching a spine-adjacent hot path for ~0 gain → NOT pursued. PIVOT to grasp reliability ≥90% (→D43). Spine byte-unchanged.

## D43 — R26: grasp reliability ~80%→9/10 (~90%) via vy-correcting nudge · 2026-06-21
- Root cause of the residual ~20% miss: lateral y-drift the pure-vx nudge didn't close. Fix: the post-approach nudge now corrects BOTH forward (vx) AND lateral (vy toward the object's y-line) until ik_top_down succeeds. Validated 9/10 HOLD (~90%); honest (gated on IK success). Reported 9/10, not 100% (small sample). Grasp DONE + GROUNDED (D39) + ~90% reliable + fast producer. Spine byte-unchanged.

## D44 — R27 MILESTONE REVIEW: bare-cli GROUNDED grasp CONSOLIDATED + re-confirmed · 2026-06-21
- Milestone review of the grasp campaign (R1-R26): verify spine vcli/cognitive/ BYTE-UNCHANGED the whole campaign (empty diff); tests green; git clean; docs hygiene OK. Red-team re-confirmed the full perception→IK→grasp→weld→lift→GROUNDED route on bare vector-cli + NL (bridge log `welded ... (41mm)` = real EE reach). Top goal DONE (D39) + polished (~90% D43, fast D42); red-team caught 3 inflated sub-agent claims; spine never loosened. REMAINING frontiers: RECOVER pillar (no FAR); nav+grasp (needs FAR un-park — CEO gate); VLM+EdgeTAM (timm net-blocked).

## D45 — R28: RECOVER pillar already covered (tested + real-LLM); all 4 North-Star pillars DONE · 2026-06-21
- Checked rather than rebuilt: the producer already CONTINUES after any verify, so the model freely re-acts until PASS; D10 honestly keeps the FAIL rows (a recovered turn grades verified=False — never a false green). ALL 4 pillars now demonstrated on the grasp: PLAN · ROUTE · VERIFY · RECOVER. Campaign core COMPLETE; spine BYTE-UNCHANGED across 45 decisions. NEXT-EVOLUTION FORK for Yusen: (A) grasp+PLACE; (B) attribute/color grasp; (C) nav+grasp (FAR un-park, CEO gate); (D) VLM+EdgeTAM (net-blocked).

## D46 — R29: GRASP+PLACE multi-skill orchestration — bare-cli BOTH-GROUNDED · 2026-06-21
- Built frontier (A): ONE NL command `把前面的东西抓起来放到旁边` the producer decomposes into perception_grasp → verify holding_object → place_top_down → verify placed_count(region) >= 1 → finish — plan·route·verify across TWO skills. Added a GROUNDED `verify_hint` to place_top_down (read via the FROZEN vocab_from_registry — spine BYTE-UNCHANGED). GROUNDING honest by construction (placed_count is not in actor_causation's robot sets → NOT_GRADED, backed by real physics). KEY GEOMETRY: the place target MUST be the FLOOR (placed_count counts z<0.10; the pedestal is z=0.32). Real-verify bare cli.main: verdict GROUNDED 2/2. RELIABILITY not yet multi-run-measured. Residual: region goal-authenticity (D11/D12 class).

## D46-confirm — R30 RED-TEAM: grasp+place BOTH-GROUNDED, reproduced · 2026-06-21
- Independently reproduced D46 on bare cli.main (both legs GROUNDED). CAVEAT (moat ledger): the GRASP verify is causation-gated (weld 0→1); the PLACE verify (placed_count) is R1-gated only (world-state, not causation-gated) — honest HERE only because the place region starts EMPTY (a GROUNDED placed_count>=1 means the robot genuinely moved green there). RESIDUAL: placed_count would false-green if the region pre-contained an object; causation-gating it would touch the spine (CEO gate). Logged for honesty. Spine BYTE-UNCHANGED.

## D47 — R31: ATTRIBUTE (colour) GRASP — "抓红色的东西/抓蓝色的" selects the right colour, GROUNDED · 2026-06-21
- Built frontier (B): NL colour selection on the deictic resolver, NO VLM. `parse_color` (zh/en→red/green/blue, deictic→None) + HSV hue bands + a `color=` kwarg on `front_object_mask` (keeps only in-band blobs, FAIL LOUD if none, color=None unchanged byte-for-byte); colour maps to the verify LABEL via `_COLOR_TO_SCENE`, the grasp POINT still from depth+mask. Measured render hues cleanly separable. Real-verify: RED/BLUE/GREEN each grasp_world nearest the right GT, oracle holding_object True for the right name; bare-cli "抓红色的东西" → GROUNDED. Colour SELECTION 100%; offset-colour full-grasp reach-limited (~50-75%). Spine byte-unchanged.

## D47-confirm — R32 RED-TEAM: attribute grasp WORKS; selection 100%, offset reliability reach-limited · 2026-06-21
- Red-teamed: colour SELECTION correct on every run (100%); full-grasp RED 3/4, BLUE 2/4 — the vr-dev's "RED 1/1" was variance. FINDING: laterally-OFFSET cylinders are REACH-limited vs central green — same root cause as the residual miss (lateral reach-envelope edge); colour selection itself is solid. Bare-cli RED → GROUNDED. Manipulation frontier RICHLY covered via bare-cli (deictic / recover / grasp+PLACE / attribute). Spine byte-unchanged across 47 decisions; red-team caught 4 inflated claims. Next in-KIND leap (nav+grasp / explore / VLN) needs un-parking those stacks = CEO gates.

## D48 — R34: FIRST real cross-MODEL route — grounding-dino DetectorCapability, GROUNDED via bare-cli · 2026-06-21
- DIRECTION (CEO-chosen): prove "route to the right MODEL" — the runtime had routed to ONE LLM + classical skills (CapabilityRegistry held only `chat`). Registered a REAL 2nd model family: `IDEA-Research/grounding-dino-tiny` localizes all 3 cylinders 3/3 on the SAME render moondream failed (D18 was a VLM-vs-DETECTOR gap, not a fidelity gap); runs OFFLINE via already-installed transformers (cached weights → the CEO-approved "one new dep" is effectively ZERO-install).
- SEAM was already built + inert (Capability protocol + CapabilityRegistry + StrategySelector + GoalExecutor._execute_capability, all since Phase C.1) → filling `robot.register_capabilities` makes the route LIVE with ZERO cognitive/ edits. BUILT (all OUTSIDE cognitive/): `perception/grounding_dino.py` (offline, lazy torch, reads only (rgb,query) NEVER a GT pose), `perception/detector_capability.py DetectorCapability`, routing in perception_grasp (deictic→geometry, colour→HSV, NAMED→detector).
- REAL-VERIFY + adversarial RED-TEAM: in-process 3/3 GROUNDED via the detector route (route-fired proven, grasp_world 2.3cm from a box+depth pointcloud not a GT pose, weld, +23cm lift, oracle True); **BARE-CLI ACCEPTANCE CLOSED** (two-turn REPL: 启动 go2 仿真 带机械臂 → 拿起绿色的瓶子 → grounding-dino loads+runs → verdict GROUNDED verified=True). Caveats (logged, honest): colour grounding is staging-dependent (zh prompt drops the colour → all labelled "a bottle", green wins by centrality; the moat correctly FAILs a mislabel) → fix in D49; grasp REACH is the existing limit; detector LOCALIZATION is solid 3/3. MILESTONE: the FIRST real 2nd model family is routable + graded by the UNCHANGED moat. Spine byte-unchanged across 48 decisions.

## D49 — R35: colour-conditioned detection — colour selection is PERCEPTUAL (closes D48 caveat 1) · 2026-06-22
- Closes D48 caveat 1 (perception-side, spine byte-unchanged): `query_to_prompt` now PRESERVES the colour adjective (绿色的瓶子→"a green bottle.", colour-only never dropped); `_select_detection` prefers the box whose label NAMES the requested colour over raw max-confidence. PERCEPTUAL PROOF (real go2+arm, OFFSET scene, frames eye-verified): colour-localization 3/3 — red→y=3.21, blue→y=2.79 (NOT 3.00) prove perception beats centrality. SCOPE: this proves LOCALIZATION; full grasp-to-GROUNDED still inherits the existing reach limit (offset reach-miss is RAN-honest). Spine byte-unchanged across 49 decisions.

## D50 — R36: the PRODUCER routes `detect` to grounding-dino via _execute_capability (orchestration-layer route) · 2026-06-22
- GAP: D48 ran grounding-dino SKILL-INTERNALLY; the North-Star "the RUNTIME routes to the right MODEL" is fully proven only when the PRODUCER routes a sub-goal to the capability via the engine's capability-dispatch path. ROUTE: producer → GoalTree → GoalExecutor → StrategySelector → `_execute_capability` → grounding-dino. The CLEAN producer route is an EMPTY strategy + a detect-keyword description (the decomposer leaves an empty strategy intact; the keyword ladder routes it) → needs ZERO cognitive/ edits; captured boxes flow to the Blackboard (`${detect.output.boxes}`, rule 4).
- REAL-VERIFY (probe): producer plan = [(detect,<empty>), (grasp, perception_grasp)]; detect = strategy "detect" CAPABILITY branch, success, actor=NOT_GRADED (read-only, honest RAN), 4 real boxes captured; grasp = GROUNDED via the real weld oracle. NON-COGNITIVE fix (route hit "no RGB frame"): `DetectorCapability(perception=…)` bound to the agent's live RGB at registration — worlds/robot.py + detector_capability.py only, cognitive/ EMPTY. HONEST grading split: detect=RAN, grasp=GROUNDED. BOX-FLOW (true composition) = follow-up (→D51); this round the grasp RE-PERCEIVES. PRODUCT-PATH wrinkle: register_capabilities runs before NL sim-start boots the arm → cold-turn perception=None (→D51 rebind).

## D51 — R37: TRUE box-flow composition (grasp CONSUMES the routed detector's box) + cold-turn perception rebind — both GROUNDED · 2026-06-22
- Closes D50's two caveats, NON-cognitive, spine byte-unchanged (across 51 decisions): (A) TRUE COMPOSITION — the grasp BINDS `${detect.output.detections}` and back-projects the producer's box to 3D via the same depth math; its own detect/front_object is NOT called (grasp_consumed_bbox=True, grasp_reperceived=False, GROUNDED 2.3cm). (B) COLD-TURN REBIND — DetectorCapability binds the AGENT and pulls `agent._perception` LAZILY at invoke, so a capability registered while perception=None still perceives once a mid-session NL sim-start restores it (GROUNDED attempt 1).
- HONEST residuals: the `${detect.output.detections}` binding is authored by the probe, not a live LLM (the standing faked-token-stream caveat, → a live-LLM round); grading split detect=RAN/grasp=GROUNDED.
- **SPINE KNOWN-ISSUE → DECISION QUEUE (CEO gate, NOT touched per rule 5)**: on a flaked pick the harness Layer-3 re-decompose re-emits the grasp with an EMPTY query (strategy_params lost on re-plan) → "Nothing localizable". Lives in vcli/cognitive/; FIX (preserve strategy_params on re-plan) needs CEO approval. Does NOT affect first-attempt grasp.

## D52 — R38: nav+grasp cross-skill — FAR un-park CONFIRMED (drives), but end-to-end GROUNDED NOT landed (honest partial) · 2026-06-22
- Yusen CEO-approved un-parking FAR for "去桌子那里把红色的罐子拿起来". HONEST RESULT (corrects the build agent's optimistic table): nav_drove 2/2 (FAR genuinely drives ~5m cross-room — un-park WORKS) but nav_ran 0/2 (FAR stops at arrival_radius=0.8m > at_position tolerance → a failed-verify, not a clean RAN) and grasp_GROUNDED 0/2 (from the oblique FAR-arrival pose the d435 frames floor/wall → perception mislocalizes → IK unreachable). The chain COMPOSES; spine byte-unchanged; no regression (scripted-from-spawn grasp still GROUNDS).
- RESIDUALS for R39 (NON-cognitive): a precise terminal DOCK (close FAR's last ~0.8m to the proven head-on standoff so at_position grades RAN); perception-from-arrival framing. CEO-GATE item (DECISION QUEUE): the D51 strategy_params/empty-query-on-retry spine residual bit nav too — fix needs CEO approval.

## D53 — R39: nav+grasp chain COMPLETES end-to-end + GROUNDS the right object intermittently; real blocker was silent PERCEPTION degradation · 2026-06-23
- Debug Protocol REFUTED the "off-axis IK" theory: grasp-z was mislocalized LOW even from the docked +X pose — a perception problem. ROOT CAUSE (two silent-perception bugs, both fixed): (1) `timm` declared in pyproject but MISSING from `.venv` → EdgeTAM fell back to a coarse box-rect mask → centroid z collapsed toward the table → no weld. Fix = env-sync the declared dep (NOT a new dep, no gate). (2) `tracker.py float(scores[i])` — transformers>=5 returns (N,1) → segment() box-rect-degraded every time. Fix = flatten scores to 1-D.
- RESULT (real sim, retries pinned 0, EdgeTAM live): GREEN grasp_GROUNDED 1/3 (a real end-to-end pick: 2.3cm → weld + shoulder lift + holding_object True); RED 0/3; nav_ran 0/3 (the known D52 grade). The chain mechanically completes (FAR → terminal_dock +X → perceive → approach → weld → lift). REFUTED: re-perceiving after the approach makes it WORSE (the d435 looks OVER the table close in). REMAINING (R40, non-gated): detection SELECTION at the dock framing (the 3 cans are ~22cm apart, ~0.85m away, slightly off-center → grounding-dino intermittently picks a neighbour's box). Spine byte-unchanged.

## D54 — R40: nav+grasp reliability did NOT land — dock still not repeatable; honest GREEN ~1/3; red-team caught a contamination false-positive · 2026-06-23
- The FRAMING fix works (perceive-moment frame now head-on at the cans). HONEST RESULT (corrects the agent's inflated "2/3"): GREEN grasp_GROUNDED **1/3 CLEAN** — t2 real GROUNDED; t1 perception good but grasp MISSED (RAN-honest); t3 SPURIOUS grounded=True with grasp_world=None = TRIAL CONTAMINATION (t2's weld persisted; the oracle honestly reads green-still-held — NOT a spine false-green; the probe's trial isolation is broken). ROOT ISSUE (open): the DOCK is STILL not repeatable (~1/3 after FOUR rounds). Spine byte-unchanged. Surfaced to Yusen as a strategic call (dock deep-dive vs accept-intermittent-and-pivot). Cross-MODEL axis (D48-D51) remains richly proven.

## D55 — R41: closed-loop dock + gate — GATE works (0 false grounds) but the dock's BRIDGE walk is BROKEN → GREEN 0/4 (regression) · 2026-06-23
- R41 (Yusen-approved "one more dock round"): rebuilt terminal_dock as a closed-loop controller + a pose-verification gate (heading/centerline/x window → else abort RAN, no perceive). HONEST verify: GREEN 0/4, dock_converged 0/4 — the closed-loop walk FAILS on the BRIDGE ("publisher's context is invalid" — corrected to a PHANTOM in D56) so the dock never converges; the GATE aborts every trial (honest RAN, ZERO false grounds — the gate WORKS); trial-isolation reset also broken on the bridge. REGRESSED from R40 1/3 to 0/4 (an in-process-vs-bridge gap). nav+grasp is OSCILLATING across rounds — a genuine integration WALL. Surfaced to Yusen for keep-vs-bank. Spine byte-unchanged.

## D56 — R42 FINAL nav+grasp: dock convergence FIXED (bridge-verified) + publisher bug was a PHANTOM; grasp still misses (standoff too far back) — nav+grasp BANKED per hard-stop · 2026-06-23
- R42 (final, Yusen hard-stop after): Debug Protocol DISPROVED the "publisher's context invalid" bug — a PHANTOM (cross-trial leaked-bridge artifact). Real 0/4 cause was dock NON-CONVERGENCE; FIXED in `terminal_dock.py` (higher face gain, a holonomic fine-position leg, a no-orbit guard). BRIDGE-verified: the dock now CONVERGES (0 publish failures). Verify (loop died after trial 1, 0/1 sampled): dock converged → perception 2.3cm → approach → pre-grasp IK OK → BUT grasp MISSED. ROOT of the remaining miss: the post-dock approach stops at x≈10.41 but green is at x=10.86 (~0.45m fwd, at the EDGE of the Piper's reach) — the proven scripted-from-spawn grasp jams CLOSER (~10.6).
- HONEST VERDICT: nav+grasp NOT reliably landed after SIX rounds (R38-R42) — each fixed a real layer (perception/EdgeTAM → framing → gate → dock convergence) and exposed the next; now the post-dock standoff/reach. The chain is mechanically COMPLETE, dock converges, perception is accurate — the wall is the approach getting close enough. BANKED here per the hard-stop; R42's dock-convergence fix KEPT (not reverted). "One identified layer from working." Spine byte-unchanged across 56 decisions. NEXT = CEO decision (ship cross-MODEL→master vs new frontier).

## D57 — G1 R1 cross-EMBODIMENT: place Unitree G1 in the EXISTING go2 room, lidar+camera working · 2026-06-23 (autonomous G1 campaign)
- GOAL (cross-EMBODIMENT axis): g1 as a switchable embodiment standing IN the go2 apartment scene (same walls/table/cans), with a real lidar scan + a real camera frame. ARCHITECTURAL CHOICE: MIRROR the proven go2 in-room pattern (Yusen requires g1 in the SAME geometry; go2's lidar mj_ray + camera are battle-tested in this room) rather than resurrect g1's own flat-scene room. The recovered mujoco_g1.py is kept for R2 gait reference.
- IMPLEMENTATION NOTE (durable reusable mechanism): the meshdir conflict (room furniture meshes vs g1's; one top-level meshdir can't serve both) was solved by the go2_piper `MjSpec.attach` pattern (NOT string `<include>`) — load each spec separately, rewrite mesh paths ABSOLUTE + meshdir="", attach g1 into a room-only spec. This is the mechanism for any future embodiment-in-room.
- OUTCOME — R1 LANDED + foreground real-verified: g1_pelvis AND pick_table in the SAME compiled model. (1) STANDS (base-z stable ~0.79, no fall). (2) LIDAR real + detects the room (360 returns, points span the full apartment, 0 self-hits — legs filtered). (3) CAMERA renders the room (g1_head_rgb, eye-confirmed). Real spine guard `git diff 7b220d9..HEAD -- vector_os_nano/vcli/cognitive/` EMPTY. NOT a CEO gate (sim asset / in-sim world-registration; no new ROS2 interface/dep). HONEST gaps for R2: no lidar returns on the (occluded) table (a camera target); static PD stand, NOT locomotion; _start_g1 in-process only (NL-switch = R2).
- NOTE on the guard spec: the goal's literal `vcli/cognitive/` (no package prefix) is trivially empty at repo root; the REAL spine is `vector_os_nano/vcli/cognitive/`, verified byte-unchanged against that actual path.

## D58 — G1 R2 cross-EMBODIMENT: G1 WALKS to a point (policy-driven gait), sensors update while moving · 2026-06-23 (autonomous G1 campaign)
- THE REAL DECISION (gait model): R1's 29-DOF menagerie g1 has no gait. The recovered locomotion is a TorchScript RL policy (motion.pt) trained on a 12-DOF model — a MATCHED PAIR (a 12-DOF policy can't drive a 29-DOF model → topple). CHOSE: attach `assets/g1_gait/g1_12dof.xml` into R1's room via the same MjSpec-attach pattern, recover the policy gait. The lead spiked the bare 12dof policy first BEFORE committing the architecture; the 29dof model is set aside (no matched policy).
- THE #1 CORRECTNESS RISK + FIX (combined-scene index shift): in the combined room scene the g1 freejoint is NOT at qpos[0] — a `_G1Offsets` class computes pelvis/leg qpos/dof addresses from the freejoint, used everywhere instead of hardcoded slices. HIDDEN BUG (D57's mesh-dir lesson recurred): rewrite every g1 mesh.file ABSOLUTE + meshdir="" before attach (setting g1_spec.meshdir doesn't survive the round-trip). A VERIFY DESIGN BUG the probe caught: the first probe pointed +x INTO the pick_table (g1 stalls, no fall — an unreachable target, not a gait bug); re-pointed at reachable open points.
- OUTCOME — R2 LANDED + foreground real-verified: g1 WALKS to commanded points without falling (base-z 0.770-0.793, never near the 0.4 fall floor), sensors UPDATE while moving (lidar ranges shift; head camera VISIBLY distinct start/mid/arrival, 3 PNGs eye-confirmed). NL EMBODIMENT-SWITCH DONE (additive): IntentRouter + SimStartTool enum already routed g1; added an explicit g1 line to vcli/prompt.py (prompt.py is in vcli/ NOT vcli/cognitive/ → spine byte-unchanged). NOT a CEO gate (sim asset + in-sim locomotion + a prompt string).
- HONEST shakiness for R3: navigate_to is open-loop straight-line, NO obstacle avoidance (will walk INTO the table/walls; the recovered vgraph/_nav_controller were NOT recovered — →D64); only 1 deterministic sample (add randomized starts for a rate); table not in close lidar range at standoff (camera target); mujoco_g1.py 917 lines (>800 — extract lidar, →D59).

## D59 — G1 R3 cross-EMBODIMENT: g1 is a MOAT-GRADED ROUTABLE EMBODIMENT via BARE cli + NL (parity with go2; at_position RAN, the honest D14 grade) · 2026-06-23 (autonomous G1 campaign)
- GOAL: prove the SAME orchestration (NL → switch embodiment → command → moat grades) works on g1 EXACTLY as on go2 — the literal bare cli REPL: 启动 g1 仿真 → 走到坐标 (10.0,1.5) → g1 walks via RL gait → spine grades at_position.
- THE REAL FINDING: the wiring was already ~90% body-agnostic — resolve_for_agent wraps any agent in RobotWorld; build_verify_namespace registers at_position/facing whenever base has get_position+get_heading (g1 has both → reads g1's PELVIS pose with ZERO change); make_at_position + _NativeBaseNavigateTool are generic; coord_goal.py parses the NL goal (spine, frozen). The ONE body-specific bug: go2's `navigate_to(...)→bool` vs g1's `→dict` → `bool(non-empty-dict)` is ALWAYS True → a fall/timeout mis-read as "arrived". DISHONEST. FIX (body-agnostic, NON-cognitive): `_G1NavResult(dict)` with `__bool__`==`get("reached")`, accept `timeout=None,**_ignored`; now bool(navigate_to) is True ONLY on arrival. DE-SLOPPIFY: extracted the ~120-line lidar ray-cast into `sensors/g1_lidar.py` (byte-identical); mujoco_g1.py 917→797 lines.
- OUTCOME — R3 LANDED + foreground BARE-CLI acceptance: two-turn REPL — 启动 g1 仿真 (NL switch) → 走到坐标 (10.0,1.5) → `▸ navigate → verify at_position(10.0, 1.5) ✓ (actor=UNCAUSED)` → `verdict RAN verified=False`. The `✓` proves at_position evaluated TRUE — g1 PHYSICALLY ARRIVED; RAN (not GROUNDED) because base gait is NOT causation-gated (D14) — the CORRECT HONEST grade, identical to go2, a SUCCESS. Corroborating pose probe confirms g1 walked upright. PARITY: cross-EMBODIMENT proven end-to-end on a 2nd body-class (humanoid) THROUGH THE ACCEPTANCE INTERFACE. Spine BYTE-UNCHANGED.
- HONEST shakiness for R4: navigate_to still open-loop (no avoidance, →D64); deterministic single sample; at_position RAN not GROUNDED (GROUNDED needs extending actor-causation to cmd_vel — a SPINE/CEO change, D14 — do NOT do autonomously); a cross-embodiment PERCEPTION task on g1 would close the loop further (→D60).

## D60 — G1 R4 cross-EMBODIMENT × cross-MODEL: grounding-dino routed to g1's HEAD camera, localizes the red object, via BARE cli + NL · 2026-06-23 (autonomous G1 campaign)
- GOAL: compose the two proven axes — route the LEARNED detector (D48-D50) to g1's (D57-D59) HEAD camera. THE GAP: register_capabilities was guarded on `agent._arm` (the go2+arm world) so g1 never got it; _start_g1 left _perception=None; the bare-cli native loop hits the classical DetectSkill not grounding-dino. THE FIX (4 changes, ALL NON-cognitive, spine byte-unchanged): loosen the guard from arm-presence to CAMERA-presence (`_agent_has_camera`, world-agnostic — rule 7); `G1HeadPerception.get_color_frame()`; bind it in _start_g1; surface a `_NativeDetectTool` routing to the SAME registered capability (wins over DetectSkill); a perception-fed `detect_objects()` verify oracle for the no-arm case.
- REAL-VERIFY — bare cli.main two-turn REPL: 启动 g1 仿真 → 找前面的红色的东西 → "Loading weights: 978/978" (the LEARNED model genuinely loaded+ran on g1's camera) → verdict GROUNDED verified=True. VISUAL (annotated frame Read back + pixel red-team): grounding-dino boxed the red stool (3-15× redder than background) — a GENUINE localization. NOTE: this GROUNDED was later found a FALSE GREEN (the detect_objects oracle self-read the detector's own boxes) and CORRECTED to RAN in D61 — D50's pure-RAN grade was the honest one.
- R5 TARGETS: navigate-then-detect on g1; a live-LLM producer; colour/named DISCRIMINATION; randomized spawn for a rate. NOT a CEO gate.

## D61 — G1 R5 CORRECTION (moat integrity): R4's g1-detect GROUNDED was a FALSE GREEN (self-certifying oracle); FIXED to RAN; the route still holds (RAN-honest) · 2026-06-23 (autonomous G1 campaign)
- THE FALSE GREEN: R4's `detect_objects()` oracle (`_make_perceived_detections`) read `agent._last_detection` — the LEARNED DETECTOR's OWN stashed boxes → `len(detect_objects())>0` is a TAUTOLOGY (the detector certifying ITSELF). Spine bytes were unchanged but the honest-verify STANDARD was loosened — a moat violation in SPIRIT, contra rule 5 (stricter-only). D50 had correctly graded read-only detection RAN.
- THE FIX (world-side, vcli/cognitive/ BYTE-UNCHANGED): REMOVED `_make_perceived_detections` + its binding. With NO `detect_objects` in the g1 (camera-no-arm) verify namespace, the classifier finds no oracle → returns RAN (fail-closed to the honest D50 grade), achieved entirely by the existing single-sourced `oracle_names` (no spine edit). A detector localizing is READ-ONLY perception; verify must NEVER read the means' own output as an independent oracle.
- KEPT INTACT: the DetectorCapability still REGISTERS for g1, still ROUTES to its head camera, still localizes — only the dishonest grade is removed (now RAN-honest). The ARM path is untouched (its detect_objects reads independent GT, still GROUNDED). **SIBLING-ORACLE AUDIT** (the "audit ALL sibling serializers" discipline): swept all world-side verify bindings — the self-read was the ONLY one; every other reads INDEPENDENT GT. REAL-VERIFY (bare cli, two turns): the verdict FLIPPED R4-GROUNDED→**R5-RAN** (the `✓` became `·`; zero `verdict GROUNDED` in the transcript), detector still localizes. A future LEGITIMATE GROUNDED needs a GT-BACKED SPATIAL MATCH (→D63).

## D62 — G1 R6 REVIEW (post-violation consolidation): INDEPENDENT honesty audit confirms the moat is CLEAN of false-greens; honest g1 cross-embodiment capstone · 2026-06-23 (autonomous G1 campaign)
- INDEPENDENT HONESTY AUDIT (re-done from scratch, not trusting R5; cross-checked by a 2nd sweep agent): audited EVERY GROUNDED-eligible oracle in the live verify namespace — every one reads INDEPENDENT GT (arm: object xpos / weld; base go2 AND g1: get_position/get_heading, at_position DOWNGRADED to RAN by actor-causation D14; dev: disk/exit-code; engine memory oracles: world-model state, never in the grasp/nav moat). CONCLUSION: NO self-read or trivial-true GROUNDED-eligible oracle remains; the D61 fix HOLDS. Read-only perception → RAN (D50/D61); base nav → RAN (UNCAUSED, D14); grasp → GROUNDED via weld causation (the one legit GROUNDED). Moat clean.
- ONE finding fixed (world-side, cognitive/ BYTE-UNCHANGED): `agent._last_detection` (written native_loop.py) was ORPHANED after D61 — REMOVED the dead write + a stale comment that documented the exact self-read pattern, so no future round re-wires the means' output into verify. TIDY: deleted 15 scratch round-probes; kept `scripts/g1_capstone_demo.py`. Docs current (root 2 .md, docs/ 7).
- CAPSTONE (honest cross-embodiment milestone): one bare cli.main session — 启动 g1 仿真 → 走到坐标 (10.0,1.5) → 找前面的红色的东西. Nav at_position ✓ → RAN (UNCAUSED, D14); detect `·` → RAN (no oracle in the camera-no-arm namespace, D50/D61). GROUNDED=0 (zero false-greens), RAN=2 (both correct). g1 walked, no fall; grounding-dino genuinely ran. RED-TEAM (eyes-on): the first capstone navigated to a vantage facing a BLANK WALL where grounding-dino returned a DEGENERATE FULL-FRAME box a naive centre-check accepted; CAUGHT + FIXED in the demo (detect at the FRAMED vantage; `_localization_check` REJECTS any box >70% of frame area). LESSON: a full-frame "detection" is NOT a localization — area-gate it. The MOAT was unaffected (graded RAN both runs); only the demo's visual claim was hardened.
- R7 frontier: honest GT-backed GROUNDED detection (box back-projects to where sim GT says the object is — an oracle INDEPENDENT of the means, the ONLY honest path) (→D63); OR obstacle-aware g1 nav (→D64).

## D63 — G1 R7 the HONEST redemption of R4: g1's FIRST LEGITIMATE GROUNDED — a GT-backed PERCEPTION match against INDEPENDENT sim ground truth, via bare cli; correct→GROUNDED, wrong→RAN · 2026-06-23 (autonomous G1 campaign)
- GOAL: g1 (camera, no arm) earns its FIRST non-RAN grade — an HONEST GROUNDED detection. R4 did this via a SELF-READ (false green, removed D61); R7 does it honest BY CONSTRUCTION with a MANDATORY refutation: the detector's box = the CLAIM; the verify checks it against INDEPENDENT sim GT the detector cannot author.
- THE MECHANISM (all WORLD-side; spine vcli/cognitive/ BYTE-UNCHANGED across 63 decisions): `vcli/worlds/g1_perception_oracle.detection_matches_gt(target)` — INDEPENDENT GT = a MuJoCo SEGMENTATION render (per-pixel geom-id from g1's head camera, never shown to the detector); CLAIM = grounding-dino's box on the RGB ONLY (the firewall); True IFF a box center is within tol of the matching-colour geoms' segmentation centroid. Bound ONLY for a camera-bearing ARM-LESS sim base. SPINE UNTOUCHED — `detection_matches_gt('红色') == True` is a STATE-oracle-vs-CONSTANT compare the EXISTING classifier already grades GROUNDED (the oracle's RESULT gates the verdict); a bare call grades RAN (not self-certifying).
- HONEST PIVOT (recorded as the round demanded): the original plan (world_to_pixel projection of get_object_positions GT) was UNRELIABLE (the freejoint pickables are OCCLUDED at spawn; the head-camera pose-vs-frame didn't reconcile under a standard pinhole convention). SWITCHED to the SEGMENTATION render — strictly MORE independent + MORE reliable, ZERO convention assumed (the red localized is the static red STOOL, in view). world_to_pixel kept (unit-tested) for a future round.
- REAL-VERIFY (bare cli REPL, foreground): 启动 g1 仿真 → 找前面的红色的东西 → 找前面的绿色的东西. RED `detection_matches_gt('红色') == True ✓` → **GROUNDED verified=True**; GREEN (MANDATORY REFUTATION — same frame, same detector run, green NOT in view) `detection_matches_gt('绿色') == True ·` → **RAN verified=False**. EYES-ON: red box + GT seg-centroid coincide; the refute frame is a blank wall with NO GT crosshair. WHY HONEST: detector input is ONLY rgb (firewall); the oracle reads independent sim GT (not len(detect_objects())>0); the REFUTATION fires (a trivial oracle would ground all colours; this distinguishes red-in-view from green/blue-not). GROUNDED is EARNED by a spatially+colour-correct localization. NOT a CEO gate. (mujoco_g1.py 845 LOC — flag for next Review.)
- R8 frontier: multi-trial honest rate (randomized spawn); a live-LLM producer on g1 (the standing D46-D63 caveat); obstacle-aware g1 nav (→D64); nail the head-camera world_to_pixel convention.

## D64 — G1 R8: obstacle-aware navigation — g1 routes AROUND the pick_table via recovered vgraph (RAN-honest) · 2026-06-23
- Recovered `g1_vgraph.py` (visibility-graph planner: obstacle polygons → inflate by robot radius → vgraph → Dijkstra → waypoints; path=inf when boxed-in) from git 62ee0de; built the go2-room obstacle set (pick_table + walls) + wired g1.navigate_to to PLAN via vgraph instead of open-loop straight-line. (R8's RECORD was cut by a transient API 500 mid-record; the orchestrator verified + completed it.)
- REAL-VERIFY: goal (12,3) is on the FAR side of the pick_table (~x=11), straight-line-blocked. BASELINE straight-line: reached=False (stalls at the table edge, the R2 behaviour). PLANNED vgraph: reached=True (routed AROUND via a waypoint detour, plotted). BARE-CLI: 切换到 g1 仿真 → 走到坐标 (12,3) → "已绕过桌子" → `▸ navigate → verify at_position(12,3) ✓ (actor=UNCAUSED)` → `verdict RAN` — g1 reached the blocked goal (impossible without getting around x=11), at_position RAN-honest (D14), no fall.
- HONEST: g1 nav is now obstacle-AWARE (parity with go2's FAR route) vs the prior walk-into-it. RAN-honest (base nav UNCAUSED, D14). Spine vcli/cognitive/ BYTE-UNCHANGED (64 decisions). Recovered code, not rebuilt.

## D65 — Plug-and-play refactor S1: embodiment config foundation + BaseProtocol conformance · 2026-06-23 (arch/plug-and-play)
- DIRECTION (CEO): make the OS a PLUG-AND-PLAY platform — a robot is a CONFIG file, not a driver class (Rule 11); routing model-driven not keyword (legacy producer strangled). North Star expanded (5 bring-your-own contracts: Embodiment/Policy/Skill/Capability/Verify + the honest-verify moat as the IP); Rules 11 (embodiment-as-config) + 12 (e2e mandatory, never skipped) added. New branch `arch/plug-and-play` off feat/orchestrator-redesign; staged strangler-fig, bare-cli e2e each stage. Full audit/analysis at /tmp/pnp_synthesis.md (to be filed as a phase plan).
- S1 (additive, behavior-preserving, spine BYTE-UNCHANGED): `embodiments/config.py` frozen `EmbodimentConfig` + fail-loud `load_embodiment_config`; `embodiments/{go2,g1}/robot.yaml` faithfully capture the current driver constants (spawn / stance-by-name / sensors / root_body / capability profile); MuJoCoG1 made BaseProtocol-uniform with MuJoCoGo2 (+name/disconnect/get_velocity/get_odometry/supports_* — 6 missing members). SURFACED A LATENT OOM BUG: sim_tool.stop() called `base.disconnect()` on g1 which only had `close()` → swallowed → g1 sims never torn down; the additive `disconnect()` alias fixes it. 67 offline unit green; real-sim e2e: g1 connects, methods sane on live data, disconnect tears down. Config NOT yet wired into the driver (S2). Hygiene: removed 5 dead files (round probes + orphan go2_bridge); DECISIONS compacted 494→250.
- NEXT (staged, e2e each): S2 generic driver READS config (delete hardcoded constants, DoF self-introspect from root_body); S3 ONE shared impl per capability (navigate / lidar / scene-build — kill the go2-vs-g1 forks); S4 embodiment registry (replace `sim_tool` enum — CEO gate); S5 ControlPolicy plugin (interface + convex_mpc dep — CEO gate); S6 capability planner-exposure + side-effecting permission path (CEO gate); S8 delete the legacy keyword producer + tables; S9 doc CI drift gate. Docs: ARCHITECTURE high-level rewrite + getting-started.md.

## D66 — Plug-and-play S2: the drivers READ the config + generic DofLayout (behavior-preserving) · 2026-06-24 (arch/plug-and-play)
- The two MuJoCo drivers now SOURCE spawn + nominal stance from `embodiments/<id>/robot.yaml` (via `load_embodiment_config`), and use a GENERIC `embodiments/dof_layout.py` `DofLayout(model, root_body, num_actuated)` (generalizes g1's `_G1Offsets`) for ALL qpos/qvel slice addresses — go2's ~14 hardcoded `qpos[7:19]`/`qpos[0:3]` literals + g1's bespoke offsets are GONE. `DofLayout` introspects the root freejoint: go2 (`base_link`) → root=0/legs=7 (== the old literals); g1 (`g1_pelvis`) → introspected. THE TRAP (handled): go2's actuator order (FR,FL,RR,RL) ≠ its qpos leg order (FL,FR,RL,RR), and `_STAND_JOINTS` is a qpos slice → the stance vector is assembled in qpos-address order (a test pins it; go2's uniform triple would otherwise mask a wrong order). g1's `_G1Offsets` is now a thin `DofLayout` subclass (zero call-site churn).
- BEHAVIOR-PRESERVING by construction (config values == old constants; same resolved indices). Spine `vcli/cognitive/` BYTE-UNCHANGED. VERIFIED (orchestrator, not the build agent's word): 78 unit green incl. stance==`_STAND_JOINTS`/`_DEFAULT_ANGLES` + spawn==constants proofs; real-sim e2e go2 (connect/stand/walk/turn/sit, 3 passed) + g1 (spawn + RL-gait walk-forward displacement, 6 passed). SAFE stage (no new interface/dep). Sensors-from-config + the single generic driver class are later stages.
- NEXT S3: ONE shared impl per capability — collapse the go2-vs-g1 forks (navigate / lidar / scene-build). Then S4 embodiment registry (replace the `sim_tool` enum — CEO gate).

## D67 — Plug-and-play S3a: lidar raycast de-triplication (behavior-preserving) · 2026-06-24 (arch/plug-and-play)
- The identical `mj_ray` polar-grid lidar loop lived in 3 places (LIVE: `g1_lidar_scan` + `go2._update_lidar`; PAUSED: `lidar360.MuJoCoLivox360`). Extracted the shared ray loop VERBATIM into `hardware/sim/sensors/lidar_raycast.py raycast_lidar(...) -> RaycastResult`; both live drivers call it with their own params + keep their own LaserScan/diag/storage assembly (so external output is byte-identical). `lidar360` (paused SysNav) untouched.
- Real divergence handled (observable, not refactor freedom): go2's elev==0 mid-ring does NOT apply the `max_range` bound while g1's does → surfaced as `mid_ring_apply_max_range` (g1=True / go2=False), pinned by a test; `max_range` 15(g1)/12(go2) parameterized; near-zero-self-hit diag is g1-only (go2 ignores the field).
- BEHAVIOR-PRESERVING (golden equivalence tests byte-identical at fixed model+pose). Spine `vcli/cognitive/` BYTE-UNCHANGED. VERIFIED (orchestrator, not the build agent's word): 175 unit green; real-sim e2e go2 lidar (360 ranges/360 finite) + g1 lidar (360/360, n_returns=360). SAFE stage.
- S3 remaining: S3b scene-build (go2 string-template `.replace` → `MjSpec.attach`, the one path) ; S3c navigate (g1 in-driver vgraph vs go2 external ROS2-FAR — genuinely different mechanisms; needs a design call, possibly gate-adjacent).

## D68 — Plug-and-play S3b: ONE scene builder (`MjSpec.attach`) for go2 + g1; go2 driver finished qpos-order-agnostic (behavior-preserving) · 2026-06-24 (arch/plug-and-play)
- THE FORK KILLED (Rule 11 "two scene builders"): go2's string-template `.replace` of `go2_room.xml` (tokens GO2_MODEL_PATH/GO2_ASSETS_DIR/GRASP_WELDS + `<include>`) and g1's bespoke MjSpec path are now ONE shared `hardware/sim/scene_builder.py build_room_scene(robot_model_path, room_template_path, room_assets_dir, *, attach_prefix, spawn_xy, welds=(), out_path=None, robot_meshes_dir=None, camera=None) -> (MjModel, path)`. World-agnostic: strips the robot `<include>` from the room template, `MjSpec.from_file` the room-only base, `.attach(robot_spec, prefix=…, frame=spawn)`, adds config welds + optional head camera, `compile()` → MjModel; writes a reloadable file (cross-process `scene_xml_path` contract) via `normalize_attach_defaults(spec.to_xml())`. Both `_build_room_scene_xml` (go2, prefix="", 3 grasp welds piper_link6↔pickable_* when arm) and `_build_g1_room_scene_xml` (g1, prefix="g1_", head_rgb camera) now call it and keep their `-> Path` signature (callers unchanged).
- ROOT CAUSE of the historic blocker (the reason go2 was on `<include>`): MuJoCo 3.9's `MjSpec.attach`+`to_xml()` re-serializes go2's schema-mandated nested `<default class="go2">` tree wrapped in a SECOND anonymous (class-less) `<default>`, which `from_xml_path` rejects (`XML Error: empty class name`). The COMPILED model is fine; only the SERIALIZER is buggy. Fix (B): keep the in-memory `compile()` model authoritative + repair the FILE with a contained ElementTree `normalize_attach_defaults` that hoists the anonymous wrapper (no-op for g1's flat tree). Fix (A) "flatten go2.xml" was REJECTED — MJCF *requires* the class-less root `<default>` wrapper; go2's tree is correct, not redundant. NO source asset edited (go2.xml / go2_room.xml / piper.xml / go2_piper.xml / build_go2_piper.py all CLEAN).
- THE SECOND, REAL BLOCKER (caught by the build agent stopping at the byte-identical guardrail, not by the spike): `attach` lands the robot LAST (so the room's `pickable_*` keep unprefixed names) → go2's base freejoint moves qpos[0]→qpos[21], arm qpos[19:27]→[40:48]. D66 migrated go2's base+leg literals to `DofLayout` but MISSED 4: the arm-stow slice `qpos[19:27]` (connect + reset_pose) and the MPC Pinocchio mapping (`mujoco_q[0:3]`/`[3:7]`/`[7:7+n]`, `mujoco_dq[0:3]`/`[3:6]`/`[6:6+n]`). Finished them → all `DofLayout`-derived (`root_qpos_adr`/`quat_start`/`joint_qpos_start`/`root_dof_adr`/`angvel_start`/`joint_dof_start`); arm slice = `joint_qpos_start + num_actuated` (verified: legacy=19, attach=40). `ctrl[12:19]` left literal — actuator order is identical under attach (verified). NOT a new CEO gate: finishing S2's already-approved order-agnostic migration; model byte-identical; no new interface/dep/hardware/cross-package flow.
- LATENT BUG fixed (→tricky-bugs): the arm-stow guard `model.nq >= 27` mis-fires for BARE go2 — the room's `pickable_*` free joints make bare-go2 nq=40 (not 19) in BOTH build paths, so it tried to stow a nonexistent arm. Legacy only "got away with it" because `qpos[19:27]` was in-bounds and `_PIPER_STOW_QPOS` is all-zeros (a silent pickable-zeroing no-op). Gate is now `model.nu >= 19` (arm = +7 actuators; nu = 12 bare / 19 arm in both paths) — strictly more correct.
- BEHAVIOR-PRESERVING — byte-identical COMPILED model all 3 cases (go2+arm, bare go2, g1): same nq/nv/nu/nbody/ngeom/ncam/neq, identical body/joint/actuator/camera NAME-sets, identical class-default-derived jnt_range/geom size/friction/condim/actuator ctrlrange BY NAME (NOT by row — include orders robot-first, attach robot-last; by-row compare is INVALID, the first false-fail caught in the spike), 4 condim=6 foot geoms friction[0]=0.8. Spine `vcli/cognitive/` BYTE-UNCHANGED. VERIFIED (orchestrator, re-run not the build agent's word): phase-1 legacy `test_mujoco_go2.py` 33 green (migration alone behavior-preserving); phase-2 attach build `test_mujoco_go2.py` 33 + `test_scene_builder.py` 6 + `embodiments/` 24 = 63 green; byte-identical spike ALL THREE True; headless smoke arm z=0.350 root=21 arm_q0=40 stowed / bare z=0.350 root=21 / g1 z=0.793. `test_dof_layout` updated: the obsolete `root_qpos_adr==0` legacy-include literal → the order-independent CONTRACT (quat=root+3, legs=root+7, …) the driver actually relies on (fixture now sees root=21, exercising the attach build). 2 git-tracked scene-output artifacts regenerated (same model, new normalized serializer bytes). Full bare-cli e2e NOT yet run (S3b e2e per Rule 12 still owed before "done").
- S3 remaining: S3b bare-cli e2e (go2+arm grasp + bare go2 nav + g1 — Rule 12 acceptance) ; S3c navigate (g1 in-driver vgraph vs go2 external ROS2-FAR — genuinely different mechanisms; design call, possibly gate-adjacent).

## D69 — Close the FAKEABLE-GRASP false-green: physical goals route to the SKILL (Prong 1) + a goal-authenticity gate proves possession via the GT oracle (Prong 2, stricter-only) · 2026-06-24 (arch/plug-and-play)
- THE DEFECT (real bare-cli run, deepseek-v4-flash on go2+arm): NL "抓前面的东西" let the model FABRICATE a grasp — it used the kernel-generic dev tool `file_write` (offered as a "motor tool" in the native loop) to write `grabbed.txt`, then self-authored `verify(file_exists('grabbed.txt'))`. `file_exists` is in `_PREDICATE_ORACLES` so it classified GROUNDED structurally, AND it is a non-robot predicate so actor-causation never graded it, AND there was no coordinate goal so the D15/D16 turn gate failed open → `evidence_passed` returned GROUNDED. A physical grasp satisfied by touching a file. The object-goal analogue of the coordinate authenticity gap D11–D16 closed for coordinates. The DEFECT is in OUR system, not the model (CEO).
- PRONG 1 (routing, world-side; NO spine edit): in `native_loop._build_motor_tools`, a ROBOT world (connected `agent`) now DROPS the mutating code tools (`file_write`/`file_edit`/`bash`) from the loop's ACTION surface — a physical robot goal cannot be "accomplished" by a file/shell write — while KEEPING the read-only diagnostics (`file_read`/`glob`/`grep`) the persona inspects code with. `_MUTATING_CODE_TOOLS` frozenset, gated only when `agent is not None`; the DEV world (no agent) keeps the full set unchanged (world-agnostic, no "if dev" branch). The native system prompt also gains a hard binding: "a PHYSICAL action is performed ONLY by the robot SKILL and proven ONLY by a GT oracle; NEVER fake it with a file/marker/shell — if no skill can do it, say so and stop."
- PRONG 2 (the un-fakeable backstop; rule-5 STRICTER-ONLY; deliberate edit to the byte-frozen-since-2dafff4 `vcli/cognitive/`): new PURE helper `cognitive/object_goal.py` (the grasp analogue of `coord_goal.py`) — `goal_has_object_intent(goal)` (抓/夹/拿/握/捡/pick/grasp/grab — INTENT only, never extracts the target) + `has_necessary_manip_oracle(expr)` (a NECESSARY-conjunct call to a GT manipulation oracle `holding_object`/`placed_count`, reusing the D16 necessity AST walk; `not holding_object()` place-release counts; an `or`-decoy does not). A new turn-gate `_object_goal_turn_ok` in `trace_store.evidence_passed`, placed AFTER the D15/D16 coordinate gate and SCOPED to a manipulation world (`holding_object` in the live oracle set): for a grasp-intent goal it requires ≥1 checked step that BOTH classifies GROUNDED AND carries a necessary manipulation-oracle conjunct; else the turn → RAN. Like the coord gate it can ONLY reject (all-GROUNDED already held) and can NEVER fake-pass (the satisfying step must itself be GROUNDED = actor-CAUSED + honest oracle).
- STRICTER-ONLY proof (adversarial, through the REAL `evidence_passed`): real grasp (`holding_object('banana')` CAUSED) → GROUNDED; three fabrication vectors (`file_exists`, `len(describe_scene())>0`, `path_contains`) → RAN; pick-and-place (`placed_count()==1`), release (`not holding_object()`), generic pick (`holding_object()`), grasp+home chain → GROUNDED unchanged; place-only / coordinate nav (go2, no arm oracle) / dev file task / g1-detect (no arm oracle) → UNCHANGED; a grasp that legitimately RAN (verify False) stays RAN. NO honest GROUNDED/RAN turn flips; nothing is greened. Spine bytes touched: `trace_store.py` (+47 lines: the `_object_goal_turn_ok` gate + a lazy local import like the existing `coord_goal` one) + the new `object_goal.py` helper — both PURELY subtractive (rule 5 "only ever stricter"). `evidence_classifier.py` / `actor_causation.py` / `coord_goal.py` UNTOUCHED; the native-loop import firewall is unaffected (the gate lives in trace_store).
- VERIFIED: 169 affected-file unit green (37 new `test_object_goal.py` + native-loop incl. 2 new Prong-1 toolset tests + coord/evidence/firewall); full `tests/unit/vcli` 745 passed (the only 3 reds are the pre-documented environmental deepseek-config reds, red on clean HEAD too). Regression pinned: a "抓" goal verified by `file_exists(...)` grades RAN (`test_fake_grasp_via_file_exists_grades_ran`). BARE-CLI LIVE DEEPSEEK ACCEPTANCE (PTY `run_repl_session`, MUJOCO_GL=egl, real key): run 1 NL "启动 go2 仿真 带机械臂"→"抓前面的东西" — the grasp turn used ONLY read-only glob/file_read (the model's old `.txt` instinct still visible) but had NO file_write/bash to fabricate with → NO marker file created (Prong 1 PROVEN in the live product). Run 2 (with the arm booted) NL grasp turn ROUTED to the REAL `perception_grasp(query=…)` + `detect`(grounding-dino, offline weights loaded) + `walk` — again ZERO file_write/bash/marker anywhere. The grasp did not reach a captured final verdict (harness 380s SIGKILL) because of a PRE-EXISTING, unrelated arm bug surfaced by the run: `PiperROS2Proxy.move_joints: expected 6 positions, got 5` (the `scan` skill, NOT D17 code — left for a separate fix). HONEST: routing to real grasp + the un-fakeable property are both proven; a clean end-to-end GROUNDED awaits the Piper 6-vs-5 DoF fix.
- NEXT: a moat hardening, not a stage; S3b bare-cli e2e + S3c navigate remain. `_MANIP_ORACLES` should eventually single-source from skill `verify_hint` (as `_PREDICATE_ORACLES` notes for R2) so any new GT manipulation oracle is tracked automatically.

## D70 — scan DoF-aware fix (Piper 6-vs-5) + LOOP HANDOFF (autonomous `vector-plugnplay` loop) · 2026-06-24 (arch/plug-and-play)
- scan-DoF fix (committed `1712a64`): `skills/scan.py` hardcoded the 5-DoF SO-101 default pose (`_DEFAULT_SCAN_JOINTS`) and sent it to ANY arm; the 6-DoF Piper's `move_joints` rejected the 5-vector ("expected 6 positions, got 5") → the grasp chain (scan→detect→pick) crashed in `scan` before the grasp. FIX: scan holds the arm's CURRENT pose (a safe no-op) when the configured/default pose length != the connected arm's DoF (`getattr(arm,'dof',None)` else `len(get_joint_positions())`); SO-101 (5==5) byte-identical. Same morphology-hardcoding class as tricky-bugs Case 8/9 (a fixed-DoF constant on an arbitrary-DoF arm). Regression: `tests/unit/skills/test_scan_dof.py` (3). For go2+Piper the workspace view is the go2 head d435, not the arm wrist, so the scan arm pose is non-critical.
- LOOP HANDOFF: this session ends (compacting); an autonomous EvolvingLoop **`vector-plugnplay`** continues the plug-and-play refactor while Yusen is away. Registered: `~/.claude/loops/vector-plugnplay.goal` + a REGISTRY.md row (supersedes the stale `vector-os` / `g1-crossembodiment` rows — same dir). Resume invariant holds: {goal + STATUS.md + DECISIONS + git + .sdd} cold-starts each round. ROUND LADDER: R1 SEAL S3b via the DETERMINISTIC `FakeToolScriptBackend` grasp (NOT the live-deepseek multi-turn REPL — 5 live runs stalled on model REPL variance, never the grasp code, which is proven to route to the real `perception_grasp` + can't fake); then S3c navigate convergence, S8 delete the legacy keyword producer + tables, S9 docs + CI drift gate, non-gated hardening (deepseek launch-routing reliability; gitignore the generated scene XMLs). CEO-GATE stages S4 (embodiment-registry interface) / S5 (ControlPolicy + convex_mpc dep) / S6 (capability permission/security) → design + spike + executive-summary to a decision queue, pivot to non-gated work, NEVER cross. Disciplines (full set in the goal file + STATUS `next`): spine ONLY-EVER-STRICTER (D69 precedent), byte-identical + e2e per refactor, serialize sims + nuke, never-trust-subagent (verify diff+rerun+e2e), docs every round.

## D71 — Plug-and-play S3b SEALED: deterministic perception-grasp GROUNDED on the go2+Piper attach scene (loop R1) · 2026-06-24 (arch/plug-and-play)
- WHY: S3b (the ONE `MjSpec.attach` scene builder for go2+g1) was code-proven + byte-identical, but its bare-cli grasp had never reached a CAPTURED GROUNDED verdict — 5 live-deepseek runs stalled on multi-turn REPL variance (network drop / cold-load timeout / a faked grasp [pre-D69] / bash-wander / launch-wander), NEVER the grasp code. The acceptance gap was the LLM's REPL-driving reliability, not the pipeline. Sealing it through a flaky live model is the wrong instrument.
- WHAT: new deterministic e2e `tests/vcli/test_native_loop_grasp_attach_pty.py` (`-m sim`, 35 s). Builds the go2+Piper world IN-PROCESS exactly as `sim_tool._start_go2(with_arm=True)` does but without ROS2 proxies (`VECTOR_SIM_WITH_ARM=1` → `MuJoCoGo2(room,backend="mpc")` + `MuJoCoPiper(go2)` + `MuJoCoPiperGripper(go2)` + `Go2GraspPerception(go2)`; skills = `get_go2_skills()` + the 4 Piper manip skills + `PerceptionGraspSkill` last). A scripted `FakeToolScriptBackend` replays the single pair `perception_grasp("抓绿色的瓶子")` → `verify(holding_object('pickable_bottle_green'))` → end, through the REAL `eng.run_turn_native` — so NO live LLM, but the FULL pipeline executes: colour/HSV `front_object` resolve → EdgeTAM/rect mask → rendered-depth pointcloud (NEVER GT) → MPC-gait approach walk → Piper top-down IK → weld 0→1 → shoulder lift. Green chosen because it sits dead-ahead on the spawn-y line (the D47-proven head-on "前面的东西" target) and a colour query resolves the verify LABEL to `pickable_bottle_green` so the TARGET-AWARE oracle matches the welded body (the faithful analogue of the arm template's `holding_object('banana')`).
- PROVEN (graded by the UNTOUCHED spine + the D69 gate; no new spine bytes): `make_holding_object(agent)()==False` pre-asserted (genuine 0→1) · `step.strategy=="perception_grasp"` (routing) · `verify` startswith `holding_object` (predicate) · `verify_result is True` (grounding) · `ActorCaused.CAUSED` (fresh Piper weld via `weld_is_active`, the gripper-weld channel on the go2 physics thread) · `classify_step_evidence==GROUNDED` · `VerdictReport.verified True / evidence==GROUNDED / exit 0` · `evidence_passed True` (the D69 object-goal turn gate, active because the goal "把前面的绿色瓶子抓起来" carries 抓 intent and the world exposes `holding_object`). Mirrors `test_native_loop_arm_pty.py` (SO-101 `pick`) but proves the THIRD honest path: a perception-FIRST grasp on the COMBINED attach scene, never a GT-pose `pick`.
- SCOPE/HONESTY: net-new TEST only — no source/spine change, no new interface/dep/hardware (not a CEO gate). bare-cli + NL launch-routing reliability is now a SEPARATE non-gated hardening item, no longer a blocker on S3b. NEXT round (recommended): S8 — delete the legacy keyword producer + the should-be-model-driven keyword tables (inventory live dependencies first); then S3c navigate convergence (design-first, gate-adjacent).

## D72 — Plug-and-play S5a: single-source the 3 capability gates onto resolve_capability_profile; S8 deferred as premature (loop R2) · 2026-06-24 (arch/plug-and-play)
- COURSE CORRECTION (a Decision Workflow, not a guess): the loop's planned next round was S8 "delete the legacy keyword producer + tables". Cold-ORIENT + a dynamic Decision Workflow (3 read-only recon agents + an Opus judge) REFUTED the STATUS/audit claim that "run_turn_native is already the default interactive producer": the TRUTH (verified file:line) is run_turn_native is the default ONLY for REPL ACTION turns; chat / sim-launch / embodiment-switch turns still fall to the legacy run_turn_unified (cli.py:2570) + vgg_execute_async (cli.py:2533), the -p path is native-default-OFF, and classify_intent/IntentRouter.should_use_vgg is a LIVE routing gate at 4 sites (cli.py:414, cli.py:2445, engine.py:1090, engine.py:1707). So S8 would CUT a live dependency AND change a routing-contract interface = both a hard-constraint violation and gate-adjacent → DEFERRED until its real preconditions land. This is the "don't bulldoze a wrong plan" discipline working: the optimistic ladder was corrected by analysis, not carried out blindly.
- CHOSEN ROUND S5a (NON-gated, no live-cut, byte-identical) — the honest precondition for S8 (audit §5 Cap-Gating). Three scattered ad-hoc "what can this body do" checks all duck-typed getattr probing (the capability-by-code drift Rule 11 forbids): native_loop._build_motor_tools navigate gate (`agent._base is not None`), worlds/robot._agent_has_camera, engine._has_base. Consolidated onto ONE `resolve_capability_profile(agent) -> CapabilityProfile` (NEW `embodiments/capability_profile.py`) over the already-declared CapabilityProfile (embodiments/config.py, parsed from each robot.yaml, loaded into the driver as `_base._config`). AUTHORITY (deliberate): the GATED flags has_base + camera are runtime-authoritative (the exact prior checks) → BYTE-IDENTICAL; the ENRICHMENT flags has_arm/has_gripper/lidar reconcile runtime-OR-declared → makes the go2+Piper RUNTIME-ATTACH honest (bare go2 manifest declares has_arm:false yet a Piper binds at runtime → runtime member wins). No current gate reads the enrichment flags, so the reconcile is behavior-inert today (the forward seam for S5b+). Shifting the GATED flags' authority to the manifest is a deliberate FUTURE step (a behavior change), NOT this round.
- VERIFIED (byte-identical proof + e2e, Rule 12): RED→GREEN 13 new parity tests (tests/unit/vcli/test_capability_profile.py) asserting resolve(agent).has_base/.camera == the legacy ad-hoc results for bare-go2 / go2+arm / g1 / arm-only / dev(None) + the runtime-OR-declared reconcile; full `tests/unit/vcli tests/vcli -m "not sim"` = 1435 passed with ZERO assertion edits to existing tests (the byte-identical proof). E2E: the R1 deterministic grasp (test_native_loop_grasp_attach_pty.py) STILL GROUNDS on the real go2+Piper sim through the rewired _build_motor_tools (32 s). Red-teamed: resolve(None).has_base/camera == False == the prior `agent is not None and ...` guard; the rewire is byte-identical by construction + by suite + by real-agent e2e. Committed c31c6d3 (code) + this RECORD. NOT a CEO gate (internal resolver; does NOT change the World 4-thing contract; no new interface/dep/hardware).
- PRE-EXISTING REDS (NOT this round — proven by `git stash` + re-run on clean HEAD): 3 tests/unit/vcli/test_config_deepseek_provider (documented env reds) + 3 tests/vcli/test_repl_native_cutover.py::test_repl_attempt_native_* (the REPL native-CUTOVER attempt path returns acted=False / native_calls==0 on clean HEAD). The latter are undocumented and RELEVANT to S5b (the native-default-on-more-paths round) — investigate there.
- CORRECTED dependency-ordered ladder (the S5a/S5b/S5c "routing/capability-gating track" is distinct from the GATED "S5 ControlPolicy + convex_mpc dep" stage): S5a✅ → S5b (native default on the -p path, additive; fix the repl_cutover reds) → S5c (registry-driven should_attempt_native HINT at-parity with should_use_vgg, shadow-mode) → S3c-design (navigate convergence, design-only, gate-adjacent) → S8 (GATED, requires S5a+S5b+S5c). S4/S5-ControlPolicy/S6 remain CEO gates.
- CORRECTION (verify-the-subagent): the judge claimed "generated-scene-XML gitignore is ALREADY done (.gitignore:17-18,49-51)". PARTLY WRONG — `git check-ignore` shows mjcf/go2/scene_room_piper.xml is NOT ignored and is still TRACKED (it churned on every R1/R2 sim run); mjcf/g1/scene_g1_*.xml likewise. .gitignore L17-18 cover only go2 scene_flat/scene_room; L49-51 cover the OLD hardware/sim/ path. The hardening micro-task (add the real paths + `git rm --cached`, after confirming no byte-identical scene-builder test reads the committed XML) remains OPEN. Logged so a future ORIENT does not trust the false "done".

## D73 — Plug-and-play S5b: native is the DEFAULT producer on the -p path (print cutover) + stale repl-cutover test fixed (loop R3) · 2026-06-24 (arch/plug-and-play)
- WHY: S8 (delete the legacy producer) requires native to be the default EVERYWHERE. The interactive REPL already cuts over to native by default (`_repl_native_enabled`, owner-approved 2026-06-19), but the non-interactive `-p` acceptance entrypoint (`run_one_turn`) defaulted to the legacy decompose+execute. S5b closes that gap (S8 precondition #2).
- WHAT: new `_print_native_enabled()` (cli.py) — DEFAULT ON, reversible escape hatch `VECTOR_PRINT_NATIVE` in {0,false,off,no} — mirrors `_repl_native_enabled` on the `-p` path. `run_one_turn`'s native-attempt-then-fallback block now fires on `_native_first_enabled(args) or (_print_native_enabled() and not _native_loop_enabled(args))`: (a) the default cutover runs native-first-then-fall-through by default; (b) the PRECEDENCE GUARD `not _native_loop_enabled(args)` ensures the default does NOT pre-empt an EXPLICIT `--native-loop` (pure-native) — caught by a real interaction red (test_native_devworld_verify_only_noop_is_ran flipped RAN→FAILED) and fixed; (c) the user-facing `--native-first`/`VECTOR_NATIVE_FIRST` flag DEFAULT stays OFF (NOT changed — that would be the interface/behavior-contract gate the R2 judge flagged); the new default-ON behavior is an INDEPENDENT knob, exactly as the REPL cutover is independent of `--native-loop`. Strictly ADDITIVE: a goal native cannot route (e.g. the VECTOR_FAKE_LLM decompose-plan seam → no tool_calls) falls through to the byte-identical legacy verdict.
- REPL-CUTOVER TEST FIX (Debug Protocol, the 3 pre-existing reds): ROOT CAUSE (confirmed via signature inspection + a stash-on-clean-HEAD repro) = the test double `_FakeEngine.run_turn_native` was missing the `on_progress` param the REAL `VectorEngine.run_turn_native` gained (the D9 #2 live-spinner work); `_repl_attempt_native` passes `on_progress=`, so the call raised TypeError at binding, caught as "no action" → `native_calls==0`, masking the cutover entirely. A STALE TEST — the production REPL cutover was correct. FIX = add `on_progress=None` to the fake (1 line). 20/20 green.
- VERIFIED (Rule 12): full `tests/unit/vcli tests/vcli tests/integration/vcli -m "not sim"` = 1460 passed (only the 3 pre-existing deepseek-config env reds; everything else green with the new/updated wiring tests — escape-hatch, default-attempts-native, no-preempt-explicit-native-loop). E2E on the real sim via the bare `-p` ACCEPTANCE ENTRYPOINT: a raw `cli -p --sim-go2 --headless` run with NO native flag + NO VECTOR_NATIVE_LOOP (default cutover) → native OWNED the turn (per_step strategy=walk, n_steps=1) and emitted an honest verdict through the UNTOUCHED frozen spine (evidence=RAN this run — the open-loop walk didn't land within at_position(11,3) tol; sim-physics nondeterminism, NOT a producer defect; the moat honestly reported it). This proves native is the default `-p` producer end-to-end.
- HONEST GAP: no COMMITTED default-cutover sim test — `run_cli_turn` (pty_cli.py) FORCES `VECTOR_NATIVE_LOOP=1` for a tool_script without `--native-first`, so it cannot exercise the default `_print_native_enabled` path (a harness limitation; a `native_default` param to suppress that injection is a small future harness enhancement). Committed coverage = the deterministic unit wiring tests (default triggers block 1, falls through, escape hatch, no-preempt) + the existing committed sim test (c) `test_native_first_covered_go2_routes_to_native` (block-1 mechanism on the real sim). NOT a CEO gate (additive + reversible; mirrors the owner-approved REPL cutover; no new interface/dep/hardware; the user-facing flag defaults are unchanged). Committed 8c1200e (code+test fix) + this RECORD. NEXT: S5c (registry-driven should_attempt_native HINT at-parity with should_use_vgg, shadow-mode).

## D74 — Plug-and-play S5c: registry-driven should_attempt_native hint (shadow superset of should_use_vgg); S8 preconditions COMPLETE (loop R4) · 2026-06-24 (arch/plug-and-play)
- WHY: S8 (retire the legacy keyword producer) must rewire the 4 `should_use_vgg` gate sites (cli.py:414/2445, engine.py:1090/1707) onto a NON-keyword signal — otherwise routing still depends on `IntentRouter`'s keyword cascade (the thing S8 deletes). S5c builds + proves that replacement (the 3rd and LAST S8 precondition).
- WHAT: new `should_attempt_native(user_input, *, agent, engine)` (native_loop.py). The native producer routes by the MODEL reading tool DESCRIPTIONS, so the only PRE-gate question ("worth attempting native?") collapses to "does this world expose any actionable tool?" — derived SINGLE-SOURCE from `_build_motor_tools` (Rule 3 — the EXACT toolset the native loop offers; NO keyword table). FAIL-OPEN: actionable world + non-trivial input → attempt; trivial input → skip; toolset-build error → attempt (never silently skip the redesign).
- SAFE-SUPERSET property (the S8-enabling guarantee) — CORRECTED R9/D79; the original "for EVERY input" wording was an OVERCLAIM. The superset holds **WITHIN AN ACTIONABLE WORLD** (a world whose `_build_motor_tools` is non-empty): there, for every input `should_use_vgg` routes to VGG, `should_attempt_native` also attempts — so rewiring the gates onto it misses no actionable command. Two scoping facts (both correct-by-design, surfaced by the R9 adversarial review): (1) in a TOOLLESS world (sensorless/dev — empty toolset) it returns False even for an action-shaped command, which is RIGHT (nothing for the model to route to; the keyword router's VGG attempt fails there too); (2) the trivial-input threshold was an off-by-strip MISS (`len(strip())<2` rejected "去 " which `should_use_vgg` accepts) — FIXED in D79 to `len(raw)<2` matching `should_use_vgg`, with a regression test. Extra attempts (chat/questions) are fail-open fallbacks (a wasted LLM call, never a missed/wrong command). S8 must rely on the SCOPED (actionable-world) superset, not an unconditional one.
- VERIFIED: 29 tests (tests/vcli/test_native_routing_hint.py) — the superset corpus (8 actionable commands: every one should_use_vgg=True AND should_attempt_native=True), purity (trivial<2chars→False, empty toolset→False), fail-open-on-error, the documented chat divergence, AND a REAL-derivation test (NO mock): a real Agent with the 10 default skills → attempt True; trivial → False; a bare-registry no-agent engine (empty toolset) → False. SHADOW: NOT wired into the live routing — zero behavior change (the rewiring is S8's job). Committed a563c62.
- MILESTONE: S5a✅(D72) + S5b✅(D73) + S5c✅(D74) = ALL S8 preconditions MET. S8 is now UNBLOCKED but is a CEO GATE (routing-contract + acceptance-entrypoint + flag-default + big deletion). Per loop discipline it is NOT crossed autonomously: an executive summary is queued in STATUS "Pending CEO gates" (the deletion set, the rewire targets, the risk + escape hatches, the verify plan) for Yusen's go/no-go. The loop PIVOTS to non-gated work: S3c-design (navigate convergence, design-only) + the hardening micro-tasks (git rm --cached the tracked generated scene XMLs scene_room_piper.xml/scene_g1_*; the stale-docstring cleanup engine.py:1637/1677-1680).

## D75 — Tidy/de-slop: stale producer docstrings corrected; scene-XML untrack found entangled (loop R5) · 2026-06-24 (arch/plug-and-play)
- Milestone tidy round (docs-hygiene / de-sloppify) after the S8-precondition milestone. Two producer docstrings in engine.py were ACTIVELY MISLEADING after S5b (D73) made native the default producer — a future ORIENT/reader would mis-model the producer architecture:
  - `run_turn_native` said "(M1, flag-gated OFF)… caller cli.run_one_turn behind --native-loop" → CORRECTED: it is now the DEFAULT acceptance producer (REPL `_repl_native_enabled` + `-p` `_print_native_enabled`); `--native-loop`/`--native-first` are explicit forces, not the only entry.
  - `run_turn_unified` said "DARK-LAUNCHED — nothing in cli.py/mcp/server.py calls this yet" → REFUTED: it is LIVE on the REPL non-VGG tool_use/chat route (cli.py:2601). Corrected the docstring + the section header.
- Docstring-only (zero behavior change): imports OK + 49 focused tests (repl-cutover + routing-hint) green.
- FINDING (verify-first paid off): the queued "git rm --cached the tracked generated scene XMLs" hardening is NOT a trivial micro-task — `tests/unit/test_scene_builder.py:58-59` reads the committed scene XML (`scene_room_piper.xml`/`scene_room.xml`) as the byte-identical GOLDEN REFERENCE, and ~10 @sim fixtures `git checkout` the file to restore per-connect churn. Untracking it breaks both. DEFERRED to its own round (re-point the test reference + the fixtures, or generate a fresh reference in-test). STATUS hardening note corrected so a future ORIENT doesn't attempt the naive untrack. NEXT: S3c-design (navigate convergence, design-only).

## D76 — Plug-and-play S3c-design: navigate already converged at the tool layer; planner-plugin GATED+deferred (loop R6) · 2026-06-24 (arch/plug-and-play)
- DESIGN round (design-only per the ladder). FINDING (verified by reading the code, not assumed): the `navigate` capability is ALREADY converged at the TOOL layer — `vcli/native_loop._NativeBaseNavigateTool.execute` calls `base.navigate_to(x, y, timeout=…)` POLYMORPHICALLY with NO go2-vs-g1 branch (Rule 11 already met there). The apparent "fork" is two PLANNER BACKENDS behind one compatible interface: go2 → `Go2ROS2Proxy.navigate_to(x,y,timeout=60,on_progress) -> bool` (external ROS2 CMU-FAR over lidar terrain; the bare in-process MuJoCoGo2 has NO navigate_to — coord-nav needs the proxy), g1 → `MuJoCoG1.navigate_to(x,y,tol,speed,timeout,**_ignored) -> _G1NavResult` (in-driver visibility-graph planner; `**_ignored` is explicitly "for call-shape compatibility with go2"). Both honor (x,y,timeout)→truthy-on-arrival.
- DECISION (ADR-001): the Rule-11 convergence is NOT "make both robots use one planner" — it is "navigate is ONE generic delegate + a PLUGGABLE planner backend declared in robot.yaml (`planner: far|vgraph|…`)", the 5-contract Capability pattern (bring-your-own planner, like the gait Policy). GATED: a new Planner protocol/registry interface + a `nav` robot.yaml field + formalizing the FAR external dep. Recommendation: DEFER until N≥3 planners/embodiments motivate the abstraction (YAGNI at N=2; zero regression risk meanwhile — the tool layer is already converged). Queued in STATUS "Pending CEO gates" for Yusen, batched with S8/S4/S5/S6.
- DELIVERED: docs/ADR-001-navigate-convergence.md (the design + the gate + the defer recommendation) + a runnable invariant guard `tests/unit/embodiments/test_navigate_contract.py` (2 green): both backends honor the polymorphic (x,y,timeout) contract, and `_NativeBaseNavigateTool` carries no concrete-driver-class coupling (no MuJoCoGo2/MuJoCoG1/Go2ROS2Proxy/isinstance). No behavior/interface change this round. NEXT: S9 (rewrite docs/cli-tool-system.md to the native path + a CI doc-drift gate — non-gated, substantial).

## D77 — Plug-and-play S9: cli-tool-system.md rewritten to the native path + CI doc-drift gate (loop R7) · 2026-06-24 (arch/plug-and-play)
- WHY: the audit flagged docs/cli-tool-system.md as STALE — it described `IntentRouter` + `VectorEngine.run_turn()` + the VGG decompose path as the canonical/current flow (native_loop mentioned once), so a new developer would build against the legacy keyword path instead of the model-driven native producer that is now the default (S5b/S5c).
- WHAT: surgical rewrite (not a full re-author — preserves the useful tool-registry/permissions/sessions reference content). Added an AUTHORITATIVE "当前 producer 架构 (cutover 后)" section at the top: a producer-per-path table (REPL action→`run_turn_native`; REPL chat→`run_turn_unified`; `-p`→native via `_print_native_enabled`; `--native-loop`→pure native; legacy/`VECTOR_LEGACY_TURN`→`vgg_decompose`+`vgg_execute`) + the truth (native is the DEFAULT producer, routes by the model reading tool descriptions, NO keyword table; `IntentRouter`/`should_use_vgg` is LEGACY being strangled → S8; `should_attempt_native` (D74) is the registry-driven replacement) + a pointer to the 5 plug-and-play contracts. Marked the now-stale sections (系统架构 diagram / Tool Call 流程 / IntentRouter / VGG 层) as LEGACY with pointers to the authoritative section.
- CI DOC-DRIFT GATE (the "never lag again" mechanism): `tests/unit/vcli/test_doc_drift_gate.py` (green) parses the machine-readable `<!-- doc-drift-gate: default_producer=run_turn_native -->` marker in the doc and asserts the LIVE default AGREES — `_repl_native_enabled()` AND `_print_native_enabled()` both default ON (native default on REPL + `-p`). If a future change flips either default without updating the doc, the gate fails. This ties the documented producer to the code's actual behavior.
- Doc count = 8 (≤8 docs-hygiene limit OK, excl ADR-001). No source/behavior change (docs + one new test). Committed this RECORD. MILESTONE: the NON-GATED ladder is now nearly EXHAUSTED — remaining non-gated = R8 (the deferred scene-XML untrack hardening, fiddly); everything else substantial (S8, S3c-impl, S4, S5, S6) is CEO-GATED and queued for Yusen. After R8 → a milestone report + await-CEO cadence.

## D78 — Plug-and-play R8: scene-XML churn fixed (untrack generated scenes + delete orphan); non-gated ladder exhausted · 2026-06-24 (arch/plug-and-play)
- WHY: the runtime-generated room scenes were TRACKED in git and regenerated on every connect/test, dirtying the tree every R1-R7 sim round (I reverted them by hand each time). The R5 note had flagged the untrack as "entangled".
- CORRECTION (verify-first caught my own earlier misread): the R5 "entangled" claim was WRONG. test_scene_builder.py reads the `go2_room.xml` TEMPLATE (`_ROOM`, line 30/59) and WRITES/regenerates `scene_room_piper.xml` (line 63-64) — it does NOT read the committed generated scene as a golden reference; the ~10 @sim fixtures' `git checkout <scene>` restore is in a caught `except`. So untracking is safe for both.
- WHAT: `git rm --cached` the 2 LIVE generated scenes — `mjcf/go2/scene_room_piper.xml` (regenerated by `build_room_scene`/`_build_room_scene_xml`) + `mjcf/g1/scene_g1_12dof_room.xml` (by `_build_g1_room_scene_xml`) — and added both to `.gitignore` (next to the already-ignored go2 bare scenes). `git rm` the DEAD orphan `mjcf/g1/scene_g1_room.xml` (no references in code/tests/docs; an old scene name superseded by scene_g1_12dof_room.xml, committed in g1 R1 WIP 65bc6c7).
- VERIFIED: tests/unit/test_scene_builder.py 6/6 green (it regenerates the now-untracked scenes from the template — proving the untrack didn't break the byte-identical equivalence test) AND `git status` stays CLEAN after the regeneration (the churn is gone — the R8 goal). No source/behavior change.
- MILESTONE: with R8 the NON-GATED ladder is EXHAUSTED. The autonomous loop drove R1-R8 (D71-D78): S3b SEALED, S5a/S5b/S5c (all S8 preconditions), R5 de-slop, S3c-design (ADR-001), S9 docs+drift-gate, R8 churn-fix. All remaining substantial work is CEO-GATED + queued in STATUS "Pending CEO gates": S8 (retire the legacy keyword producer — preconditions all met), S3c-impl (navigate planner-plugin, deferred N≥3), S4 (embodiment registry interface), S5 (ControlPolicy + convex_mpc dep), S6 (capability permission/security). A batched executive summary was delivered to Yusen; the loop moves to a long await-CEO cadence (resume a gated item only on Yusen's approval / new direction).

## D79 — R9 milestone adversarial review of the R1-R8 arc: build HOLDS, 2 overclaims FIXED, live-model reliability is the real unproven frontier · 2026-06-24 (arch/plug-and-play)
- WHAT: an adversarial Review Workflow (6 read-only skeptics each trying to REFUTE one R1-R8 headline + an ambition critic → Opus judge) + my own empirical re-verify (offline suite 1516 passed, only the 3 documented deepseek env reds; S3b grasp RE-GROUNDS on the real sim in 36 s; tree stays clean after a sim run = R8 confirmed).
- WHAT HOLDS (independently re-verified, trustworthy at face value): the honesty SPINE (`vcli/cognitive/`) is byte-frozen across the entire R1-R8 arc (spine tree-hash identical c93a236→HEAD; the one D69 edit PREDATES R1 and is structurally stricter-only) — NO false-greens anywhere. S3b grasp grading is honest (real weld + perceived body + target-aware verify, no GT pose). S5a resolver byte-identical to the legacy oracles. S5b -p cutover additive+reversible+precedence-guarded. S3c navigate contract, S9 doc-drift gate, R8 scene untrack all hold. The campaign's honesty discipline (nav graded RAN/UNCAUSED not faked GROUNDED; perception fails loud not GT) is its strongest asset.
- 2 REAL HOLES FOUND + FIXED this round (both honesty/doc overclaims, NOT broken capabilities): (1) MAJOR — the D74 "safe superset" was claimed UNCONDITIONALLY ("for EVERY input"). FALSE: it holds only WITHIN AN ACTIONABLE WORLD, and there was a real off-by-strip MISS (`should_attempt_native` checked `len(strip())<2` while `should_use_vgg` checks `len(raw)<2`, so "去 " was dropped). FIX: aligned the threshold to `len(raw)<2` (native_loop.py) + a regression test (test_strip_asymmetry_former_miss_now_attempts) + scoped the D74 wording to "actionable world". The empty-toolset case (returns False for an action command in a TOOLLESS world) is CORRECT-by-design, not a bug. This matters because the GATED S8 is predicated on the superset — it must rely on the SCOPED version. (2) MINOR — the stale absolute slogan `vcli/cognitive/ BYTE-UNCHANGED since 7b220d9` (STATUS) became literally false after D69; corrected to "byte-unchanged 7b220d9→D68, D69 the one stricter-only edit since, frozen across R1-R8".
- THE REAL FRONTIER (the ambition critic's load-bearing finding; "non-gated ladder exhausted" was PREMATURE): the literal North Star — "route by the MODEL, not keywords" — is PAPER-TESTED. Every seal is a deterministic FakeToolScriptBackend replay; all 7 live-LLM test files are skipif'd off (VECTOR_LIVE_LLM=1 required). The green suites mock out the exact capability everything depends on. DECISIONS notes "5 prior live runs stalled on model variance" → the producer was too flaky to seal, so the deterministic half was sealed. The model-producer's end-to-end routing reliability has NEVER been measured. Related: nav is RAN-only on every body (honest, but a half-capability — closing needs cmd_vel causation = spine/CEO); the nav+grasp composite (~1/3 reliability since D52-D56) was never recovered; Rule-11's "ONE generic driver" is unbuilt (drivers read YAML, but per-morphology classes remain — S4 gated); the killer demo has no spike/harness, only prose (ADR-001 even uses it to JUSTIFY deferring the planner-plugin).
- JUDGE'S #1 RECOMMENDATION: build a LIVE-LLM end-to-end grasp reliability harness (swap FakeToolScriptBackend for the real model in test_native_loop_grasp_attach_pty, N≈20 on the attach scene, publish the routing reliability %). Assessed NON-GATED (the already-approved acceptance path + the real model + a sample size). BUT it directly conflicts with the standing loop discipline "不用 live-deepseek 多轮 REPL" (Yusen's instruction; the 5-stall history) → SURFACED TO YUSEN AS A STRATEGIC DECISION, not run autonomously while away. Recorded as the top residual risk: <80% confident the model ALONE reliably routes a grasp end-to-end.

## D80 — R10: live-model grasp routing reliability MEASURED (87%) + visually confirmed (authorized by Yusen) · 2026-06-24 (arch/plug-and-play)
- AUTHORIZED by Yusen ("A 去测") — measure the one thing every deterministic seal sidestepped: how reliably does the REAL model ALONE route a grasp to GROUNDED. Built a robust harness (tools/measure_grasp_reliability.py) that runs N fresh, ISOLATED `cli.main -p --sim --native-loop` subprocesses (live=True → repo-root .env DeepSeek drives it; no scripted backend), each a SINGLE -p turn so the flaky multi-turn REPL launch (the real cause of the "5 prior stalls") is out of the measurement.
- RESULT (N=15, Chinese NL "把香蕉抓起来拿在手里"): **13/15 = 87% GROUNDED+verified** — the model autonomously routed pick → verify(holding_object('banana')) → GROUNDED, graded by the untouched honest spine (real weld 0→1, no GT). 2/15 RAN (the model over-perceived — routed to detect/scan with verify=detect_objects() and stopped, instead of committing to pick). 0 NO_TRACE, 0 ERROR/TIMEOUT. This CORRECTS the pessimistic prior: the core model-routing of a grasp is reliable (~87%) with ZERO infra/stall failures; the "5 stalls" were the multi-turn REPL LAUNCH, a different+harder path.
- VISUAL CONFIRMATION (per REAL-VERIFY + Yusen's request to "see the effect"): tools/visual_grasp.py renders the grasp offscreen (EGL). before/after at the overhead + a 3/4 free camera: banana ON the table → arm raised, banana REMOVED from the table (in the gripper); data banana z 0.06→0.26 (lifted 20cm), holding_object True. A real grasp seen both in the picture and in the physics ground truth — not a verdict-only claim. (The headless reliability runs are data-only; this is the eyes-on render.)
- THIS CLOSES the R9/D79 #1 residual risk (the North Star "route by the MODEL" was paper-tested). The model-producer IS reliable enough to be real (87%), and the failure mode is ACTIONABLE: a system-prompt / tool-description nudge to commit to the grasp instead of over-detecting could lift it. SCOPE: standalone-arm grasp routing (the cleanest isolation of the model's decision); the go2+Piper perception_grasp (full North Star path) is a harder follow-up worth the same treatment. Harnesses committed (the judge's "make the live measurement the seal"). Reliability log: tools/grasp_reliability.log (gitignored runtime artifact).

## D81 — R10b: live-model go2+Piper PERCEPTION-grasp reliability MEASURED (90%) + visually confirmed — the full North Star path (chosen by Yusen) · 2026-06-24 (arch/plug-and-play)
- Yusen chose (b): measure the REAL North Star path — the go2+Piper PERCEPTION grasp (VLM/EdgeTAM perceive → depth pointcloud 3D grasp point → approach walk → Piper top-down IK → weld), NOT the GT-pose arm pick. Harness: tools/measure_go2piper_grasp_reliability.py + tools/_live_grasp_go2piper_once.py — each run a FRESH isolated subprocess that builds the go2+Piper attach world in-process (as the R1 deterministic test) but drives run_turn_native with the REAL DeepSeek backend (no scripted turns); the model alone must route a perception grasp on the combined dog+arm world.
- RESULT (N=10, Chinese NL "把前面的绿色瓶子抓起来"): **9/10 = 90% GROUNDED+verified** — the model autonomously routed `perception_grasp` (7×) or `detect→perception_grasp` (2×) → holding_object → GROUNDED, graded by the untouched spine (real weld 0→1, perceived not GT). 1/10 RAN (the model did `detect`×3 and never committed to the grasp — the SAME over-perceive failure mode as the arm). 0 ERROR/TIMEOUT/NO_RESULT.
- This MATCHES (slightly beats) the standalone-arm 87% (D80): the HARDER perception path is equally reliable. Both measurements together answer the R9/D79 #1 residual — the North Star "route by the MODEL, perceive→grasp" is PROVEN at ~87-90%, with a single, ACTIONABLE failure mode (over-detect instead of grasp; a prompt/tool-desc nudge should lift it).
- VISUAL CONFIRMATION (tools/visual_go2piper_grasp.py, EGL offscreen, free 3/4 cam): before = dog at spawn facing the table, green bottle ON the table (between red+blue); after = dog WALKED UP, Piper arm raised, green bottle UP IN THE GRIPPER (gone from the table); data green-bottle z 0.32→0.567 (lifted 0.25m), holding_object True. NOTE: perception_grasp's own `.success` self-report was False (the known unreliable Piper is_holding no-weld false-negative) — the GT oracle (holding_object + lifted bottle z) is the truth and confirms a real grasp; we trust the GT, never the skill self-report (feedback_measure_ground_truth).
- BOTH live harnesses committed (the judge's "make the live measurement the seal", now done for the standalone arm AND the full go2+Piper North Star). Logs are gitignored runtime artifacts. NEXT options for Yusen: raise ~90% via a grasp-commit prompt nudge; or the queued CEO gates (S8 etc.).

## D82 — R11: prompt nudge ELIMINATES the over-detect failure (92%); + Yusen's IK-margin hypothesis VALIDATED (the grasp is marginal) · 2026-06-24 (arch/plug-and-play)
- NUDGE (the over-detect fix): added native-prompt guidance "the grasp skill perceives the target itself; do NOT detect/scan first, never repeat detect — commit to the grasp" (native_loop.py system prompt). Offline 1471 green (no regression). RE-MEASURE go2+Piper N=12: **11/12 = 92% GROUNDED, and ALL 11 successes are DIRECT perception_grasp (zero detect-first)** — the over-detect failure mode (detect×3) that caused the D80/D81 misses is GONE. The 1 remaining failure was an EMPTY trace (model produced no action — a rarer, different variance, not over-detect). The nudge is honest (correct tool-usage guidance, not metric-gaming) and one-prompt-change benefits both grasp paths.
- IK-MARGIN FINDING (Yusen's domain hypothesis "抓取距离太远 IK 无法计算" — VALIDATED by a direct IK probe, tools/_ik_probe_once.py): for green AND the laterally-offset blue/red bottles, perception_grasp's first grasp-point is "**out of reach**" and IK FAILS repeatedly — green & blue burn ALL 5 post-approach nudges ("IK fails" ×5) before the dog creeps close enough to BARELY succeed; red needed 1. Every object still GROUNDED (held + lifted ~0.23m), so IK did NOT cause the measured reliability failures (those were model-routing, now fixed). BUT the IK reach is MARGINAL: the dog jams against the table edge at ~0.44m — right at the Piper top-down reach limit — so IK only works via the brute-force forward-creep recovery. A slightly farther object, or fewer nudges, would fail. This is a real robustness risk (the grasp barely makes it every time), distinct from the routing failure. Root cause: scene geometry — the table edge stops the dog at the IK envelope boundary; a robust design would approach close enough that IK succeeds on the first try (0 nudges), with the nudge as rare recovery.
- Honest scope: still sim, 3 objects, small N. The 92% (routing) is solid; the IK-margin is a separate fragility to harden. NEXT (Yusen's call): (a) harden the IK reach (tighter standoff / grasp pose / table geometry so IK isn't always on the edge); (b) the queued CEO gates. Tools committed (the harnesses + the IK probe).

## D83 — R12 (in progress): IK envelope mapped — reach is WIDE (0.24-0.54m); the grasp is marginal from APPROACH misalignment + ramming, not distance · 2026-06-24 (arch/plug-and-play)
- Yusen directed (after the D82 IK-margin finding): make IK reach robust ("理论上其实是抓得到的"), ADD obstacle avoidance ("现在狗会直接撞上去"), and make a SCALABLE approach (replace the brute-force nudge hack).
- IK ENVELOPE PROBE (tools/_ik_envelope_sweep.py — teleport the dog across standoffs with the gait paused, check ik_top_down at the green bottle's grasp point): the reachable band is standoff ~0.54m DOWN TO 0.24m — a WIDE 0.30m band where both grasp + pre-grasp IK succeed. So IK is NOT distance-limited; the D82 marginality (out-of-reach + 5 nudges, dog ending at 0.44m) comes from the open-loop approach leaving the dog LATERALLY DRIFTED + mis-headed + jammed at the table edge — at the ALIGNED 0.44m standoff IK is comfortably OK. Yusen's "理论上抓得到" is confirmed.
- DESIGN (scalable obstacle-aware approach, reusing the existing g1_vgraph visibility-graph planner — ADR-001's "one planner", config-parameterized): (1) compute the target standoff = ~0.35-0.40m from the object, laterally aligned (dog y = object y) + facing it (deep in the reachable band -> max IK margin); (2) g1_vgraph.plan_path(dog, standoff, obstacle_polygons) -> a COLLISION-FREE path around the table+objects (no ramming); (3) drive the dog along the waypoints (walk-toward + heading) + final align; (4) grasp — IK now has margin (target 0 nudges). Replaces the open-loop _approach_object walk+stall+seat+5×nudge hack. Authorized by Yusen (he is the gate-approver + directed it). VERIFY-to-come: IK probe shows 0 nudges; no table/object collision; reliability holds/improves.

## D84 — R12 (in progress): navigate_to (obstacle avoidance) built+verified; grasp nav-approach integrated (WIP); IK-margin root cause narrowed to alignment, not distance/height · 2026-06-24 (arch/plug-and-play)
- BUILT + VERIFIED + committed (4d1aeb8): `MuJoCoGo2.navigate_to` — obstacle-aware, reuses g1's `obstacles_from_model` + `g1_vgraph.plan_path`, drives the real gait via `self.walk` (no qpos teleport), honest dist≤tol return. Independently re-ran the test (dog 9.98,3.0 -> 10.38,2.94, table avoided). Converges go2+g1 onto ONE planner (ADR-001/S3c). pyproject `sim` marker registered + tests/hardware/sim/test_go2_navigate.py (2 green).
- INTEGRATED (WIP, 06a3f31): perception_grasp's out-of-reach branch now uses `_approach_via_nav` (compute an aligned standoff ~0.40m on the object→dog line, `base.navigate_to` there — vgraph avoids the table/furniture, no ramming — then `_face_object`) when the base has navigate_to, else the old scripted approach. The OBSTACLE AVOIDANCE the user asked for is now in (the dog plans around obstacles instead of barreling open-loop). green + red still GROUND (held + lifted ~0.24m).
- NOT YET DONE (honest): (1) the IK-margin nudge loop STILL fires 5× — the dog ends at ~0.43m standoff where the REAL gait pose's IK is marginal; (2) the laterally-offset BLUE bottle produced no result (regression to investigate). ROOT CAUSE NARROWED by two probes: the IK envelope is WIDE (0.24-0.54m, D83) AND IK is OK at 0.44m aligned across ALL base heights 0.26-0.38m — so the marginality is NOT distance and NOT base height; it is the dog's ORIENTATION + LATERAL alignment after the gait approach (ended 0.022m off the bottle y-line + gait tilt; the clean static pose reaches ~0.06m farther). FIX (next): drive to a CLOSER standoff (navigate to ~the vgraph table-limit ~0.36m with a tighter tol) + add a LATERAL align (sidestep onto the object y-line, not just heading) so the dog ends precisely aligned where IK reaches with 0 nudges; re-probe green/blue/red; debug blue. Then re-measure live reliability.

## D85 — R12 (honest checkpoint): IK-margin root cause fully diagnosed; nav-approach integration REVERTED (hangs green) — navigate_to kept; needs a proper design pass · 2026-06-25 (arch/plug-and-play)
- FULL DIAGNOSIS (via paused-vs-moving probes, tools/_ik_align_tol.py + _ik_envelope_sweep.py + _ik_probe_once.py): the grasp IK marginality is NOT distance (reach band wide 0.24-0.54m), NOT base height (OK 0.26-0.38m), and NOT alignment-at-rest (at 0.42m standoff IK is OK across ±8° heading AND ±0.06m lateral). When the gait is PAUSED + the dog placed at the standoff, IK reaches comfortably. The REAL grasp fails because: (a) navigate_to UNDERSHOOTS (~0.09m short of the standoff, tol 0.12 + the gait can't stop precisely) so the dog ends ~0.46-0.49m out where IK is marginal — the "post-approach nudge" loop then presses the last ~0.09m to where IK reaches (so the nudges are undershoot-COMPENSATION, not a failure); (b) the moving/just-walked base adds residual tilt. So the open-loop's jam-and-nudge actually WORKS (grounds) — the "5 nudges" is the final press, not breakage.
- FIXES that landed in the WIP: head-on standoff (stand on the object's y-LINE, not the diagonal dog→object line) — corrected the offset blue/red mis-alignment. BUT two blockers remained: (1) GREEN's navigate_to HANGS consistently (dead-ahead, very close to the inflated table — the gait-steering gets stuck; >160s, not bounded by the 60s nav deadline as expected — a real navigate bug to root-cause); (2) the nudges still fire (the navigate undershoot). A hang is WORSE than the open-loop's jam, so the nav-approach integration is REVERTED (perception_grasp back to 4d1aeb8 open-loop, grounds 90%); `MuJoCoGo2.navigate_to` (the verified avoidance primitive, 4d1aeb8) is KEPT for the proper integration.
- DECISION (honest): the obstacle-aware grasp approach needs a PROPER design pass, not incremental session-tail patching: (i) root-cause + fix the green navigate hang (likely the waypoint-capture/gait-stall near the table); (ii) eliminate the undershoot (navigate to a closer standoff that accounts for the gait stop, or a final closed-loop IK-reach dock that is principled not a 5×-nudge); (iii) consider a settle that doesn't hang. Surfaced to Yusen for steer. Tools (probes + harnesses) committed + reusable. The avoidance PRIMITIVE is done; the GRASP INTEGRATION is deferred to a focused pass.

## D86 — R12 DONE: green "hang" was a DATA-RACE SEGFAULT (fixed); obstacle-aware grasp integrated; approach solid, live number dragged by pre-existing routing variance · 2026-06-25 (arch/plug-and-play)
- ROOT CAUSE of the green navigate "hang" (the R12 blocker): NOT a gait stall — a SEGFAULT. navigate_to called `mj_forward(model, data)` to snapshot the pose; mj_forward WRITES derived fields (geom_xpos, …) and RACED the 1 kHz gait daemon's mj_step → concurrent MjData mutation → core dump (only with the Piper attached + the grasp path; the agent's bare test got lucky). FIX (447a100): read the start pose via the thread-safe `get_position()` accessor, read static-furniture obstacle geometry WITHOUT mj_forward (the daemon keeps geom_xpos current). + a PIPER-ATTACHED regression test (test_navigate_to_with_piper_attached_does_not_crash) that reproduces the crashing config and asserts bounded return (3.3 s, no crash). Verified the diagnostic: navigate_to(10.46,3.0) now returns True in 2.6 s (was >100 s / core dump).
- GRASP INTEGRATION re-applied (9d5adbe): perception_grasp's out-of-reach branch uses `_approach_via_nav` (HEAD-ON standoff on the object's y-line via base.navigate_to — vgraph routes around the table/furniture) when the base has navigate_to, else the open-loop. DETERMINISTIC e2e (tools/_ik_probe_once.py, all 3 colours): NO hang, ALL GROUND (green/blue/red, lift ~0.22-0.24 m), dog plans around the table. The post-approach dock (nudge loop) still fires 3-5× to close the ~5 cm navigate undershoot — the grasp grounds regardless (the IK pre-check is conservative).
- LIVE measure (real DeepSeek, N=6, tools/measure_go2piper_grasp_reliability.py): 4/6 = 67% GROUNDED+verified. HONEST breakdown: ALL 4 runs that routed to perception_grasp GROUNDED (4/4 — the approach is solid); the 2 non-grounds are MODEL-ROUTING variance, NOT the approach — 1 routed to pick_top_down (wrong skill, holding=False), 1 TIMEOUT at 340 s (the D82 over-detect loop: the model kept perceiving without committing to grasp). Both failure modes pre-date R12 (D81's baseline also had a RAN). So R12 did NOT regress the grasp approach; the live number reflects pre-existing routing/over-detect variance amplified by small N. Larger N + the over-detect mitigation (D82 nudge) are the levers for the live number — separate from R12's avoidance+reach work.
- NET R12: the segfault blocker is FIXED (real bug, real fix, regression-tested); obstacle-aware avoidance is integrated and grounds deterministically across all 3 colours; navigate_to converges go2+g1 onto one planner. Remaining polish (non-blocking): trim the dock (navigate undershoot), lift the live number via over-detect mitigation.

## D87 — R12 follow-up (RED-TEAM refutes D86's "noise" claim): live grasp = 40% (N=10); the bottleneck is MODEL UNDER-COMMIT, not the approach · 2026-06-25 (arch/plug-and-play)
- Red-teamed my own D86 claim ("67% is routing variance / R12 didn't regress"). Re-measured N=10 (real DeepSeek) with timeout diagnostics (831a992): 4/10 GROUNDED+verified (40%), 5 RAN, 1 FAILED. LOWER than the D81 9/10 sample — so D81's 90% was a LUCKY sample; the true autonomous-routing rate is ~40-67% with HIGH variance.
- DIAGNOSIS (per-run strategies make it unambiguous): the approach is NOT the problem — ALL 4 GROUNDED runs were a SINGLE perception_grasp call (grounds 100% when called). The drag is SYSTEMATIC MODEL UNDER-COMMIT: the RAN failures dithered on detect/navigate/look WITHOUT ever grasping — e.g. ["navigate"], ["detect","navigate","detect"], ["detect","detect","navigate","detect"], ["detect","navigate","pick_top_down"]. The model treats "抓前面的绿瓶子" as "first navigate / first detect / first look" and burns its steps without committing to the grasp skill (which internally perceives + walks + grasps). This is NOT noise and NOT the nav-approach (the model always had NavigateSkill; the nav-approach lives INSIDE perception_grasp).
- HONEST IMPACT: R12's reach+avoidance work is solid (deterministic all-3-ground + 4/4 live perception_grasp calls grounded), but the LIVE acceptance number (the bare-cli NL face) is gated by model routing reliability, which is the real bottleneck (~40%). This is the #1 thing to lift for "it works" via NL. Lever: drive COMMITMENT to the grasp skill (don't navigate/detect/look first — the skill does it all). The deeper structural fix is verify+recover in the native loop (continue until holding, not until the model stops) — bigger, deferred.

## D88 — Manual test (Yusen) exposes the REAL gap: find-and-grasp pipeline is unbuilt; my R12 work was only the last stage · 2026-06-25 (arch/plug-and-play, CEO gate — PROPOSAL pending Yusen)
- TEST: full 8-room house sim, "把前面的绿色瓶子抓起来" → robot loaded grounding-dino then STARTED EXPLORING all 8 rooms instead of locating+grasping. Root cause mapped by a 4-agent read-only Workflow (wf_f4aa14d1-2a9), verified line-by-line.
- ROOT CAUSE: "where is the green bottle" is UNANSWERABLE. (1) SceneGraph (core/scene_graph.py) is never seeded at startup — load_layout(config/room_layout.yaml) only fires lazily inside explore; starts 0 rooms/0 objects. (2) MuJoCo GT bottle positions go to WorldModel/ObjectState (gated VECTOR_SIM_DEMO_GROUND_TRUTH=1+with_arm), a SEPARATE store the SceneGraph never reads — two unbridged spatial stores. (3) When objects DO enter the graph (explore VLM), positions are coarse 2D guesses, deduped per category, never depth-refreshed → "completely inaccurate" (Yusen's words, confirmed). (4) native_loop is a FLAT ReAct loop — NO planner/decomposer (firewalled out); model picks ONE tool from a menu that INCLUDES ExploreSkill ("explore the house", also non-functional in MuJoCo-only — needs TARE). Model elected explore. (5) perception_grasp (R12) is ONLY stage 5: assumes object already in view (or ≤1.6m in-place scan), explicitly does NOT re-perceive after arrival; zero map/room/scene-graph awareness.
- 5-STAGE PIPELINE STATUS (Yusen's correct design): (1 map) config exists, startup-unseeded; (2 detect→scenegraph) BROKEN (bottles never in graph; detect.py calls perception.track() which Go2GraspPerception lacks → x=y=z=0; positions inaccurate); (3 navigate-to-named-object) MISSING as a standalone step (navigate takes coord/room, not object name; the object-name→position→navigate bridge exists only fused inside MobilePick/perception_grasp reading the empty world_model); (4 arrival depth re-perceive) MISSING; (5 approach+grasp) EXISTS (R12, solid, reusable).
- TWO ROOT DEFECTS: A) spatial memory empty/split/inaccurate ("where is X" unanswerable); B) NO orchestration sequences find→go→approach→grasp — left to a model that picks unreliably (40%, here picked explore). Defect B is exactly the North Star OS job (plan/route/verify/recover) that the flat native loop abandoned.
- PROPOSAL (pending Yusen, CEO gate): Layer 1 (do regardless) — seed map at startup + UNIFY spatial stores (SceneGraph as source of truth) + depth-refresh object positions on arrival. Layer 2 (Yusen's call) — sequence the 5 stages reliably: (A, recommended) a COMPOSED "fetch object" skill that runs locate→navigate→arrival-re-perceive→approach→grasp internally+verifiably (model picks ONE skill = reliable; sequencing leaves the flaky model), vs (B) restore a lightweight planner (closer to "verify every step" but revisits the native-is-the-design ruling, feedback_native_is_the_design). Plus: drop ExploreSkill from the grasp tool menu (wrong+non-functional here). R12 reach/avoidance is the reusable stage-5 substrate.

## D89 — Find-and-grasp REFACTOR PLAN (study done; PROPOSAL pending Yusen) · 2026-06-25 (arch/plug-and-play, CEO gate)
- Study Workflow wf_4b98e3da-03d (3 read-only agents: SysNav, sensor-accuracy, persistence/config). KEY CORRECTION to D88 + Yusen's hypothesis:
- SENSORS ARE CORRECT (refutes "sensor data wrong"): depth (metric, no flip), intrinsics (fovy=42->fx=fy), camera pose (d435_camera, resolved cam_xpos/cam_xmat), camera->world (OpenCV->MuJoCo flip, GT-tested) all verified line-by-line (mujoco_go2.py:1215-1253, depth_projection.py:64-152). The accurate depth->world pipeline (grasp_point_from_rgbd) EXISTS and is correct.
- REAL ROOT CAUSE of "scene graph completely inaccurate": the explore->scene-graph observe path NEVER uses depth/projection. explore.py:558-584 feeds VLM only RGB; DetectedObject (vlm_go2.py:69-74) carries NO coordinate; observe passes object NAMES only (look.py:104-119); merge_object defaults x=y=0.0 -> every object stored at room-center/0.0 (scene_graph.py:419-475). The detected_objects=[(cat,x,y)] API exists but is never filled. So objects in the graph are useless for navigation. FIX = route object seeding through the EXISTING correct depth->world projection (SysNav-style: detect mask -> depth points -> centroid -> world via camera pose). One change, no sensor fix.
- SYSNAV: object global position = centroid of masked lidar/depth cloud lifted by robot pose. Adopt this for scene-graph object seeding. Its live_bridge currently writes WorldModel (not SceneGraph) and is PAUSED.
- PERSISTENCE/CLEAR ALREADY EXIST: SceneGraph.save/load -> ~/.vector_os_nano/scene_graph.yaml (rooms+viewpoints+objects-with-xyz+doors), restore-at-startup, save-on-exit; /clear_memory wipes it. MISSING: startup room seed (load_layout only lazy in explore), object xyz into SceneGraph (split-brain with WorldModel), a real MuJoCo-only explore (ExploreSkill hard-depends on external vector_navigation_stack TARE/FAR; emits tare_not_running otherwise), and a /rebuild (clear->explore->seed->save) command.
- REFACTOR PLAN (scalable, agent-adaptive — NOT a hardcoded pipeline; building blocks the model composes): (1) explore->build map+scenegraph->persist; (2) #1 FIX: seed object global positions via the existing depth projection (SysNav-style); (3) locate(object) tool (scene-graph lookup); (4) navigate_to_object(object) = locate+navigate_to(xy); (5) grasp(object) = arrival depth re-perceive (adjust) + R12 approach+grasp. Agent orchestrates per situation. NEW config: world manifest config/worlds/<world>.yaml (rooms/boundary/persistence/cameras; objects discovered not declared). Unify SceneGraph as single object-position source.
- DECISIONS PENDING YUSEN (CEO gate): (A) explore-in-sim: reuse external vector_navigation_stack TARE/FAR (one-time, persisted) vs build MuJoCo-native frontier explore [rec: external first]; (B) approve direction: SceneGraph single source + SysNav depth-seeding + world manifest + building-block tools. R12 = reusable stage-5 substrate. Start point (uncontroversial, highest leverage): #1 depth->scenegraph seeding fix.

## D90 — Refactor stage #1 DONE (offline-verified): accurate object positions wired into the scene graph · 2026-06-25 (arch/plug-and-play)
- perception/object_localizer.py: localize_objects_3d(perception, queries) REUSES grasp_point_from_rgbd (detect->segment->depth->world centroid, SysNav-style) — no re-implemented projection; guards safely (returns [] if perception unusable, never raises). Wired into skills/go2/look.py observe path: object names are now localized to real world (x,y) and passed via observe_with_viewpoint(detected_objects=[(cat,x,y)]), replacing the names-only path that stored every object at room-center/0.0. Result objects also carry world_x/y/z. Safe fallback to names-only when perception absent.
- VERIFIED offline (myself, no sim): re-ran tests/unit/perception/test_object_localizer.py = 18 passed; read object_localizer.py + the look.py diff (reuse confirmed, fallback safe). Signature correction the agent found: observe_with_viewpoint takes 2D (cat,x,y) tuples (z stored only in result_data). E2E in the real sim is PENDING (Yusen owns the sim) — offline tests are necessary, NOT acceptance.
- This is the foundation: once observe runs (via look, or re-enabled during the external explore), the scene graph gets metric object positions, making "where is the green bottle" answerable + accurate. Plan + remaining stages (#2 navigate_to_object, #3 arrival depth-adjust+grasp, #4 external-explore integration, #5 seed+world-manifest) in docs/plan-find-grasp-refactor.md.
- HANDOFF: loop stopped (no more autonomous wakeups) so a FRESH session can drive the refactor cleanly without double-driving. Resume from docs/plan-find-grasp-refactor.md + STATUS + D88/D89/D90.

## D91 — Refactor #1 REAL-SIM VERIFIED + a label≠query bug only e2e caught; #2 navigate_to_object built · 2026-06-25 (arch/plug-and-play)
- REAL-SIM e2e harness tools/verify_localize_scenegraph.py (headless go2+arm, MUJOCO_GL=egl): reads MuJoCo GT via go2._mj.data.xpos free-body loop and checks 3 things — P1 localize_objects_3d vs GT, P2 scene-graph save/load round-trip, P3b full LookSkill (stub VLM naming, REAL localization). RESULT overall_pass:true: green/blue/red localized to 2.3/2.4/2.8 cm xy (z ≤4 mm) vs GT; full look→observe_with_viewpoint→scene graph stores all three with accurate coords + persists round-trip. Camera-frame PNG read back: 3 correctly-coloured cylinders in view, lateral layout matches GT. This replaces the old broken state (every object at 0,0/room-centre, D88).
- BUG FOUND (only real-sim e2e exposed it; 18 offline tests + the look-wiring tests all passed because their MOCK detector returns label==query): look stored every object at (0,0). Root cause — localize_objects_3d keyed world_positions by the DETECTOR's returned label, but grounding-dino returns "a green bottle" for query "green bottle"; look then looked up by the VLM name "green bottle" → miss → (name,0.0,0.0) fallback. FIX (commit ed2e810): object_localizer.py now keys results by the INPUT QUERY (the caller's handle), each distinct query localized once; +2 regression tests (label≠query; query-keyed per-query). Lesson → mocks that mirror the happy path hide integration bugs; the metric truth is real sim, not green units (tricky-bugs candidate).
- RED-TEAM (independent skeptic, refutation-first): NARROW claim CONFIRMED — "localizer accurate at the tested spawn viewpoint + look→scenegraph wiring stores it" survived every attack: GT (object xpos) is independent of the localizer (camera FK pose, never reads object xpos), depth is a real metric GL render (valid 0.993, depth_scale=1.0), detector runs offline on RGB only, P3b calls the real LookSkill.execute. HEADLINE confirmed but scope-limited.
- OPEN CAVEATS (flagged to CEO; NOT yet closed) — (a) ONE viewpoint only: robot fixed at spawn (10,3,θ0), objects well-framed ~0.9 m ahead; other poses/distances/angles, occlusion, clutter, multiple same-category objects UNTESTED. (b) REAL GPT-4o VLM path UNPROVEN: P3a failed vlm_failed (OpenRouter SSL/network down) — only the stub-named path ran; the real name→detect→store chain has zero real-sim evidence yet (needs network). (c) merge_object x=0/y=0 SENTINEL TRAP is latent (scene_graph.py:454 `x=x if x!=0.0 else existing.x`): a real object genuinely at world origin/axis (or look's (name,0,0) un-localized fallback) is silently kept at the zero/stale value — same bug class #1 kills, re-entering via merge; not triggered by the bottles (x≈10.9) — DEFERRED hardening (needs coordinated merge None-sentinel + look fallback fix). (d) find_objects_by_category is substring (`cat in category`) → "bottle" matches green+blue; merge dedups by exact category only → potential duplicate nodes at scale. (e) bare-cli NL ACCEPTANCE (the non-negotiable face) NOT yet exercised: the harness hand-builds the SkillContext to dodge a pre-existing `home` skill 5-vs-6 joint bug (MuJoCoPiper.move_joints expected 6 got 5) in the planner/executor path — must be fixed before the full "把绿色瓶子拿过来" NL flow can run end-to-end.
- #2 navigate_to_object BUILT (vector_os_nano/skills/navigate_to_object.py): object name → SceneGraph.find_objects_by_category → pick localized match nearest to robot (tie-break confidence) → delegate to NavigateSkill coordinate path (FAR drive + hold at vicinity, reuse not reimplement). Registered in get_go2_skills(). 9 offline tests green (tests/unit/skills/test_navigate_to_object.py). Sim e2e PENDING. Scalable/agent-adaptive: a composable building block the model picks by name+description (distinct from navigate=room), not a hardcoded pipeline.

## D92 — Refactor #2 navigate_to_object REAL-SIM VERIFIED · 2026-06-25 (arch/plug-and-play, c4cf5cc)
- Real-sim e2e (tools/verify_navigate_to_object.py, headless go2+arm): seed scene graph via real look (stub VLM naming, real depth localization) → green bottle @ (10.857,3.001); drive dog AWAY to (8.21,3.01) = 2.67 m from bottle; navigate_to_object('green bottle') → dog ends (9.94,3.12) = 0.95 m from the bottle (MuJoCo GT-measured actual position, NOT a nav flag). Checks: started_far ✓, arrived_vicinity ✓, moved_closer ✓ → overall_pass.
- DESIGN: targets a compute_approach_pose STANDOFF 0.7 m in front of the object (toward the robot), never the object centre — the object is an inflated obstacle to the vgraph planner (_GO2_BODY_RADIUS=0.28), so its exact cell returns UNREACHABLE. The 0.95 m landing = 0.7 m standoff + ~0.25 m planner arrival tol; well inside vicinity, leaves the object in camera view for #3's precise approach. Reuses NavigateSkill coordinate path (FAR/vgraph + hold at vicinity) — no reimplementation.
- GOTCHA (harness, not skill): look RIGHT AFTER connect() saw the dog mid-settle → camera not framed → 0 detections → names-only (0,0). Fix: settle (~2.5s) + detector warm-up before the first look. Production look runs after the dog is already standing/exploring, so this is a test-timing artifact, not a skill bug.
- RESIDUAL (flag): verified at ONE far-point + ONE object; reachability of the standoff depends on room geometry (planner fails loud if the standoff is inside an obstacle); the bare-cli NL leg of the full fetch still blocked by the same items as #1 (real VLM network + home 5-vs-6 executor bug).

## D93 — Refactor #3: full find→navigate→grasp pipeline COMPOSES end-to-end; grasp reliability is the R12 bottleneck · 2026-06-25 (arch/plug-and-play, bc477ea)
- REAL-SIM e2e (tools/verify_fetch_flow.py, headless go2+arm): the building blocks compose exactly as the model would orchestrate them — look (stub VLM naming, real depth localization) seeds the scene graph → navigate_to_object('green bottle') drives 2.66m→0.91m → perception_grasp('green bottle') perceives FRESH on arrival (grasp_world≈GT), R12 nav-approach + nudge, PickTopDownSkill weld → green bottle LIFTED 0.32→0.558m (+0.238m), gripper.is_holding=True, make_holding_object oracle=True (the spine predicate, independent GT). This is the whole refactor goal (找物→导航→深度微调→抓取) demonstrated end-to-end against ground truth.
- ARRIVAL RE-PERCEIVE is satisfied BY COMPOSITION, not new code: perception_grasp's FIRST perceive happens at the navigate_to_object arrival standoff (~0.9m) — exactly R12's well-framed distance. The recon's "add re-perceive AFTER the approach" would re-perceive at the 0.40m pose R12 deliberately avoids ("looks over the table") and would REGRESS it — so it was NOT added. Correct, non-bulldozing call (Debug Protocol, not assuming).
- REAL GAP FOUND + FIXED (Debug Protocol from the failure trace): perception_grasp delegates to PickTopDownSkill via the target_xyz seam, but PickTopDownSkill failed `no_world_model` — it required a populated world_model even when target_xyz was supplied. target_xyz is the honest perception path (no GT world model; SceneGraph is the source — D89). Fix (pick_top_down.py): a None world_model is fatal ONLY when there is no target_xyz to fall back on; _resolve_target returns None (not crash) when wm is None and target_xyz absent. Verify moat (make_holding_object) untouched — the grasp must still physically succeed. +1 regression test (34 green); existing no_world_model test (no target_xyz) still green.
- HONEST RELIABILITY (N=3, ran AFTER the single success to avoid a one-lucky-seed headline): 1/3 fully grasped. The 2 misses: perception ACCURATE every time (grasp_world≈10.857,3.00,0.32 ≈ GT) and the approach ran, but the gripper closed ~0.04-0.07m off the small bottle (diagnosis possibly_missed, no weld) — the dog ends slightly off the bottle y-line from gait drift at the variable navigate-arrival pose. This is the PRE-EXISTING R12 terminal-grasp precision bottleneck (D86/D87: ~67% from the fixed spawn; lower here from the more variable arrival pose), NOT a composition defect. The pipeline plumbing (#1-#3) is correct; the limiting factor is the terminal weld precision.
- NEXT: (non-gated) improve terminal grasp reliability from the arrival pose — tighten final y-line alignment before the weld (R12 territory; do NOT bulldoze the verify spine). #4 (external TARE/FAR explore — new external dep) and #5 (retire WorldModel / unify SceneGraph — cross-module data flow) are CEO GATES — queued for an executive summary.

## D94 — Grasp reliability RAISED + D93 diagnosis CORRECTED (perception framing + approach, not terminal precision) · 2026-06-25 (arch/plug-and-play, 1360006 + 56aa289)
- HEADLINE: end-to-end fetch (look→navigate_to_object→perception_grasp) reliability **13/17 = 0.765 GT-measured** with the honest oracle (weld + lift + make_holding_object). The D93 "~33% (1/3)" was an UNLUCKY N=3 — repeated measurement put the real baseline at a noisy ~0.67-0.75. N≤6 is too noisy to trust; this needs N≥12.
- D93's DIAGNOSIS WAS WRONG (Debug Protocol, falsified by a direct probe tools/_probe_drift.py): the failures are NOT "EE closes 0.04-0.07m off from gait drift at the variable arrival pose / terminal-precision". The probe proved the go2 base does NOT drift while STANDING (0.2mm over 6s; 0.0mm during the 4s pre-grasp arm move) and the Piper EE lands within **4.9mm** of the IK target after a full top-down pick from a 0.42m head-on standoff. So perception + arm execution are accurate; the grasp is reliable WHEN the dog is well-positioned.
- THE REAL FAILURE MODES (caught via GT failure traces, subprocess harness):
  (a) PERCEPTION FRAMING — DOMINANT (3/3 caught pre-fix): when navigate_to_object lands the dog too CLOSE (arrivals 0.66-0.80m), the forward head camera pitches OVER the low table bottle; the yaw-scan (turning in place) cannot fix a PITCH framing miss, so perception finds NOTHING at all 6 scan headings → grasp_world=None → fail. Successful runs land ~0.88-0.90m where the bottle is framed.
  (b) NAV-APPROACH BAIL — perception_grasp `_approach_via_nav`'s `base.navigate_to` to the tight 0.40m standoff (inside the inflated table obstacle, _GO2_BODY_RADIUS=0.28) intermittently returns False and leaves the dog ~4m PAST the object; the code ignored the False return and proceeded → out-of-reach fail with no recovery.
- FIXES (both mechanistically correct; spine UNTOUCHED): (a) navigate_to_object `_VICINITY_CLEARANCE_M` 0.70→0.95m so FAR/vgraph arrivals (overshoot +0.05/undershoot -0.20) land in the well-framed ~[0.90,1.15]m band — ELIMINATED the perceive=None mode (0/17 occurrences since). (b) `_approach_via_nav` now VERIFIES the dog ended within _NAV_APPROACH_MAX_OFF=0.35m of the standoff, else recovers with the scripted stall-seating forward creep `_approach_object` (no planner; strictly additive — no-op when nav-approach succeeded). +4 regression tests (tests/unit/skills/test_grasp_repose_seam.py + test_navigate_to_object.py via the imported constant), full grasp unit suite green.
- TRADE-OFF (honest): the 0.95m standoff eliminated perception failures but lengthened the approach, so the remaining single failure mode is now APPROACH reliability (the dog doesn't always reach the IK-reachable head-on standoff after perceiving at 0.95m). One RAM observed at 0.95m (recovery creep from a far pose knocked the bottle off the table). Net headline held/modestly improved (~0.765 vs noisy baseline) while removing a whole failure mode and isolating the next bottleneck.
- NEXT (non-gated): approach reliability — the probe shows a head-on 0.42m seat → 5mm EE → reliable weld, and the scripted `_approach_object` reliably stall-seats at the table edge; prefer it over the flaky vgraph nav-approach for the FINAL hop (guard against ramming). Measure N≥12. Then backlog #2 (home 5-vs-6 bug → bare-cli NL acceptance).

## D95 — Final-hop approach UNIFIED onto scripted seat-creep; grasp reliability 0.765→0.833 · 2026-06-25 (arch/plug-and-play, 2389e84)
- HEADLINE: end-to-end fetch (look→navigate_to_object→perception_grasp) reliability **10/12 = 0.833 GT-measured** with the frozen honest oracle (weld + lift + make_holding_object). UP from D94's 13/17 = 0.765 AND on SIMPLER code (net −33 lines).
- CHANGE: perception_grasp's out-of-reach branch now closes the final hop with ONE mechanism for EVERY base — the scripted re-pose + stall-seating creep `_approach_and_seat` (= _grasp_ready_repose → _approach_object → _face_object). The flaky vgraph `_approach_via_nav` (D94) is RETIRED; constants `_GRASP_STANDOFF_M` and `_NAV_APPROACH_MAX_OFF` deleted. Rule 11 (one impl, no per-path fork). The test seam test_grasp_repose_seam.py was rewritten to the unified entry; 16 grasp/nav unit tests green.
- RATIONALE: the last short hop to the tight near-obstacle standoff was the FLAKIEST link when handed to the vgraph planner — targeting a standoff this close to the inflated table obstacle, navigate_to intermittently (a) returned False and dumped the dog metres PAST the object, or (b) returned True a few cm short of the THIN Piper forward-reach band → IK still failed. The scripted forward creep instead stall-SEATS the dog against the table edge: a kinematically-pinned standoff, repeatable run-to-run and independent of planner variance (the proven D34-D51 placement the no-navigate_to path already used). So D95 promotes it from "recovery fallback" (D94) to the PRIMARY and only final-hop mechanism.
- HONEST REMAINING MODES (N=12, GT failure traces): 2/12 fails are now two DIFFERENT modes, no single dominant one left — trial 8 = perception-FRAMING miss (perceive_err=None; the pre-D94 framing mode resurfaced once at an unlucky arrival pose), trial 9 = terminal-GRASP miss despite good perception. perceive_err ≈ 0.024 m on every PASS = the localizer's systematic ~2.4 cm bias the grasp tolerates (not the failure cause).
- RED-TEAM / KEEP-DECISION: +0.068 over a noisy ~0.67-0.77 baseline is partly within measurement noise, so the DECISIVE keep-justification is not the delta alone but "rate raised-or-held + retired a flaky path + −33 lines" — true even under the pessimistic reading that the true rate is ~0.80. Loop keep-criterion (improved OR held-with-cleaner-code) satisfied → KEPT.
- NEXT (non-gated): grasp reliability is PLATEAUED at ~0.83 after R-D94 + R-D95 real pushing; per the loop invariant (plateau after ~4-5 honest rounds → pivot down backlog, don't thrash), do backlog #2 next — fix the `home` skill 5-vs-6 joint bug (skills/home.py passes 5 joint positions, MuJoCoPiper.move_joints expects 6) — which unblocks the bare-`vector-cli` + NL acceptance of the full fetch (the non-negotiable face). Then #3 merge_object x=0/y=0 sentinel trap. Spine untouched. #4/#5 remain CEO GATES.

## D96 — `home` skill 5-vs-6 DoF bug REAL-SIM VERIFIED on the 6-DoF Piper (backlog #2 done) · 2026-06-25 (arch/plug-and-play, 064294f + this RECORD)
- HEADLINE: the `home` skill's hard-coded 5-DoF SO-101 home pose crashed the planner/executor path on the go2+Piper (6-DoF) arm — `move_joints: expected 6 positions, got 5`. Because `Agent.execute_skill()` APPENDS a trailing `home` step to manipulation plans, this ValueError crashed the WHOLE bare-`vector-cli` + NL fetch route. FIXED (064294f, DoF-aware, mirrors the scan.py fix) and now REAL-SIM VERIFIED on the actual 6-DoF Piper.
- FIX (skills/home.py, Rule 11 — no per-robot code): when `len(configured_home_pose) != arm.dof`, fall back to the URDF-zero neutral home `[0.0]*dof` (exactly what `MuJoCoPiper.home()` uses). SO-101 (5==5) is byte-identical — the branch never fires for it. arm.dof read with a `get_joint_positions()` length fallback.
- REAL-VERIFY (tools/verify_home_dof.py, REAL go2+Piper sim, the ONLY acceptance — not a mock): drove the arm away to [0.283,0,0,0.45,-0.24,0.15] then ran the previously-crashing `Agent.execute_skill('home')` → GoalExecutor → HomeSkill → real 6-DoF `move_joints`. RESULT: no crash, success=True/status=completed, executor "Step s1 (home) OK in 3.023s", arm reached near-neutral [0.01,0,0,0.03,-0.047,0.041] (max |q|<0.05) — `overall_pass=true`, exit 0. The log shows the DoF-aware branch firing: "configured home pose len != arm DoF (6); using URDF-zero neutral home". +4 regression unit tests (tests/unit/skills/test_home_dof.py, green).
- IMPACT: the executor→home path that blocked the full NL fetch is unblocked. NEXT (non-gated): drive "把绿色瓶子拿过来" through the bare-`vector-cli` REPL by NL as the TRUE acceptance (harnesses are internal only) — bundles the now-fixed home step into the full look→navigate→grasp→home chain. Then backlog #3 (merge_object x=0/y=0 sentinel trap). Spine untouched; #4/#5 remain CEO GATES.

## D97 — Bare-CLI find-grasp foundation FIXED: perception wired into the real go2 sim path (Yusen-found, my stub-harness missed it) · 2026-06-25 (arch/plug-and-play, 90dd65c)
- YUSEN TESTED the bare `vector-cli` and reported scene-graph positions "很不准": all 27 objects (incl. the bottles, VLM-named "green/red/blue cylinder") at (0,0). My #1-#3 "real-sim verified" used a STUB-PERCEPTION harness that ATTACHED Go2GraspPerception by hand → it never exercised the real CLI wiring. Exactly the red-team caveat ("bare-cli is the only acceptance; the harness is not the acceptance interface").
- ROOT CAUSES (3, all fixed via a fan-out Workflow design→implement→verify): (1) sim_tool.py _start_go2 set `agent._perception = None` ("until the SysNav bridge is wired") → look.py guards `if context.perception is not None` → localize_objects_3d NEVER ran → every object stored at (0,0). (2) explore.py auto-observe passed coordinate-less DICTS to observe_with_viewpoint (which unpacks (cat,x,y) 3-tuples) → ValueError → explore stored NOTHING; and its VLM gate `getattr(base,"_vlm")` was always None on the real driver → the hook was DEAD. (3) look.py's `(name,0.0,0.0)` fallback for un-localized objects polluted the graph with fake origins.
- FIX (90dd65c, verify spine untouched): (1) shared guarded `_build_go2_perception(base)` in sim_tool.py → `Go2GraspPerception(base)` when base exposes the camera methods, else None (never crashes a launch); wired into BOTH _start_go2 (line 607) and the in-process --sim-go2 launcher in cli.py (single-sourced, Rule 3/11). (2) explore.py now depth-localizes via localize_objects_3d, captures perception/vlm as module globals from context, passes real (cat,x,y) tuples for localized objects only. (3) look.py drops the (0,0) fallback — stores localized objects only.
- REAL-VERIFY through the ACTUAL launcher (SimStartTool._start_go2(with_arm=True), NOT a hand-built perception): agent._perception now usable; look stores green/blue/red at (10.857,3.001)/(10.877,2.787)/(10.873,3.214) vs GT — err < 3 cm, NONE at (0,0). +98 tests (196 in the focused suite), no regressions; I independently re-ran 36 focused tests green + confirmed the edits via git.
- RESIDUAL (honest): the verify STUBBED the VLM object-naming (live GPT-4o is network-flaky — api.anthropic.com resolves to a VPN fake-IP) while keeping localization REAL. So the live-VLM end-to-end (Yusen's exact scenario) is not yet re-proven end-to-end — but the root cause (perception=None) is fixed, so localize now runs on whatever the VLM names. NEXT loop priority: drive the full fetch "把绿色瓶子拿过来" through bare `vector-cli` + NL with the real VLM when network is up. WATCH: explore's auto-observe hook now FIRES for the first time (was dead) → VLM+localize run inside the 2s sample loop (try/except non-blocking, should_add_viewpoint gates frequency) — watch latency.
- LESSON (folded into the loop goal): the bare `vector-cli` is the ONLY acceptance; verify through the REAL launcher, NEVER a hand-built/stub perception. A green harness that bypasses the CLI wiring is not acceptance.

## D98 — merge_object x=0/y=0 sentinel trap FIXED with a None sentinel (backlog #3) · 2026-06-25 (arch/plug-and-play, e762f12)
- CONTEXT: backlog #1 (live-model bare-cli full-fetch e2e) was NETWORK-BLOCKED this round — DeepSeek (agent routing) returned http=000 and OpenAI/GPT-4o (VLM naming) returned 421 (VPN fake-IP, the recorded caveat). Per loop discipline (don't thrash, pivot to non-gated work), did backlog #3 — a fully offline, completable hardening of the SAME bug class that hid the D97 (0,0) catastrophe.
- HEADLINE: `SceneGraph.merge_object` used `x=x if x != 0.0 else existing.x` — a real observation localizing an object at the world origin or anywhere on the x=0 / y=0 axis was silently discarded as "no update", keeping the stale/zero position. FIX (e762f12, spine untouched): the sentinel is now `None`. `x: float | None = None` — `None` means "no new localization" (keep existing on a merge, default 0.0 on a new object); a real float INCLUDING 0.0 is always applied.
- WHY SAFE (caller audit): all internal callers (scene_graph.py 649/708/744 coordinate-less; 701/738 coordinate-bearing from depth-localized `detected_objects` tuples) and external callers (tests only) are behavior-preserved — the coordinate-less default 0.0→None path merges identically, only the genuine-0.0 case changes (was buggy, now correct). No ObjectNode dataclass change (None resolved to float before construction; Rule 6 trivially satisfied).
- VERIFY (TDD, the honest verification for a pure in-memory data-store semantics fix — no actuator/perception behavior to watch in sim): RED first — 3 genuine-origin cases failed, 3 regression cases passed; GREEN after the fix — all 6 pass. Regression: 100 scene-graph tests + 175 core/perception/skills tests green, no regressions. tests/unit/core/test_merge_object_origin_sentinel.py.
- HONEST RESIDUAL: this is unit-verified, not bare-cli-verified — but a merge sentinel has no sim-observable behavior beyond the stored coordinate, which the deterministic test proves round-trips. The live-model bare-cli full-fetch e2e (backlog #1, top priority) remains UNRUN, network-blocked; pick it up next round when DeepSeek + GPT-4o are reachable. NEXT (non-gated): backlog #1 when network up, else re-measure grasp reliability via the offline tools/measure_fetch_reliability.py. #4/#5 remain CEO GATES.

## D99 — The EYES are wired into the loop's per-round REAL-VERIFY + gate disagreement-semantics corrected (ADR-002) · 2026-06-26 (arch/plug-and-play)
- CONTEXT: the visual-acceptance pipeline (ADR-002 Stages 0-4) was built + reviewed + the bare-cli+REAL-VLM fetch GROUNDED once (2026-06-26 milestone), but the LOOP still grounded via the GT oracle ONLY — the eyes were not in its per-round acceptance. Closing that gap (Yusen: "接眼睛进 loop").
- WIRING (7336b1a): the loop's REAL-VERIFY now calls `tools/acceptance/measure_fetch_visual.py --n N` — N live `--sim-go2 --with-arm` fetch trials through the full visual harness (real DeepSeek routing + real VLM + GT oracle + visual witness + temporal cross-check). It prints one `RESULT {json}` with grounded_rate AND disagreements; the loop goal treats `disagreements>0` as NOT-acceptance → red-team + inspect frames BEFORE believing the rate. Adds `run_once(..., with_arm=True)` to visual_e2e.py. Supersedes the GT-only tools/measure_fetch_reliability.py.
- GATE FIX (this commit): integration surfaced a false-positive — a GT-FAIL trial with vision PASS was flagged `disagreement=True`, so the loop would red-team EVERY ordinary failed-but-rendered trial (noise). ROOT: the visual rubric is ORTHOGONAL to task success (upright/floating, rendered/black, body-intact, workspace-in-frame); a vision PASS never *claims* the task succeeded, so it cannot contradict a GT fail. FIX (gate.py, spine untouched, DOWNGRADE-ONLY invariant intact): the GT-fail branch is `disagreement=False` for every vision value — only the INVERSE (GT-PASS + vision-FAIL: "oracle says success but the picture looks wrong") is a disagreement. measure_fetch_visual's `disagreements` now counts ONLY `gt.verified AND disagreement` = the D91-D95 false-green catch; a failed trial is already REJECT, re-flagging it is noise.
- WHY MOAT-SAFE: no ACCEPT path touched (GT fail is still REJECT + block_headline); the false-green catch (`gt verified AND eyes object`) is fully intact, so no convincing-but-wrong green can slip through. Verify: 73 acceptance tests green incl. +2 new (`test_gt_fail_is_never_an_oracle_vs_vision_disagreement`).
- NEXT: drive the eyes-on REAL-VERIFY live (N>=5) for a real grounded_rate with the corrected harness; the bare-cli full-fetch live e2e (loop backlog #1) remains top priority when the network is up.

## D100 — VECTOR_FETCH_FAR scenario knob exposes the composed out-of-reach fetch is broken (backlog #1) · 2026-06-28 (arch/plug-and-play, d84aa5c)
- CONTEXT: the in-reach fetch grounds 0.8 (eyes-verified, D99) but the FULL look->navigate->grasp chain never routed — the green bottle spawns 0.88m dead-ahead (go2_room.xml) and perception_grasp self-approaches that gap (scripted forward creep, ≤1.6m _SCAN_MAX_LOCAL_M), so the model never needs navigate_to_object. The composed cross-room fetch (the north-star agent-adaptive pipeline) was therefore UNEXERCISED.
- CHANGE (d84aa5c, additive + env-gated, spine untouched): `VECTOR_FETCH_FAR` knob. Adds a `pick_table_far` pedestal ~3m down the clear +X hall (go2_room.xml, inert in the default scene) and, in MuJoCoGo2.connect(), relocates the green bottle's freejoint onto it (13.88,3.0,0.32 — beyond the 1.6m self-approach) ONLY when the env is set. Default off = the in-reach near-grasp baseline is byte-preserved. The honest-verify spine reads live GT (arm_sim_oracle get_object_positions + weld), so moving the body does not weaken verification (Rule 5/honest-verify invariant). TDD: tests/unit/test_fetch_far_scene.py (RED green@10.88 -> GREEN green@13.88, dist-from-spawn>2m). 
- REAL-VERIFY (far-fetch, bare cli + real model via OpenRouter, verify_fetch_cli N=3 + eyes harness): the knob WORKS as designed — it forces the model OFF the 1-step grasp. Routes mobile_pick x2 / perception_grasp x1 (never the explicit navigate_to_object->perception_grasp 3-step chain); the dog PHYSICALLY navigates (read a temporal strip frame = dog mid-doorway driving toward the far table; the temporal motion witness finally has multi-step data). BUT grounds 0/3 — the composed out-of-reach fetch is BROKEN: mobile_pick navigates but fails to seat/weld the grasp; bare perception_grasp can't reach 3.9m and fails loud (no_detections) without the model replanning to navigate first.
- HONEST STATE: a real, newly-exposed reliability gap (composed cross-room fetch 0/3 vs in-reach 0.8). NOT a harness artifact — unit test pins the bottle at 13.88, the dog visibly navigated, RAN (no weld) with exit=2+verdict is a real failure not a truncation. NEXT (non-gated, Debug Protocol): why mobile_pick doesn't weld at the far table (standoff/approach mis-tuned for the far pedestal? perception re-localize at arrival?) + whether to steer decompose routing toward navigate_to_object->perception_grasp for out-of-reach targets. Far trials are slow (~3-4min/trial, the nav leg) — budget N and timeouts accordingly.

## D101 — CORRECTION to D99: the "eyes cleared a false-green" claim was over-attribution (adversarial review) · 2026-06-28 (arch/plug-and-play)
- A dynamic Review+Verify workflow (4 adversarial skeptics) REFUTED the D99-era claim that "the ADR-002 eyes are a real 2nd witness that caught/cleared the handover-only false-green". CORRECTION (honesty, the #1 cross-day risk): the 0.8 in-reach grounded_rate is REAL (D94/D95/D99, frozen GT weld+lift oracle, three consistent runs) and the moat is sound WHEN holding_object is the gating oracle. BUT: (a) the visual rubric (config/visual_acceptance_rubric.yaml: scene_rendered/robot_upright/body_intact/workspace_in_frame) is ORTHOGONAL to and STRUCTURALLY BLIND to whether the gripper holds the object — a vision PASS cannot confirm OR refute a grasp. (b) The handover-only GROUNDED was actually cleared by the GT WELD oracle + a human reading a frame, then MIS-ATTRIBUTED to the eyes. (c) The "拿给我" eyes run (grounded 0.4, disagreements=0) was terminal-only, never committed as a RESULT artifact; commit 13548dc (docs-only) silently rewrote 124c835's "never eyes-checked" into "eyes cleared it" — a self-flattering edit, now corrected. (d) The eyes have NEVER recorded catching a false-green and cannot catch a grasp-fakery one under the current rubric. NET: the eyes are architecturally a real, fail-closed, downgrade-only 2nd witness (separate gpt-4o VLM, gate.py test-verified) but have a REAL COVERAGE GAP on grasp authenticity — do not credit them with a catch they cannot make. Records (STATUS, memory) corrected to match.

## D102 — Round 1 "honest-acceptance" hardening: close the moat hole, fix the handover crash, instrument the far-fetch (review-driven) · 2026-06-28 (arch/plug-and-play, 0db2abb/acdffb4/0ea72f8)
- DRIVEN BY a dynamic Review+Verify+Plan workflow whose adversarial skeptics REFUTED/downgraded the surrounding "it's verified" comfort (see D101: the eyes never caught a false-green; the moat had a hole; the 0/3 was partly a harness artifact). The judge ranked an OBSERVE-and-harden round above any new far-fetch build. Executed as a 3-task TDD workflow + a moat skeptic; the orchestrator verified the spine edit firsthand.
- (1) MOAT HOLE CLOSED (object_goal.py, FROZEN spine, only-stricter): goal_has_object_intent returned FALSE on the EXACT primary commands 把绿色的瓶子拿过来 / 拿给我 — the gate meant to force a grasp through the holding_object oracle never fired on the real commands. Extended _OBJECT_INTENT_RE to fire on 拿(过来|来|给)? / 带过来|带回|带走 / 取过来|取回|去取 / bring / give me. A first attempt added bare 取/带 which the moat skeptic flagged as material OVER-MATCH (读取/获取/取消/选取/带领/带我去 wrongly gated -> false RED on nav/perception turns); FIXED by anchoring 取/带 to fetch-directional forms only. Probe: 9 fetch targets True, 13 over-match guards False. only-stricter vs the original (every original alternative retained; over-rejection only, never a false-GREEN — Rule 5 monotonicity holds).
- (2) HANDOVER 6-DoF CRASH FIXED (handover.py): used a 5-element SO-101 home pose; on the 6-DoF Piper, move_joints(5) raised ValueError, crashing every 拿给我 turn. Ported the D96 DoF-aware fallback (resolve arm.dof -> [0.0]*dof when lengths mismatch; SO-101 byte-identical). LIVE-verified: bare-cli "拿给我" now runs to a verdict with NO crash.
- (3) FAR-FETCH INSTRUMENTED (mobile_pick.py / verify_fetch_cli.py / measure_fetch_visual.py): _NAV_TIMEOUT 20s->60s (matches the Go2 driver default; the 20s budget false-failed the ~3-4m nav leg); pure _build_rec() helper surfaces a per-trial diagnosis + VECTOR_SIM_LOCK=1. HONEST INCOMPLETENESS: the skill-level diagnosis is still BLIND because StepVerdict (verdict.py) does not serialize result_data -> diagnosis=null in real runs; a true skill-code diagnosis needs an additive StepVerdict field (Round 2).
- REAL-VERIFY (bare cli + eyes, OpenRouter): "拿给我" no-crash (handover fix); near "把绿色的瓶子拿过来" N=3 = 2/3 GROUNDED+ACCEPT, disagreements=0 -> in-reach 0.8 baseline NOT regressed by the moat change. Far "拿过来" N=2 still 0/2: model routes perception_grasp (fails-loud >1.6m) / mobile_pick, NEVER navigate_to_object->perception_grasp -> the far failure is ROUTING. 269 unit tests green.
- NEXT (ROUND 2): skill-condition the kernel prompt (native_loop.py ~1067) so an out-of-reach target routes navigate_to_object->perception_grasp; add result_data/diagnosis to StepVerdict (additive). CEO GATE: a STRUCTURAL verify->FAIL->replan addition to the native kernel ReAct loop = architectural plan.md -> pause + executive summary before merge.

## D103 — Round 2 non-gated (StepVerdict diagnosis + kernel recovery prompt): the prompt nudge is INSUFFICIENT for the far fetch → the structural replan is a CEO GATE · 2026-06-29 (arch/plug-and-play, ee22131/d582d33)
- NON-GATED CODE DONE + unit-verified: (1) StepVerdict.diagnosis — a bounded, INFORMATIONAL per-step failure code (prefers the deterministic failure_class, falls back to result_data['diagnosis']); never feeds `verified` (moat invariant test added: verified==evidence_passed holds). (2) Kernel grasp prompt now carries a CONDITIONAL recovery branch (only on a no_detections-style grasp failure -> navigate_to_object(name) -> re-grasp; NEVER preemptive). In-reach unaffected by design; native_loop + verdict + 219-test sweep green.
- REAL-VERIFY (bare cli, OpenRouter, VECTOR_FETCH_FAR): far "拿过来" N=4 (1+3) = 0/4 GROUNDED. Routing still CHAOTIC across runs — ["detect"] / ["perception_grasp"] / ["stop","detect"] / one SIGKILL(exit -9, the recovery nav leg exceeded the 300s per-run timeout). The model never reliably composes navigate_to_object->perception_grasp. CONCLUSION: the prompt nudge ALONE does NOT fix the far fetch — the model's far routing variance is too high for guidance to steer deterministically. Near "拿过来" NOT regressed (TRIAL 1 GROUNDED+ACCEPT; recovery branch can't fire in-reach).
- TWO SECONDARY FINDINGS: (a) diagnosis=null on the far failures because they are RAN (success-but-no-ground), not fail-loud FAILED-with-a-code — the diagnosis field can't illuminate the dominant "ran but didn't weld" mode (needs a ran-no-weld signal). (b) The VECTOR_FETCH_FAR scenario moves ONLY green far; the near blue/red distractors at (10.9) confound the far-green fetch (perception may engage a near bottle) — a cleaner test moves ALL bottles far.
- CEO GATE (QUEUED, not crossed): the real far-fetch fix is a STRUCTURAL verify->FAIL->replan in the native kernel ReAct loop (deterministically inject navigate_to_object->perception_grasp when a far grasp returns no_detections / RAN-no-weld, instead of relying on the model's self-direction) = the absent Rule-1 "recover" pillar = an architectural plan.md change. Logged to the decision queue; do NOT cross autonomously. Batch into an executive summary for Yusen.
- NEXT (non-gated, for the autonomous loop): (1) cleaner far scenario (move ALL bottles far) + re-measure to remove the distractor confound; (2) ran-no-weld diagnosis; (3) pick-and-place kind-leap (place skill exists, placed_count in the frozen spine); (4) slop cleanup (dead _recenter_lateral, /tmp/pgrasp debug writes, inline hot-path imports) + find_objects substring hardening.

## D104 — Round 3 (cleaner far scenario): removing the distractor confound does NOT unblock the far fetch — it burns the whole ReAct budget without grounding → the gated structural replan stands · 2026-06-29 (arch/plug-and-play, 8dd79f8)
- NON-GATED CODE DONE + unit-verified (backlog #1): VECTOR_FETCH_FAR now relocates ALL THREE pickables (green+blue+red) the SAME +3 m onto pick_table_far, not green alone (mujoco_go2.py: per-body qpos[x]+=3.0 over _FAR_FETCH_BODIES; default off byte-preserves the in-reach baseline). This removes the D103(b) confound where a near blue/red distractor sat inside the spawn's 1-step reach and perception_grasp could engage it instead of routing to the far target. TDD: tests/unit/test_fetch_far_scene.py — added test_default_keeps_all_three_near + test_far_knob_pushes_all_three_out_of_reach (blue/red 10.90->13.90, y-spread 2.78/3.22 preserved, dist-from-spawn>2 m); 4 tests GREEN. Spine untouched (verify reads live GT).
- REAL-VERIFY (bare cli + eyes harness, OpenRouter, VECTOR_FETCH_FAR=1, "把绿色的瓶子拿过来"): a clean-far fetch turn does NOT complete within a usable budget. Triangulated four ways: PTY harness @280 s -> killed mid-run, EMPTY output (no verdict); direct `cli --sim-go2 --headless --native-loop` @240 s -> empty/timeout; direct @480 s -> empty/timeout; single run_once @600 s -> exceeds the harness cap. cli `--json` buffers the verdict to the end, so "empty output" == "did not finish", NOT a logging gap. Code-read confirms it is BOUNDED not infinite: native_loop _MAX_NATIVE_TURNS=24, and with the far object each ReAct turn issues a nav/grasp attempt of up to ~60 s (_NAV_TIMEOUT), so the turn can run ~10-20 min — far past the ~280 s acceptance budget — without ever grounding.
- CONCLUSION (honest): the distractor confound was NOT the cause of the far failure. With a CLEAN far scene the model STILL cannot compose look->navigate_to_object->perception_grasp within a usable budget — its free-form ReAct burns the entire 24-turn cap on repeated mis-sequenced nav/grasp attempts (a more severe manifestation of D103's "routing chaotic, 0/N"). This STRENGTHENS, does not weaken, the case that the real fix is the CEO-gated STRUCTURAL verify->FAIL->replan in the kernel ReAct loop (deterministically inject navigate->grasp on a far no-weld). Residual uncertainty (flagged, <100% sure): a turn might eventually ground beyond 600 s — but "grounds only after 10+ min of flailing" is itself not a reliable bare-cli far fetch, so the conclusion holds either way.
- NEXT (non-gated): backlog #2 ran-no-weld diagnosis (illuminate the dominant far failure mode without the gated replan), then #3 pick-and-place kind-leap, then #4 slop cleanup. The far-fetch reliability is now PLATEAUED on non-gated levers (D100/D102/D103/D104 all confirm the model won't self-compose the far fetch) — per the loop's plateau guidance, pivot down the backlog rather than thrash on more far measurements; the structural replan remains queued for Yusen.

## D105 — Backlog #2 ran-no-weld diagnosis: the dominant out-of-reach failure mode is now diagnosable (informational only) · 2026-06-29 (arch/plug-and-play, 98b169d)
- CONTEXT: D103 found StepVerdict.diagnosis=null on far failures because they are RAN (the grasp runs to completion but no holding_object weld forms), not fail-loud FAILED-with-a-code. The skills emitted only the vague 'possibly_missed' and native_loop discarded every skill diagnosis (StepRecord carried no result_data), so the dominant "ran but didn't weld" mode was invisible.
- CHANGE (additive, moat-untouched): (a) perception_grasp + mobile_pick read the GT weld (gripper.is_holding — the oracle the actor cannot author) and stamp result_data['diagnosis']='ran_no_weld' (+ weld_formed flag) when the attempt completes without a weld; (b) skill_wrapper carries the failure diagnosis through ToolResult.metadata (not only the success path); (c) native_loop captures the last action skill's diagnosis from ToolResult.metadata and threads it onto StepRecord.result_data at verify time (reset per step). Informational ONLY — never feeds `verified`; spine cognitive/* + arm_sim_oracle byte-unchanged (D103's verified==evidence_passed invariant test still holds).
- STATE: committed 98b169d, unit-green; this is the catch-up RECORD (the chunk shipped without a DECISIONS entry). The diagnosis now lights up the far no-weld mode; it does not by itself fix the far fetch (that remains the queued CEO-gated structural replan, D103/D104).

## D106 — Backlog #3 go2+arm pick→place: the WIRING is proven (grasp→release); placed_count is blocked by arm↔table collision + floor-only oracle (CEO gate) · 2026-06-29 (arch/plug-and-play)
- DELIVERED: tests/vcli/test_go2_pick_place_composition.py — the go2+Piper (mobile-manipulation) pick→place composition proof. perception_grasp lifts the green bottle (GT weld forms, holding_object True), then mobile_place descends in front of the table and RELEASES it (weld breaks, holding_object False). holding() reads the live MuJoCo weld — an oracle the actor cannot author — so False-after-True can only be a real grasp then a real release. Real-sim GREEN (40s). Guards the go2+arm place wiring (skill registration, DoF-aware 6-DoF arm, gripper open) the way test_pick_place_chain guards the standalone arm.
- CORRECTION of the WIP retreat's stated reason (the retreat docstring claimed "Piper IK can't reach floor height" — FALSE). Two probes (2026-06-29) settled it empirically: (1) collision-free top-down IK CONVERGES to z=0.10 at x=10.55/10.70 (fk(q) lands exactly at the commanded low target); (2) but the LIVE loaded arm, reaching from the dog's back over the table edge, COLLIDES with the table front-top corner (~x=10.80, z=0.28) and stalls at z~0.30 — ee_fk(q)=[10.70,3.00,0.20] vs ee_live=[10.80,2.98,0.34]; the released bottle settles back near the table top (z~0.31), so placed_count (credits only z<0.10 floor-rest) stays 0. So the retreat's OUTCOME was right ("releases the object back near the table") but its mechanism was wrong — it is arm↔table collision under load, NOT IK reach.
- WHY placed_count stays out of reach non-gated: even with clear floor, dropping a bottle on the ground is the wrong task for tabletop manipulation; the natural place target is a RECEPTACLE at height, which the frozen placed_count (z<0.10) structurally cannot grade. A placed_count-grounded place on a tall mobile manipulator needs a receptacle-relative resting oracle = a spine semantics change = CEO GATE (queued in plan-find-grasp-refactor.md ## Decisions pending). The honest grasp→release primitive is the non-gated deliverable; the directed-placement credit waits on the oracle gate.
- DISCIPLINE NOTE: ORIENT caught the uncommitted retreat asserting an unverified kinematic claim; rather than accept it, ran two foreground real-sim probes (Debug Protocol) to characterise the true blocker before committing — exactly the "don't bulldoze a wrong theory" guard, applied to a retreat as well as a fix.

## D107 — Backlog #4 slop cleanup: dead lateral-recenter removed + /tmp/pgrasp debug writes gated · 2026-06-29 (arch/plug-and-play)
- REMOVED dead code: `_recenter_lateral` (+ its 3 orphaned `_DOCK_LATERAL_*` consts) in skills/utils/terminal_dock.py — superseded by the holonomic `_body_fine_position` closure (R42/D55); zero remaining references (grep-confirmed). The dock test that pinned it was renamed `test_dock_lateral_offset_closed_by_fine_position` and re-anchored on the fine-position residual tolerance (kept green).
- GATED side effect: the 3 unconditional `/tmp/pgrasp_{rgb,depth,mask}.png` writes in perception_grasp._perceive_grasp_point are now behind `VECTOR_PGRASP_DEBUG` (default OFF) — they were a /tmp write on every perceive in the grasp hot path. The mask compute itself is untouched; only the diagnostic image dumps are gated.
- INVESTIGATED + LEFT AS-IS (not slop): find_objects_by_category substring match is INTENTIONAL + tested (navigate_to_object relies on it); the cv2 inline import is a deliberate optional-dep lazy import.
- BEHAVIOR-NEUTRAL: 109 skills unit tests green, imports clean, _PGRASP_DEBUG defaults False. No reliability/behavior claim → no sim re-verify needed.

## D108 — Multi-object sequential fetch PROVEN (first composed physical task): green+blue both weld on real sim, retreat-to-standoff root cause found · 2026-06-29 (arch/plug-and-play)
- CONTEXT: the per-item backlog (#1-#4) was exhausted (far-fetch + place CEO-gated). A Decision Workflow returned "all gated" but ALL THREE of its recon agents had died (pool_size=0, StructuredOutput-cap + a network drop) — it just re-derived the prior HANDOFF, the premature-"exhausted" failure mode (R9/D79). A focused recon agent validating candidates against the CODE refuted it: MULTI-OBJECT SEQUENTIAL FETCH is genuinely non-gated + high-ambition (composes existing perception_grasp, frozen spine untouched).
- DELIVERED: tests/vcli/test_native_loop_multi_object_fetch_pty.py — a deterministic (FakeToolScriptBackend, network-free) seal on the REAL go2+Piper attach sim. "把绿色和蓝色的瓶子都拿过来" -> grasp green -> verify holding_object('pickable_bottle_green') GROUNDED+CAUSED -> gripper_open (RELEASE the green weld) -> walk backward (retreat) -> grasp blue -> verify holding_object('pickable_bottle_blue') GROUNDED+CAUSED -> finish. The set of GROUNDED holding_object targets must equal {green, blue} (a green-only completion FAILS the seal). Real perception pipeline (NOT a stub, cf D97), two real GT welds graded by the UNTOUCHED spine. PASSES (69s).
- HONESTY of the proof: one gripper holds one object, so the blue weld 0->1 can form ONLY if gripper_open actually released green (open() breaks the weld) — step-1 GROUNDED+CAUSED is itself the proof the release worked. The frozen evidence_passed does NOT know the user named two objects, so the multi-object success criterion (a GROUNDED holding_object per named object) is asserted in the TEST, not auto-graded — no spine change, nothing loosened.
- ROOT CAUSE found by probe (Debug Protocol OBSERVE): the 2nd offset object hit no_detections because a grasp leaves the dog DOCKED close to the table (x~10.46) where NO table object is in the camera FOV (both bottles -> 0 px); from the spawn/standoff (x~9.7-10.0) BOTH are visible (~490-850 px). navigate_to_object was a no-op here (scene_graph None in the bare rig). FIX (non-gated, no skill edit): retreat ~0.8 m to a perceiving standoff before the next grasp; the next perception_grasp then perceives + self-approaches.
- PRODUCER LEVER (native_loop._build_system_prompt): multi-object guidance — name>1 object => fetch one at a time, grasp->verify->gripper_open->BACK OFF ~0.8 m to a viewing distance->next; finish only after EVERY named object has its own passed holding_object. Enables the LIVE model to emit the sequence the seal proves gradable.
- NOT YET DONE (next round, non-gated): the LIVE bare-cli + NL multi-object run is the full acceptance, but the eyes harness (measure_fetch_visual) grades single-object only (a green-only completion would count grounded), so an honest multi-object live measurement needs the harness extended to require BOTH welds in the trace. Deferred; the deterministic real-sim seal + lever is this round's verified floor. NO live multi-object reliability number is claimed (untested) — honesty discipline.

## D109 — Far-fetch recovery (skill-level) GROUNDS in isolation; through-the-model still RAN (next: routing) · 2026-06-29 (arch/plug-and-play, 8c5fc00/34e2dbc/ee57a05)
- CEO-APPROVED far-fetch fix (Gate A). Architecture decision (Opus workflow, judge-confirmed): NOT a kernel replan (re-grows the hardcoded planner = violates native-is-the-design + the native_loop import firewall). Instead SKILL-LEVEL: perception_grasp self-completes. ROOT CAUSE: a colour target beyond front_object._MAX_DEPTH=2.0m perceives as no_detections (HSV gate) though the un-gated open-vocab localizer (object_localizer.localize_objects_3d) sees it at 2cm.
- IMPLEMENTED (perception_grasp.py, kernel/moat UNTOUCHED, firewall green): single-shot, no_detections-gated recovery — localize via the un-gated detector from the CLEAN FORWARD pose captured BEFORE the perceive-scan rotates the dog (re-localizing post-scan mislocalized a phantom: real (13.9,3.0) -> (6.0,8.6)), drive to the 0.95m standoff via the FAR planner, re-perceive fresh at arrival (<2m), grasp. + visual_e2e harness sets HF_HUB_OFFLINE=1 (cached detector; a flaky network made detect/localize phone home + fail -> no verdict).
- VERIFY: 7 recovery + 116 skills unit tests green. ISOLATED real-sim (perception_grasp directly on VECTOR_FETCH_FAR, no look/nav/model): FAR_GRASP_PASS — localized the REAL bottle (13.86,3.01), drove (arrived 0.24m), GRASP success=True, lifted +0.23m, gripper.is_holding=True, GT make_holding_object oracle=True. Grounds the out-of-reach object with a real un-authorable weld.
- OPEN (next): the FULL bare-cli + model + eyes far fetch still GT=RAN/not-grounded — the recovery did not fire via the model path. The skill works; getting the model to route perception_grasp end-to-end (cli output suppressed; possibly model routes the pre-existing describe 'no caption' crash, or the recovery's in-process behavior differs) is the remaining step.

## D110 — Far fetch CLOSES THE LOOP through bare-cli + model (GROUNDED verified=True); reliability variable · 2026-06-29 (arch/plug-and-play, 35307bc)
- DIAGNOSED the D109 model-path gap: the model DOES route perception_grasp first on the far scene (verify_fetch_cli: 2/3), but the recovery first failed because grounding-dino is ENGLISH open-vocab while the model passes the Chinese NL query ("绿色的瓶子") — the seed localize matched nothing -> recovery bailed -> no_detections. FIX (35307bc): the far seed localizes by the colour-English name (f"{color} bottle"; color resolves to green/blue/red) instead of the raw query.
- CLOSED THE LOOP (demonstrated): a logged bare-cli run (cli -p "把绿色的瓶子拿过来" --sim-go2 --native-loop, VECTOR_FETCH_FAR=1, model free-routing, no flag/script) -> verdict GROUNDED verified=True (1/1) — the honest holding_object GT weld on the out-of-reach bottle, end-to-end through the model. Isolated FAR_GRASP_PASS reproduces with both English and Chinese queries (real weld lifted +0.23m).
- RELIABILITY still VARIABLE (open follow-on): verify_fetch_cli far N=3 = 0/3 (model routed perception_grasp 2/3 but those returned no_detections — colour-param/routing variance suspected; mobile_pick 1/3). The model also routes look/describe/stop on some turns (D103/D104 routing variance); look needs the VLM (GPT-4o/OpenRouter) which is intermittently SSL-flaky right now, blocking the eyes harness. So the loop CAN close (proven) but does not yet ground every turn.
- NEXT (non-gated): (1) why a model-routed perception_grasp sometimes still no_detections (logged probe of the params/color the model passes); (2) routing consistency; (3) eyes confirmation + grounded_rate over N when the VLM network is stable; (4) the pre-existing describe 'no caption' crash.

## D111 — Far-fetch reliability: the perception_grasp "no_detections" variance ROOT-CAUSED (nav terminal-heading) + FIXED; end-to-end now gated by mobile_pick routing · 2026-06-29 (arch/plug-and-play, 7756132)
- DROVE the D110 open follow-on ("why a model-routed perception_grasp sometimes still no_detections") with a LOGGED PROBE (Debug Protocol). Root cause is NOT colour/routing/seed (all worked — the seed localized the real bottle every run, even from a Chinese query): the far-recovery drives to the 0.95m standoff POSITION but FAR's `navigate_to` has NO terminal-heading control, so the dog arrives mis-faced; the re-perceive's ONE-directional ~200deg scan (`_SCAN_MAX_STEPS=6 * _SCAN_STEP_RAD=0.6`, vyaw always +) then misses a bottle in the uncovered ~160deg arc → mask_px=0 at all 6 steps → no_detections. Decisive evidence: two near-identical-depth standoff arrivals masked 2073 vs 0 green px PURELY on arrival heading (full casebook entry in tricky-bugs.md).
- FIX (7756132, skill-level, kernel/moat UNTOUCHED, import-firewall green): the recovery KNOWS the target xy (the seed) → after `navigate_to(standoff)` it deterministically TURNS TO FACE it via the existing `_grasp_ready_repose(base, (ox,oy), clearance=_FAR_STANDOFF_M)` (idempotent when already head-on → zero regression to the lucky-arrival / in-reach paths) before re-perceiving. No search, no luck, no native-loop replan. TDD: tests/unit/skills/test_far_fetch_recovery.py +2 (faces-when-off / benign-when-head-on); 9 recovery + 120 skills + import-firewall green.
- REAL-VERIFY (skill-direct, the EXACT cli sim config — backend=auto→MPC, 640x480, VECTOR_NO_ROS2=1, far scene): 3/3 GROUNDED. Each run the recovery arrived ~22-26deg off, the facing turn corrected heading→~0, `perceive (no scan)` then succeeded on the first frame (no scan-luck), real GT weld lifted green +0.23m, holding_object oracle=True. Before the fix the SAME path masked 0 on a mis-faced arrival. So the perception_grasp far path is now reliable, not variable.
- HONEST END-TO-END STATE (bare-cli + model, N=2 sampled): both trials the model routed `mobile_pick`, NOT perception_grasp — and mobile_pick fails the far fetch for a SEPARATE reason: it resolves the target from `world_model` (not live perception) and gets a BOGUS far position (computed an approach at ~(0.5,0.2) near the origin for a bottle at (13.86,3.0)) → nav_failed / ReAct flail. So end-to-end far reliability is now gated by (a) model routing choosing mobile_pick over the (now-fixed) perception_grasp, and (b) mobile_pick's stale/wrong far-target resolution — NOT by the no_detections bug this round closed. EYES not yet run (the model-path never reached a clean perception_grasp ground to grade; VLM-eyes confirmation still pending).
- NEXT (non-gated): (1) mobile_pick far-target resolution — why world_model gives a near-origin position for the relocated far bottle (likely the VECTOR_FETCH_FAR relocation isn't reflected in world_model's object table); (2) routing consistency (make BOTH far skills reliable so whichever the model picks grounds — native-is-the-design, don't hardcode the choice); (3) eyes grounded_rate over N once a perception_grasp route grounds end-to-end and the VLM net is stable.

## D112 — Far end-to-end gate #2 fixed: mobile_pick's origin-phantom (a position-less detection stored at (0,0,0)) → fast honest object_not_found · 2026-06-29 (arch/plug-and-play, c6f62a7)
- CONTEXT: D111 fixed perception_grasp's far path but found the bare-cli far fetch is now gated by MODEL ROUTING — the model often picks mobile_pick, which failed by driving to the ORIGIN and flailing (61-line ReAct burn). Root-caused (Debug Protocol, casebook entry): DetectSkill defaults x,y,z=(0,0,0)/has_3d=False; for a far object the tracker yields no 3D pose (no valid depth at ~3.9 m), so the object was ADDED to world_model at the (0,0,0) SENTINEL and mobile_pick's _resolve_target navigated to it. A 2D-only detection (existence) was stored as a TARGETABLE object at origin.
- FIX (c6f62a7, additive + skill-level, moat untouched): `ObjectState.has_position: bool = True` (additive, Rule-6-safe, D103 precedent for a non-gated additive dataclass field). DetectSkill sets has_position=has_3d for a fresh detection and PRESERVES a previously-known position instead of clobbering it to origin when a re-detect has no fresh 3D; pick/_resolve_target SKIPS position-less objects so resolution falls through to autodetect/object_not_found. The frozen verify spine is untouched (world_model is the actor's targeting BELIEF, never the verification oracle).
- REAL-VERIFY (bare cli + model, OpenRouter, VECTOR_FETCH_FAR): the far mobile_pick turn went from a 61-line origin flail (`nav_failed`, navigate_to ~(0.5,0.15) repeated) to an 11-line FAST `object_not_found` — the phantom is skipped, the dog never drives to origin, the ReAct budget is preserved and the model gets a clean re-route signal. perception_grasp far path unaffected (3/3 from D111 stands; it doesn't use world_model). Unit: 4 new + 11 existing world_model/detect tests green; 233-test skills/world sweep green (6 reds are PRE-EXISTING stale skill-registry/diagnostics tests, confirmed red on clean HEAD — not regressions).
- HONEST STATE: the far fetch now has TWO reliable outcomes — perception_grasp grounds (D111), mobile_pick fails fast+honest (D112). What remains is purely MODEL ROUTING: the model picking/re-routing to perception_grasp for the far target (D103/D104 routing variance). The model-path far GROUNDED rate over N + eyes confirmation is still pending a stable routing run (OpenRouter was intermittently dropping connections this session).
- NEXT (non-gated): (1) routing — help the model prefer/re-route to perception_grasp for an out-of-reach fetch WITHOUT a hardcoded table (skill descriptions / the native producer guidance, native-is-the-design); (2) optional far-localise for mobile_pick (use the un-gated object_localizer like perception_grasp's seed so mobile_pick can ALSO ground far — needs world/base frame verification first); (3) eyes grounded_rate over N once routing lands a perception_grasp far ground end-to-end.

## D113 — FAR FETCH CLOSES END-TO-END through bare-cli + NL + model: routing fix lands it on perception_grasp; green 5/5 + blue 1/1 GROUNDED (campaign #1 problem) · 2026-06-29 (arch/plug-and-play, b10516e)
- THE far-fetch milestone. After D111 (perception_grasp faces the seed) + D112 (mobile_pick no origin-phantom), the only remaining far gate was MODEL ROUTING — every far trial routed mobile_pick (which honestly can't far-localize) instead of the working perception_grasp. ROOT: the skill DESCRIPTIONS (the native routing surface — model reads them, no hardcoded table) were inverted vs reality: mobile_pick advertised "walk to a reachable pose near the object" (sounds right for far) while perception_grasp never said it now SELF-NAVIGATES to out-of-reach targets.
- FIX (b10516e, native-is-the-design — accurate metadata, model still decides; NOT a keyword table): perception_grasp.description now states it is THE default fetch/grasp skill near OR across the room and SELF-NAVIGATES to an out-of-reach target (localize -> drive -> face -> grasp); mobile_pick.description now states it REQUIRES an already-localized target and to use perception_grasp for a fresh/un-localized/out-of-reach fetch. 120 skills + import-firewall green.
- REAL-VERIFY (bare cli + real model, OpenRouter, VECTOR_FETCH_FAR, GT holding_object weld oracle): GREEN "把绿色的瓶子拿过来" N=5 = 5/5 GROUNDED verified=True, ALL routed perception_grasp (each: far seed localizes the real bottle -> recovery drives -> re-pose turn faces it (D111) -> perceive no-scan -> real weld). BLUE "把蓝色的瓶子拿过来" 1/1 GROUNDED. So the composed cross-room fetch — the campaign's #1 open problem, 0/N for the whole campaign — now closes reliably end-to-end through bare cli + NL for 2/3 colours. Pre-fix the same command was 0/3 (all mobile_pick fails).
- RED FAILS (1 trial, NEW separate issue — NOT a regression): "把红色的瓶子拿过来" RAN/no_detections. The open-vocab seed localizes the red object fine (13.87,3.22) and the facing turn fires (-141deg), AND the HSV red mask works AT DISTANCE (mask_px~1000 during the 3.9m spawn scan) — but mask_px=0 at the CLOSE standoff (d_min=0.6m). The red object is a CAN (pickable_can_red), SHORTER than the green/blue bottles, so at the ~0.9m standoff it falls BELOW the camera's vertical FOV (the camera looks over it) while the taller bottles stay in frame. A short-object-at-close-range perception-geometry issue, distinct from D111/D112/routing.
- HONEST STATE: far fetch grounded_rate green 5/5 + blue 1/1 = 6/6 (GT weld); red 0/1 (FOV). EYES (ADR-002 VLM 2nd witness) NOT yet run on these — the GT holding_object weld is the authority; eyes confirmation pending a stable-VLM run (OpenRouter dropped connections intermittently this session). Per red-team discipline: the 6/6 is a strong sample, not a statistical guarantee; model routing is non-deterministic (5/5 routed perception_grasp this session, but routing can still vary).
- NEXT (non-gated): (1) RED short-can FOV — raise the camera tilt / widen the standoff for short objects so a short can stays in frame at the grasp standoff (then red far should ground like green/blue); (2) eyes grounded_rate over N (green/blue) when the VLM net is stable; (3) near all-3-colours confirmation. Then the find-fetch half of the goal is reliably closed; the PLACE half remains CEO-gated (D106 receptacle oracle).

## D114 — RED-TEAM CORRECTION to D113: the far-green "5/5" was a routing STREAK; honest grounded_rate ≈ 9/12 (0.75), gated by ~25% mobile_pick routing variance · 2026-06-29 (arch/plug-and-play)
- Running MORE trials (the red-team discipline — "never believe a number until you've tried to break it") REFUTED the D113 implication that the far fetch is reliably closed. The D113 5/5 streak (all perception_grasp) was luck of non-deterministic routing, not determinism.
- HONEST FAR-GREEN TALLY over 12 model-path samples ("把绿色的瓶子拿过来", VECTOR_FETCH_FAR, GT holding_object weld): 9 GROUNDED (routed perception_grasp -> all 9 ground) + 3 RAN (routed mobile_pick -> fast object_not_found, D112) = grounded_rate ≈ 0.75. The split is PURE ROUTING: perception_grasp grounds 9/9 when chosen (the D111 fix is solid); the model still picks mobile_pick ~3/12 ≈ 25% of the time, and a far mobile_pick honestly can't 3D-localize -> no ground. The 2 eyes-harness trials that read GT=RAN were this SAME routing variance (both mobile_pick) — the eyes harness is CORRECT, not buggy (earlier "eyes wrapper bug" hypothesis withdrawn).
- SO: capability is real (perception_grasp far is reliable when routed) but the NET reliable-fetch number is 0.75, BELOW the >=0.8 target — the binding constraint is now ROUTING CONSISTENCY, not any single skill. The D113 description fix SHIFTED routing toward perception_grasp (~75% vs ~0% pre-fix) but did not make it deterministic.
- ROBUST FIX DIRECTION (next): make the far fetch ROUTING-INDEPENDENT — give mobile_pick the same un-gated object_localizer far-acquire that perception_grasp uses (so a far mobile_pick ALSO grounds instead of object_not_found). Then whichever skill the model picks grounds -> rate -> ~1.0 regardless of routing variance. (Needs the world/base frame check flagged in D112.) Alternative/secondary: strengthen the routing signal further. Either way, stop relying on the model picking the one working skill.
- DISCIPLINE NOTE: this is the loop's headline cross-day risk caught in-session — a convincing 5/5 that was a streak. The fix was simply N more trials. Eyes (ADR-002) confirmation of a perception_grasp far ground still pending (the VLM works; ~75% routing means an eyes N>=3 should catch one).

## D115 — Routing-independent far fetch (mobile_pick delegates to perception_grasp) + closed-loop recovery facing; remaining gate is model PLANNING variance · 2026-06-29 (arch/plug-and-play, e3dabcb/e3c13da)
- CEO-approved next lever (D114): make the far fetch ROUTING-INDEPENDENT so whichever skill the model picks grounds. FRAME AUDIT first (the D112 flag): object_localizer.localize_objects_3d returns WORLD frame (camera->world via get_camera_pose), which is exactly what mobile_pick's compute_approach_pose needs; DetectSkill's camera_to_base (arm-base, identity default) is the frame-inconsistent path. So the clean, frame-correct, no-duplication fix is DELEGATION.
- DELEGATION (e3dabcb, skill composition — NOT a kernel planner, firewall green): on a resolve+autodetect miss (the far case — a far object can't be 3D-placed by the tracker), mobile_pick DELEGATES to perception_grasp (find + un-gated world-frame localize + drive + face + grasp) instead of returning object_not_found. Honest: perception_grasp grounds only via the real GT weld. REAL-VERIFY (skill-direct, far scene, EMPTY world_model): DELEGATE_GROUND_PASS — mobile_pick delegated, perception_grasp's recovery localized (13.86,3.00), grasped, green +0.243m, holding_object oracle True. 26 mobile_pick + 239 skills/world tests green (3 existing object_not_found tests updated to assert the new delegation terminal).
- CLOSED-LOOP FACING (e3c13da): a model-path far trial exposed that the recovery's _grasp_ready_repose turn is OPEN-LOOP and UNDERSHOOTS a large facing turn (~75deg commanded -> ~63deg achieved -> ~12deg residual -> re-perceive at the close standoff missed the bottle). Added _face_object after the repose (closed-loop, iterates to _FACE_TOL_RAD ~0.08rad — the same repose->face pair _approach_object uses pre-grasp). Idempotent when already facing (no regression); +1 test, 10 recovery tests green. Skill-direct CLICFG_GRASP_PASS after the change.
- HONEST REMAINING GATE — MODEL PLANNING variance (not routing, not any single skill): post-fix model-path far trials still vary by the PLAN the model emits. A clean single-step perception_grasp (or mobile_pick->delegate) GROUNDS; but the model sometimes plans a MULTI-STEP find/detect/explore sequence (verify find_object, then explore) that never reaches a grasp step (trial: n_steps=5 of detect/explore, RAN). That multi-step-plan variance is the deeper D103/D104 "routing/planning chaotic" residue and is the next gate — NOT fixed by the delegation or the facing. No firm new grounded_rate claimed (the post-fix trials are too few + hit mixed failure modes; needs a clean N after a planning-variance fix — red-team discipline, no streak-claims).
- NEXT (non-gated): (1) reduce the MULTI-STEP-PLAN variance — nudge the model toward a single grasp step for a simple fetch (native producer guidance / the grasp-step verify binding), WITHOUT a hardcoded planner; (2) RED short-can FOV; (3) eyes grounded over N. The PLACE half stays CEO-gated (D106; CEO approved the height-aware oracle but asked for the full executive summary FIRST — to be prepared before any spine edit).

## D116 — Receptacle-place oracle BUILT + adversarially moat-proven (CEO-approved D106 spine change) · 2026-06-29 (arch/plug-and-play, 782a6ec/62e7ffc/eb834e8)
- CEO APPROVED (D106) the height-aware place oracle "build it with the moat proof". DELIVERED additively in the FROZEN spine `vcli/worlds/arm_sim_oracle.py`: `make_resting_on_receptacle(agent, region, rest_z)` credits an object as PLACED ON a height receptacle ONLY when ALL hold from independent GT the actor cannot author — xy STRICTLY in the region + z within _RECEPTACLE_Z_BAND of rest_z + AT REST (finite world speed < _AT_REST_SPEED) + RELEASED (not held, reuses holding_object). + a `get_object_velocities` GT accessor (mujoco_piper, reads freejoint qvel — actor controls ctrl, never qvel). The floor-only `make_placed_count` + `make_holding_object` are BYTE-UNCHANGED (pure additions, zero deletion lines across all 3 commits) so MONOTONICITY holds by construction — no existing ACCEPT path widened, and the new symbol is not yet in any verify namespace so no existing verify string can reach it.
- ADVERSARIAL MOAT PROOF (security-reviewer skeptic, the CEO's required proof): FIRST pass returned UNSAFE with TWO concrete false-greens — (1) the at-rest check was fail-safe-to-SKIP (get_object_velocities is absent on every arm but MuJoCoPiper -> velocities={} -> in-flight object credited; also NaN speed bypassed `nan > thr`=False); (2) holding_object skips z<_LIFT_MIN_Z so a held object over a LOW receptacle read as released. FIXED (62e7ffc): at-rest is now fail-safe-to-REJECT (no/NaN/inf/malformed velocity -> not credited, `math.isfinite` guard) + a factory guard refusing a receptacle whose band dips below _LIFT_MIN_Z. RE-VERIFIED SAFE (skeptic 2nd pass: all six velocity-bypass variants reject, the 0.16 boundary is exact, monotonicity intact, no new hole). + a final non-finite-rest_z guard (eb834e8). 13 moat tests.
- DISCIPLINE WIN: the adversarial skeptic caught two real moat holes I'd otherwise have shipped — the exact reason the honest-verify spine requires an independent refutation pass, not author confidence. The fix made the oracle STRICTER (reject-on-unconfirmable), never looser.
- NOT DONE (next, non-gated now the spine is proven): (1) WIRE the oracle into the verify namespace (robot.py world registration) so a place skill can verify against it; (2) add a height RECEPTACLE (bin/shelf) to the go2 scene; (3) the arm↔table descent/collision fix (D106) so the place physically completes; (4) real-sim ground a bare-cli pick-AND-place. Then the "complete...PLACE" half of the goal closes. The ORACLE (the CEO-gated moat piece) is done + proven; the rest is non-spine plumbing.

## D117 — EYES harness strip-segfault ROOT-CAUSED + fixed; first EYES-VERIFIED grounded frame (near fetch: GT weld + VLM PASS) · 2026-06-29 (arch/plug-and-play, 6df3e83)
- The goal's REAL-VERIFY is the EYES harness (ADR-002 measure_fetch_visual). It grounded 0/5 far + NO_TRACE on every near trial — while the DIRECT GT probe grounds 9/12. ROOT-CAUSED by isolation (not network, not routing): the eyes harness routes perception_grasp fine (diagnostic: strategy=perception_grasp), but the per-step TEMPORAL STRIP (VECTOR_SNAPSHOT_STRIP=1) SEGFAULTS the turn — it opens a NEW EGL Renderer context while perception_grasp's cam/depth/seg EGL renderers are live on the same thread (multiple EGL contexts MuJoCo can't co-host -> a C-level crash try/except cannot catch; _render_agent_frame already uses the Case-13 isolated-MjData copy, so the crash is the EGL CONTEXT, not the data race). DECISIVE PROBE: near + the eyes PTY path (run_cli_turn) WITH strip -> NO_TRACE; the SAME turn with strip OFF -> GROUNDED verified=True.
- FIX (6df3e83): the per-step strip is now OPT-IN (default OFF; VECTOR_EYES_STRIP=1 to re-enable after the GL-context sharing fix). It is a DOWNGRADE-ONLY temporal witness (bonus), so defaulting it off lets the CORE eyes acceptance — GT weld + the VLM judging the verdict frame — work. run_once handles the empty strip gracefully (no temporal cross-check).
- EYES-VERIFIED (near fetch, the goal's named acceptance, previously MISSING from the whole campaign): an eyes-path turn GROUNDED verified=True (perception_grasp, holding_object('pickable_bottle_green') weld), captured the verdict frame, and the VLM (gpt-4o, the orthogonal 2nd witness) judged it WITNESS=PASS (scene_rendered/robot_upright/body_intact/workspace_in_frame all yes). Orchestrator also READ the frame (dog at the pick table, arm down, green bottle at the gripper — consistent with the weld). GT + VLM + human-read all agree, no disagreement. The D101 caveat stands (the rubric is orthogonal to grasp authenticity — the GT weld is the grasp authority; the VLM confirms a valid rendered intact scene at the workspace).
- HONEST REMAINING: FAR eyes-verification (the >=0.8 far eyes-rate) still pending — the full far harness run needs a stable routing-model network (OpenRouter dropped connections intermittently this session, NO_TRACE); the eyes PATH now works (strip fixed), so a far grounded frame can be VLM-judged the same way once the net is stable. The PROPER strip fix (share/serialize the EGL context so the temporal witness works) is a seeded harness task.
- NEXT (non-gated): (1) far eyes-rate over N when the net is stable (now unblocked by the strip fix); (2) the PLACE plumbing (wire the D116 oracle + scene receptacle + descent fix + ground); (3) fetch planning-variance; (4) EGL-context strip fix to restore the temporal witness.

## D118 — PLACE plumbing started: scene receptacle added; pick-AND-place blocked by the arm-close DOCK (ik_unreachable), precisely scoped · 2026-06-29 (arch/plug-and-play, 7e4d568)
- Drove the non-gated PLACE half (network-independent, skill-direct). Added `place_bin` to go2_room.xml — a low-walled tray on a pedestal whose top is at z=0.28 (the SAME height the arm proves it reaches by grasping; a release at table-top height sidesteps the D106 table-front-edge DESCENT collision, which was about reaching down to z=0.10). The D116 oracle binds to it via make_resting_on_receptacle(region, rest_z=0.32).
- SKILL-DIRECT PROBE (grasp green -> mobile_place onto place_bin -> resting_on_receptacle): the GRASP succeeds (green lifted, holding_object True), the release mechanism + the oracle are wired — BUT the place fails `ik_unreachable`. ROOT: mobile_place navigate_to's the compute_approach_pose clearance (~0.55 m) and the in-process nav undershoots (arrived (10.63,3.47) for a bin at (10.95,4.20) = 0.80 m away), while the Piper top-down reach is only ~0.22 m forward. So the dog never docks ARM-CLOSE to a FREE-STANDING receptacle — unlike the GRASP, which works because perception_grasp's `_approach_object` JAMS the dog forward against the pick-table edge to its closest stall standoff. This is NOT the oracle (moat-proven, D116) and NOT the descent collision (the release is at table height) — it is the arm-close DOCK.
- FIX (precisely scoped, next round): give mobile_place the SAME jam-approach perception_grasp uses — after navigate_to, `_grasp_ready_repose -> _approach_object -> _face_object` toward the receptacle to dock arm-close, THEN the top-down release IK (PlaceTopDownSkill, now reachable). Reuses proven helpers (a clean extraction to skills/utils/ would de-couple them from perception_grasp). The `place_bin` geometry likely also needs to be JAMMABLE+REACHABLE like the wide pick_table (it is a small 0.15 m pedestal) — tune the bin so the dog jams its front + the arm reaches the tray top; sim-iterate. The oracle + grasp + release are READY; the dock + bin-geometry is the remaining manipulation work.
- NEXT (non-gated): (1) the mobile_place jam-dock + bin-geometry tuning -> skill-direct PLACE_GROUND_PASS (resting_on_receptacle 0->1, released); (2) wire resting_on_receptacle into the verify namespace (robot.py, scene-config receptacle per Rule 11); (3) model-path bare-cli pick-and-place; (4) far eyes-rate over N when the routing net is stable.

## D119 — PLACE dock fix lands the dog arm-NEAR the receptacle (0.55m → 0.06m short); remaining = bin-geometry + place-target-nearest-reachable + grasp reliability · 2026-06-29 (arch/plug-and-play, 2988df2)
- Drove the D118 fix: mobile_place now reuses perception_grasp's jam-approach (_grasp_ready_repose -> _approach_object -> _face_object) to dock arm-close before the release IK. Real progress — the held bottle ended at (11.00,4.14) for a bin at (10.95,4.20): the dock closed the gap from ~0.55 m (D118, ik_unreachable by a wide margin) to ~0.06 m short of arm-reaching the bin CENTRE. 15 mobile_place tests green.
- STILL ik_unreachable, by a hair, for two compounding reasons (precisely characterised): (1) REACH at the bin edge — the small 0.15 m place_bin pedestal stalls the forward-jam ~0.06 m short of the centre, and a low-z top-down place IK reaches LESS far than the lifted carry pose, so the centre is just out of the ~0.22 m envelope. Targeting the bin's NEAR edge (10.95,4.12, in the tray region) is the right "place at the nearest reachable point" lever. (2) GRASP variance — one re-run had grasp success=True but holding_object=False (the ~0.8 grasp didn't seat; bottle settled back at origin), so the place had nothing to carry. The place test is flaky on BOTH fronts at once.
- HONEST STATE: the place oracle (D116, moat-proven), the release mechanism, AND the dock are all in place; the remaining work is iterative MANIPULATION TUNING — (a) widen/position place_bin so the dog jams + the arm reaches the tray (like the wide pick_table), (b) target the nearest reachable point in the receptacle region (a mobile_place/PlaceTopDown refinement, not the centre), (c) retry-on-grasp-miss so a non-seating grasp doesn't fail the place. Converging but needs a fresh focused round (each iteration is a ~2-3 min real-sim run; grasp+place compounding variance).
- NEXT (non-gated): (1) the bin-geometry + nearest-reachable-target + grasp-retry tuning -> skill-direct PLACE_GROUND_PASS (resting_on_receptacle 0->1, released); (2) wire resting_on_receptacle into the verify namespace; (3) bare-cli model-path pick-and-place; (4) FAR eyes-rate over N when the routing net is stable. The CEO-gated oracle is DONE+proven; this is all non-gated manipulation plumbing.

## D120 — PICK-AND-PLACE GROUNDS through the moat-proven oracle (proven once) but is NOT yet RELIABLE (red-team caught 1/2) · 2026-06-30 (arch/plug-and-play)
- The "complete...PLACE" half GROUNDED for the first time: a skill-direct pick-and-place — grasp green from pick_table -> DOCK the arm over the place_bin tray (perception_grasp's jam-approach) -> RELEASE (gripper.open) -> the bottle falls into the tray and rests at (11.04,4.08,0.308) IN-REGION -> the D116 resting_on_receptacle oracle credits it (=1, released, at rest). A genuine 0->1 place via the moat-proven oracle (real grasp->carry->release, the oracle reads independent GT). MECHANISM FINDING: the top-down precise place IK is UNREACHABLE at the carry pose over a free-standing tray (the low-z reach is shorter than the lifted carry), so a DROP-when-docked-over-the-tray is the right place primitive for a bin/tray — not a precise IK.
- RED-TEAM (the honest catch): a 2nd run FAILED — the ~0.10 m dock-depth variance put the bottle at the tray EDGE (3.96 vs region 3.96), and the HIGH drop (z~0.56 -> tray top 0.28) bounced/rolled it OFF onto the floor (z=0.028, resting=0). So place_v5 alone would have been a FALSE "place reliably grounds" claim; red-teaming revealed reliability is LOW (1/2). The place GROUNDS (capability proven) but does NOT yet ground every turn.
- HONEST STATE: oracle (moat-proven), grasp, dock, release ALL work; the gap is RELIABILITY — the dock-depth variance straddles the containment edge + the high drop scatters the bottle. ROBUST PLACE needs: (a) a containment geometry the arm reaches even on a SHALLOW dock (the arm reaches only ~0.01-0.11 m past the jam face, so the drop zone hugs the near edge) + (b) a GENTLE (low) release so the bottle doesn't bounce/roll off — likely a lower-z release pose, which the top-down IK can't currently reach. This is iterative manipulation tuning (each run ~2-3 min + grasp variance), genuinely multi-round.
- NEXT (non-gated): (1) robustify the place (containment + gentle release) to a reproducible PLACE_GROUND_PASS over N; (2) mobile_place drop-release fallback (when the top-down IK is unreachable, release at the dock if over the receptacle) so the SKILL grounds; (3) wire resting_on_receptacle into the verify namespace; (4) bare-cli model-path pick-and-place; (5) FAR eyes-rate over N when the routing net is stable. The CEO-gated oracle is DONE+proven; this is all non-gated manipulation plumbing.

## D121 — PLACE robustification confirmed INSUFFICIENT via bin geometry alone (~50% over 4 runs); ROOT = carry-reach drops at the receptacle near edge + high-drop random bounce → the fix is a GENTLE LOW RELEASE · 2026-06-30 (arch/plug-and-play, 37100ae)
- Tried robustifying the D120 place by GEOMETRY (deeper place_bin pedestal so the arm reaches well inside + taller 0.05 m walls to contain a bounce). Real-sim over 4 total runs: 2 PASS / 2 FAIL = still ~50%. The deeper bin did NOT fix it.
- ROOT CAUSE fully characterised: the CARRY-reach (arm holding the bottle at z~0.56) reaches only ~0.02 m past the dog's jam face — NOT the ~0.11 m I estimated — so the drop point is ALWAYS right at the receptacle NEAR EDGE / -y wall, regardless of bin size. From there the HIGH drop (z0.56 -> tray top 0.28 = 0.28 m) bounces the bottle RANDOMLY: inward (lands in-tray, PASS) or over the wall onto the floor (FAIL). Same dock pose (3.87) gave both outcomes across runs. So bin geometry can't fix it — the bottle is always dropped at the edge, and the bounce is a coin-flip.
- THE FIX (identified, not yet built): a GENTLE LOW RELEASE — lower the carried bottle close to the tray top (z~0.32-0.40) BEFORE opening the gripper, so it settles where dropped instead of bouncing. The top-down place IK to that low pose is unreachable from the dock (D120), so this needs a DIRECT arm-lowering (joint move) or a relaxed-orientation IK — a focused arm-control refinement, genuinely multi-iteration manipulation engineering.
- HONEST STATE: pick-and-place GROUNDS through the moat-proven oracle (~50%, the capability + mechanism + oracle all proven) but is NOT reliable; reliability is blocked on the gentle-release arm-control, not the oracle or scene. mobile_place also needs the drop-release fallback (its top-down IK is unreachable). This is the honest ceiling reached this session on the place after ~10 real-sim iterations.
- NEXT (non-gated): (1) gentle low-release arm control (direct joint-lower or relaxed-orientation IK) -> reproducible PLACE_GROUND_PASS over N; (2) mobile_place drop-release fallback so the SKILL grounds; (3) wire resting_on_receptacle into the verify namespace; (4) bare-cli model-path pick-and-place; (5) FAR eyes-rate over N when the routing net is stable.

## D122 — PLACE improved to ~67% (taller bin walls) but NOT reliable; the gentle-low-release is IK-BLOCKED — reliable place needs dock-pose/IK debugging (honest ceiling this session) · 2026-06-30 (arch/plug-and-play, 9ba3c3e)
- Continued robustifying the D120/D121 place. Taller place_bin walls (top 0.44, the held bottle bottom ~0.48 clears them on the way in but they contain the bounce apex ~0.40): 2/3 PASS (improved from ~50% -> ~67%) but W3 still bounced OUT past a side wall to the floor. Walls help but cannot 100%-contain the high drop.
- The reliable fix — a GENTLE LOW RELEASE (descend the carried bottle to ~0.32 before opening, no bounce) — is IK-BLOCKED: PlaceTopDownSkill's descent IK at the dock returns ik_unreachable (the arm reaches the tray only at HIGH carry-z; the low-z top-down IK is unreachable from the place_bin dock pose, UNLIKE the grasp which reaches low-z at the tuned pick_table). So the place can only HIGH-DROP, which bounces ~33%.
- ROOT (now isolated): the grasp's pre-grasp top-down IK SUCCEEDS at the pick_table (low-z) but the place's pre-place top-down IK FAILS at the place_bin (low-z) — same arm, same top-down IK, same ~0.08m-past-the-jam-face geometry. The difference must be the dog's dock POSE relative to the receptacle (the +y-approached place_bin leaves the arm where the low IK can't solve, vs the clean +x grasp). Fixing WHY (dock pose so the place IK reaches low) is focused arm-IK/dock-pose debugging — the honest CEILING this session after ~14 real-sim place iterations.
- HONEST STATE: pick-and-place GROUNDS through the moat-proven oracle (~67%, capability+mechanism+oracle proven) but is NOT reliable; reliability is blocked on the low-reach place IK / dock pose, not the oracle or scene. A real result (the place WORKS, intermittently) + a precisely-isolated blocker.
- NEXT (focused fresh round): (1) DEBUG why the place top-down IK is unreachable at the dock where the grasp IK succeeds (likely approach the place receptacle FRONT-first +x like the pick_table) -> gentle low release -> reliable PLACE_GROUND_PASS over N; (2) mobile_place nav-to-close-receptacle + drop-release so the SKILL grounds; (3) wire resting_on_receptacle into the verify namespace; (4) bare-cli model-path pick-and-place; (5) FAR eyes-rate over N when the routing net is stable.

## D123 — PICK-AND-PLACE RELIABLY GROUNDS (6/6, N>=5) through the moat-proven oracle — the PLACE floor CLOSES (capability level) · 2026-06-30 (arch/plug-and-play, 38325be)
- BREAKTHROUGH after the D120-D122 ~50-67% struggle: the place reliably grounds on a BIG FLAT receptacle. ROOT of the prior flakiness: a WALLED bin let the high drop (carry z~0.56 -> top 0.28) bounce OVER the walls (~33% escape); a big FLAT table (no walls to bounce over) just CATCHES the drop and the bottle settles on the top. Extended the flat place_bin in y (top 4.20-5.00) to cover the dock's ~0.34 m drop y-variance (4.32-4.66), and lengthened the settle to 5 s (the AT-REST oracle needs the dropped bottle to come to rest). RESULT: pp1-pp6 = 6/6 PASS (the non-passes in the run set were GRASP misses — the separate ~0.8 grasp, retried — plus one pre-extension near-edge drop and one too-short-settle, both fixed). The mechanism is DETERMINISTIC (bounded drop onto a big flat top), so 6/6 is credible, not a D114-style high-variance routing streak.
- HONEST what grounded: a skill-direct pick-AND-place — grasp green from pick_table -> jam-approach DOCK over the place_bin -> RELEASE (gripper.open; the top-down place IK stays unreachable at the dock, so a DROP onto a flat receptacle is the place primitive) -> the bottle rests ON the place_bin (a DIFFERENT receptacle) at z~0.31 in-region -> the moat-proven D116 resting_on_receptacle credits it (=1, released, at rest). A genuine 0->1 place (object moved pick_table -> place_bin), graded by independent GT. The PLACE-oracle floor ("PICK-AND-PLACE grounded via placed_count") is met at the CAPABILITY level.
- NOT YET (the bare-cli model-path place, non-gated): mobile_place's top-down IK is unreachable at a free-standing receptacle + its navigate_to fails for a close target -> the SKILL needs (a) nav-to-the-receptacle-then-jam-dock + (b) a drop-release (release at the dock onto a flat receptacle when the precise IK is unreachable). Then the model can route a bare-cli pick-and-place. The CAPABILITY + oracle + mechanism are proven; this is skill productionization.
- NEXT (non-gated): (1) mobile_place nav-to-receptacle + drop-release -> bare-cli model-path pick-and-place GROUNDED; (2) wire resting_on_receptacle into the verify namespace; (3) FAR eyes-rate over N when the routing net is stable; (4) place grasp-retry hardening. The PLACE half is now CAPABILITY-complete; the FETCH eyes-rate floor remains network-gated.

## D124 — mobile_place PRODUCTIONIZED (nav 0.9 + jam-dock + drop-release) GROUNDS the place via the SKILL, but the skill's diagonal approach makes the drop position FLAKY (capability is 6/6; skill needs approach-control) · 2026-06-30 (arch/plug-and-play, c6f1210)
- Built the bare-cli place path: (1) _DEFAULT_CLEARANCE 0.55->0.90 so navigate_to stops OUTSIDE the receptacle's obstacle inflation (a closer approach pose is inside it -> nav_failed); (2) DROP-RELEASE fallback — when the precise top-down place IK is ik_unreachable at a free-standing receptacle, open the gripper to DROP the held object onto the (flat) receptacle (the proven 6/6 primitive). 18 mobile_place tests (updated propagate + new drop-release) + firewall green.
- REAL-VERIFY (skill-direct, MobilePlaceSkill on the flat place_bin): GROUNDS — mp1: nav SUCCEEDS (no nav_failed), dock, place IK unreachable -> DROP-RELEASE -> bottle rests on the table in-region, resting_on_receptacle=1, released, success=True/diag=drop_release. So the SKILL now grounds a real pick-and-place. BUT mp4 FAILED: the drop landed at x=11.70 (0.75 m OFF the table) -> resting=0. ROOT: the skill's compute_approach_pose gives a DIAGONAL approach (from the grasp pose at y=3.0 toward the receptacle at y=4.6), and the jam-dock from that diagonal lands the drop VARIABLY — unlike the manual 6/6 probe's controlled FRONT-first fixed approach.
- HONEST STATE: PLACE CAPABILITY is reliable (6/6, D123 — floor 2 met at the capability level via the moat-proven oracle). mobile_place is PRODUCTIONIZED and GROUNDS (mp1) but is NOT yet reliable through the skill — the remaining gap is APPROACH CONTROL (dock the dog FRONT-on to the receptacle so the drop lands on it), plus the separate grasp-retry-heading robustness (a failed perceive leaves the dog rotated; mp2/mp3 grasp-failed). Both non-gated manipulation control.
- NEXT (non-gated): (1) mobile_place FRONT-on approach control (face + jam square to the receptacle so the drop lands reliably) -> reproducible SKILL place over N -> bare-cli MODEL-PATH pick-and-place; (2) grasp-retry that resets the dog heading; (3) wire resting_on_receptacle into the verify namespace; (4) FAR eyes-rate over N when the routing net is stable.

## D125 — mobile_place SAFE-DROP guard makes the skill's place report HONEST (no false success / off-target scatter); residual blocker is jam-dock QUALITY (EE lands off-centre), fully characterised · 2026-06-30 (arch/plug-and-play, 9d4da0a)
- Added a SAFE-DROP guard to the D124 drop-release: read the arm's fk EE (where the held object hangs) and RELEASE only when it is within _DROP_XY_TOL (0.30 m) of the target xy (over the receptacle); else do NOT drop and return an HONEST `dock_off_receptacle` failure. This closes the D124 false-success hole (gripper.open always "succeeds", so the skill reported success=True even when the drop missed 0.75 m off-table). Reads the arm's OWN fk (the predicate the verify oracle uses), not a self-report. +2 tests; 19 mobile_place + firewall green.
- REAL-VERIFY (skill-direct): the guard fires honestly — sd1: the jam-dock left the EE at (11.00, 4.24), 0.36 m from the target centre (10.95, 4.60), so the skill did NOT drop, kept the bottle HELD, and returned dock_off_receptacle (success=False). No off-target scatter, no false success. Confirms the residual blocker: the jam-dock lands the EE VARIABLY off the receptacle centre (4.24 edge here; 11.70, 0.75 m off in mp4 D124), so the skill grounds only when the dock happens to land the EE over the receptacle.
- HONEST STATE: PLACE CAPABILITY is reliable (6/6, D123) and the bare-cli SKILL is now SAFE + HONEST (it never falsely claims a place it didn't make). What remains for a RELIABLE skill place is jam-dock QUALITY — dock the dog so the EE lands over the receptacle CENTRE (consistent lateral position + the reach-vs-centre offset: the arm reaches the near edge, not the centre). Focused dock-control manipulation, not a quick tune.
- NEXT (non-gated): (1) jam-dock quality — land the EE over the receptacle centre reliably (lateral-centred jam; or target the reachable near-edge + a receptacle-extent-aware tolerance); (2) bare-cli MODEL-PATH pick-and-place once the skill grounds reliably; (3) wire resting_on_receptacle into the verify namespace; (4) FAR eyes-rate over N when the routing net is stable.

## D126 — EYES harness far 0/5 ROOT-CAUSED + fixed (temp-HOME broke the offline model cache); far fetch now GROUNDS through the eyes (GT=GROUNDED); eyes ABSTAIN on the far frame is snapshot OCCLUSION · 2026-06-30 (arch/plug-and-play, 43c9b84)
- With the network UP (deepseek + gpt-4o both HTTP 200), the eyes far harness still scored 0/5 (all GT=RAN) while the DIRECT probe GROUNDED (verified=True, perception_grasp) on the SAME cli -p --json --native-loop. ROOT-CAUSED: tests/harness/pty_cli.run_cli_turn sets a TEMP HOME to isolate ~/.vector, but that also moves ~/.cache, so with HF_HUB_OFFLINE=1 the cached grounding-dino (~/.cache/huggingface) + EdgeTAM (Path.home()/.cache/vector_os/models) CANNOT LOAD -> the far recovery's localize returns nothing -> no_detections -> GT=RAN. (The D117 strip fix removed the segfault; this temp-HOME cache break is the SECOND, distinct eyes-harness bug.)
- FIX (43c9b84): visual_e2e.run_once sets HOME=real (os.path.expanduser('~')) in the eyes env; extra_env overrides the PTY temp HOME (a documented hook), and the native-loop turn does NOT use ~/.vector goal_templates, so it is safe + makes the eyes harness match the grounding direct probe. CONFIRMED: an eyes far trial now reads GT=GROUNDED verified=True (1/1) — the cross-room fetch grounds THROUGH the eyes harness. Verdict frame READ: the Go2+Piper at the far table with the GREEN bottle in the gripper (consistent with the GT weld).
- EYES ABSTAIN (the residual on the far eyes-VERIFIED rate, not a fetch failure): the VLM returned ABSTAIN on the far frame -> gate RED_FLAG (GT verified + vision ABSTAIN = disagreement). ROOT: the third-person verdict snapshot of the FAR table is OCCLUDED by the room's doorframe pillars (the dog is mostly hidden), so the VLM can't confirm robot_upright/body_intact -> ABSTAIN. So the far fetch GROUNDS (GT + human-read) but the eyes can't CONFIRM it from an occluded snapshot. Near (D117) had a clear frame -> eyes PASS.
- HONEST STATE: the eyes harness is now WORKING for the far fetch (grounds, GT). The far eyes-VERIFIED rate (>=0.8) has two residual sub-blockers: (a) the GT ground rate is model-routing-gated (~0.75, D114 planning variance — < 0.8); (b) the far verdict snapshot is OCCLUDED -> eyes ABSTAIN (needs a clear far-table snapshot framing). N=5 GT grounded_rate measuring now.
- NEXT (non-gated): (1) far verdict-snapshot framing — position the third-person camera to clearly view the far table (no doorframe occlusion) so the eyes CONFIRM; (2) reduce the model planning variance so the GT ground rate clears 0.8; (3) the place receptacle-config (skill reliability + verify-namespace wiring).

## D127 — FAR eyes-VERIFIED now WORKS: the far verdict-snapshot OCCLUSION fixed (side view) -> the VLM CONFIRMS (vision PASS, ACCEPT) a far ground · 2026-06-30 (arch/plug-and-play, e9f88be)
- After D126 (cache fix -> far GROUNDS through the eyes, GT), the far frames still read vision FAIL/ABSTAIN -> RED_FLAG disagreements. ROOT (rendered candidate cams on a REAL far grasp + READ them): the agent-tracking third-person snapshot of the +X-hall far table (13.95,3.0) is CROPPED by the room->hall doorframe pillars at the default room-side az 135 — even at a closer distance the LOS clips a pillar depending on the dog's final pose. A SIDE view (az 270) sits in the OPEN hall beside the dog (no doorframe between cam and robot) -> clear full-body + green-bottle-in-gripper frame.
- FIX (2 commits): (adf98fe) CamSpec distance 3.2->2.0 (fills the frame, partial help); (e9f88be) far-aware view — for agent x > _FAR_HALL_X (11.8, the hall) the verdict snapshot uses az 270 / el -18 / dist 2.4. capture.py is the acceptance harness's go2-scene-aware module (already defaults its lookat to the room), so a far-table view selector is in-scope, not a kernel hack. 47 capture/snapshot tests green.
- REAL-VERIFY: an eyes far trial now reads GT=GROUNDED verified=True **vision=PASS decision=ACCEPT disagree=False** ("GT verified AND visual witness PASS") — the FIRST clean eyes-VERIFIED far ground (no red-flag). The two prior fixes (cache D126, distance) read GROUNDED but vision FAIL/ABSTAIN on the occluded frame; the side view flips the VLM to PASS.
- HONEST STATE: the far fetch is now EYES-VERIFIED (GT ground + independent VLM PASS + ACCEPT), the eyes harness 0/5 (cache) + the disagreements (occlusion) both root-caused + fixed. Far GT grounded_rate is ~0.8+ (4/4 GROUNDED across the cache-fixed runs; routing variance D114 is the only sub-0.8 risk). N=5 eyes-verified far rate measuring (each far trial ~5-6 min: 3.9 m nav + grasp + VLM, so N=5 ~30 min — prior 0/5 + 1/2 were my too-short outer timeouts, NOT failures).
- NEXT: (1) the N=5 eyes-verified far rate (running); (2) near all-3-colours eyes-verified at the closer distance; (3) the place receptacle-config (skill reliability + verify wiring). Then both floors are demonstrated.

## D128 — FAR GREEN fetch is EYES-VERIFIED 5/5 (grounded 5/5 + eyes-accept 5/5) — the FETCH eyes-rate floor MET for green far · 2026-06-30 (arch/plug-and-play, c395f29)
- REAL-VERIFY (the goal's command, VECTOR_FETCH_FAR, deepseek routing + real gpt-4o VLM + GT oracle): N=5 far green -> grounded=5/5, eyes_accept=5/5. Every trial: GT=GROUNDED verified=True AND vision=PASS AND decision=ACCEPT, disagree=False ("GT verified AND visual witness PASS"). grounded_rate 1.0 (>=0.8 floor), N=5, ALL eyes-verified (zero red-flags). The FETCH eyes-VERIFIED floor MET for green far.
- WHAT CLOSED IT this session (two harness root-causes; the fetch capability was already grounding): D126 the eyes-harness TEMP-HOME cache break (offline grounding-dino/EdgeTAM couldn't load -> recovery localize returned nothing -> RAN; fix = real HOME) and D127 the far verdict-snapshot doorframe OCCLUSION (VLM FAILed an occluded frame; fix = a side-view cam for the +X hall). Before these the eyes far scored 0/5 purely from the two harness bugs.
- HARNESS multi-trial bug (worked around, logged honestly): measure_fetch_visual --n>1 SIGKILLs trial 2's cli at sim re-init (exit=-9; GPU/EGL not released between trials in one process). Ran N=5 as 5 INDEPENDENT single-trial processes (fresh GPU each) -> the clean 5/5. The in-process multi-trial path needs a per-trial GPU/EGL teardown fix (non-gated harness hardening; does NOT affect the result).
- REMAINING for the FULL fetch floor (all 3 colours far): GREEN done (5/5). BLUE likely fine (1/1 grounded D114) -> needs a 5x run. RED is FOV-blocked (D114: the short can's mask ~0 at the ~0.9 m grasp standoff, below the head camera vertical FOV; needs a camera-tilt / standoff fix for short objects). NEAR all-3 at the new closer cam is a quick confirm.
- NEXT: (1) blue far 5x + red short-can FOV fix -> all-3-colours far; (2) the harness per-trial GPU teardown; (3) the place receptacle-config (skill reliability + verify wiring). Green far fetch eyes-verified is DEMONSTRATED.

## D129 — BLUE far fetch EYES-VERIFIED 4/5 (0.8, meets the floor) — 2 of 3 colours now eyes-verified far · 2026-06-30 (arch/plug-and-play, f4e390d)
- REAL-VERIFY (5 independent single-trial processes, VECTOR_FETCH_FAR, "把蓝色的瓶子拿过来", deepseek routing + gpt-4o VLM): grounded=4/5, eyes_accept=4/5 (RUNs 1,2,3,5 each GT=GROUNDED + vision=PASS + ACCEPT). grounded_rate 0.8 = the >=0.8 floor. RUN 4 produced NO verdict line (its single-trial process crashed/timed out at 470 s — same harness instability family as the multi-trial SIGKILL; honestly counted NOT grounded, hence 4/5 not 5/5).
- So FAR fetch is now eyes-VERIFIED for 2 of 3 colours: GREEN 5/5 (D128) + BLUE 4/5 (>=0.8), both via the cache fix (D126) + side-view occlusion fix (D127). The eyes-verified far rate floor is MET for green and blue.
- RED remains the 3rd colour, FOV-BLOCKED (D114, unchanged): the red CAN is short; its mask ~1000 px at the 3.9 m scan but ~0 at the ~0.9 m grasp standoff — the head camera (d435, fovy 42, fixed forward mount) loses the short can up close. Fix = widen the standoff for short objects OR grasp from the distant 3D localization (the recovery already localizes it from range) instead of the close re-perceive. A real perception change (next round).
- Borderline note (red-team): blue 4/5 = exactly 0.8; the single miss was a harness no-verdict, not a routing RAN. The harness per-trial stability (single AND multi-trial) is the measurement's main noise source now — worth hardening so a clean 5/5 isn't process-luck.
- NEXT: (1) red short-can FOV fix -> all-3-colours far; (2) harness per-trial GPU/EGL teardown (stable --n>1); (3) place receptacle-config (skill reliability + verify wiring).

## D130 — RED far fetch EYES-VERIFIED 5/5; the D114 'red FOV-blocked' was STALE — ALL 3 COLOURS far now eyes-verified (FETCH far floor MET) · 2026-06-30 (arch/plug-and-play, 620ebcb)
- ROOT-CAUSE of the stale red blocker: pickable_can_red is geom size="0.033 0.04" — the SAME half-height (0.04 -> 0.08 m) as the green/blue bottles (0.028 0.04), only WIDER (r 0.033 vs 0.028), NOT shorter. The D114 "short can below the head-cam FOV at the 0.9 m standoff" reason was wrong / predates the D111+ far-recovery fixes. EMPIRICAL probe: far-seed localized the red can (13.87,3.22), front_object_mask mask_px=2829 (NOT ~0), grasp success, red lifted to z=0.56 (held).
- REAL-VERIFY (5 independent single-trial processes, VECTOR_FETCH_FAR, "把红色的罐子拿过来", deepseek + gpt-4o): grounded=5/5, eyes_accept=5/5 — every trial GT=GROUNDED + vision=PASS + ACCEPT.
- FAR fetch EYES-VERIFIED for ALL 3 COLOURS: GREEN 5/5 (D128) + BLUE 4/5 (D129) + RED 5/5 (D130), each >=0.8, N=5, via the cache fix (D126) + side-view occlusion fix (D127). The FETCH eyes-verified FAR floor (>=0.8, all 3 colours, N>=5) is MET.
- NOTE: the room has RED furniture (stools/counter) behind the far table — potential distractors — yet red grounded 5/5 (seed-localize + centrality handles them).
- REMAINING: NEAR (>=0.9, all 3 colours) not yet measured at the new closer cam — next batch. PLACE bare-cli: place-design workflow producing the plan.

## D131 — NEAR fetch eyes-verified: GREEN 5/5 + BLUE 5/5 (>=0.9 MET); NEAR RED 4/5 grounded (0.8 < 0.9) + 2/5 eyes-accept — the near-red distractor gap · 2026-06-30 (arch/plug-and-play, 10f9a74)
- REAL-VERIFY (5 independent single-trial processes each, near = no VECTOR_FETCH_FAR, deepseek + gpt-4o): GREEN grounded=5/5 eyes_accept=4/5; BLUE grounded=5/5 eyes_accept=5/5; RED grounded=4/5 (RUN 1 GT=RAN) eyes_accept=2/5 (RUNs 2,4 vision FAIL). So the NEAR floor (>=0.9, N=5) is MET for green + blue, NOT for red (grounded 0.8 < 0.9).
- WHY near-red is the weak colour (far red was 5/5, D130): in the NEAR scene the dog stands IN the room where the RED FURNITURE (bar stools + orange counter) is in frame -> (a) one grounding miss (RUN 1 RAN — grasp/routing variance among red distractors) and (b) the VLM FAILs the near red verdict frame 2/5 (multiple red objects confuse "the red can is grasped"). The FAR scene's side-view frame puts the dog in the clean hall, so far red was 5/5. The near-red fix is a clearer near red verdict frame and/or grasp-among-red-distractors robustness (next round).
- HONEST fetch floor status: FAR all-3 eyes-verified >=0.8 (D128-130) DONE; NEAR green+blue >=0.9 DONE, near red 0.8 (gap). Green/blue grounded 5/5 near; the single near-green eyes FAIL (4/5 accept) was VLM variance, frame fine.

## D132 — PLACE verify WIRING verified: resting_on_receptacle is in the bare-cli verify namespace (from live geometry) and GRADES a place 0->1 · 2026-06-30 (arch/plug-and-play, 10f9a74)
- REAL-VERIFY (probe_place_wiring, skill-direct through the ACTUAL namespace path): _place_receptacle_extent read the live place_bin region=(10.77,4.20,11.13,5.00) rest_z=0.32 (config-from-scene, Rule 11, not hardcoded); RobotWorld.build_verify_namespace registers "resting_on_receptacle" (alongside holding_object, placed_count); the bound oracle reads 0 BEFORE and 1 AFTER a real grasp->controlled-dock->drop (green rests at z=0.308, released). WIRING_PASS.
- So the bare-cli model-path PLACE is now GRADABLE: the model authors verify="resting_on_receptacle() >= 1" (mobile_place's verify_hint, D130) and the verdict GROUNDS the place via the moat-proven D106/D116 oracle reading independent GT. Spine untouched (additive world-layer wiring); firewall green.
- REMAINING for a reliable bare-cli model-path pick-AND-place: the model routing a place command + the skill's jam-dock landing the EE over the receptacle (D124/D125 — grounds when the dock is good; the safe-drop guard keeps a bad dock HONEST). NEXT: a bare-cli model-path place attempt; near-red robustness.

## D133 — Bare-cli model-path PLACE attempted: the verify wiring is LIVE in-cli, but the model can't RESOLVE the receptacle reference (navigates to a hallucinated xy) — receptacle DISCOVERY is the gap · 2026-06-30 (arch/plug-and-play, d492cea)
- REAL-VERIFY (bare cli -p --native-loop --json, near scene, "把绿色的瓶子放到架子上"): VECTOR_VERDICT lists "resting_on_receptacle" in oracle_names — the D130/D132 wiring is LIVE in the real bare-cli (the model COULD author verify="resting_on_receptacle() >= 1"). But n_steps=5, n_grounded=0, RAN: step1 mobile_pick (grasp green) -> RAN (ran_no_weld, grasp variance); steps 2-5 NAVIGATE to (6.93, 5.16) -> RAN. The model routed navigate to a HALLUCINATED location, NOT mobile_place to the place_bin (10.95,4.60), because nothing tells it WHERE "架子" (the receptacle) is.
- THE GAP (precise): receptacle DISCOVERY/REFERENCE. The place_bin is furniture, NOT a detected pickable, so it is absent from detect_objects/describe_scene/the world_model — the model has no anchor to resolve "the shelf" to a location, and no mobile_place target. The earlier complex command gave n_steps=0. So the bare-cli place needs: (1) the place receptacle exposed to the model as a referenceable object (describe_scene / world_model receptacle entry with its xy from the same live geometry _place_receptacle_extent reads) so the model routes mobile_place(target/receptacle_id); (2) grasp seat + jam-dock reliability (D124/D125). The verdict GRADING (the moat-proven oracle) is DONE.
- HONEST status: PLACE capability 6/6 (D123) + skill safe/honest (D125) + verify wiring verified 0->1 (D132) + the oracle LIVE in bare-cli (D133); the bare-cli model-path place is BLOCKED on receptacle discovery (the model can't see the receptacle) — a world-layer integration, non-gated, next round.
- NEXT: (1) expose the place receptacle to the model (describe_scene / world_model) so it routes mobile_place to it; (2) near-red robustness (>=0.9); then a bare-cli pick-AND-place grounds end-to-end.

## D134 — Bare-cli model-path PLACE pipeline COMPLETE + proven end-to-end (resolver + region-guard + settle + classifier recognition); grounds when model plans grasp+place and the drop rests · 2026-06-30 (arch/plug-and-play)
- Closed the D133 receptacle-discovery gap in FOUR additive steps (all committed, spine-respecting):
  1. mobile_place SELF-RESOLVES the scene place receptacle from the live place_bin geometry when the model gives no coords (_scene_place_geom; target_xyz/receptacle_id now optional) + description/aliases advertise "放到架子上 / place on the shelf" -> the model now ROUTES mobile_place (not a hallucinated navigate). (4efb4c4)
  2. robot.py wraps resting_on_receptacle ARG-TOLERANT (the model authors verify="resting_on_receptacle('obj','shelf')") so a grounded place isn't lost to a TypeError; the GT check is unchanged. (e8c33bf area)
  3. REGION-aware safe-drop guard: drop iff the EE is ON the receptacle REGION (the jam-dock lands the EE near the -y edge, in-region but >0.30 m from centre) instead of a fixed centre-radius -> no-arg skill-direct place now GROUNDS (resting_on_receptacle=1). (e8c33bf)
  4. mobile_place WAITS _RECEPTACLE_SETTLE=2.5 s after the drop so the verdict's AT-REST oracle sees a settled object (in-cli it dropped but the immediate check read 0). (e8c33bf)
  5. evidence_classifier + object_goal RECOGNISE resting_on_receptacle as a grounding oracle (the 2nd deliberate vcli/cognitive edit since D69 — additive, recognising the D106 CEO-APPROVED + D116 moat-proven oracle; STRICTLY STRONGER than placed_count: a floor drop does NOT ground). Firewall + 55 evidence_classifier tests green.
- REAL-VERIFY (bare cli -p --native-loop, "先拿起绿色的瓶子，然后把它放到架子上"): the model routes perception_grasp + mobile_place; the GRASP step GROUNDS (holding_object verify_result=true, GROUNDED); mobile_place self-resolves the receptacle, docks, drops (drop_release) + settles; resting_on_receptacle reads TRUE on a good-dock run (att 1) -> with the classifier recognition that is a full pick-AND-place GROUNDED. So the pipeline is PROVEN end-to-end, piece by piece.
- HONEST variance (red-team): a clean single end-to-end capture (grasp GROUNDED + place GROUNDED in ONE verdict) is gated by (a) MODEL PLANNING variance (n_steps=0 / just-grasp / grasp+place — D103/D104 known) and (b) the DROP-REST rate ~50% (the jam-dock lands the EE near the receptacle -y edge, so the drop bounces off ~half the time; att 1 rested, att 5 bounced). Both are reliability refinements (dock-depth / model-planning), NOT missing pieces.
- NEXT: dock-depth so the drop lands centre (reliable place); capture a clean full-ground verdict; near-red robustness.

## D135 — PLACE FLOOR MET: a bare-cli model-path pick-AND-place GROUNDS end-to-end (n_grounded=2/2, both via moat-proven oracles) · 2026-06-30 (arch/plug-and-play, a291b72)
- REAL-VERIFY (bare cli -p --native-loop --json, near scene, NL goal "先拿起绿色的瓶子，然后把它放到架子上"): VECTOR_VERDICT verified=TRUE, evidence=GROUNDED, n_steps=2, n_grounded=2. per_step: (0) perception_grasp verify="holding_object('pickable_bottle_green')" verify_result=true GROUNDED; (1) mobile_place verify="resting_on_receptacle('pickable_bottle_green')" verify_result=true GROUNDED diagnosis=drop_release. The model decomposed the NL into grasp+place, the grasp grounded (weld), the place self-resolved the receptacle + docked + dropped + settled, and resting_on_receptacle (the D106/D116 moat-proven oracle) graded it GROUNDED.
- RED-TEAM (honest): both grounds are GT oracles the actor cannot author — holding_object (weld) + resting_on_receptacle (xy in region AND z at rest_z+-band AND at-rest AND NOT held). Receptacle empty before, bottle held then released onto it -> a genuine held->placed 0->1, not a pre-existing object. HONEST end-to-end ground, not a false-green.
- ENABLER: OpenRouter hit its weekly credit limit (HTTP 402: requested 16384 max_tokens > 14767 affordable) -> several attempts failed NO_TRACE/n_steps=0 (NOT model-planning). Added VECTOR_MAX_TOKENS (engine.py) to lower the cap; at 8000 the request fit the credit -> this GROUNDED verdict.
- HONEST RELIABILITY: the CAPABILITY is DEMONSTRATED (a bare-cli pick-and-place grounds end-to-end, honestly). Reliability over N is gated by (a) model-planning variance and (b) ~50% drop-rest (jam-dock lands the EE near the receptacle -y edge -> ~half the drops bounce off), now also by the credit limit. The place FLOOR is MET; reliability-over-N is the next hardening.
- NEXT: dock-depth so the drop lands centre (reliable place over N); near-red robustness (>=0.9); a place eyes-frame.

## D136 — PLACE now RELIABLE over N: the -X controlled approach makes the skill-direct place rest 5/5 (was ~50% near-edge bounce) · 2026-06-30 (arch/plug-and-play, 8ae0ed7)
- ROOT of the D135 ~50% drop-rest: mobile_place used compute_approach_pose's dog-side DIAGONAL approach, landing the EE at the receptacle near -Y edge (~0.39 m off centre) -> ~half the drops bounced off the -Y edge. FIX: for the scene receptacle, approach from -X facing +X (the open face, the 6/6-verified controlled dock, D123) so the EE lands over the CENTRE.
- REAL-VERIFY (skill-direct mobile_place, no model credit, NO target -> scene resolver + -X approach): 5 independent runs ALL PASS — EE lands centre-ish (4.49-4.69 vs old near-edge 4.25), bottle settles in-region (4.34-4.74), resting_on_receptacle=1, released. place_rest=5/5 (vs ~50%). Deterministic controlled approach, red-teamed as not a streak.
- So PLACE is RELIABLE at the skill level (5/5 via the moat-proven oracle). With D135 (bare-cli model-path pick-AND-place GROUNDS) the place FLOOR is met AND reliable; the remaining model-path variance is model PLANNING + the OpenRouter credit limit, not the place skill.
- NEXT: bare-cli model-path place over N when credit refreshes; near-red >=0.9; a place eyes-frame.

## D137 — RED-TEAM of D135/D136: near-red PERCEPTION is robust (0.8 is model-routing, not perception); the place -X approach is reliable for GREEN but its nav STALLS for BLUE (not fully colour-agnostic) · 2026-06-30 (arch/plug-and-play, a7c0788)
- NEAR-RED (network-independent): skill-direct perception_grasp "红色的罐子" near -> 2/2 GROUNDED, localize picks the RED CAN (10.87,3.21) not a red-furniture distractor, mask_px~5697, grasp seats. So near-red perception+grasp is ROBUST; the model-path grounded_rate 0.8 (D131) is the MODEL ROUTING variance (D114) + the eyes VLM, NOT perception. Re-measure needs the model/VLM path (credit-gated).
- PLACE COLOUR-AGNOSTIC (red-team of D136 5/5-green): a skill-direct BLUE place FAILED diag=nav_failed — the dog (grasped blue further -Y at 2.78) STALLED at (10.25,4.29), 0.37 m short of the -X approach (10.05,4.60). So the D136 -X approach rests reliably ONLY when the nav REACHES the -X point (green 5/5); from blue's grasp pose the around-to-(-X) nav stalls -> place NOT yet fully colour-agnostic. The EE-centre drop-rest fix is real; the -X-approach NAV robustness is a new network-independent gap.
- HONEST: PLACE grounds + rests reliably for green (D135/D136); near-red perception robust. NOT met: near-red eyes-verified >=0.9 (routing+VLM, credit-gated) + place colour-agnostic reliability (-X nav stalls) + place eyes-verified over N (credit-gated). OpenRouter weekly credit EXHAUSTED -> model-path re-measures blocked until refresh.
- NEXT (network-independent): place -X-approach nav robustness (intermediate waypoint / closer clearance / non-fatal-nav+re-dock); then on credit refresh near-red routing + eyes-verified re-measures.

## D138 — L-path nav makes the place's -X approach COLOUR-AGNOSTIC (blue now PASSES); the wider RED CAN still bounces ~half the time (needs a gentler drop) · 2026-06-30 (arch/plug-and-play)
- FIX (D137 blue nav-stall): mobile_place navigates to the scene receptacle's -X approach as an L-PATH — first -X to the approach x at the dog's current y, then +Y to the approach — instead of one diagonal leg that cut the corner into the obstruction inflation. VERIFIED: the BLUE place now PASSES (L-nav leg logged, EE over receptacle, resting_on_receptacle=1) where it stalled nav_failed before.
- RED CAN (red-team, 3rd colour): 1 PASS / 1 FAIL over 2 skill-direct runs. The red object is a CAN, WIDER (r=0.033 vs bottles' 0.028); it bounces MORE on the drop — run #1 bounced +0.49 m in y to the +Y edge (4.98), resting=0; run #2 settled, resting=1. So drop-rest is reliable for the slim bottles (green 5/5, blue) but ~50% for the wider can.
- HONEST: place GROUNDS end-to-end (D135), nav colour-agnostic (L-path), drop-rest reliable for bottles; the wider RED CAN bounces ~half -> needs a GENTLER drop (partial arm descent before release to cut the ~0.27 m fall). Network-independent next chunk.
- FETCH near-red perception robust (D137); near-red eyes-verified >=0.9 + all model-path eyes re-measures CREDIT-GATED (OpenRouter weekly limit exhausted).
- NEXT (network-independent): gentle/partial-descent drop so the wider can rests reliably; then on credit refresh the eyes-verified re-measures + near-red routing.

## D139 — RED CAN place has COMPOUNDING variance (grasp + dock +X depth + wider-can bounce); the gentle partial-descent is a no-op (IK unreachable) and was reverted · 2026-06-30 (arch/plug-and-play)
- Over the red-can place runs (skill-direct): 1 PASS, then FAIL modes = (a) wider-can BOUNCE +0.49 m to the +Y edge (resting=0), (b) dock +X UNDERSHOOT (EE x=10.69 < receptacle -X edge 10.77 -> honest dock_off_receptacle), (c) GRASP never seated. So the red can is unreliable from THREE compounding variance sources, vs the slim bottles (green 5/5, blue PASS) which clear all three.
- Tried a GENTLE partial descent before release (lower the EE to cut the ~0.27 m fall) — but arm.ik_top_down is UNREACHABLE at every intermediate z (0.48..0.37) at this receptacle, so it was a NO-OP (green still passed, no "gentle descent" log). Reverted (dead code for this scene). The wider-can bounce needs a contained/lower receptacle or a softer release primitive, not a partial descent — a deeper manipulation fix.
- HONEST FINAL place status: GROUNDS end-to-end (D135), nav colour-agnostic (L-path, D138), drop-rest RELIABLE for the slim bottles (green 5/5, blue); the wider RED CAN is unreliable (grasp+dock+bounce compounding) — a deep dock-control/dynamics problem. FETCH near-red perception robust (D137); model-path eyes re-measures CREDIT-GATED (OpenRouter exhausted).
- NEXT: red-can place needs a contained receptacle (low lip) or a softer release + a deeper dock +X-depth fix; the model-path eyes re-measures (near-red routing, place over N) need the OpenRouter credit refresh.

## D140 — OpenRouter-INDEPENDENT eyes pipeline (Qwen routing + Qwen3-VL judge via DashScope) + Clash rule-mode fix + self-evolve loop · 2026-06-30 (arch/plug-and-play, 6b6582a/b0f7f68)
- WHY: OpenRouter weekly credit EXHAUSTED (402) and DeepSeek-direct api.deepseek.com is network-blocked (HTTP 000) -> the model-path eyes re-measures (near-red >=0.9, place over N) stalled. CEO chose DashScope/Qwen (阿里百炼) to bypass both.
- CLASH FIX (root of the block): clash-verge-rev was mode:GLOBAL -> all traffic through the SG node, which can't reach aliyuncs.com (HTTP 000). Switched to RULE mode (runtime via unix socket /var/tmp/verge/verge-mihomo.sock + persisted in ...clash-verge-rev/config.yaml; backup config.yaml.bak-global). Then DashScope CN endpoint -> DIRECT -> HTTP 200. CONFIRM "Rule" in the GUI so it survives app restart. (intl endpoint = 401: the QWEN key is a China-region account -> use https://dashscope.aliyuncs.com/compatible-mode/v1.)
- JUDGE (committed b0f7f68): vision_judge.py made provider-agnostic — VECTOR_JUDGE_BASE_URL / VECTOR_JUDGE_MODEL / VECTOR_JUDGE_API_KEY. qwen3-vl-plus via DashScope VERIFIED on real frames: PASS on the clear far side-view (dog upright, == gpt-4o), FAIL on the near-grasp low/pitched pose (read as "lying on its side" — STRICTER than gpt-4o; CEO: accept the stricter judge). Default unchanged.
- ROUTING (committed 6b6582a): config.py adds VECTOR_PROVIDER=qwen -> openai_compat + DashScope + QWEN_API_KEY (default qwen-plus). Pairs with the judge for a fully OpenRouter-independent pipeline.
- LOOP (CEO-requested self-evolution): ~/.claude/loops/vector-evolve.{goal,env} + REGISTRY. Short prompt: goal + each round read docs -> one chunk -> REAL-VERIFY (Qwen routing + Qwen3-VL judge, sandbox OFF) -> update docs (= self-evolve) + commit. Start: nohup ~/.claude/bin/evolving-loop.sh vector-evolve ~/Desktop/vector_os_nano ~/.claude/loops/vector-evolve.goal &
- PENDING (do FIRST on resume): the Qwen routing + Qwen3-VL judge FULL PIPELINE is not yet confirmed for one complete eyes round (cli turns kept getting interrupted). Confirm qwen-plus decomposes+tool-calls perception_grasp (bump to qwen-max if weak), then re-run measure_fetch_visual N>=5 for near-red + the place floor via the new pipeline. THEN the floors are re-measurable off OpenRouter.

## D141 — [LOOP R1] OpenRouter-independent eyes pipeline CONFIRMED end-to-end (Qwen routing + Qwen3-VL judge) · 2026-06-30 (arch/plug-and-play)
- REAL-VERIFY (default sandbox now reaches DashScope in Clash rule-mode — no sandbox-off needed): direct cli turn, VECTOR_PROVIDER=qwen QWEN_MODEL=qwen-plus VECTOR_MAX_TOKENS=8000, near green "把绿色的瓶子拿过来" -> qwen-plus DECOMPOSED + routed perception_grasp -> verified=true GROUNDED (holding_object). Captured the verdict frame; Qwen3-VL-plus judge (DashScope) -> PASS (upright/intact/scene/workspace all yes). So GT GROUNDED + eyes PASS = clean eyes-verified ACCEPT, fully off OpenRouter.
- WORKAROUND: measure_fetch_visual (the PTY harness, tests/harness/pty_cli.run_cli_turn) SIGKILLs the cli (exit=-9) right after the MPC QP init, before the turn -> AssertionError no VECTOR_VERDICT. The DIRECT cli (same env, no PTY) GROUNDS fine. So eyes re-measure uses DIRECT cli (VECTOR_SNAPSHOT_DIR set -> verdict frame) + vision_judge on the frame, instead of the PTY harness. (PTY -9 root-cause = a later chunk.)
- NEXT (R2): re-measure the floors off OpenRouter via direct-cli + Qwen3-VL judge — near-red >=0.9 (perception robust D137, was routing-variance) and the place floor.

## D142 — [LOOP R2] near-red re-measured OFF OpenRouter (Qwen routing + Qwen3-VL judge): GT 4/5, eyes 0/5 — two real gaps · 2026-06-30 (arch/plug-and-play)
- REAL-VERIFY (measure_qwen.py, 5 direct-cli trials, qwen-plus routing + qwen3-vl-plus judge, all DashScope): near "把红色的罐子拿过来" -> grounded_rate=4/5 (GT; TRIAL 3 RAN), eyes_accept=0/5.
- GAP 1 (routing): GT 4/5 = 0.8 < 0.9 — the Qwen routing has the SAME model-planning variance as deepseek (D114): 1 trial routed a non-grounding plan -> RAN. native-no-hardcoded-planner means this is a model ceiling; mitigations are skill-level (already done) + maybe a stronger routing model (qwen-max).
- GAP 2 (eyes): Qwen3-VL judge FAILs ALL 5 near frames. ROOT (looked at the frame): the near GRASP pitches the dog LOW/FORWARD to reach the pick-table; the near verdict snapshot (capture.py default az135/dist2.0) catches that pitched pose -> judge reads "robot not upright" -> FAIL. STRICTER than gpt-4o (CEO accepted). The FAR side-view (az270, x>11.8) showed the dog clearly upright -> PASS. So this hits ALL near colours, not just red.
- NEXT (R3): the near verdict frame needs a clearer angle (side-view like far, or settle the dog upright after the grasp before snapshot) so the judge can confirm an upright robot -> raises near eyes_accept. Then near GT routing reliability (qwen-max?).
