# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R714 (E237, BUILD). HEADLINE: **FIRST 3-LEG ORCHESTRATION BAR LANDED — CLEAN 3/3 GROUNDED.** The chained NL `导航到坐标(12,3.5)，然后找前面的红色的东西，最后再导航到坐标(9.5,4)` (g1, deepseek-v4-flash, bare vector-cli, launch_explore_seen=False) self-decomposed into route→perceive→ACT in ONE turn and GROUNDED verified=True 3/3: leg1 `at_position(12,3.5)` actor=CAUSED + leg2 `detection_matches_gt('红色的东西')` seg-GT GROUNDED + leg3 `at_position(9.5,4)` actor=CAUSED (a 2.55m turn-around back-hop, DISTINCT coord, DEFAULT tol0.5 ⇒ NOT the E235 no-op-retry). ACT leg = a 2nd at_position waypoint (g1 has_arm:false ⇒ grasp/place off-embodiment; place causation refuted E64). All 3 unforgeable oracles in ONE trace; verified=True requires ALL grounded same-trace; ZERO spine edit (VerdictReport composes per-sub-goal). plan·route·verify now proven at chain-DEPTH 3. Also §1b: promoted R713/E236 2-leg → confirmed (supersedes E235 refuted). Scoped teardown clean (rosm nuked 12 MINE; sibling isaac pid 103510 UNTOUCHED — Inv.8).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The loop CLIMBS the ORCHESTRATION axis (multi-leg plan·route·verify); DEPTH now proven to 3 legs. Re-check the ACTUAL gate (`pgrep mujoco|vcli` — a SIBLING isaac session is live, G-713-1) + `available` RAM/free VRAM each round; HOLD only if a sim/loop is genuinely running AND headroom absent (available<~10G or free VRAM<~3G).
last-round: R714 (E237 build, GROUNDED 3/3 provisional). First 3-leg orchestration bar LANDED clean; §1b E236 2-leg promoted confirmed.
frontier: The 3-leg orchestration bar (route→perceive→ACT) is GROUNDED clean 3/3 — chain DEPTH proven to 3. RAISE it via DATA-DEPENDENCY not more legs: (d) a chain whose leg2/leg3 target is CHOSEN by leg1's result (past mere sequencing — the real orchestration test; a 4-leg is diminishing). OR (c) does a multi-leg bar TRANSFER to go2 / into tundra (shell-fragile?). World axis (4 shells) proven-by-repetition. Deep rungs (S4/D182/D76/G-323-1) stay CEO-gated.
next:
  1. [ADJUDICATE §1b FIRST] E237 g1.orchestration.route-perceive-act GROUNDED 3/3 provisional (R714) — red-team survived + reproducible from var/evidence/R714/repl.raw.log → append `confirmed` row. This is the round's first job.
  2. [NON-GATED CLIMB — raise the orchestration bar by DATA-DEPENDENCY] frontier (d): a chain whose leg2 target is CHOSEN by leg1's result (e.g. "走到最近的红色物体那里，然后报告你面前是什么" — leg2 grounds on WHATEVER leg1 arrived at, not a hardcoded coord). This is the real orchestration test past sequencing; needs a moat-graded terminal that reads the data-dependent target. Recipe base: tools/acceptance/g1_chain3_accept.py. bg per L82. OR frontier (c): does a multi-leg bar TRANSFER to go2/tundra?
  3. [NON-GATED FLOOR — interleave ≤1 per ~3 climbs] oldest confirmed STALE rows: place.nl-plain-colour (age~416, vlm-judge — WATCH gemma4:e4b abstain) / verify.eyes-place-judge-fires (age~416) / place.nl-new-world-courtyard+warehouse (age~412). BROAD pgrep (incl. sibling isaac) + `available`/VRAM FIRST.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-713-1 multi-session (sibling isaac-go2w loop live — coordinate scope?) · G-323-1 hard-channel quartet (~120 rounds) · G-383-1 has_arm · G-354-1 preflight-counter · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-713-1 MULTI-SESSION (R713): a concurrent SIBLING session runs an isaac-go2w BYO acceptance (pid 103510, ~38min live this round) in THIS repo/host. Not double-driving MY loop (preflight lock held); rosm scoped-teardown nuked ONLY my tracked PIDs, sibling survived (Inv.8 verified). Two sims can share the 64G host — headroom stayed present (37G avail, 7.3G VRAM free). Per CLAUDE.md multi-session: claim scope in COORDINATION.md before horizontal-file edits. OWNER: is this an intended parallel session? Refs Inv.5/Inv.8.
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen arreared; deepseek + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R710
