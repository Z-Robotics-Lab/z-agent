# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R670 (E193, REVIEW). HEADLINE: **§7 review — substrate ROT-FREE, the R664-R669 finiteness vein is SATURATED, loop back to the R312-R380 HARD-EXTERNAL plateau.** First review since R390 (the R394→R661 weekly-limit outage jumped the R-counter; E-numbers stayed contiguous). SKEPTIC pass on the real bare-cli face was BLOCKED — the sibling isaac go2w warehouse_nav is ACTIVELY cycling (pids 3969261/288/296/555/561/568 + a fresh e2e run 3992336/343), holds the ONE sim slot (Inv-5 WAIT, NEVER-KILL-INFRA); sim-free audit stands in. AUDITS: (1) all 3 docs/WIRING.md cards prose-verified line-by-line vs code (native-loop `_MAX_NATIVE_TURNS=24`/`_MAX_TURNS_WITHOUT_VERIFY=12`/`_UNPRODUCTIVE_NUDGE_AT=6` exact, `_PREDICATE_ORACLES`=the 8 listed, embodiments has_arm ENRICHMENT runtime-OR-declared split, verify-spine `VERDICT_SENTINEL`) ⇒ stamps refreshed to 437e09d (were R368/R389, all >25 rounds stale). (2) gate/token audit CLEAN — exactly ONE crossed gate ever (owner G-183-12, R183); the R665 `git log --grep` hit is audit-TEXT ("No GATE-APPROVED token needed"), not a crossing. (3) Casebook 12/15 (no fold), BOARD.md regen'd (stale supervisor-append ages — the E102/E145 pattern). AMBITION-CRITIC: E187(genuine LATENT loader find)→E188-E191(4 real sink boundaries)→E192(defense-in-depth, closes NO open hole) is the same genuine-find→consolidation→no-hole trajectory the lidar-offset mirror vein showed at E151/E158 ⇒ vein SATURATED; the R340/E130 call STANDS — escalate + longer PARK cadence, NOT hourly HOLD/sliver commits.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R670 review confirmed the offline substrate is rot-free AND the finiteness vein saturated; both levers still SHUT).
last-round: R670 (E193 REVIEW: 81st sim-block. Substrate ROT-FREE — 3 WIRING cards re-verified accurate+stamped 437e09d; gate/token clean; BOARD regen; Casebook 12/15. Real-face skeptic BLOCKED by an actively-cycling sibling isaac sim. Finiteness vein SATURATED ⇒ HARD-EXTERNAL plateau re-affirmed; plateau row banked).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED + REVIEW-re-audited R670. SATURATED veins: name-list, hardcoded-SET (E177), hardcoded-NAME (E178), numeric-threshold (E180), tool-registry-overwrite (E181), vocab-derivation (E182), persona-swallow (E183), skill-registry-swallow (E184), silent-swallow grep-empty (E185), lidar-offset mirror (E151/E158), AND external-input FINITENESS at both ends — five SINKS (E187-E191) + the plan-JSON SOURCE (E192). R312-R669 plateau REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. Highest-leverage unblock = owner acts on G-323-1 (queued 87 rounds).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — BROAD `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac|warehouse_nav'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot [narrow `isaac` alone MISSES the `python3`-named warehouse_nav; the sibling RESTARTS so PIDs rotate — match by CMD not PID], inbox+gates.jsonl for owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis (tools/verify/vocab/persona/skill-registry/loader/arm/base-vel/nav-goal/blocking-locomotion/plan-JSON-source finiteness all scanned), (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144; authoritative owed=0), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145 — just `python3 loop/board.py`), (g) re-force any SATURATED vein, (h) re-mine a 6th finiteness SINK — root-gated at source (E192) AND every known sink (E187-E191). Before mining ANY "new" gap: grep ledger + docs/LESSONS.md FIRST — if E#-recorded/gated/benign, it is NOT a find.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check BROAD `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all; NEVER `echo "$EMPTY" >>` (blank line ⇒ check.sh RED). Caps: line ≤1024 BYTES; each string field ≤280 CHARS (R670 E193 trimmed result 3× to fit). status ∈ {confirmed,inconclusive,plateau,refuted} for experiments; key `e`. board: `python3 loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R670 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check BROAD `pgrep` FIRST (match by CMD, sibling PIDs rotate); WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet; ALSO subsumes the moat _PREDICATE_ORACLES BYO-bare-predicate fix, E116/E118). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] the external-input-finiteness vein is CLOSED at both source (E192) AND every known sink (E187-E191) AND now REVIEW-affirmed SATURATED — do NOT re-mine a 6th sink, do NOT manufacture an audit sliver. Checked-benign (do NOT re-chase): navigation.py `wait_until_near(tolerance)` fail-SAFE; perception.py `measure_distance(angle_rad)` fail-safe (`int(round(nan/inc))` RAISES); `openai_compat:446` tool-arg `json.loads` = a 2nd untrusted source but sinks gated ⇒ fail-safe. Genuinely-open only if a NEW numeric API with NO downstream gate appears (e.g. a gripper set-width/force). Else → HOLD (one plateau experiments row); prefer a clean HOLD to a manufactured sliver (E130/E193).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367; fresh-axis-negative E180/E181/E182, fresh-axis-FIXED E183/E184/E187/E188/E189/E190/E191/E192): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. Its `_PREDICATE_ORACLES` edit ALSO closes the moat BYO-bare-predicate gap (E116/E118). OWNER CALL OWED — queued 87 rounds (R318..R670). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - GATE-LEDGER NOTE (R670 audit): gates.jsonl still lists the original G-183-1..11,13 ELP design gates + G-315-1 as `queued`; these predate the crossed G-183-12 MVP approval (which noted "DESIGN.md decisions 1-4 approved") and are likely stale, but reconciling/dropping design gates is an OWNER call — flagged, NOT self-dropped.
  - SELF-APPROVAL AUDIT (R670 §7): `git log --grep='GATE-APPROVED\|CEO-APPROVED'` since R390 = ZERO real crossings (hits are review/build audit-TEXT; gates.jsonl `state:crossed` count=1 = owner G-183-12 R183). R670 = a REVIEW round: no code/interface/spine/manifest edit (only WIRING stamps, LESSONS line, ledger rows, STATUS, BOARD regen) ⇒ NOT a CEO gate, no token.
last_review: R670
