# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R715 (E238, BUILD). HEADLINE: **FIRST DATA-DEPENDENT CONDITIONAL ORCHESTRATION BAR LANDED — CLEAN 3/3 GROUNDED, branch=f(detection).** The chained NL `导航到坐标(12,3.5)，看看前面有没有红色的东西；如果没有红色物体就导航到坐标(8,3)，如果看到了红色物体就导航到坐标(9.5,4)` (g1, deepseek-v4-flash, bare vector-cli, launch_explore=False) GROUNDED verified=True 3/3: leg1 `at_position(12,3.5)` CAUSED + leg2 `detection_matches_gt('红色的东西')` seg-GT + leg3 `at_position(9.5,4)` CAUSED. THE NEW THING: leg3's coord is CHOSEN by leg2's detection RESULT, not authored — red IS present ⇒ planner took the CORRECT branch (9.5,4), which was listed SECOND to DEFEAT the always-take-first confound; the WRONG no-red branch (8,3) was NEVER commanded/grounded (every navigate cmd in the raw log is (12,3.5) or (9.5,4)) ⇒ branch=f(detection), unforgeable. Orchestration proven PAST mere sequencing (the real test); ZERO spine edit (moat composes the data-dependent sub-goal tree). Also §1b: promoted R714/E237 3-leg route→perceive→ACT → confirmed. Scoped teardown clean (12 MINE nuked; sibling isaac pid 103510 UNTOUCHED — Inv.8).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The loop CLIMBS the ORCHESTRATION axis; DEPTH proven to 3 legs (E237), now DATA-DEPENDENCY proven (E238, leg3=f(leg2)). Re-check the ACTUAL gate (`pgrep mujoco|vcli` — a SIBLING isaac session is live, G-713-1) + `available` RAM/free VRAM each round; HOLD only if a sim/loop is genuinely running AND headroom absent (available<~10G or free VRAM<~3G).
last-round: R715 (E238 build, GROUNDED 3/3 provisional). First data-dependent conditional orchestration bar LANDED clean; §1b E237 3-leg promoted confirmed.
frontier: The DATA-DEPENDENT conditional bar (leg3=f(leg2 detection)) is GROUNDED clean 3/3, branch NOT blind-first. RAISE it: (d2) the NEGATIVE falsifier — a red-ABSENT pose (leg1 target facing away from red) ⇒ planner MUST pick (8,3); GROUNDED there closes the branch-SYMMETRY proof (defeats "always-picks-red-branch" residual). OR (c) does a multi-leg/conditional bar TRANSFER to go2 / tundra (shell-fragile?). A 4-leg chain is diminishing (depth proven). Deep rungs (S4/D182/D76/G-323-1) stay CEO-gated.
next:
  1. [ADJUDICATE §1b FIRST] E238 g1.orchestration.conditional-data-dependent GROUNDED 3/3 provisional (R715) — red-team survived + reproducible from var/evidence/R715/repl.raw.log (only (12,3.5)&(9.5,4) nav cmds, (8,3) absent) → append `confirmed` row. This is the round's first job.
  2. [NON-GATED CLIMB — close the branch-symmetry proof] frontier (d2): the NEGATIVE falsifier — steer leg1 to a pose where red is OUT of head-cam view (e.g. face away / a red-free coord), so detection FAILS and the planner MUST take the (8,3) branch; GROUNDED at (8,3) + detection-negative proves the branch is TRULY data-dependent both ways, not "always-red-branch". Recipe base: tools/acceptance/g1_cond3_accept.py (swap leg1 coord + assert wrong-branch=(9.5,4) not taken). bg per L82. OR frontier (c): does a multi-leg/conditional bar TRANSFER to go2/tundra?
  3. [NON-GATED FLOOR — interleave ≤1 per ~3 climbs] oldest confirmed STALE rows: place.nl-plain-colour (age~417, vlm-judge — WATCH gemma4:e4b abstain) / verify.eyes-place-judge-fires (age~417) / place.nl-new-world-courtyard+warehouse (age~413). BROAD pgrep (incl. sibling isaac) + `available`/VRAM FIRST.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-713-1 multi-session (sibling isaac-go2w loop live — coordinate scope?) · G-323-1 hard-channel quartet (~121 rounds) · G-383-1 has_arm · G-354-1 preflight-counter · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-713-1 MULTI-SESSION (R713): a concurrent SIBLING session runs an isaac-go2w BYO acceptance (pid 103510, live 3 rounds now) in THIS repo/host. Not double-driving MY loop (preflight lock held); rosm scoped-teardown nuked ONLY my tracked PIDs, sibling survived (Inv.8 verified R715). Two sims share the 64G host — headroom stayed present (38G avail, 7.4G VRAM free). Per CLAUDE.md multi-session: claim scope in COORDINATION.md before horizontal-file edits. OWNER: is this an intended parallel session? Refs Inv.5/Inv.8.
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen arreared; deepseek + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R710
