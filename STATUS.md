# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R297 (E88, build/verify) — §1c adjudicated the E87 deadline-clamp (HELD: round started CLEAN, no quarantine/orphan/inflight), then ran the OWED next#3 real MODE=place eyes=vlm-judge e2e: the vision-judge now FIRES on the place verdict frame (R273/E70 reorder verified on the real face).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~50r unanswered; R290 oldest-untested-bar backlog EXHAUSTED; loop hardening the verify-pillar + non-gated debug while the gates stand).
last-round: R297 (E88, build/verify). §0/§1a: BOOTSTRAP clean (E87 clamp HELD — no re-quarantine, inflight empty); adopted the R296 rounds append + regen BOARD. §1b: latest acceptance provisionals already adjudicated (R291 nav->R292 confirmed, R292 perc->R294 confirmed). BUILD/VERIFY: ran next#3 (bare vcli + NL, deepseek-v4-flash, MODE=place, eyes=vlm-judge gemma4:e4b).
  RESULT (E88, provisional): judge_witness.log now records {tag:place,witness:FAIL} + driver 'vlm-judge place: witness=FAIL' — the judge FIRES on the place verdict frame; R272's EMPTY-place-witness gap is CLOSED by R273's drain-before-_eyes_frame reorder. Frame=settled post-verdict; FAIL is DISCRIMINATING (robot_present_upright=no on a genuinely TOPPLED go2, bottle off-bin) — not a rubber-stamp. Place itself RAN verified=False (1/2; brain post-place wander describe->navigate, E60 family) — a SEPARATE residual, NOT the headline. Red-team survived (fresh dir, live driver line, eyes_place.png==verdict_*.png).
frontier: R290 backlog EXHAUSTED. Verify-pillar: eyes=vlm-judge now fires on BOTH fetch (E69/E71) AND place (E88) verdict frames — the R272 place gap is closed. RESIDUAL non-gated: (a) HOUSE place flaked this run (go2 toppled during mobile_place) — N=1, does not refute place.nl-plain-colour confirmed 2/2, but a place-locomotion-stability data point worth a repro; (b) ord-posinv SCENE_SWAP isolated debug. NEW capability still needs owner S4/D182. eyes verdict-render off-centers workspace -> gate G-282-1.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via the .env-select env vars (default qwen = Arrearage -> PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): SCENE_CLUTTER=1, ROOM_TEMPLATE={warehouse|courtyard}, SCENE_SWAP=1.
  HARNESS: eyes vlm-judge AUTO-routes to local ollama gemma4:e4b when up (resolve_judge_env overrides the stale supervisor VECTOR_JUDGE_MODEL). MODE=place opens a FRESH session (bottle on table, not pre-held). RUN sim BACKGROUND under `systemd-run --user --scope -p MemoryMax=24G`; pass ROUND_DEADLINE_EPOCH so the E87 clamp is active; read the evidence log (Bash-tool 120s timeout kills foreground polls). NEVER pkill mujoco -> scripts/sim-teardown.
  LEDGER HAZARD (E27/E81/E87): append ONE preformatted line via `>>`; NEVER load-all->dumps->write-all (reorders -> quarantine). Row caps: <=1KB JSON bytes, each string <=280 PYTHON len (INCL redteam/result — assert fires => trim & re-run). experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); the supervisor's rounds.jsonl append shifts BOARD ages so the NEXT round MUST regen it.
next:
  1. [§1b ADJUDICATE] Promote the E88 verify.eyes-place-judge-fires provisional -> confirmed IF it reproduces across this boundary (re-run MODE=place eyes=vlm-judge; the judge must FIRE on place again — place-ground need NOT succeed for the wiring claim). If the witness log is EMPTY on place again, RE-OPEN E70.
  2. [NON-gated verify / repro] Re-run HOUSE MODE=place: does place GROUND (N>=1) or did R297's toppled-go2 flake reproduce? A repeated topple = a place-locomotion-stability finding (localize with VECTOR_PLACE_DIAG / machinery test); a clean ground = R297 was a one-off brain-wander flake (E60 family, guard blocks re-grasp only).
  3. [NON-gated DEBUG, bounded] Root-cause the ISOLATED ord-posinv SCENE_SWAP regression (Hypothesis Loop, DEBUG.md): grasp-execution vs target-selection under swap vs the 8x-GROUNDED no-swap fetches.
  4. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL->object grounder). CEO-gated ~50r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#4.
  - SPINE (D182): world-owned NL->object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#4.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R280..R297 CLEAN. R297 touched NO spine path (tools/acceptance/ is outside the SPINE regex vector_os_nano/acceptance/; NO code change this round — verify-only run + ledger append).
last_review: R290
