# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R327 (E118, BUILD/test-only). HEADLINE: the plug-and-play VERIFY proof is lifted from the CLASSIFIER to the RUNTIME — E116 proved `classify_verify_expr` grades a BYO expr but fed it a HAND-BUILT oracle set; E118 drives the WHOLE path end-to-end for a BYO world wired into a REAL engine: `verify_oracle_names(agent,engine)`→`_build_verifier_namespace`→`_merge_world_verify_namespace`→`build_verify_namespace` DELIVERS the BYO predicate name, `classify_step_evidence`=GROUNDED, and the done-gate `evidence_passed`=True — all offline, zero kernel edit. Moat preserved: the name is ABSENT without `engine._world` (non-vacuous — the merge delivers it, not a default), the actor-causation downgrade governs a BYO predicate (UNCAUSED→RAN with `verify_result` held True — causation flips it, not the sentinel), and `engine=None` fails closed→RAN. Guard `test_plug_and_play_boundary.py` 10→14, no spine/kernel edit (test-only; spine excludes tests/).
  §1 ORIENT: inflight absent; no WIP; no experiments provisionals (E116/E117 confirmed). 67 acceptance provisionals all check-exempt → check.sh GREEN. inbox: no unanswered decisions. SIM-BLOCKED A 9TH TIME: a sibling loop holds the one sim slot (isaac_sim launch PID 3569461 + localPlanner 3569488 + laser_mapping 3569496 + go2w warehouse_nav 3569767, live) → Inv-5 + NEVER-KILL-INFRA: NOT touched, launched NO competing sim ⇒ next#1 (real-face stale-row re-run) stays blocked, as R320-R326.
  §4 REAL-VERIFY: offline, sim-free (like E116). Acceptance = TDD-green 14/14 on the REAL engine seam + done-gate, red-teamed in-round (non-vacuous: name absent w/o world wired; verdict flips ONLY on causation, verify_result held True). eyes: self-read. experiments row only (E118, confirmed), NO acceptance row (no sim face). NO spine path touched — only tests/vcli/test_plug_and_play_boundary.py + 1 LESSONS line.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE-blocked: every acceptance-face advance is CEO-gated — G-323-1 (the doctor-guarded quartet landing) · S4 · D182 · D76)
last-round: R327 (E118 BUILD: plug-and-play Verify proven END-TO-END at the runtime done-gate for a BYO world — real engine delivery seam + evidence_passed, moat-preserving, offline 14/14; test-only, no spine. Sim-blocked 9th).
frontier: the offline plug-and-play Verify CONTRACT axis is now FULLY CLOSED — proven at the world seam (present, E109-E115 quartet), the classifier (graded, E116), the adoption docs (E117), AND now the RUNTIME delivery+done-gate end-to-end (E118). The 15-round acceptance-face plateau (R312-R327) is REAL and STRUCTURAL: the face is GATE-blocked, not idea-blocked. ONE honest lever left: the OWNER approves G-323-1 (import + 4 predicate registrations + `_PREDICATE_ORACLES` entries + rubric — one low-risk diff, doctor-confirmed no interface change). A 5th oracle / 2nd guard / 2nd adjudication guard = treadmill — do NOT. Verify-contract docs AND runtime proof are done — do NOT re-open.
watch: E118 delivery finding — non-spine, no sim:
  The FULL runtime verdict path for a BYO verify predicate is now guarded end-to-end: `verify_oracle_names(agent,engine)` (trace_store.py:79, engine=None→frozenset() fail-closed) → `engine._build_verifier_namespace` (engine.py:891) → `_merge_world_verify_namespace` (engine.py:966, `ns.update(world_ns)`) → `world.build_verify_namespace`; then `classify_step_evidence`/`evidence_passed`. Guard: `./scripts/run-tests tests/vcli/test_plug_and_play_boundary.py` (14). The BYO world + engine wiring lives ENTIRELY in the test (`_AcmeArmWorld`, `_engine_with_byo_world`).
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. `hard_channels.py {upright|height|near|facing}`; `make_verify_namespace(agent)` → the 4 GT-reading fail-safe-False predicates. Run doctor BEFORE landing G-323-1.
  LANDING SHAPE (gate round): merge `make_verify_namespace` into `RobotWorld.build_verify_namespace` (robot.py:116 go2-base `ns.update`, beside at_position/facing), add each name to `evidence_classifier._PREDICATE_ORACLES` (bare-idiom grounding, per E116) + `verify_signatures()` to vocab, reword rubric — WIRING verify-spine §. All on EXISTING base API (get_position/get_heading/get_odometry quat); identity stays VLM/D182.
  SIM-SAFETY (Inv-5, R318/R320-R327): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (NOT just `mujoco`); WAIT if live, NEVER teardown a sim you did not launch.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (E118 row landed 1003 B after 4 trims), each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board: `.venv/bin/python loop/board.py`.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R327 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (confirm READY-TO-LAND), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` (bare-idiom grounding, E116) + rubric (one low-risk diff, no interface change). Consolidates G-318-1 · relational-near · G-322-1. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] Every non-gated axis is now worked out (offline hard-channels E109-E115; plug-and-play Verify: classifier E116, docs E117, runtime end-to-end E118). Do NOT build a 5th oracle, a 2nd guard, a 2nd adjudication guard, NOR re-document/re-prove E116-E118 (treadmill). Honestly hold and push the owner to unblock G-323-1, OR pick a genuinely-new non-spine ergonomic ONLY if it advances the North Star. Do NOT reopen any eyes-VLM pose/heading probe (refuted E105-E108).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing, DOCTOR-GUARDED E115, bare-idiom rationale E116, adoption-doc'd E117, runtime-proven-for-BYO E118) <- next#2: merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114-packaged 27/27 + cores 72/72; landing precondition E115-guarded) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind to the EXISTING base API (get_position/get_heading/get_odometry quat) ⇒ NO interface change (doctor confirms on the live tree); the `_PREDICATE_ORACLES` edit is REQUIRED for the bare-predicate idiom (E116), not an interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. One low-risk diff. Owner call owed. SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1 (kept for history). (R323/E114..R327/E118)
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - META (world-declared predicate-oracles): a proper seam letting a BYO world declare its bare-predicate oracles (so bare `pred()` grounds without editing `_PREDICATE_ORACLES`) is a SPINE/interface gate — NOT needed for the contract (E116: `pred()==True` is the zero-edit path, doc'd E117, runtime-proven E118), queued as a nice-to-have. (R325/E116)
  - G-315-1 SPINE (VLM pose rubric): MOOT — pose belongs to the hard-channel quartet (G-323-1). (R315/E106..R320/E111)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py; CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — the IDENTITY half the quartet's geometry channels leave to the VLM (E46, E112/E113). <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R327 CLEAN. R327 (build/test-only) touched NO spine path (extended tests/vcli/test_plug_and_play_boundary.py 10→14; refined 1 LESSONS line; appended experiments E118; STATUS; NO acceptance/rubric/interface/classifier/_PREDICATE_ORACLES/engine edit).
last_review: R320
