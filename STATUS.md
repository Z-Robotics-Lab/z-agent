# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R322 (E113, BUILD). HEADLINE: the deterministic hard-channel family gains a FOURTH member — `tools/acceptance/heading_facing.py`, a HEADING oracle for "go to / face the target" (VLN, E14/D178). The trio (pose orient E109 + pose height E110 + relational near E112) reads own-pose + object-to-object proximity, but NONE reads whether a robot is TURNED TOWARD a target. This grades that from GT root yaw + a resolved target position (unforgeable, Inv-1): `yaw_from_quat` (homogeneous atan2, scale-invariant), `bearing_to`=atan2(dy,dx), `heading_error` wrapped [-pi,pi], `facing_margin`=tol-|err| (signed deg, pose_height idiom), `classify` FACING/AWAY. Unit 30/30, sim-free proof 6/6 incl. the VLM-blind case a frame can't read: target directly BEHIND = AWAY. Distance-orthogonal ⇒ COMPOSES with place_near (approached AND oriented), never duplicates it. Identity (which object is the target) stays the VLM/D182 job.
  §1 ORIENT: inflight absent; no WIP; inbox clean (G-183-12 answered). No provisional experiments rows (enum has none). §1a floor: supervisor appended the R321 rounds row → BOARD regen (ages +1). 67 acceptance provisionals all check-exempt → check.sh GREEN. SIM-BLOCKED AGAIN: a sibling loop holds the one sim slot (localPlanner PID 3535348 + laser_mapping PID 3535356 LIVE at orient) → Inv-5 + NEVER-KILL-INFRA: NOT touched, launched NO competing sim ⇒ next#1 (real-face stale-row re-run) stays sim-blocked, as R320/R321.
  §4 REAL-VERIFY: pure-math non-spine oracle ⇒ acceptance is the deterministic proof on realistic GT (as E109/E110/E112 were verified sim-free). eyes: self-read. `var/evidence/R322/prove_heading_facing.py` 6/6 correct (faces-ahead FACING, overshot-target-behind AWAY, g1-turned-toward-left FACING, 30deg-off-tight-cone AWAY, target-directly-BEHIND AWAY, same-behind-at-50m AWAY = distance-orthogonal). CLI smoke both --yaw and --quat paths; coincident robot/target fails LOUD. REDTEAM survives as-if-not-mine: inputs=GT root yaw + target pos (unforgeable Inv-1, the same source pose_upright reads); margin reported not knife-edge; tol caller-owned in (0,180]; OFFLINE oracle NOT bare-cli face ⇒ NO acceptance row (experiments only); grades HEADING half, identity stays VLM/D182. NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE-blocked: every acceptance-face advance is CEO-gated — G-318-1 (pose pair) · relational near(a,b) (E112) · G-322-1 (facing, E113) · S4 · D182 · D76)
last-round: R322 (E113 BUILD: 4th deterministic hard channel heading_facing — yaw-vs-bearing facing oracle, TDD 30/30 + sim-free proof 6/6; the VERIFY half of D178 VLN; identity stays VLM. Sim-blocked again by a sibling; no spine touched).
frontier: the DETERMINISTIC hard-channel family is now a QUARTET — pose orient (E109) + pose height (E110) + relational near (E112) + heading facing (E113). Each grades from unforgeable GT, each SHELVED behind a spine-landing CEO gate. The non-gated hard-channel frontier is now NEARLY EXHAUSTED: own-pose (orient+height), object-proximity, and heading-to-target are all covered offline. 10-round plateau (R312-R322) on the acceptance face is REAL: the face is GATE-blocked, not idea-blocked. The honest lever is the OWNER unblocking ONE gate to convert a proven channel into an acceptance-face verify-channel — the offline-oracle well is running dry.
watch: hard-channel QUARTET (non-spine, pure math, no sim):
  pose: `python tools/acceptance/pose_upright.py <w> <x> <y> <z> [--thresh 0.5]` (STANDING/FALLEN); `python tools/acceptance/pose_height.py <root_z> [--stance-z 0.35] [--tol 0.12]` (SUNK/NOMINAL/AIRBORNE).
  place: `python tools/acceptance/place_near.py <ax> <ay> [az] --to <bx> <by> [bz] [--radius 0.5] [--full]` (NEAR/FAR).
  heading: `python tools/acceptance/heading_facing.py --at <rx> <ry> --to <tx> <ty> (--yaw <rad>|--quat <w> <x> <y> <z>) [--tol-deg 45]` (FACING/AWAY).
  Units: test_pose_upright.py(11)+test_pose_height.py(13)+test_place_near.py(18)+test_heading_facing.py(30) via `./scripts/run-tests`. Sim-free proofs: var/evidence/{R319/prove_pose_pair,R321/prove_place_near,R322/prove_heading_facing}.py.
  SIM-SAFETY (Inv-5, R318/R320/R321/R322): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco'` (NOT just `mujoco`); WAIT if live, NEVER teardown a sim you did not launch. R320/R321/R322 were all sim-blocked exactly this way.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; scripts/sim-teardown after. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (E113 trimmed to 1018B). experiments key `e`; experiments status ∈ {confirmed,inconclusive,plateau,refuted} (NO provisional); acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320/R321/R322 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a proven channel into the acceptance face] the QUARTET is ready to wire: G-318-1 (pose pair orient+height) · relational near(a,b) (E112 place_near) · G-322-1 (facing E113 heading_facing). All edit acceptance/ + rubric = honest-verify-spine/moat. Owner call owed. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] the non-gated hard-channel frontier is NEARLY EXHAUSTED (own-pose + proximity + heading all done). Remaining candidates are thin: a CONTACT/stability channel (foot-contact count, tip-over margin) or a VELOCITY/settled channel (is the body at rest). But FIRST weigh honestly vs pushing the owner to unblock a gate — the offline well is running dry. Do NOT run another eyes-VLM probe (pose refuted E105-E108; VLM heading unreliable by foreshortening, can't see behind).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-322-1 SPINE/MOAT (facing verify-channel): E113 proves `heading_facing.py` grades "face/approach the target" from GT root yaw + target pos (unforgeable, Inv-1), TDD 30/30 + proof 6/6, distance-orthogonal (composes with place_near). The deterministic VERIFY half of the D178 near_object VLN need (G-183-3); identity stays VLM/D182. Landing edits acceptance/ + rubric = honest-verify-spine semantics. Owner call owed. <- next#2. (R322/E113)
  - G-318-1 SPINE/MOAT (pose verify-channel): E109+E110 prove a deterministic pose PAIR (pose_upright + pose_height) reads go2 pose 4/4 across all R316 topologies where every local VLM failed (R320/E111 re-verified 4/4 sim-free). Landing edits acceptance/ + rubric (ADR-002 moat). Per Invariant 1 STRICTLY better than a VLM. Owner call owed. <- next#2. (R318/E109, R319/E110, R320/E111)
  - RELATIONAL near(a,b) SPINE/MOAT (proximity verify-channel): E112 proves `place_near.py` grades "put A NEAR B" geometry from two GT positions (unforgeable, Inv-1), TDD 18/18 + proof 5/5, non-redundant vs D106. Landing edits acceptance/ + rubric; identity half stays VLM/D182. Owner call owed. <- next#2. (R321/E112)
  - G-315-1 SPINE (VLM pose rubric): MOOT FOR POSE — pose belongs to the hard-channel PAIR (G-318-1). Superseded-in-spirit by G-318-1. (R315/E106..R320/E111)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); the IDENTITY half E112/E113's geometry channels leave to the VLM. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R322 CLEAN. R322 (build) touched NO spine path (added tools/acceptance/heading_facing.py + tests/unit/test_heading_facing.py, both non-spine pure-math; appended experiments E113 + gates G-322-1 queued; extended 1 LESSONS saga line; regen BOARD; STATUS; gitignored var/evidence/R322; NO acceptance/ or rubric edit).
last_review: R320
