# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R374 (E163, BUILD). HEADLINE: generalized the E162 dead-branch class from a hand-hunt to a MECHANICAL package-wide scan — `ruff check vector_os_nano tools loop scripts --select F821` (undefined names). Under `from __future__ import annotations` an undefined name in a STRINGIZED annotation is never evaluated at runtime so it survives every green suite, yet raises `NameError` under `typing.get_type_hints` — the exact E162 shape (dead/untested path). Found ONE REAL dormant defect OUTSIDE all saturated veins: `vcli/intent_router.py::should_use_vgg(skill_registry: Any=None)` annotates `Any` but the module never imports it — `get_type_hints(IntentRouter.should_use_vgg)` RAISED `NameError: name 'Any' is not defined` (proven pre-fix). FIX: `from typing import Any` (behavior-neutral) + a regression guard that resolves `get_type_hints` for EVERY callable in the module. RED-teamed: pre-fix RAISED / post-fix RESOLVES; guard drift = inject a bogus annotation ⇒ RED, restore ⇒ GREEN (detects re-introduction, not just this instance); 52/52 intent tests green; ruff F821 now clean pkg-wide (only a benign F811 shutil re-import in cli.py remains — not a defect). `intent_router.py` is NOT a spine path ⇒ no gate, no interface change. Both levers still SHUT (52nd sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; a real non-gated find landed — a dormant undefined-name annotation defect in intent_router, caught by a mechanical F821 scan + machine-guarded; TDD red→green, no manufactured sliver).
last-round: R373 (E162 BUILD: 5th home-pose fallback + DEAD config.py missing-file branch fixed; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2 (4)+g1 (E147-E151/E158), piper-proxy↔driver const block (12, E160), verdict contracts (E152/E153), Inv-4 import allowlist (E154), causation-baseline frozen (E155), embodiment-config schema (E156), arm-playground catalog↔MJCF (E157), SO-101 home-pose vein FULLY CLOSED (E161/E162), undefined-annotation class (E162→E163) now MECHANICALLY scanned (ruff F821 clean pkg-wide + get_type_hints guard on intent_router). Spine + config-schema frozen + mirror coverage broad. The R312-R374 acceptance-face plateau (62 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R374). Substrate-hardening finds (E152-E163) are increasingly marginal — a domain expert would say the loop polishes invariant-guards while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill. (E162's dead-fallback + E163's dormant undefined-name are the substrate finds that were live DEFECTS, not just unguarded mirrors — and F821 is now mechanically clean, so this vein is spent.)
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline capability axis, (b) re-audit an already-audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g) re-force the lidar-offset mirror — SATURATED all 4 go2 + g1 + piper proxy (E151/E158), (h) re-force Inv-4 import guard (E154), (i) re-force a SPINE frozen-dataclass guard (E152/E153/E155), (j) re-force embodiment-config schema (E156), (k) re-force arm-playground catalog↔MJCF (E157), (l) re-force the piper-proxy↔driver const mirror (E160), (m) re-force the SO-101 home-pose vein — now FULLY CLOSED incl wave + config.py missing-file fallback (E161/E162), (n) re-run the F821/undefined-annotation scan — now MECHANICALLY CLEAN pkg-wide (E163); only re-scan if a NEW undefined name is added. But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E162 = ten found post-saturation; even a "SATURATED"/"CLOSED" vein can hide a missed copy — grep pinning comments across ALL drivers/proxies/config). Candidates if a gap is real: a load-bearing const with a comment-pinned mirror not yet guarded (grep "must match"/"kept in sync"/"duplicate of" across `vector_os_nano/` + config/*.yaml), a hardcoded enumeration that should introspect, an untested except/fallback branch (E162 pattern), or a real test-coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R374: sibling isaac warehouse_nav.py LIVE, pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (checks_schema.py — R374 E163 = 999B; status ∈ {confirmed,inconclusive,plateau,refuted} — NOT provisional for experiments). experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (CPython 2nd-granularity .pyc mtime invalidation reuses mutated bytecode) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R373 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (lidar-offset E151/E158, spine frozen E152/E153/E155, Inv-4 import E154, config-schema E156, catalog↔MJCF E157, piper-proxy const E160, home-pose vein E161/E162, undefined-annotation/F821 E163): a comment-pinned const mirror not yet guarded (grep "must match"/"kept in sync"/"duplicate of"), a hardcoded enumeration that should introspect, an UNTESTED except/fallback branch (E162 found a dead one), or a real test-coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, re-run a now-clean mechanical scan (F821 E163), re-investigate the preflight nag (E144) or BOARD age drift (E145).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E162 re-red-teamed E150/E159): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 56 rounds (R318..R373). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R370 §7): gate/token last-review CLEAN — 0 real approvals. R374 touched ONLY vector_os_nano/vcli/intent_router.py (add `from typing import Any`) + tests/ + docs/LESSONS + STATUS + ledger/events — no spine/manifest-gated path, no interface change (behavior-neutral import).
last_review: R370
