# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R376 (E165, BUILD). HEADLINE: substrate re-scan OUTSIDE all saturated veins found a DISTINCT unenforced comment-pinned mirror on the North-Star PERCEPTION path. `manipulation_setup.py:62` says "the go2 head camera / bridge publishes 320x240; intrinsics must match" — but NOTHING guarded it, and the `Go2GraspPerception` class DEFAULT had drifted to a STALE 640x480 (the original grasp feat commit 19ef11d's arbitrary value) while 3 AUTHORITATIVE refs agree on 320x240: `Go2ROS2Proxy.get_camera_frame` default (the real frame source), `depth_projection.mujoco_intrinsics` default, and the DELIBERATE grasp pin (eddf8ad "to match the bridge"). The stale 640 default leaked into the look/explore ACCEPTANCE path via `_build_go2_perception` (which uses the class default) — so sim look/explore rendered at a resolution the real robot never publishes (a sim-vs-real fidelity + look-vs-grasp divergence). NOT a wrong-math bug: intrinsics are res-consistent per instance ⇒ grasp geometry correct at either res. FIX: single-source `GO2_HEAD_CAM_WIDTH/HEIGHT=320x240` in go2_grasp_perception; class default + manipulation_setup reference it; drift-guard test asserts the constant == `Go2ROS2Proxy.get_camera_frame` default via `inspect.signature`. TDD: 5 new guards RED pre / GREEN post; tamper const→640 ⇒ 2 guards RED, restore ⇒ 13/13; 28 detector-exercising suites (courtyard/pick-place/detector-cap) pass at 320. Behavior-neutral for the grasp path (already 320); look-path default 640→320 aligns sim to the real bridge — its VISUAL re-check is SIM-OWED. `perception/`,`skills/`,`vcli/tools/` NOT spine ⇒ no gate. Both levers still SHUT (54th sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; a real non-gated find landed — an unenforced comment-pinned resolution mirror on the perception path with a stale default leaking into the look/explore acceptance path; single-sourced + drift-guarded; TDD red→green, no manufactured sliver).
last-round: R375 (E164 BUILD: ruff bugbear scan → 1 live async-closure late-binding defect in cli.py fixed; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2 (4)+g1 (E147-E151/E158), piper-proxy↔driver const block (E160), verdict contracts (E152/E153), Inv-4 import allowlist (E154), causation-baseline frozen (E155), embodiment-config schema (E156), arm-playground catalog↔MJCF (E157), SO-101 home-pose vein CLOSED (E161/E162), undefined-annotation/F821 class MECHANICALLY scanned (E163), ruff bugbear real-bug classes swept (E164), go2 head-cam RESOLUTION mirror single-sourced+drift-guarded (E165). Spine + config-schema frozen + mirror coverage broad. The R312-R376 acceptance-face plateau (64 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R376). Substrate-hardening finds (E152-E165) are increasingly marginal — a domain expert would say the loop polishes invariant-guards while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill. (E162 dead-fallback + E163 dormant undefined-name + E164 live async closure + E165 stale-default resolution mirror are the substrate finds that were REAL live divergences, not just unguarded copies.)
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g) re-force the lidar-offset mirror — SATURATED all 4 go2 + g1 + piper proxy (E151/E158), (h) re-force Inv-4 import guard (E154), (i) re-force a SPINE frozen-dataclass guard (E152/E153/E155), (j) re-force embodiment-config schema (E156), (k) re-force arm-playground catalog↔MJCF (E157), (l) re-force the piper-proxy↔driver const mirror (E160), (m) re-force the SO-101 home-pose vein (E161/E162), (n) re-run the F821/undefined-annotation scan — CLEAN (E163), (o) re-run the ruff bugbear scan — SWEPT (E164), (p) re-force the go2 head-cam resolution mirror — now single-sourced `GO2_HEAD_CAM_*` + drift-guarded vs Go2ROS2Proxy (E165). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E165 = twelve found post-saturation; even a "SATURATED"/"CLOSED" vein can hide a missed copy). Candidates if a gap is real: a load-bearing const/param with a comment-pinned mirror not yet guarded incl. a stale DEFAULT ARG leaking into the acceptance path (grep "must match"/"kept in sync"/"publishes"/"duplicate of" across `vector_os_nano/` + config/*.yaml, then check the default not just the pinned call — E165 pattern), a hardcoded enumeration that should introspect, an untested except/fallback branch (E162), a closure on a deferred/async path capturing a reassigned var (E164), or a real test-coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  OWED SIM-CHECK (E165): when the sim slot frees, eyeball look/explore on the bare-cli go2 face at 320x240 to confirm the 640→320 default reduction did not degrade VLM detection (unit+detector suites pass at 320; acceptance-face VISUAL behavior is the only unverified aspect).
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R376: sibling isaac warehouse_nav.py LIVE, pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (checks_schema.py — R376 E165 = 980B; status ∈ {confirmed,inconclusive,plateau,refuted} — NOT provisional for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (CPython 2nd-granularity .pyc mtime invalidation reuses mutated bytecode) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R373 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore visual re-check. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (E151/E158 lidar, E152/E153/E155 spine frozen, E154 Inv-4 import, E156 config-schema, E157 catalog↔MJCF, E160 piper const, E161/E162 home-pose, E163 F821, E164 bugbear, E165 head-cam resolution): a comment-pinned const/param mirror not yet guarded incl. a stale DEFAULT ARG leaking to the acceptance path (E165 pattern), a hardcoded enumeration that should introspect, an UNTESTED except/fallback branch (E162), a closure on a deferred/async path capturing a reassigned var (E164), or a real coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, or re-run a now-clean mechanical scan (F821 E163, bugbear E164).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E165 re-red-teamed E150/E159): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 58 rounds (R318..R376). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R370 §7): gate/token last-review CLEAN — 0 real approvals. R376 touched ONLY perception/go2_grasp_perception.py (add single-source consts + default 640→320) + skills/manipulation_setup.py (reference consts) + tests/ + docs/LESSONS + STATUS + ledger — no spine/manifest-gated path, no interface change (internal defaults only).
last_review: R370
