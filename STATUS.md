# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R307 (E97, build) — §1a adopted R306 rounds append + regen BOARD. §1b: inflight empty, no WIP; check.sh green (the 66 provisional-count is naive — all adjudicated via supersedes back-pointers). §2-4 next#1 SKEPTIC hygiene DONE: re-verified the 3 remaining STALE self-read HOUSE fetch bars on the CURRENT shipped face (deepseek-v4-flash) AND upgraded eyes self-read→vlm-judge (Inv.1 stricter-only). ALL 3 GROUNDED verified=True 1/1: plain-colour(把绿色的瓶子拿过来→holding green, judge PASS), category-only(把罐子→holding red can=only can, judge ABSTAIN), negated-distractor(不要蓝/绿只红罐→holding red can, negation resolved, judge PASS). LOOKED at all 3 eyes_fetch.png: correct target aloft, other 4 pickables REMAIN=discrimination. GT holding_object(target) actor=CAUSED is the moat; gemma4 judge PASS/ABSTAIN a recorded-not-trusted bonus (E72 non-determ). Superseded R260/R263/R268. HEADLINE: the LAST stale self-read HOUSE fetch bars are closed — eyes pillar is now vlm-judge across ALL confirmed fetch+place bars & worlds.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~55r unanswered; non-gated NEW-capability frontier EXHAUSTED — loop treads on eyes-pillar hygiene + skeptic re-verify of stale bars while the gates stand). Concurrent SIBLING sim this round: Dockerized Isaac go2w nav (/isaac-sim warehouse_nav.py, ~9GB python3, PID 2817818) — NOT this repo's loop, NEVER-KILL-INFRA; my 3 MuJoCo fetch runs each hard-capped MemoryMax=24G serial, avail stayed ≥33G → no host risk.
last-round: R307 (E97, build). §1a adopt R306 rounds+BOARD. §1b clean. next#1 skeptic: 3 STALE house fetch bars re-verified GROUNDED 1/1 + eyes self-read→vlm-judge (2 PASS, 1 ABSTAIN), superseding R260/R263/R268. Banked 3 acceptance confirmed rows + E97 experiments, regen BOARD, LESSONS eyes-pillar line extended.
frontier: Verify-pillar eyes CLOSED at CONFIRMED strength on ALL single-object fetch+place bars across ALL 3 worlds (house/warehouse/courtyard): eyes=vlm-judge FIRES + GT-GROUNDED. N-OBJECT quantity-place remains openly RED (E96, 0/4 v4-flash) — the brain cannot self-decompose "两个瓶子" into N grasp+place cycles: a REAL non-gated capability gap whose fix (decompose guardrail / stronger routing brain, E36/E72) overlaps the gated D182 grounder. gemma4:e4b judge non-determ PASS↔ABSTAIN↔FAIL (E72/E92/E97) — a TRUSTED discriminator still needs a stronger VLM (funded qwen3-vl / better local). NEW embodiment still needs owner S4/D182.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek + v4-flash via env (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`.
  HARNESS: house-fetch launcher = var/evidence/R307/run_house_fetch.sh <tag> <fetch_nl> (default HOUSE world = NO VECTOR_ROOM_TEMPLATE; MODE=fetch; vlm-judge + perception both auto-route to local gemma4:e4b). Non-house fetch pattern = var/evidence/R305/run_fetch.sh <room> <colour> <fetch_nl>. A single fetch is ~5-6min end-to-end — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G) and POLL the log for "RESULT R307<tag>"; a foreground poll trips the 10-min Bash-tool ceiling. SERIAL one-at-a-time; `free -g` + `pgrep -x mjpython` (NOT `pgrep -f mujoco` — self-matches) before launch; scripts/sim-teardown after. NEVER pkill mujoco.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps (checks_schema.py): line ≤1024 BYTES (Chinese=3B/char — trims bit me 3× this round), each string field ≤280 chars. experiments row key `e` NOT `experiment` + needs `source∈{round,backfill}`; acceptance status∈{provisional,confirmed,refuted,superseded}; confirmed rows' redteam MUST start 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); a supervisor rounds.jsonl append shifts BOARD ages → NEXT round MUST regen.
next:
  1. [SKEPTIC hygiene] Last 2 STALE BOARD rows: verify.eyes-vlm-judge-wired (age 35, vlm-judge — refresh by superseding with a fresh fetch run showing the judge FIRES) and quantity-place.nl-guardrail (age 98, deepseek-chat REFUTED 0/1 — supersede/refute on v4-flash, tie to E96 quantity RED). Both close the last stale rows; note R310 is a review round.
  2. [FRONTIER — non-gated but overlaps D182] Quantity-place N-object decompose is openly RED (E96). A decompose guardrail (cf. R206 quantity guard / R257 post-place guard) making the brain emit N grasp+place cycles is a genuine non-gated verify-pillar build — weigh vs the gated D182 grounder before a multi-round build.
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~55r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65; overlaps the E96 quantity-decompose gap. <- next#2/#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R281..R307 CLEAN. R307 touched NO spine path (banked 3 acceptance + 1 experiments row + STATUS + BOARD regen + LESSONS eyes-pillar line + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
