# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R321 (E112, BUILD). HEADLINE: the deterministic hard-channel family gains a THIRD member — `tools/acceptance/place_near.py`, a RELATIONAL proximity oracle for "put A NEAR B". The placement geometry already deterministic is ABSOLUTE-region containment (make_placed_count/resting_on_receptacle, D106); RELATIONAL near(a,b) had NO hard channel — only vlm-judge. This grades the GEOMETRY half from two GT world positions (unforgeable, Inv-1): `separation` xy|xyz, `proximity_margin`=radius-sep (signed, pose_height idiom), `classify` NEAR/FAR. Unit 18/18, sim-free proof 5/5 incl. the case a VLM can't give — B stacked over A reads NEAR(xy) yet FAR(xyz). grep-confirmed no prior near(a,b); non-redundant vs D106 AABB. Identity ("which object is B") stays the VLM/D182 job.
  §1 ORIENT: inflight absent; no WIP; inbox clean (G-183-12 answered). No provisional experiments rows (E109/E110 build-confirmed; enum has no provisional). 67 acceptance provisionals all check-exempt → check.sh GREEN. SIM-BLOCKED AGAIN: a sibling loop holds the one sim slot (localPlanner+laser_mapping PID 3535348/56 LIVE at orient) → Inv-5 + NEVER-KILL-INFRA: NOT touched, launched NO competing sim ⇒ next#1 (real-face stale-row re-run) stays sim-blocked, as R320.
  §4 REAL-VERIFY: pure-math non-spine oracle ⇒ acceptance is the deterministic proof on realistic GT (as E109/E110 were verified sim-free). eyes: self-read. `var/evidence/R321/prove_place_near.py` 5/5 correct (beside NEAR, across-room FAR, edge-inclusive NEAR, stacked planar NEAR + xyz FAR). REDTEAM survives as-if-not-mine: inputs=GT get_object_positions (unforgeable Inv-1, the source resting_on_receptacle uses); margin reported not knife-edge; radius task-defined=caller-owned not a faked constant; OFFLINE oracle NOT bare-cli face ⇒ NO acceptance row (experiments only); grades GEOMETRY half only, identity stays VLM/D182. NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE-blocked: every acceptance-face advance is CEO-gated — G-318-1 (land the pose pair) · relational near(a,b) (land E112) · S4 · D182 · D76)
last-round: R321 (E112 BUILD: 3rd deterministic hard channel place_near — relational proximity oracle, TDD 18/18 + sim-free proof 5/5; identity stays VLM. Sim-blocked again by a sibling; no spine touched).
frontier: the DETERMINISTIC hard-channel family is now a TRIO — pose orient (E109) + pose height (E110) + relational near (E112). Each grades from unforgeable GT, each SHELVED behind a spine-landing CEO gate. NON-gated hard-channel frontier is thinning: pose SETTLED, absolute-place = D106 (done), relational-place GEOMETRY = E112 (done). What remains non-gated is sparse — the honest lever is the OWNER unblocking ONE gate to convert a proven channel into an acceptance-face verify-channel. 9-round plateau (R312-R321) on the acceptance face is REAL: the face is GATE-blocked, not idea-blocked.
watch: hard-channel trio (non-spine, pure math, no sim):
  pose: `python tools/acceptance/pose_upright.py <w> <x> <y> <z> [--thresh 0.5]` (STANDING/FALLEN); `python tools/acceptance/pose_height.py <root_z> [--stance-z 0.35] [--tol 0.12]` (SUNK/NOMINAL/AIRBORNE).
  place: `python tools/acceptance/place_near.py <ax> <ay> [az] --to <bx> <by> [bz] [--radius 0.5] [--full]` (NEAR/FAR; planar xy default, --full=xyz).
  Units: test_pose_upright.py(11)+test_pose_height.py(13)+test_place_near.py(18) via `./scripts/run-tests`. Sim-free proofs: var/evidence/R319/prove_pose_pair.py, var/evidence/R321/prove_place_near.py.
  SIM-SAFETY (Inv-5, R318/R320/R321): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco'` (NOT just `mujoco`); WAIT if live, NEVER teardown a sim you did not launch. R320+R321 were both sim-blocked exactly this way.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; scripts/sim-teardown after. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (E112 trimmed to 1005B). experiments key `e`; experiments status ∈ {confirmed,inconclusive,plateau,refuted} (NO provisional); acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320/R321 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a proven channel into the acceptance face] G-318-1 (wire the pose PAIR orient+height as the pose verify-channel); AND relational near(a,b) (wire place_near E112). Both edit acceptance/ + rubric = honest-verify-spine/moat. Owner call owed. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] the non-gated hard-channel frontier is THINNING (pose+abs-place+rel-place-geometry all done). Candidate: a deterministic ORIENTATION-relative channel (facing/heading a target) or a stability/contact channel — but FIRST weigh honestly vs pushing the owner to unblock a gate. Do NOT run another eyes-VLM probe (pose refuted E105-E108; relational-distance VLM unreliable by foreshortening).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-318-1 SPINE/MOAT (pose verify-channel): E109+E110 prove a deterministic pose PAIR (pose_upright + pose_height) reads go2 pose 4/4 across all R316 topologies where every local VLM failed (R320/E111 re-verified 4/4 sim-free). Landing edits acceptance/ + rubric (ADR-002 moat) = honest-verify-spine semantics. Per Invariant 1 STRICTLY better than a VLM. Owner call owed. <- next#2. (R318/E109, R319/E110, R320/E111)
  - RELATIONAL near(a,b) SPINE/MOAT (proximity verify-channel): E112 proves `place_near.py` grades "put A NEAR B" geometry from two GT positions (unforgeable, Inv-1), TDD 18/18 + proof 5/5, non-redundant vs D106. Landing as a verify-channel edits acceptance/ + rubric = honest-verify-spine semantics; identity half stays the VLM/D182. Owner call owed. <- next#2. (R321/E112)
  - G-315-1 SPINE (VLM pose rubric): MOOT FOR POSE — pose belongs to the hard-channel PAIR (G-318-1). Superseded-in-spirit by G-318-1. (R315/E106..R320/E111)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65; is the IDENTITY half E112's relational geometry channel leaves to the VLM. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R321 CLEAN. R321 (build) touched NO spine path (added tools/acceptance/place_near.py + tests/unit/test_place_near.py, both non-spine pure-math; appended experiments E112; extended 1 LESSONS saga line; STATUS; gitignored var/evidence/R321; NO acceptance/ or rubric edit).
last_review: R320
