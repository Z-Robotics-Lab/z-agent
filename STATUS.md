# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R361 (E151, BUILD). HEADLINE: 4th HOLD-reflex break — machine-enforced the LAST comment-pinned lidar-offset mirror (vnav bridge _SENSOR_X/_Z), closing the driver↔manifest↔bridge fidelity vein E149 named as remaining. The bridge imports rclpy+MuJoCoGo2 at load, so an import-based guard cant run in CI; instead read its float literals via `ast` (no import ⇒ ROS2/sim-free) and assert they mirror driver _LIDAR_OFFSET. 10/10 fidelity GREEN peak94M; embodiments 36/36 no-regr peak1.5G. DISCRIM: drift driver 0.3→0.77 ⇒ new guard RED (0.3=bridge-via-ast vs 0.77=driver-via-import ⇒ binds BOTH real sources, not a copied literal), reverted byte-clean; helper fails loud on missing/non-numeric so a rename cant silently disable it. Both levers reverified SHUT cold (40th sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; the R357-R361 substrate-hardening vein — machine-enforced embodiment manifest↔driver↔bridge lidar/policy/stance/spawn fidelity — is now SATURATED: all 3 go2 lidar-offset mirrors + both g1 guarded, no comment-pinned mirror left. Future non-gated hardening lies OUTSIDE this vein; genuinely re-scan the substrate before defaulting to HOLD.)
last-round: R360 (E150 REVIEW: §7 janitor+skeptic — E149 headline SURVIVES independent re-red-team; 2 stale WIRING cards re-verified accurate; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED across the FULL driver↔manifest↔bridge mirror for go2+g1 (E147-E151, vein now SATURATED). The R312-R361 acceptance-face plateau is REAL + HARD-EXTERNAL (not a local hill): the two lever paths to a NEW confirmed capability are sim slot frees (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R361).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` to confirm a LIVE run not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline capability axis, (b) re-audit an already-audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED as a defect (mechanical age+1 drift, E102/E145), NOR (g) force a manifest/bridge-mirror guard — that vein is now SATURATED (E151). But DO genuinely re-scan the substrate for a REAL unenforced invariant / missing drift-guard OUTSIDE the mirror vein before defaulting to HOLD — real non-gated BUILD, not a sliver.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 last re-confirmed R352; skip re-running unless >~10 rds stale).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R361 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R352), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE the manifest/bridge mirror vein (now SATURATED, E151): an unenforced invariant, a missing drift-guard on a NEW driver const, or a test-coverage gap. ONLY if no real hardening exists AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a mirror guard (E151 saturated), re-investigate the preflight nag (E144) or the BOARD age drift (E145).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R352, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E151 re-red-teamed E150): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 44 rounds (R318..R361). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0. FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer, planner-free contract) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-10 CLEAN (git log R350..R361 grep GATE-APPROVED/CEO-APPROVED = 1 hit, R354, which only NAMES G-354-1's requirement — crossed nothing). R361 touched ONLY tests/unit/embodiments/ + docs (LESSONS) + ledger + STATUS + events — no spine/manifest-gated path (bridge & driver unedited). No self-approval.
last_review: R360
