# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R382 (E171, BUILD). HEADLINE: proved Inv-3 (plug-and-play, "a new robot is CONFIG not code") is NOT overfit to the two shipped robots. The generic stand-up path (`parse_embodiment_config`→`DofLayout`→`build_stance_vector`→`CapabilityProfile`) was tested ONLY on go2/g1 — both 12-DoF, both with joint conventions the code was co-developed against. NEW test (tests/unit/embodiments/test_synthetic_embodiment_plug_and_play.py) stands up a SYNTHETIC 3rd robot that ships nowhere: novel `syn_` attach prefix, 3 actuated joints (NOT 12, so any implicit ==12 fails), robot placed AFTER a free prop so its freejoint is NOT at qpos 0 (proves DofLayout INTROSPECTS, never assumes 0), manifest stance keys in un-prefixed convention matched to prefixed model joints via suffix-strip. It flows the WHOLE generic path with ZERO production-code change and NO driver in its call path (7/7 GREEN, embodiments suite 56/56 no regress, offline model-compile peak 112M — no physics/GL/sim-slot). Red-team: non-vacuous (root_qpos_adr==7, 3≠12, suffix-map [.1,-.2,.3], missing-key raises); driver modules in sys.modules are `vector_os_nano/__init__` top-level re-exports (MuJoCoArm/SO101), NOT the stand-up path. HONEST SCOPE: this hardens the OFFLINE half of a 3rd embodiment; the sim-face WALK stays S4-gated. Both levers still SHUT (60th sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R382 hardened North-Star Inv-3 OFFLINE — a real coverage hole on a load-bearing invariant, OUTSIDE all saturated veins, non-gated, deterministic).
last-round: R381 (E170 BUILD: go2 get_self_mask _seg_renderer per-call dims — 3rd head-cam-renderer-family sibling; head-cam renderer vein CLOSED).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror go2+g1 (E147-E158), verdict contracts (E152/E153), Inv-4 import allowlist (E154), embodiment-config schema (E156), arm-catalog↔MJCF (E157), home-pose (E161/E162), F821/bugbear (E163/E164), head-cam res default (E165), render defect (E166), head-cam renderer FAMILY per-call dims go2 rgb/depth+g1+go2 seg (E167/E168/E170), R380 review survives (E169), Inv-3 plug-and-play NOT overfit — synthetic 3rd-embodiment offline stand-up regression-guarded (E171). Head-cam renderer vein CLOSED. Substrate finds (E152-E171) are increasingly marginal EXCEPT E171 which is squarely on the North Star (offline half of the 3rd-embodiment bar). The R312-R382 acceptance-face plateau (70 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves G-323-1. A domain expert would still say the loop cannot land a NEW sim-face capability while externally blocked — honestly acknowledged.
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g-t) re-force any SATURATED vein incl. the ENTIRE head-cam renderer FAMILY (E167/E168/E170) or the offline embodiment-config path now proven generic (E171). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E171 = sixteen found post-saturation; E171 shows even a "closed" area — plug-and-play, thought proven by go2/g1 — hid a real generic-coverage hole). Candidates if a gap is REAL: a comment-pinned const/param mirror incl. a stale DEFAULT ARG (E165); a shared-resource per-call arg silently ignored across ALL siblings AND all resources on one object (E167/E170); a hardcoded enumeration that should introspect; an untested except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant proven only for the shipped cases (E171). Verify the gap on disk FIRST.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim. (R382: sibling isaac warehouse_nav.py LIVE pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (R382 E171 = 1005B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R382 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask-drops-arm after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (see watch candidates). E171 proves the OFFLINE side of S4 (config→layout→stance) is now guarded — natural offline extensions IF REAL: a synthetic-embodiment CAPABILITY-resolution proof through `resolve_capability_profile` (runtime-OR-declared reconcile on a novel body), or a synthetic-manifest round-trip through the generic driver constructor without a sim. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver or re-force a saturated guard.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, substrate rot-free E121/E147-E171 re-red-teamed E150/E159/E169): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 64 rounds (R318..R382). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0, re-confirmed R370). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1 (provenance R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half now guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-review CLEAN (R380 §7) — `git log --grep=GATE-APPROVED\|CEO-APPROVED` since R370 = 0 real approvals. R382 touched ONLY a NEW test file + docs/LESSONS.md + STATUS.md + loop/ledger — no spine/manifest-gated path, no interface change, no production code.
last_review: R380
