# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R381 (E170, BUILD). HEADLINE: head-cam renderer FAMILY re-scan (E167/E168 sibling class) finds+fixes the 3rd unfixed sibling — `MuJoCoGo2.get_self_mask`'s `_seg_renderer` still had the raw cache-once guard (`if not hasattr`) while RGB/depth honor per-call dims via `_ensure_render`. Its SOLE caller (`Go2GraspPerception.get_depth_frame`) applies the self-mask only `if self_mask.shape==depth.shape`, so a locked first-call resolution SILENTLY fails that guard on a differently-sized later call ⇒ the arm's pixels are NOT dropped ⇒ the Piper arm becomes a grasp distractor (the very D30 self-occlusion the mask exists to prevent), no error. FIX: port the per-call-dims contract (`_seg_renderer_dims`, re-create+close-old on change); 6 new fake-`_get_mujoco` seg-dims tests + E167(5)+E168(3) = 14/14 GREEN, go2 driver+perception 55/55 no regress. Renderer inventory swept: `mujoco_arm.render` fresh+closes per call, `pano360` fixed square, g1 E168 — `_seg_renderer` was the ONLY remaining cache-once renderer. LATENT not live (E165 single-sources go2 head-cam to 320 ⇒ every perception instance renders at one res today); both levers still SHUT (59th sim-block: sibling isaac warehouse_nav.py pid3569755 + localPlanner + laser_mapping hold the one global slot, Inv-5=WAIT; G-323-1 unanswered). Head-cam renderer vein now CLOSED across the family.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R381 found a REAL latent API-contract defect OUTSIDE all previously-saturated veins via the "same-object family, not just sibling drivers" sharpening of E168).
last-round: R380 (E169 REVIEW: skeptic re-red-team of E165/E167/E168 headlines — all SURVIVE; wiring/gate/token/memory audits clean; substrate ROT-FREE; 58th sim-block).
frontier: Offline Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2(4)+g1 (E147-E151/E158), piper-proxy const (E160), verdict contracts (E152/E153), Inv-4 import allowlist (E154), causation-baseline (E155), embodiment-config schema (E156), arm-catalog↔MJCF (E157), SO-101 home-pose (E161/E162), F821 (E163), ruff-bugbear (E164), go2 head-cam RESOLUTION default (E165), F841/F401 render defect (E166), go2 head-cam RGB/depth render MECHANISM per-call dims (E167), g1 head-cam RGB render per-call dims (E168), R380 REVIEW re-red-team survives (E169), go2 head-cam SEGMENTATION render per-call dims — 3rd family sibling (E170). Head-cam renderer vein CLOSED across the family. Spine + config-schema frozen + mirror coverage broad. The R312-R381 acceptance-face plateau (69 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R381). Substrate finds (E152-E170) are increasingly marginal — a domain expert would say the loop polishes plumbing while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill.
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g-s) re-force any SATURATED vein incl. the ENTIRE head-cam renderer FAMILY — go2 rgb/depth (E167), g1 rgb (E168), go2 seg (E170) all honor per-call dims; arm.render+pano360 audited safe; (t) re-red-team E165/E167/E168 headlines again (R380/E169 did — all SURVIVE). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E170 = fifteen found post-saturation; even a "fixed" mechanism hides a SIBLING — E168 was the go2-fix's driver mirror, E170 was a THIRD renderer on the SAME go2 object). Candidates if a gap is real: a comment-pinned const/param mirror not yet guarded incl. a stale DEFAULT ARG leaking to the acceptance path (E165); a shared resource whose per-call arg is silently ignored (E167/E170 — grep `if …_renderer is None`/`if not hasattr` AND enumerate EVERY resource on the same object, not just sibling drivers); a hardcoded enumeration that should introspect; an untested except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  OWED SIM-CHECK (E165+E167+E168+E170): when the sim slot frees — (a) eyeball go2 look/explore at 320x240 (E165); (b) go2 `describe`/`look` THEN a `pick` in one bare-cli session, confirm the grasp still grounds `holding_object` (E167); ALSO confirm the self-mask still drops the arm at the grasp res after an interleaved look (E170); (c) instantiate a non-default-res g1 perception after a bare g1 look, confirm the frame comes back at the requested resolution (E168). All are the only unverified aspects; unit+detector suites pass.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R381: sibling isaac warehouse_nav.py LIVE pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (checks_schema.py — R381 E170 = 1016B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R381 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask-drops-arm after an interleaved look), AND the E168 owed g1 non-default-res perception-after-look (one session). Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (E151/E158 lidar, E152/E153/E155 spine frozen, E154 Inv-4 import, E156 config-schema, E157 catalog↔MJCF, E160 piper const, E161/E162 home-pose, E163 F821, E164 bugbear, E165 head-cam res, E166 render, E167/E168/E170 head-cam renderer FAMILY): a comment-pinned const/param mirror not yet guarded incl. a stale DEFAULT ARG (E165); a shared-resource per-call arg silently ignored ACROSS ALL siblings AND all resources on the same object (E167/E170); a hardcoded enumeration that should introspect; an UNTESTED except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, or re-run a now-clean mechanical scan.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E170 re-red-teamed E150/E159/E169): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 63 rounds (R318..R381). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R380 §7): gate/token last-review CLEAN — `git log --grep=GATE-APPROVED\|CEO-APPROVED` since R370 = 0 real approvals. R381 touched ONLY vector_os_nano/hardware/sim/mujoco_go2.py (get_self_mask body) + a new unit test + docs/LESSONS.md + STATUS.md + loop/ledger — no spine/manifest-gated path, no interface change.
last_review: R380
