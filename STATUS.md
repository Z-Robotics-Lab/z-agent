# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R303 (E93, verify) — STATUS next#1: upgraded the LAST stale self-read warehouse FETCH colour bars to vlm-judge eyes. HEADLINE: warehouse fetch blue/purple/red now vlm-judge-eyes GROUNDED 1/1 (provisional, N=1 each) — self-read→vlm-judge (Inv.1 stricter-only), mirroring the R301→R302 place eyes upgrade. All 3 verified=True on holding_object(colour) GT oracle (actor=CAUSED); LOOK on each verdict frame: blue bottle / purple BOX / red can aloft in gripper, other 4 pickables remain on the warehouse pick-table = discrimination. CAVEAT (E72/E92): the gemma4:e4b judge FIRED on every run but returned ABSTAIN (all 4 fields) — NON-DETERMINISTIC weak judge, fail-closed so verified= NEVER rests on it, only on the GT oracle + human LOOK; eyes recorded honestly as vlm-judge (an independent VLM graded, not the actor).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~51r unanswered; non-gated NEW-capability frontier EXHAUSTED — loop treads on eyes-pillar upgrades of stale self-read bars + skeptic hygiene while the gates stand). Concurrent SIBLING sim noted this round: a Dockerized Isaac go2w nav (~9GB, /workspace/go2w) — NOT this repo's loop, NEVER-KILL-INFRA; honest OOM math (peak ~34/62GB; my MuJoCo runs hard-capped MemoryMax=24G, serial one-at-a-time) → no host risk, proceeded.
last-round: R303 (E93, verify). §0/§1: BOOTSTRAP clean (preflight OK, no quarantine, inflight empty). §1a: adopted R302 supervisor rounds.jsonl append + regen BOARD (ages shifted, pure-append). §1b: NO dangling provisionals (R301's were promoted in R302). §1d: inbox G-183-12 already answered+crossed R183. §2-4: re-ran warehouse fetch blue/purple/red bare-face under vlm-judge (deepseek-v4-flash, MODE=fetch, in-process); banked 3 provisional acceptance rows + E93; LOOKED at all 3 frames.
frontier: Verify-pillar: eyes=vlm-judge now FIRES on fetch AND place across HOUSE + courtyard + warehouse. After R303 the warehouse fetch colour bars are vlm-judge (provisional). RESIDUAL non-gated (local-hill): (a) LAST self-read non-house fetch bar = courtyard-blue (age~13) → upgrade to vlm-judge, then the eyes pillar is UNIFORM across all non-house fetch/place bars; (b) skeptic-verify the genuinely-oldest stale bars (quantity-place.nl age~97 KNOWN brain-flaky, kernel.model-unavailable age~43). NEW capability still needs owner S4/D182. gemma4:e4b judge stays PASS↔ABSTAIN↔FAIL non-determ (E72/E92) — a TRUSTED discriminator needs a stronger VLM (funded qwen3-vl / better local).
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select env vars (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): ROOM_TEMPLATE={warehouse|courtyard}, SCENE_CLUTTER=1, SCENE_SWAP=1.
  HARNESS: launcher this round = var/evidence/R303/run_fetch_warehouse.sh <colour> <fetch_nl> (uses .venv/bin/python; systemd --scope has NO `python` on PATH). MODE=fetch → 1 fetch turn + eyes_frame; args = FETCH PLACE TAG fetch (place NL ignored but MUST be present; TAG must not be a valid mode word). eyes vlm-judge AUTO-routes to local ollama gemma4:e4b. Colour→noun: blue=蓝色的瓶子, purple=紫色的盒子(box), red=红色的罐子(can). RUN sim FOREGROUND, SERIAL one-at-a-time, under `systemd-run --user --scope -p MemoryMax=24G`; set VECTOR_EVIDENCE_DIR per colour (E54 overwrite). Check `free -g` before each launch. NEVER pkill mujoco. A ~9GB Isaac python3 (/isaac-sim, go2w) is a SIBLING project's sim — leave it.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps (checks_schema.py): line ≤1024 BYTES, each string field ≤280 chars (bit me 4× this round — trim redteam). experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); a supervisor rounds.jsonl append shifts BOARD ages → NEXT round MUST regen.
next:
  1. [§1b adjudicate] Promote the 3 R303 warehouse-fetch-colour provisionals (blue/purple/red, vlm-judge GROUNDED 1/1) → confirmed: re-run each bare-face across the R303→next boundary to meet E46 N≥2 (same as R302 did for place). Bank confirmed rows that supersede the self-read rows.
  2. [NON-gated eyes-pillar upgrade] Upgrade the LAST self-read non-house fetch bar: courtyard-blue (age~13, MODE=fetch ROOM_TEMPLATE=courtyard) self-read→vlm-judge, LOOK, promote. Closes the eyes pillar uniform across all non-house fetch/place bars.
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~51r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R280..R303 CLEAN. R303 touched NO spine path (ledger + STATUS + BOARD regen + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
