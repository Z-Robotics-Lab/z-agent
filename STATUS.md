# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)

updated: 2026-07-03 · R239 (E54) VERIFY — warehouse colour transfer SWEEP: blue PROMOTED to
  confirmed (N=2), purple + RED (the hardest) both transferred N=1; HOUSE plain-colour re-confirmed.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: green
last-round: R239 (E54, VERIFY, non-gated). 4 bare-face runs (deepseek-v4-flash, local Ollama gemma4:e4b
  eyes), each eyes-confirmed discrimination (correct object aloft, others untouched), actor=CAUSED, in-process:
  (1) warehouse BLUE fresh N=1 across the R238→R239 boundary → cum N=2 → PROMOTED -warehouse-blue to confirmed.
  (2) PURPLE-box N=1 (provisional). (3) RED-can N=1 (provisional) — the HARDEST: red hue neighbours the orange
      racking + can-geometry FOV; mask_px=5132 (NOT flooded), d_min=0.569 (Case-14 FOV did not bite).
  (4) HOUSE green (plain-colour) fresh → confirmed (§1b adjudicates the aging R237 provisional; no regression).
  ⇒ the R237 pixel-hue gate is FULLY colour-agnostic. Only YELLOW(y=3.11) untransferred — blocked by the
  separate HOUSE FOV issue (E45/E46), NOT the hue gate.

frontier: New-world TRANSFER effectively CLOSED — 4/5 colours transfer to the warehouse (green confirmed N=3,
  blue confirmed N=2, purple+red provisional N=1). Next floor: promote purple+red to N≥2; then the genuinely-NEW
  bars — a 2nd WORLD beyond warehouse, or a NEW 3rd embodiment (S4). Yellow FOV (E45/E46) is a side quest.

watch: inflight cleared; sim torn down after each of the 4 runs (43G avail, one-at-a-time). Per-run
  VECTOR_EVIDENCE_DIR used (no R238-style collision). All rows banked ONE-line via `>>` (E27/E54 hygiene).

next:
  1. [VERIFY, NON-gated] Promote fetch.nl-new-world-warehouse-purple AND -red to confirmed: re-run each N≥2
     fresh (boundary + red-team), VECTOR_ROOM_TEMPLATE=warehouse, per-run VECTOR_EVIDENCE_DIR. Red is the
     stress case — re-confirm mask_px>0 + d_min standoff so the N=1 pass wasn't a lucky framing.
  2. [FRONTIER, NON-gated] A genuinely-NEW bar past colour-breadth: author a 2nd new world (distinct enclosure
     from house/warehouse) as CONFIG (VECTOR_ROOM_TEMPLATE + build_room_scene, 0 kernel edits) and transfer a
     confirmed fetch — the deeper test than more colours in one world.
  3. [DEBUG, NON-gated] YELLOW(y=3.11) HOUSE FOV (E45/E46): raise head tilt / widen standoff so the short/high
     pickable enters the near-field vertical frustum (Case 14 pattern), then transfer yellow to warehouse.
  4. [DEBUG, NON-gated] object_localizer in-warehouse still noisy (R236: floor/far phantoms); harden the seed.
  5. [FRONTIER/breadth, GATED] NEW 3rd embodiment via BYO URDF+manifest — S4 one-generic-driver (WIRING:53).

gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD.
  - SPINE (D182): actor-authored verify target — a world-owned NL→object grounder fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46). Object plug-in not pure config (per-object weld tuple + colour maps) → S4.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (external: VLM-judge + BYO-N≥4 + PERCEPTION VLM look/describe_scene OpenRouter/dashscope-402; local Ollama gemma4:e4b is the working seam) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R230): gate/token audit R211..R230 CLEAN. R209 schema-cap approval remains the last, audited clean.
last_review: R230
