# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R379 (E168, BUILD). HEADLINE: the E167 go2 cache-once-renderer fix had a DIRECT MIRROR left unfixed in the sibling humanoid driver — `MuJoCoG1.get_camera_frame` cached ONE renderer at the FIRST call's dims (`if self._cam_renderer is None`) and IGNORED width/height on every later call, EXACTLY like go2 pre-E167. Yet g1 ADVERTISES per-call dims three ways: the method signature `(width=640,height=480)`, the configurable wrapper `G1HeadPerception(base, width=W, height=H)` (whose whole purpose is a settable render resolution → `base.get_camera_frame(self._w,self._h)`), and `g1_perception_oracle` (`get_camera_frame(_RENDER_W,_RENDER_H)`). So any non-default `G1HeadPerception` used AFTER a bare 640x480 `look`/`describe` would silently render 640x480 instead of the resolution it asked for. LATENT (not live) today only because every current g1 caller happens to pass 640x480 — but a real API-contract violation: the signature promises per-call dims and the driver drops them. FIX: ported the E167 contract to g1 — track `_cam_renderer_dims`, re-create the renderer (closing the old GL ctx) only when dims change, `close()` resets the tracker. TDD fake-`_get_mujoco` records every Renderer's construction dims (no GL/sim slot): pre-fix look(640)→perc(320) RED=(480,640) + no-close; post-fix (240,320), same-dims=1 build, dim-change closes-1 GREEN 4/4; sim/hardware unit 165/165 (161+4); ruff clean (also removed 2 pre-existing benign dead `mj=_get_mujoco()` in get_camera_pose/fovy). Verified DETERMINISTICALLY. Scanned ALL sim drivers for the idiom: `mujoco_arm.render`=fresh-per-call renderer (safe), `pano360`=fixed `face_size` cached (safe) — g1 was the ONLY remaining copy. `mujoco_g1.py` NOT spine ⇒ no gate. Both levers still SHUT (57th sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; a real non-gated find landed — the E167 renderer-dims fix was verified only on its own go2 driver, not the family; the sibling g1 driver had the identical cache-once copy → ported the per-call-dims contract; TDD red→green, deterministic fake-mujoco, no sim slot touched).
last-round: R378 (E167 BUILD: MuJoCoGo2 head-cam renderers honor per-call dims — a look's 640x480 leaked into a later grasp's 320x240 RGB; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2(4)+g1 (E147-E151/E158), piper-proxy↔driver const (E160), verdict contracts (E152/E153), Inv-4 import allowlist (E154), causation-baseline (E155), embodiment-config schema (E156), arm-playground catalog↔MJCF (E157), SO-101 home-pose vein CLOSED (E161/E162), F821 class scanned (E163), ruff-bugbear swept (E164), go2 head-cam RESOLUTION default single-sourced (E165), F841/F401 caught a live render defect (E166), go2 head-cam render MECHANISM honors per-call dims (E167), g1 head-cam render MECHANISM honors per-call dims — the E167 mirror ported to the sibling driver (E168). Spine + config-schema frozen + mirror coverage broad. The R312-R379 acceptance-face plateau (67 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R378). Substrate finds (E152-E168) are increasingly marginal — a domain expert would say the loop polishes perception/invariant plumbing while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill. (E162/E163/E164/E165/E166/E167 were REAL live divergences; E168 is a REAL but LATENT API-contract defect — the mirror of a live one, worth fixing before it goes live.)
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g-r) re-force any SATURATED vein incl. go2+g1 head-cam RENDER MECHANISM — both drivers now honor per-call dims (E167/E168); arm.render+pano360 audited safe. But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E168 = fourteen found post-saturation; even a "fixed" mechanism can hide a SIBLING copy — E168 was the go2-fix's own mirror in g1). Candidates if a gap is real: a load-bearing const/param comment-pinned mirror not yet guarded incl. a stale DEFAULT ARG leaking to the acceptance path (E165) OR a shared resource whose per-call arg is silently ignored, ACROSS ALL siblings not just the surfacing one (E168 — grep the idiom `if …_renderer is None`/`if not hasattr` in EVERY driver); a hardcoded enumeration that should introspect; an untested except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  OWED SIM-CHECK (E165+E167+E168): when the sim slot frees — (a) eyeball go2 look/explore at 320x240 (E165); (b) run a go2 `describe`/`look` THEN a `pick` in one bare-cli session, confirm the grasp still grounds `holding_object` (E167); (c) instantiate a non-default-res g1 perception after a bare g1 look and confirm the frame comes back at the requested resolution (E168). All are the only unverified aspects; unit+detector suites pass.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R379: sibling isaac warehouse_nav.py LIVE pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (checks_schema.py — R379 E168 = 1006B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R373 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick, AND the E168 owed g1 non-default-res perception-after-look (one session). Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (E151/E158 lidar, E152/E153/E155 spine frozen, E154 Inv-4 import, E156 config-schema, E157 catalog↔MJCF, E160 piper const, E161/E162 home-pose, E163 F821, E164 bugbear, E165 head-cam res default, E166 F841/F401, E167 go2 renderer dims, E168 g1 renderer dims): a comment-pinned const/param mirror not yet guarded incl. a stale DEFAULT ARG (E165) OR a shared-resource per-call arg silently ignored ACROSS ALL siblings (E168); a hardcoded enumeration that should introspect; an UNTESTED except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, or re-run a now-clean mechanical scan.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E168 re-red-teamed E150/E159): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 60 rounds (R318..R378). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R370 §7): gate/token last-review CLEAN — 0 real approvals. R379 touched ONLY vector_os_nano/hardware/sim/mujoco_g1.py (per-call renderer dims; drop 2 dead `mj=`) + tests/ + LESSONS + STATUS + ledger — no spine/manifest-gated path, no interface change (render-plumbing correctness fix).
last_review: R370
