# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R315 (E105+E106, BUILD) — eyes 2nd-witness blocker MOVED from model-capability to rubric-wording: minicpm-v is the FIRST local VLM to discriminate go2 pose, but only under NEUTRAL framing the FROZEN rubric doesn't use → new moat gate G-315-1.
  §0/§1a FLOOR: adopted supervisor R314 rounds.jsonl append + regen BOARD (ages +1). Inflight empty; no WIP; G-183-12 already answered R183; check.sh green.
  §2-4 (non-gated, no billing/spine): pulled minicpm-v (5.5GB ollama) as the "another local VLM" next#1 test + wrote tools/acceptance/pose_probe.py (isolated forced-choice pose Q; non-spine).
  E105 (research, REFUTED): qwen2.5vl:7b is POSE-BLIND (not merely the E104 presence-stamp) — a 3-framing forced-choice bracket (rubric/fault/neutral) gives an IDENTICAL answer to a clearly-upright AND a clearly-toppled go2 EVERY time (tracks the prompt's leading word, never the image). No rubric reword rescues qwen.
  E106 (research, CONFIRMED w/ caveat): minicpm-v HAS a real pose signal — NEUTRAL forced-choice 3/3 STANDING(upright) & 3/3 LYING(toppled), deterministic & model-specific (qwen fails the SAME neutral prompt). CAVEAT: (a) fault-primed prompt collapses minicpm to FALLEN-both; (b) FROZEN rubric robot_present_upright is presence/fault-framed → its parser ABSTAINs(upright)/flips(toppled) on minicpm (raw minicpm says "Yes..upright" = a PARSE artifact, not blindness).
  HEADLINE: model capability is NO LONGER the eyes-pillar blocker. The blocker is the frozen rubric wording+parser, whose change is an ADR-002 MOAT/SPINE gate (G-315-1 queued). Both rows provisional; no acceptance row (judge-quality probe, not the acceptance face). NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier: eyes 2nd-witness now MODEL-SOLVED (minicpm-v, E106) but RUBRIC-GATED (G-315-1); every OTHER new capability CEO-gated S4/D182/D76).
last-round: R315 (E105 refuted qwen pose-blindness; E106 minicpm-v first local pose discriminator under neutral framing). Queued moat gate G-315-1. No spine path touched.
frontier: eyes-pillar lever RESOLVED at the model layer (E106): minicpm-v deterministically reads go2 pose (3/3 up/3/3 down) under NEUTRAL framing where qwen is pose-blind (E105) & gemma never grades (E92). Remaining blocker = the FROZEN rubric's presence/fault-framed robot_present_upright wording+parser = ADR-002 moat gate (G-315-1). All OTHER new capability CEO-gated: S4 (3rd embodiment BYO-URDF), D182 (NL grounder), quantity N-object (D76 planner-plugin).
watch: POSE-PROBE recipe (non-spine, local ollama, no billing): tools/acceptance/pose_probe.py <model> <N> <frame..> (forced UPRIGHT/FALLEN) + judge_determinism.py <frame> <model> <N> (frozen rubric). minicpm-v needs NEUTRAL framing (STANDING/LYING); fault-priming biases it FALLEN-both. Triplet: var/evidence/R314/{upright_control,toppled_fault,robot_absent}.png; render fresh via var/evidence/R314/render_toppled.py. NEVER 2nd MuJoCoGo2 renderer (segfault); NEVER `ulimit -v` a GL/EGL render. Local VLM cold-load ~90s; warm via curl keep_alive first.
  REAL-FACE (only IF G-315-1 approved + rubric neutralized): bare vcli + NL; FORCE deepseek+v4-flash (default qwen ROUTING Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`; eyes via VECTOR_JUDGE_LOCAL_MODEL=minicpm-v:latest. CAVEAT: the LIVE harness judge times out 60s (vision_judge._call_vlm) — a CPU-slow local VLM ABSTAINs there even if the offline probe PASSes.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; `free -g` + `pgrep -af repl_accept` before launch; scripts/sim-teardown after. NEVER pkill mujoco. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (Chinese=3B/char), each string field ≤280 CHARS (trim). experiments key `e` NOT `experiment`; status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds append shifts ages → NEXT round MUST regen BOARD.
next:
  1. [GATE-FIRST — eyes path is now G-315-1 gated] If G-315-1 unapproved: do NOT edit config/visual_acceptance_rubric.yaml (moat). NON-gated pivot: does minicpm-v's neutral-framing pose signal hold on MORE fault frames (sunk/clipping/duplicated-body + a 2nd upright control, N≥3 each)? Bank provisional — strengthens the G-315-1 case without touching the spine.
  2. [OWNER GATE — ONLY path to NEW capability] Unblock S4 (3rd embodiment BYO URDF+manifest) OR D182 (world-owned NL→object grounder) OR D76 planner-plugin (quantity decompose). All CEO-gated ~60r. G-315-1 (rubric neutralization to land minicpm eyes) is a NEW, well-localized moat gate — cheapest owner call.
  3. [NEXT REVIEW = R320] Skeptic-re-verify oldest confirmed rows; re-red-team a headline; adjudicate provisionals (incl. E105/E106); wiring audit; ambition critic (plateau check).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-315-1 SPINE/MOAT (eyes rubric wording): minicpm-v (E106) discriminates go2 pose deterministically ONLY under NEUTRAL framing; the FROZEN visual_acceptance_rubric robot_present_upright item is presence/fault-framed so its parser misses the signal. Landing minicpm as real eyes needs that item reworded neutral = ADR-002 moat change (checksum-pinned, dispatcher-owned). Owner call owed. <- next#1/#2. (R315/E106)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R315 CLEAN. R315 touched NO spine path (pulled minicpm-v; new tools/acceptance/pose_probe.py; 2 experiments rows E105+E106 + LESSONS E104→E105/E106 extension + STATUS + BOARD regen + 1 gates.jsonl queued row; NO code under vector_os_nano/, NO acceptance/ or config/rubric edit).
last_review: R310
