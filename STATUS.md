# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R370 (E159, REVIEW). HEADLINE: §7 janitor+skeptic, both levers SHUT (48th sim-block). FIRST JOB: cleared the quarantine (previous round left BOARD.md stale — `python3 loop/board.py` regen, doc-gate OK). SKEPTIC: re-red-teamed the newest headline E158 (piper offset guard) as if I hadn't written it — it SURVIVES. Independently confirmed the claim is TRUE, not just the test: `mujoco_go2._LIDAR_OFFSET_X/_Z=0.3/0.2` and `piper_ros2_proxy._BODY_SENSOR_DX/_DZ=0.3/0.2`, and `_sync_ik_base` SUBTRACTS the offset (`sx - cos_h*DX`, `sz - DZ`) — the exact inverse of the go2 driver which ADDS it — so it IS the same physical offset, a real 4th mirror. Fresh mutation drift DX 0.3→0.35 ⇒ guard RED; reverted ⇒ 11/11 green. WIRING: native-loop card was 29 rounds stale (verified-against a52226e/R339, >25 cap) — checked prose against source, all 4 load-bearing consts match (_MAX_NATIVE_TURNS=24, _MAX_VERIFY_NUDGES=2, _UNPRODUCTIVE_NUDGE_AT=6, _MAX_TURNS_WITHOUT_VERIFY=12), stamp refreshed a52226e→6098b35 (verify-spine+embodiments cards are R359, within cap). MEMORY: Casebook 12<15 (no fold), LESSONS tight, no dead pointers. GATE/TOKEN AUDIT clean R360..HEAD (2 grep hits both self-referential audit notes = 0 real approvals). Provisionals owed=0 (check.sh GREEN exempts backfill+superseded; confirms E144). No janitor defect found beyond the quarantine.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; a clean §7 review round — quarantine cleared, one headline re-red-teamed SURVIVES, one stale WIRING card re-verified accurate + refreshed, no regressions, no manufactured sliver.)
last-round: R368 (E158 BUILD: guarded piper_ros2_proxy _BODY_SENSOR_DX/_DZ as the 4th lidar-offset mirror E151 missed; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2 (4 copies: driver+manifest+bridge+piper-proxy, E147-E151 corrected E158)+g1, vcli+acceptance verdict contracts (E152/E153), Inv-4 kernel-import purity seam allowlist (E154), causation-baseline frozen (E155), embodiment-config morphology schema (E156), arm-playground catalog↔MJCF (E157). Spine + config-schema frozen + FULL mirror coverage COMPLETE. The R312-R370 acceptance-face plateau (58 rounds) is REAL + HARD-EXTERNAL (not a local hill): the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R370). Substrate-hardening finds (E152-E158) are increasingly marginal — a domain expert would say the loop is polishing invariant-guards while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill.
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` to confirm a LIVE run not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline capability axis, (b) re-audit an already-audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144, re-confirmed R370: check.sh GREEN, owed=0), (f) re-frame the per-round "BOARD.md stale" check.sh RED (mechanical age+1 drift, E102/E145), (g) re-force the lidar-offset mirror — SATURATED across all 4 go2 copies + g1 (E151+E158), (h) re-force Inv-4 import guard — allowlist-covered (E154), (i) re-force a SPINE frozen-dataclass guard — spine COMPLETE (E152/E153/E155), (j) re-force embodiment-config schema — COMPLETE (E156), (k) re-force arm-playground catalog↔MJCF — COMPLETE (E157). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E158 = seven found post-saturation; E158 shows even a "SATURATED" vein can hide a missed copy — grep the pinning comment across ALL drivers/proxies). Candidates if a gap is real: a load-bearing const with a comment-pinned mirror not yet ast-guarded (grep "must match"/"kept in sync"/"must equal" across `vector_os_nano/`), a hardcoded enumeration that should introspect, or a real test-coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R370: sibling isaac warehouse_nav.py LIVE, pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (both enforced by checks_schema.py — a >cap row goes check.sh RED; R370 trimmed E159 to fit). experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R370 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (lidar-offset ALL 4 go2+g1 E151/E158, spine frozen E152/E153/E155, Inv-4 import E154, config-schema E156, catalog↔MJCF E157): a comment-pinned const mirror not yet ast-guarded (grep "must match"/"kept in sync" across `vector_os_nano/`), a hardcoded enumeration that should introspect, or a real test-coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, re-investigate the preflight nag (E144) or BOARD age drift (E145).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E158 re-red-teamed E150/E159): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 53 rounds (R318..R370). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R370 §7): gate/token last-review CLEAN — `git log R360..HEAD grep GATE-APPROVED/CEO-APPROVED` = 2 hits, BOTH self-referential audit notes ("grep ... = 0"), 0 real approvals. R370 touched ONLY docs/WIRING.md (stamp) + STATUS + ledger/BOARD + events — no spine/manifest-gated path. No self-approval.
last_review: R370
