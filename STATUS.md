# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R329 (E120, BUILD-forced/research). HEADLINE: SIM-BLOCKED AN 11TH TIME + owner-gate-blocked. R328 already re-audited the plateau's honesty, so instead of repeating that (treadmill) OR manufacturing a banned 12th offline proof, R329 did a GENUINE research probe for an overlooked NON-GATED exit — targeting the ONE North-Star leg STATUS/frontier never examined: RECOVER (plan·route·verify·RECOVER). FINDING (E120, refuted-as-exit): recover is ALREADY BUILT — `cognitive/vgg_harness._obs_replan_decompose` + `_retry_strategy` (observation-driven replan/decompose), `core.config.max_planning_retries`, skill retries (`skills/pick.py`); a src grep for top-level replan symbols returned 0 only because it lives under cognitive/. So recover is NOT a non-gated frontier. No non-gated exit exists. Doctor re-run GREEN (G-323-1 READY-TO-LAND 4/4 additive, decision current for the owner). NO spine/code edit (experiments E120 + 1 LESSONS line + STATUS + regenerated BOARD).
  §1 ORIENT: inflight absent; no WIP; experiments provisionals: none (E116/E117/E118 confirmed, E119 review). 67 acceptance provisionals all check-exempt → check.sh GREEN. inbox: no unanswered decisions.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE+SIM-blocked: every acceptance-face advance is CEO-gated on G-323-1 (owner) AND the one sim slot is held by a sibling isaac loop — S4 · D182 · D76 also queued)
last-round: R329 (E120 research: probed the RECOVER North-Star leg for a non-gated exit → recover already built (cognitive replan + planning-retries + skill retries), NOT a gap. Sim-blocked 11th. Doctor GREEN. No offline proof added).
frontier: The offline plug-and-play Verify axis stays FULLY CLOSED (seam E109-E115, classifier E116, docs E117, runtime end-to-end E118, re-audited honest E119). The RECOVER leg is now confirmed ALREADY BUILT (E120) — NOT an overlooked exit. The 17-round acceptance-face plateau (R312-R329) is REAL, STRUCTURAL, RE-AUDITED HONEST (E119) and now searched-for-exit-and-found-none (E120). Manufacturing another offline proof (5th oracle / 2nd guard / BYO-skill offline / re-prove E116-E118 / re-audit recover) is TREADMILL and BANNED. The ONLY two honest levers: the sim slot frees, OR the OWNER approves G-323-1. Escalated to owner (exec summary, R329).
watch: A future forced build round must NOT (a) add another offline plug-and-play proof, NOR (b) re-audit an already-audited axis (plateau-honesty R328/E119, recover R329/E120) — both HIDE the plateau behind motion. If forced with both levers shut and no genuinely-new axis left, HOLD cleanly (phase blocked) — do NOT manufacture green. Do NOT reopen eyes-VLM pose/heading (refuted E105-E108).
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND (confirmed GREEN 4/4 this round). Run it BEFORE landing G-323-1.
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. (Isaac ≠ MuJoCo but Inv-5 is CATEGORICAL: one sim at a time; do NOT reinterpret it to self-unblock — that is a spine/safety gate.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (E120 landed 960 B), each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R329 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (confirm READY-TO-LAND, GREEN this round), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` (bare-idiom, E116) + rubric (one low-risk diff, no interface change). Consolidates G-318-1 · relational-near · G-322-1. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] Do NOT manufacture another offline plug-and-play proof (banned). Do NOT re-audit an already-audited axis (E119 plateau-honesty, E120 recover). Only a genuinely-new, North-Star-advancing, non-spine axis qualifies; if none, HOLD cleanly (phase blocked). Do NOT reopen eyes-VLM pose/heading (refuted E105-E108).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN R329, bare-idiom rationale E116, adoption-doc'd E117, runtime-proven-for-BYO E118, plateau re-audited-honest E119, recover-axis ruled-out E120): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114-packaged 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API (get_position/get_heading/get_odometry quat) ⇒ NO interface change (doctor confirms on the live tree, 4/4 GREEN this round); the `_PREDICATE_ORACLES` edit is REQUIRED for the bare-predicate idiom (E116), not an interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. One low-risk diff. OWNER CALL OWED — queued 12 rounds (R318..R329). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - META (world-declared predicate-oracles): a BYO world declaring its bare-predicate oracles (so bare `pred()` grounds without editing `_PREDICATE_ORACLES`) is a SPINE/interface gate — NOT needed for the contract (E116 `pred()==True` is the zero-edit path, doc'd E117, runtime-proven E118). Nice-to-have. (R325/E116)
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — the IDENTITY half the quartet's geometry channels leave to the VLM (E46, E112/E113). <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R329 CLEAN. R329 (research) touched NO spine path (appended experiments E120; +1 LESSONS frontier line; overwrote STATUS; regenerated BOARD; NO test/acceptance/rubric/interface/classifier/_PREDICATE_ORACLES/engine edit).
last_review: R328
