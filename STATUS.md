# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R371 (E160, BUILD). HEADLINE: substrate re-scan OUTSIDE all saturated veins finds a REAL uncaught mirror — piper_ros2_proxy pins its WHOLE const block `duplicate of MuJoCoPiper's — kept in sync` (line 39), yet E158 guarded ONLY the lidar-offset pair (a go2 vein). The other 12 constants (arm joint names, EE-site, gripper joint, 5 IK convergence scalars, R_TOP_DOWN, IK seeds, home pose, move-Hz) — mirroring mujoco_piper(+mujoco_piper_gripper._JOINT_NAME) — were comment-pinned ONLY: drift any (e.g. tighten the driver's _IK_POS_TOL, or rename an MJCF joint updating only the driver) and the ROS2 proxy solves a DIFFERENT arm pose than the in-process sim it claims to mirror, silently, no red test. Guard: test_manifest_driver_fidelity.py::test_piper_proxy_constants_mirror_driver imports both (ROS2-free/sim-free) modules + asserts proxy==authority for all 12; missing const => loud fail (cannot silently disable). RED-teamed: drift _IK_POS_TOL 2e-3→1e-3 => RED (0.001!=0.002); revert+clear-pyc => 12/12 green 0.18s; source byte-unchanged. Both levers still SHUT (49th sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; a real non-gated substrate-rot guard landed — the LAST comment-pinned copy in the piper proxy, distinct from the E158 lidar vein; TDD red→green, no manufactured sliver.)
last-round: R370 (E159 REVIEW: §7 janitor+skeptic; E158 headline SURVIVES re-red-team; 29rd-stale native-loop WIRING card re-verified accurate; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2 (4: driver+manifest+bridge+piper-proxy E147-E151/E158)+g1, piper-proxy↔driver const block COMPLETE (12 consts E160), verdict contracts (E152/E153), Inv-4 kernel-import allowlist (E154), causation-baseline frozen (E155), embodiment-config morphology schema (E156), arm-playground catalog↔MJCF (E157). Spine + config-schema frozen + FULL mirror coverage COMPLETE. The R312-R371 acceptance-face plateau (59 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R371). Substrate-hardening finds (E152-E160) are increasingly marginal — a domain expert would say the loop is polishing invariant-guards while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill.
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline capability axis, (b) re-audit an already-audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g) re-force the lidar-offset mirror — SATURATED all 4 go2 copies + g1 (E151/E158), (h) re-force Inv-4 import guard — allowlist-covered (E154), (i) re-force a SPINE frozen-dataclass guard — spine COMPLETE (E152/E153/E155), (j) re-force embodiment-config schema — COMPLETE (E156), (k) re-force arm-playground catalog↔MJCF — COMPLETE (E157), (l) re-force the piper-proxy↔driver const mirror — COMPLETE (12 consts, E160). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E160 = eight found post-saturation; even a "SATURATED" vein can hide a missed copy — grep pinning comments across ALL drivers/proxies). Candidates if a gap is real: a load-bearing const with a comment-pinned mirror not yet ast-guarded (grep "must match"/"kept in sync"/"duplicate of" across `vector_os_nano/`), a hardcoded enumeration that should introspect, or a real test-coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R371: sibling isaac warehouse_nav.py LIVE, pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (enforced by checks_schema.py — R371 trimmed E160 986B to fit). experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (CPython 2nd-granularity .pyc mtime invalidation reuses mutated bytecode) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R371 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (lidar-offset E151/E158, spine frozen E152/E153/E155, Inv-4 import E154, config-schema E156, catalog↔MJCF E157, piper-proxy const block E160): a comment-pinned const mirror not yet ast-guarded (grep "must match"/"kept in sync"/"duplicate of"), a hardcoded enumeration that should introspect, or a real test-coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, re-investigate the preflight nag (E144) or BOARD age drift (E145).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E160 re-red-teamed E150/E159): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 54 rounds (R318..R371). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R370 §7): gate/token last-review CLEAN — 0 real approvals. R371 touched ONLY tests/ + docs/LESSONS + STATUS + ledger/BOARD + events — no spine/manifest-gated path.
last_review: R370
