# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R668 (E191, BUILD). HEADLINE: **broke the HOLD reflex again with a REAL fresh non-gated fix — the BLOCKING-locomotion primitives silently accepted NaN/±inf**. `walk_forward(distance_m)`/`turn(angle_rad)` are the FIFTH external-input-finiteness boundary (E187 loader · E188 arm move_joints · E189 base velocity · E190 nav goal — now the blocking-primitive threshold). UNLIKE the prior four, the numeric arg is a loop-TERMINATION threshold NEVER passed to `set_velocity`, so the E189 velocity gate STRUCTURALLY cannot catch it: `covered >= abs(nan)` / `delta >= abs(nan)` is always False ⇒ a NaN/inf distance/angle drives REAL motion at the default speed for the FULL 30s/15s timeout, then returns False (looks like a timeout while the robot physically moved ~12m/2.4rev — invisible to the honest-verify spine). REACHABLE via untrusted LLM plan JSON: `goal_decomposer.py:490` uses bare `json.loads` which ACCEPTS `NaN`/`Infinity` tokens (`{"distance":NaN}`→`float('nan')`), `strategy_selector.py:181` passes it straight through to `walk_forward`. A security-floor violation ("reject NaN/inf before acting"). Fix = ONE pure `ensure_finite_travel(value,axis)` in `vcli/primitives/locomotion.py` wired at the entry of `walk_forward`+`turn` (loud axis-scoped ValueError BEFORE any `set_velocity`) + an ast source-scan mirror test (a new blocking primitive can't silently skip it). RED (nan HANGS the 30s loop — the bug, directly observed)→GREEN 13/13; 257/257 regress clean (incl. `test_walk_forward_displacement`, no false-reject of finite targets). Red-team ALL pass: json NaN token→float(nan) reachable via the untrusted plan path; `covered>=abs(nan)` always False ⇒ genuine fail-OPEN (RED tests hung 30s); finite passes 257 ⇒ no false-reject; loud raise ⇒ strictly stricter. NON-spine (vcli/primitives/locomotion.py not in check.sh SPINE), no signature change ⇒ NOT a CEO gate, self-crossed like E187-E190. Both levers RE-VERIFIED SHUT cold: (1) SIM — sibling isaac go2w warehouse_nav LIVE (pids 3751659/65/72 + nav stack 3751367/93/3751401, unchanged) holds the ONE slot (Inv-5 WAIT); (2) OWNER — inbox only long-answered G-183-12; gates.jsonl ZERO answers for G-323-1 (queued 85 rounds)/G-383-1/G-354-1. (80th sim-block — but a REAL build, not a HOLD.)
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R668 landed a fresh non-gated external-input-validation hardening at the BLOCKING-locomotion primitive boundary; both levers still SHUT).
last-round: R668 (E191 build: NaN/inf silent-accept at the walk_forward/turn loop-termination threshold FIXED — `ensure_finite_travel()` gate at entry of both blocking primitives + ast mirror test, RED nan-hangs-30s→GREEN 13/13, 257/257 regress, red-team clean, json-NaN reachability caught, LATENT/self-crossed non-spine).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED. SATURATED veins: name-list (E173-E175/E116/E118), in-kernel hardcoded-SET (E177), hardcoded-NAME exclusion (E178), numeric-threshold-in-verify (E180), tool-registry silent-overwrite (E181), vocab-derivation (E182), persona-swallow (E183), skill-registry-routing-swallow (E184); silent-swallow class grep-PROVABLY empty (E185). EXTERNAL-INPUT FINITENESS vein now spans FIVE boundaries: manifest loader (E187) + ARM actuator move_joints (E188) + BASE velocity set_velocity (E189) + NAV-GOAL navigate_to/publish_goal (E190) + BLOCKING-locomotion loop-termination threshold walk_forward/turn (E191, the first NON-actuator, NON-planner sink). R312-R668 plateau REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. Highest-leverage unblock = owner acts on G-323-1 (queued 85 rounds).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — BROAD `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac|warehouse_nav'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot [narrow `isaac` alone MISSES the `python3`-named warehouse_nav], inbox+gates.jsonl for owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis (tools/verify/vocab/persona/skill-registry/loader/arm-actuator/base-velocity/nav-goal/blocking-locomotion finiteness all scanned), (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144; authoritative owed=0), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145 — just `python3 loop/board.py`), (g) re-force any SATURATED vein. NEVER run `tests/hardware/sim/` OR `tests/unit/hardware/sim/` under bare pytest (spawns MuJoCo; the latter tore down clean R668 but contends the ONE slot) — only `scripts/run-tests`. Before mining ANY "new" gap: grep ledger + docs/LESSONS.md FIRST — if E#-recorded/gated/benign, it is NOT a find.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check BROAD `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all; NEVER `echo "$EMPTY" >>` (an empty var appends a blank line ⇒ check.sh RED). Caps: line ≤1024 BYTES (R668 E191 line=942B), each string field ≤280 CHARS. status ∈ {confirmed,inconclusive,plateau,refuted} for experiments; key `e`. board: `python3 loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R668 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check BROAD `pgrep` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet; ALSO subsumes the moat _PREDICATE_ORACLES BYO-bare-predicate fix, E116/E118). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] the non-gated re-scan space is NARROW but R664-R668 proved not fully dry — E187 (loader) + E188 (arm) + E189 (base vel) + E190 (nav goal) + E191 (blocking-locomotion threshold) all found fresh external-input-finiteness veins. Remaining candidate untrusted-numeric sinks — but the ADJACENT ones are CHECKED-benign (do NOT re-chase): navigation.py `wait_until_near(tolerance)` = fail-SAFE (has `timeout=60` deadline + commands NO motion, just polls then returns False on NaN tolerance); perception.py `measure_distance(angle_rad)` = fail-safe (`int(round(nan/inc))` RAISES). Genuinely-open candidates only if new APIs appear: gripper width/force IF a set-width API is added. Before claiming ANY new gap, grep ledger+LESSONS to confirm non-duplication AND that it is fail-OPEN not fail-safe (a downstream loud check / int()-raise = not a find). ONLY a genuinely fresh, on-disk, reachable, non-duplicate fail-open → fix it; else → HOLD (one plateau experiments row). Do NOT manufacture an audit sliver.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367; fresh-axis-negative E180/E181/E182, fresh-axis-FIXED E183/E184/E187/E188/E189/E190/E191): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. Its `_PREDICATE_ORACLES` edit ALSO closes the moat BYO-bare-predicate gap (E116/E118). OWNER CALL OWED — queued 85 rounds (R318..R668). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R390 §7): `git log --grep='GATE-APPROVED\|CEO-APPROVED'` since R380 = ZERO hits. R668 = a BUILD/fix: an OFFLINE non-spine self-crossed input-validation fix (E191 blocking-locomotion finiteness gate in vcli/primitives/locomotion.py; no interface signature change) — no interface/dep/hardware-contract/security-policy/spine/manifest edit ⇒ NOT a CEO gate, no token needed. Nothing self-crossed a gate.
last_review: R390
