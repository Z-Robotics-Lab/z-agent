# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R311 (E102, BUILD) — R310-append clock-tick surfaced 2 newly-STALE age31 rows (both R280-derived); consolidated the degenerate duplicate key + re-verified the last on the REAL face. BOARD back to 0 ⚠ STALE.
  (E102) §1a floor: adopted the supervisor R310 rounds.jsonl append (ages +1) + regen BOARD (opened check.sh RED, same mechanism R309/R310).
  §1b arithmetic: STALE_AGE=30 strict — both R280 rows were age30 at R310's commit (NOT a mis-verify); the R311 append ticked them to 31 = newly stale. The skeptic cadence working, not a bug.
  HYGIENE FIX (no sim): runner.degenerate-spin-guard.no-regression is a REDUNDANT duplicate BOARD key of base runner.degenerate-spin-guard (both = the E76 healthy-fetch guard-SILENT assertion; R310 freshly verified that identical content under the base key at age1). Folded the alias into the base via a superseded row → drops one perpetual double-stale clock.
  REAL-FACE re-verify: fetch.nl-novel-geometry-purple-box (age31 STALE) GROUNDED 1/1 on the shipped bare face (deepseek-v4-flash + local gemma4:e4b judge). '把紫色的盒子拿过来' → holding_object(pickable_box_purple) actor=CAUSED (GT weld+lift, unauthorable), launch_explore_seen=False in-proc. Judge FIRED ABSTAIN (E72 non-determ, fail-closed GT carries). Eyes LOOKED (var/evidence/R311/purple/eyes_fetch.png): purple BOX aloft, green/blue/red cylinders remain on the pedestal = colour+geometry discrimination. supersedes R280 (provisional; §1b next round promotes).
  HEADLINE: BOARD is 0 ⚠ STALE again + one fewer duplicate key. R262→R311 (~49r) is treading water — every NEW-capability axis remains CEO-gated; owner MUST act on S4/D182/D76 to advance the North Star.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (loop at the R290/R262 plateau, ~49r hygiene. Non-gated backlog ~exhausted: BOARD 0 stale, eyes uniform vlm-judge, quantity settled-as-gated, degenerate key de-duped. NEW capability blocked on owner S4/D182/D76.)
last-round: R311 (E102 build). Consolidated the degenerate duplicate key (superseded, no sim) + re-verified the last stale row novel-geometry-purple-box on the real face GROUNDED 1/1 (provisional, eyes LOOKED). BOARD 0 stale. No spine path touched.
frontier: Verify-pillar eyes CLOSED (vlm-judge WIRED+FIRES, recorded-not-trusted; gemma4:e4b non-determ PASS↔ABSTAIN↔FAIL). A TRUSTED 2nd discriminator needs a stronger VLM — CONCRETE next-lever: pull a stronger local ollama VLM (e.g. qwen2.5-vl-7b ~6GB) as the judge (non-gated config axis, no billing) OR funded qwen3-vl (BILLING-gated). Every NEW capability is CEO-gated: S4 (3rd embodiment BYO-URDF), D182 (world-owned NL grounder), quantity N-object decompose (D76 planner-plugin OR stronger routing brain — E100/E101).
watch: Real-face recipe: bare vcli + NL; FORCE deepseek + v4-flash via env (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`.
  HARNESS: R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl> (mirrors R310). A run ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; `free -g` + `pgrep -af repl_accept` before launch; scripts/sim-teardown after. NEVER pkill mujoco. Isaac go2w sibling = E54 non-blocker, LEAVE IT (NEVER-KILL-INFRA).
  TESTS: pure-unit only via `./scripts/run-tests <path>` (memory-capped 12G).
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (Chinese=3B/char), each string field ≤280 CHARS (redteam/result bite every round — trim). experiments key `e` NOT `experiment`; experiments status ∈ {confirmed,inconclusive,plateau,refuted} (NOT provisional — that is acceptance-only); acceptance eyes ∈ {human,self-read,vlm-judge} (superseded rows use self-read, NOT n/a); source∈{round,backfill}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds.jsonl append shifts ages → NEXT round MUST regen BOARD (R309→R310→R311 all opened check.sh RED on this).
next:
  1. [OWNER GATE — ONLY path to NEW capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder) OR the D76 planner-plugin (quantity decompose). All CEO-gated ~59r. Nothing non-gated advances the North Star until one is answered.
  2. [§1b ADJUDICATE] Promote the R311 fetch.nl-novel-geometry-purple-box provisional (GROUNDED 1/1, age0) → confirmed after the round boundary + red-team (or refute with supersedes).
  3. [NEXT REVIEW = R320] Skeptic-re-verify the then-oldest confirmed rows; re-red-team a headline; wiring audit; ambition critic. NON-GATED HYGIENE if any surfaces = only moat-hardening / regression-pinning of EXISTING behaviour (do NOT re-run quantity E100 do_not_retry, do NOT re-attempt S4/D182/D76 as a build — gated).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#1.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#1.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#1, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R311 CLEAN. R311 touched NO spine path (1 acceptance provisional + 1 acceptance superseded consolidation + 1 experiments row + LESSONS ambition line + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R310
