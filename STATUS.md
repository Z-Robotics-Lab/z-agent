# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R304 (E94, verify) — STATUS next#1+#2: promoted the 3 R303 warehouse-fetch vlm-judge provisionals (blue/purple/red) → confirmed N=2, AND upgraded the LAST stale self-read non-house fetch bar (courtyard-blue) self-read→vlm-judge. HEADLINE: warehouse fetch blue/purple/red now vlm-judge-eyes GROUNDED-CONFIRMED N=2 (E46 met across R303→R304 boundary); the eyes pillar is now UNIFORM (vlm-judge) across ALL non-house fetch/place bars. NOTE: the gemma4:e4b judge FIRED=PASS on all 4 runs this round (vs ABSTAIN in R303/R302) — a bonus independent-VLM datapoint, but verified= STILL rests only on the holding_object(colour) GT oracle (actor=CAUSED) + human LOOK, never the non-deterministic judge (E72/E92 fail-closed). LOOK on each verdict frame: correct colour aloft in gripper, other 4 pickables remain on the pick-table = discrimination (warehouse×3 + courtyard-blue).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~52r unanswered; non-gated NEW-capability frontier EXHAUSTED — loop treads on eyes-pillar upgrades of stale self-read bars + skeptic hygiene while the gates stand). Concurrent SIBLING sim this round: Dockerized Isaac go2w nav (/workspace/go2w, ~9GB) — NOT this repo's loop, NEVER-KILL-INFRA; honest OOM math (my MuJoCo runs hard-capped MemoryMax=24G, serial one-at-a-time; free dipped to 3-5G but avail stayed ~40G) → no host risk, proceeded.
last-round: R304 (E94, verify). §0/§1: BOOTSTRAP clean (preflight OK, no quarantine, inflight empty). §1a: adopted R303 supervisor rounds.jsonl append + regen BOARD (ages shifted, pure-append). §1b: adjudicated the 3 R303 warehouse-fetch provisionals by re-running each bare-face across the boundary → all GROUNDED N=2, banked confirmed rows superseding R303 provisionals. §1d: inbox G-183-12 already answered+crossed R183. §2-4: 4 sim runs (warehouse blue/purple/red re-run + courtyard-blue eyes-upgrade) bare-face vlm-judge; LOOKED at all 4 frames; banked 3 confirmed + 1 provisional acceptance row + E94.
frontier: Verify-pillar: eyes=vlm-judge now FIRES + CONFIRMED on warehouse fetch colours (N=2) AND place across HOUSE+courtyard+warehouse. After R304 the eyes pillar is UNIFORM (vlm-judge) across all non-house fetch/place bars; courtyard-blue is the last N=1 provisional (promote next round). RESIDUAL non-gated (local-hill): (a) promote courtyard-blue vlm-judge N=1→N=2; (b) skeptic-verify the genuinely-oldest stale bars (quantity-place.nl age~98 KNOWN brain-flaky, kernel.model-unavailable age~44). NEW capability still needs owner S4/D182. gemma4:e4b judge PASS↔ABSTAIN↔FAIL non-determ (E72/E92) — a TRUSTED discriminator needs a stronger VLM (funded qwen3-vl / better local).
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select env vars (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): ROOM_TEMPLATE={warehouse|courtyard}, SCENE_CLUTTER=1, SCENE_SWAP=1.
  HARNESS: launcher this round = var/evidence/R304/run_fetch.sh <room> <colour> <fetch_nl> (uses .venv/bin/python; systemd --scope has NO `python` on PATH). MODE=fetch → 1 fetch turn + eyes_frame; args = FETCH PLACE TAG fetch (place NL ignored but MUST be present; TAG must not be a valid mode word). eyes vlm-judge AUTO-routes to local ollama gemma4:e4b. Colour→noun: blue=蓝色的瓶子, purple=紫色的盒子(box), red=红色的罐子(can). RUN sim FOREGROUND, SERIAL one-at-a-time, under `systemd-run --user --scope -p MemoryMax=24G`; set VECTOR_EVIDENCE_DIR per run (E54 overwrite). Check `free -g` before each launch. NEVER pkill mujoco. A ~9GB Isaac python3 (/isaac-sim, go2w) is a SIBLING project's sim — leave it.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps (checks_schema.py): line ≤1024 BYTES, each string field ≤280 chars (bit me 4× this round — trim redteam/result/evidence). experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); a supervisor rounds.jsonl append shifts BOARD ages → NEXT round MUST regen.
next:
  1. [§1b adjudicate] Promote the R304 courtyard-blue provisional (vlm-judge GROUNDED 1/1) → confirmed: re-run bare-face across the R304→next boundary to meet E46 N≥2. Bank a confirmed row superseding the R304 provisional. Closes the eyes-pillar uniformity at confirmed strength.
  2. [SKEPTIC hygiene] Skeptic-verify the genuinely-oldest stale confirmed bars: quantity-place.nl (age~98, KNOWN brain place-decompose flaky) and kernel.model-unavailable (age~44) — re-run or refute on the real face; a stale green is worse than a known red.
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~52r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R281..R304 CLEAN. R304 touched NO spine path (ledger + STATUS + BOARD regen + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
