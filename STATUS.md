# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R320 (E111, REVIEW). §7 skeptic+janitor. HEADLINE: the real-face skeptic re-verify of the 2 oldest confirmed BOARD rows is SIM-BLOCKED — a SIBLING loop holds the one global sim slot (localPlanner+laser_mapping live, etime 3:35 at orient; Inv-5 one-sim-at-a-time + NEVER-KILL-INFRA ⇒ I did NOT touch it and launched NO competing sim). The 9 STALE BOARD rows (warehouse/courtyard/scene-clutter ~32r, all sim-requiring) stay stale, honestly unre-run. SIM-FREE substitute skeptic: re-verified the NEWEST headline E110 (deterministic pose pair) across the R319→R320 boundary — `prove_pose_pair.py` 4/4 correct (sunk_floor: orient STANDING MISS + height SUNK ⇒ pair FALLEN), pose units 24/24 (76M peak).
  §1 ORIENT: inflight absent; no WIP; inbox G-183-12 already answered (gates.jsonl). §1a: adopted supervisor R319 rounds.jsonl append (2b254d2, dur 650s) + regen BOARD (ages +1, now 9 STALE). §1b: E109/E110 are `build confirmed` experiments rows (experiments status enum has no `provisional`; the provisional→confirmed dance is acceptance.jsonl-only) — no pending adjudication. 67 acceptance provisionals all check-exempt (backfill/superseded/≤2r) → check.sh GREEN.
  §7 AUDITS: RE-RED-TEAM E110 as-if-not-mine → SURVIVES: 4/4 holds, thresholds margined (60° tilt, 12cm z) not knife-edge, honestly scoped an OFFLINE oracle on GT quat/z (Inv-1 unforgeable) GATED to reach the face (G-318-1), NO acceptance-face overclaim. GATE/TOKEN R310→R320: CLEAN (no new GATE-APPROVED/CEO-APPROVED; newest tokens are R183/R187). WIRING anchors all <25r (a7d3f1a=R300 ~20r, 6bfd0de=R310 ~10r) → no prose refresh due. CASEBOOK 12<15 → no fold. LESSONS 258→259/260 (added 1 ambition line; tight headroom — next review should consolidate the eyes-saga run-ons). PRUNED var/evidence R270-R299 (>20r, gitignored).
  AMBITION CRITIC (honest, reinforced): R312-R319 = 8 straight eyes-pillar probe rounds with ZERO acceptance-face advance. The pose lever is now SETTLED (orient E109 + height E110 hard-channel pair) yet SHELVED behind G-318-1. The face is GATE-blocked, not idea-blocked. The right move is the OWNER unblocking ONE gate, not another eyes probe.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE-blocked: every acceptance-face advance is CEO-gated — G-318-1 (land the pose pair) · S4 · D182 · D76)
last-round: R320 (E111 REVIEW: real-face skeptic SIM-BLOCKED by a sibling sim; sim-free E110 pose pair re-verified 4/4 as substitute; audits CLEAN; evidence pruned). No spine path touched.
frontier: eyes-POSE SETTLED+COMPLETE (deterministic orient E109 + height E110 pair, NOT a VLM). Non-gated pose frontier EXHAUSTED. 8-round plateau (R312-R319) is REAL — the loop treads water on a GATE-blocked face. Owner: unblock G-318-1 (cheapest, most-proven) to convert the proven pose channel into an acceptance-face verify channel; then S4 (3rd embodiment BYO-URDF) / D182 (world-owned NL→object grounder) / D76 (planner-plugin quantity).
watch: pose PAIR (non-spine, pure math, no sim):
  `python tools/acceptance/pose_upright.py <w> <x> <y> <z> [--thresh 0.5]` (STANDING/FALLEN);
  `python tools/acceptance/pose_height.py <root_z> [--stance-z 0.35] [--tol 0.12]` (SUNK/NOMINAL/AIRBORNE).
  Units: `tests/unit/test_pose_upright.py`(11) + `tests/unit/test_pose_height.py`(13) via `./scripts/run-tests`. Sim-free pair demo: `.venv/bin/python var/evidence/R319/prove_pose_pair.py`.
  SIM-SAFETY (Inv-5, R318/R320): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco'` (NOT just `mujoco`); WAIT if live, NEVER teardown a sim you did not launch. R320 was sim-blocked exactly this way.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; scripts/sim-teardown after. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (E111 trimmed result/redteam to fit). experiments key `e`; experiments status ∈ {confirmed,inconclusive,plateau,refuted} (NO provisional); acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds append shifts ages → regen BOARD.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing pose eyes into the acceptance face] G-318-1 (wire the pose PAIR — orient+height — as the pose verify-channel in acceptance/ + rubric = honest-verify-spine/moat). Owner call owed. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] a deterministic PLACEMENT-distance channel (object-in-region) to cut vlm-judge reliance on placement (D168 place-oracle territory) — weigh vs pushing the owner to unblock a gate. Do NOT run another eyes-VLM pose probe (E105-E108 refuted; pose is a hard channel now).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-318-1 SPINE/MOAT (pose verify-channel): E109+E110 prove a deterministic pose PAIR (orient `tools/acceptance/pose_upright.py` + height `tools/acceptance/pose_height.py`) reads go2 pose 4/4 across all R316 topologies where every local VLM failed on ≥1 (R320/E111 re-verified 4/4 sim-free). Landing it edits acceptance/ + the rubric (ADR-002 moat, checksum-pinned) = honest-verify-spine semantics. Per Invariant 1 STRICTLY better than a VLM. Owner call owed. <- next#2. (R318/E109, R319/E110, R320/E111)
  - G-315-1 SPINE (VLM pose rubric): MOOT FOR POSE — pose belongs to the hard-channel PAIR (G-318-1), not a reworded VLM rubric item. Superseded-in-spirit by G-318-1. (R315/E106..R320/E111)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R320 CLEAN. R320 (review) touched NO spine path (adopted R319 rounds append + regen BOARD; appended experiments E111; +1 LESSONS Frontier line; STATUS; pruned gitignored var/evidence R270-R299; NO code, NO acceptance/ or rubric edit).
last_review: R320
