# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R336 (E126, BUILD). HEADLINE: 17th SIM-BLOCK + owner-gate-block → clean HOLD. Both honest levers independently reverified from DISK (not prior STATUS prose): (1) sim slot HELD by a LEGIT ACTIVE isaac go2w warehouse_nav sibling — pid 3569767 etime 1h24m (+11m vs R335's 1h13m), RSS 8.9G → still advancing across the round boundary, so the block is TRANSIENT not a deadlock and not a leak; Inv-5 categorical (one sim at a time) ⇒ cannot launch the MuJoCo acceptance sim. (2) G-323-1 queued(R323), no answered/crossed; inbox only old G-183-12 (answered+crossed). Ollama local VLM route still UP but sim-gated → no new axis. No genuinely-new NON-gated axis (verify CLOSED E109-121, robot=S4 gate, policy/skill/capability demos=BANNED offline proofs, model=local-route-live-but-sim-gated, recover built E120, substrate rot-free E121, axis-exhausted E122-E125). → phase: blocked. E126 (plateau) banked. Inv-5 + NEVER-KILL-INFRA intact; no sim launched, sibling untouched.
  §1 ORIENT: preflight GREEN (no quarantine). inflight absent; no WIP. experiments provisionals: 0. 67 acceptance provisionals check-exempt. inbox: G-183-12 answered+crossed; NO unanswered decision. Both levers reverified from disk (ps etime/RSS + pgrep + inbox + gates.jsonl + curl ollama).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: blocked (§6 clean stop: sim slot held by a LEGIT ACTIVE ~1h24m isaac sibling AND owner gate G-323-1 unanswered; non-gated frontier EXHAUSTED R329-R336. Block is TRANSIENT (sim frees when the sibling finishes). The loop parks and self-rechecks the levers each cold start; it is NOT dead. Owner gate now 19 rounds unanswered (R318..R336).)
last-round: R336 (E126 research: 17th sim-block; reverified both levers SHUT from disk — sim sibling etime advanced 1h13m→1h24m across the boundary (live, not leak); G-323-1 still unanswered; Ollama UP but sim-gated; clean HOLD, no manufactured sliver. Real-face BOARD skeptic re-run still OWED — 17th block).
frontier: The offline plug-and-play Verify axis is FULLY CLOSED (seam E109-E115, classifier E116, docs E117, runtime end-to-end E118, re-audited honest E119); RECOVER already built (E120); substrate ROT-FREE (E121); axis-exhausted (E122-E126). The 23-round acceptance-face plateau (R312-R336) is REAL, STRUCTURAL, re-audited HONEST. The ONLY two honest levers: the sim slot frees (transient — sibling is a real active run), OR the OWNER approves G-323-1. Escalated (exec summary, R329; re-surfaced R335, R336).
watch: While phase: blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+ `ps -o etime,rss` to confirm the holder is a LIVE run, not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) add another offline plug-and-play proof, (b) re-audit an already-audited axis (plateau-honesty E119, recover E120, rot E121, axis-exhaustion E122-E126), NOR (c) invent an audit sliver to fake motion — all HIDE the plateau. Do NOT reopen eyes-VLM pose/heading (refuted E105-E108). Do NOT reinterpret Inv-5 to self-unblock the sim.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run it BEFORE landing G-323-1 (GREEN 4/4 last at R329).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. (Isaac ≠ MuJoCo but Inv-5 is CATEGORICAL: one sim at a time — a spine/safety gate.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  QUARANTINE DRILL: if preflight fails on `loop/.state/quarantine`, read it, fix the named breach (stale BOARD → `.venv/bin/python loop/board.py`), `rm loop/.state/quarantine`, re-run preflight. That is the round's FIRST job.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R336 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (confirm READY-TO-LAND), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` (bare-idiom, E116) + rubric (one low-risk diff, no interface change). Consolidates G-318-1 · relational-near · G-322-1. Also queued: S4 · D182 · D76.
  3. [IF STILL BLOCKED next round] Reverify both levers (watch above). Both still shut AND no genuinely-new non-gated axis → HOLD again (phase blocked), one experiments row saying so; do NOT manufacture an audit sliver. Only a real lever-open or a genuinely-new North-Star axis ends the hold.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN R329, bare-idiom rationale E116, adoption-doc'd E117, runtime-proven-for-BYO E118, plateau re-audited-honest E119, recover ruled-out E120, substrate rot-free E121, axis-exhausted E122-E126): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114-packaged 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API (get_position/get_heading/get_odometry quat) ⇒ NO interface change; the `_PREDICATE_ORACLES` edit is REQUIRED for the bare-predicate idiom (E116); Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. One low-risk diff. OWNER CALL OWED — queued 19 rounds (R318..R336). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - META (world-declared predicate-oracles): a BYO world declaring its bare-predicate oracles is a SPINE/interface gate — NOT needed for the contract (E116 `pred()==True` is the zero-edit path, doc'd E117, runtime-proven E118). Nice-to-have. (R325/E116)
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — the IDENTITY half the quartet's geometry channels leave to the VLM (E46, E112/E113). <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared — but perception VLM has a live local Ollama route) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R336 CLEAN (no GATE-APPROVED/CEO-APPROVED tokens). R336 (build) touched NO spine path: experiments E126 + STATUS only; NO test/acceptance/rubric/interface/classifier/_PREDICATE_ORACLES/engine/code edit; sim never touched, sibling untouched.
last_review: R330
