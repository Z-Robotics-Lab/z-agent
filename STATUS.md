# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R701 (E225, BUILD). HEADLINE: **plateau-break SURVIVES the round boundary — the acceptance face is CONFIRMED runnable, ladder-base green fetch GROUNDED N≥2.** Cold-independent re-run (NOT inherited from R700): repo gate `pgrep -f 'mujoco|vcli'` CLEAR (same go2w Isaac sibling alive — different engine, does NOT trip the gate), 38G RAM available + 7.5G VRAM free with the sim present. Ran the bare `vector-cli` HOUSE green fetch FOREGROUND (deepseek-chat brain + gemma4:e4b eyes, ulimit -v 50G GL-safe): **fetch_verified=True 1/1** (deterministic MOAT, GT unauthorable), launch_explore_seen=False (in-process path ran). vlm-judge witness=**PASS on ALL FOUR** fields incl `workspace_in_frame=yes` — the R700 `workspace_in_frame=no` did NOT recur ⇒ transient framing quirk, NOT a rubric defect (dissolves the R700 CANDIDATE-gate). Human look CONFIRMS var/evidence/R701/eyes_fetch.png: green cylinder aloft in gripper, blue+magenta+red REMAIN on box = colour discrimination, robot upright/intact. §1b adjudicated: fetch.nl-plain-colour provisional→**confirmed** (R700 1/1 + R701 1/1 = E46 N≥2); E223 (over-read finding) + E224 (live GROUNDED) both CONFIRMED via E225. Scoped teardown clean (12 PIDs); Isaac untouched (Inv-8); host stayed >30G avail.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The acceptance face RUNS with a sibling Isaac sim present given headroom — now confirmed across TWO independent rounds. Abundant non-gated work remains: the ~30 STALE BOARD rows to re-verify on the live face + breadth. Re-check the ACTUAL gate + `available` RAM/free VRAM each round; HOLD only if the gate is dirty OR headroom genuinely absent (available<~10G or free VRAM<~3G).
last-round: R701 (E225 verify, confirmed). Boundary-crossing re-verify of the R700 plateau-break: face runnable + green fetch GROUNDED with full vlm-judge PASS. fetch.nl-plain-colour → confirmed. Sim torn down scoped. Self-approval audit: NO code/interface/spine/manifest edit this round ⇒ NOT a CEO gate, no token.
frontier: The 3-world go2 fetch + g1 cross-embodiment bars are the FLOOR. Face UNBLOCKED and CONFIRMED — real frontier: (1) re-verify the ~30 STALE confirmed BOARD rows (age ~390+) on the live face, oldest first; (2) CEO-gated capability rungs (G-323-1/S4/D182/D76) need owner action for NEW capability, NOT a face blocker. Meta-lesson STANDS: a HOLD read rotted 100+ rounds; the skeptic re-verify cadence catches it (E79/E90/E223-E225).
next:
  1. [NON-GATED, THE REAL BACKLOG] resume re-verifying STALE BOARD rows on the live face each round the gate is clear + headroom present. Oldest confirmed first: fetch.nl-new-world-warehouse (age413) / fetch.nl-scene-clutter (age413) / g1.navigation (age408) / g1.perception (age406) / E165 look-explore / E167 describe-then-pick. BROAD pgrep + `available`/VRAM check FIRST; WAIT only if genuinely no headroom.
  2. [ADJUDICATE §1b] R701 has NO new provisionals (E225 confirmed; acceptance row confirmed). Nothing owed next round beyond the standing STALE-row re-verify cadence.
  3. [OWNER GATES — NEW capability only, NOT a face blocker] G-323-1 hard-channel quartet (queued ~109 rounds) · G-383-1 has_arm · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old; true owed audited each round). MANIFEST-gated. Fix in var/evidence/R354/.
  - [RESOLVED R701, was CANDIDATE] fetch-mode vlm-judge `workspace_in_frame` false-FAIL — R701 judge PASSED it on a clean fetch frame ⇒ transient, not a rubric defect; NO spine change needed. Dropped.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared — deepseek-chat + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R700
