# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R700 (E223/E224, REVIEW §7). HEADLINE: **~108-ROUND PLATEAU BROKEN — the "sim-block" was the E54-REFUTED over-read all along.** Skeptic pass did NOT inherit "both levers external": ran the ACTUAL repo gate `pgrep -f 'mujoco|vcli'` = CLEAR (the blocker is a go2w /workspace/go2w ISAAC sim — DIFFERENT engine, not the repo MuJoCo gate), measured headroom = 39G RAM available (prior rounds mis-read 13G "free") + 7.5G VRAM free. Per the repo's OWN confirmed E54(R234)/E56(R242): a sibling Isaac sim is NOT an Inv-5 blocker WITH headroom. RAN the bare `vector-cli` HOUSE green fetch (deepseek-chat brain + gemma4:e4b eyes) FOREGROUND, memory-capped, coexisting with the live Isaac sim: **GROUNDED — fetch_verified=True 1/1** (deterministic MOAT, GT actor cannot author); eyes LOOKED at var/evidence/R700/eyes_fetch.png = go2 gripper holds GREEN bottle ALOFT, purple+red cylinders REMAIN on box (colour discrimination), robot upright/body intact; vlm-judge witness=FAIL ONLY on `workspace_in_frame=no` (a place-mode rubric field mis-applied to fetch; scene_rendered/upright/body_intact all PASS) = a verdict-render framing quirk, NOT a grasp failure. Host stayed 35G avail, MuJoCo ~4.6G RSS, scoped teardown clean, Isaac untouched (Inv-8). The E119→E222 plateau rested on re-committing the over-read E54 already refuted; it was buried in a perception-grasp LESSONS line and ignored — now hoisted to the FIRST Refuted line.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE (was falsely `blocked` R328→R699). The acceptance face RUNS with a sibling Isaac sim present given headroom; abundant non-gated work remains (the ~30 STALE BOARD rows to re-verify + breadth). Re-check the ACTUAL gate + `available` RAM/free VRAM each round; HOLD only if the repo gate is dirty OR headroom genuinely absent.
last-round: R700 (E223 debug-finding + E224 verify, both provisional). E223: the sim-block is the E54 over-read (gate CLEAR, headroom present). E224: proved it — bare-cli HOUSE green fetch GROUNDED on the real face coexisting with the Isaac sim. Sim torn down (scripts/sim-teardown, 12 PIDs, scoped). Gate/token audit: ZERO self-approved crossings since R690.
frontier: The 3-world go2 fetch + g1 cross-embodiment bars are the FLOOR. With the face UNBLOCKED, the real frontier resumes: (1) re-verify the ~30 STALE confirmed BOARD rows (all age ~390+) on the live face; (2) the CEO-gated capability rungs G-323-1/S4/D182/D76 still need owner action for NEW capability. Meta-lesson banked: a HOLD read can rot for 100+ rounds — the skeptic re-verify cadence is what catches it (cf. E79/E90).
next:
  1. [ADJUDICATE NEXT ROUND, §1b] promote/refute the R700 provisionals: E223 (over-read finding), E224 + fetch.nl-plain-colour GROUNDED (re-run across the R700→R701 boundary for the E46 N≥2 bar; the vlm-judge workspace_in_frame flag is a framing follow-up, NOT a refutation — deterministic verdict + eyes both GROUNDED).
  2. [NON-GATED, THE REAL BACKLOG] resume re-verifying STALE BOARD rows on the live face each round the gate is clear + headroom present: 2 oldest confirmed (fetch.nl-* warehouse/courtyard), E165 look/explore, E167 go2 describe-then-pick, g1 nav/perception (E48/E50). BROAD pgrep + `available`/VRAM check FIRST; WAIT only if genuinely no headroom.
  3. [OWNER GATES — for NEW capability only, NOT a blocker for the face] G-323-1 hard-channel quartet (queued ~108 rounds) · G-383-1 has_arm · S4 · D182 · D76. These land NEW capability; the acceptance face does NOT need them to run.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh has no provisional gate; true owed audited each round). MANIFEST-gated. Fix in var/evidence/R354/.
  - CANDIDATE (not yet a gate): fetch-mode vlm-judge rubric requires `workspace_in_frame` — a PLACE concern that false-FAILs clean fetch frames (R700). Changing acceptance/ rubric is a SPINE gate; flag, do not touch.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared — deepseek-chat + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
  - SELF-APPROVAL AUDIT (R700): git log --grep GATE-APPROVED\|CEO-APPROVED eb35137..HEAD = ZERO real crossings. R700 = ledger rows + acceptance run + LESSONS/STATUS edits; NO code/interface/spine/manifest edit ⇒ NOT a CEO gate, no token.
last_review: R700
