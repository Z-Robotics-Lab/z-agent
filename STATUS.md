# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R301 (E91, verify) — STATUS next#1: refreshed the STALE self-read PLACE bars under the vlm-judge in the 2 non-house worlds. HEADLINE: the vlm-judge FIRES on the place verdict in BOTH warehouse AND courtyard (R273/E70 eyes-frame-ordering transfers zero-config past the house-only E88/E89). courtyard place self-read→vlm-judge UPGRADE LANDED: GROUNDED 2/2, judge=PASS 4/4 (discriminating), eyes green resting in the courtyard bin — supersedes R259 (provisional). warehouse RANed verified=False on a MESSY brain place-seq (mobile_place x2 + UNCAUSED holding re-verify + resting NOT_GRADED) though eyes show green RESTING in the warehouse bin — physical place OK, a brain place-decompose flake (E60/E70), NOT world/wiring.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~50r unanswered; loop hardening the verify-pillar — self-read→vlm-judge — on the non-gated place bars while the gates stand).
last-round: R301 (E91, verify). §0/§1a: BOOTSTRAP clean (no quarantine; inflight empty). Adopted R300 (review, cut short) WIP: supervisor R300 rounds append + regen BOARD + kept R300's WIRING verified-against bump (7bc6ca0=R270→a7d3f1a on verify-spine + embodiments, 30r stale, DUE) after VERIFYING it accurate (anchors machine-checked; cli.py run_one_turn@1841, engine init_vgg _has_base@428 both exact; prose matches code). §1b: no open provisional (R298 promoted its two; R299 debug/R300 review added none).
frontier: Verify-pillar: eyes=vlm-judge FIRES on fetch (E69/E71) AND place (E88/E89 house, E91 warehouse+courtyard) — the place-judge wiring now spans ALL 3 worlds. Ordinal/relational NL fidelity still WITNESS-only (D182). RESIDUAL non-gated: (a) re-run the warehouse place CLEAN under the judge to LAND its self-read→vlm-judge upgrade (this round's RAN was a brain place-seq flake, physical place OK/eyes); (b) gemma4:e4b judge stays PASS↔ABSTAIN non-deterministic (E72) — a TRUSTED discriminator needs a stronger VLM (funded qwen3-vl / better local). NEW capability still needs owner S4/D182.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select env vars (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): SCENE_CLUTTER=1, ROOM_TEMPLATE={warehouse|courtyard}, SCENE_SWAP=1 (swaps blue↔green only; yellow unmoved E90).
  HARNESS: launcher = var/evidence/R<N>/run_*.sh (uses .venv/bin/python — the systemd --scope has NO `python` on PATH). eyes vlm-judge AUTO-routes to local ollama gemma4:e4b (resolve_judge_env local-prefers it). MODE=place opens a FRESH session. RUN sim BACKGROUND under `systemd-run --user --scope -p MemoryMax=24G`; pass ROUND_N + set VECTOR_EVIDENCE_DIR per run (>1 run/round else E54 overwrite). NEVER pkill mujoco → scripts/sim-teardown (in-process sim dies with the python proc).
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→dumps→write-all (reorders → quarantine). Row caps ENFORCED by checks_schema.py: line ≤1024 BYTES, each string field ≤280 chars — trim redteam/note (bit me 6× this round). experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); the supervisor's rounds.jsonl append shifts BOARD ages so the NEXT round MUST regen it.
next:
  1. [NON-gated verify / §1b adjudicate + land the upgrade] Adjudicate the two E91 provisionals across the boundary: (a) PROMOTE place.nl-new-world-courtyard (vlm-judge GROUNDED 2/2 PASS) to confirmed on a fresh re-run; (b) RE-RUN place.nl-new-world-warehouse CLEAN under vlm-judge (MODE=place ROOM_TEMPLATE=warehouse) — this round's RAN was a messy brain place-seq (physical place OK/eyes); a clean seq lands the self-read→vlm-judge upgrade or the flake reproduces (then it's a brain place-reliability finding, E60/E70).
  2. [NON-gated, optional] Rebuild SCENE_SWAP so the swap actually FLIPS the ordinal referent (E90: yellow unmoved so the swap is masked) — include yellow or restrict the ordinal probe to the swapped pair — only if a live position-invariance bar is wanted; else drop it (superseded).
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~50r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R280..R301 CLEAN. R301 touched NO spine path (ledger + docs/LESSONS + STATUS + WIRING verified-against-only bump adopted from R300 + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
