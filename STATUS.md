# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-03 · R260 (E61) REVIEW — skeptic pass GREEN + wiring/lessons janitor + ambition critic.
  Re-ran the 2 OLDEST confirmed board rows on the REAL bare face: kernel.model-unavailable (~80r,
  case404 -> 'model unavailable' line True, zero credit) + fetch.nl-plain-colour (HOUSE green GROUNDED
  1/1, holding_object actor=CAUSED, eyes: green aloft, purple/yellow/red/blue REMAIN = discrimination,
  brain finished at the hold — Case16 held). Both re-CONFIRMED, STALE flags cleared. No sim leftover.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: green
last-round: R260 (E61, REVIEW, non-gated). Skeptic: 2 oldest confirmed re-confirmed on the real face
  (kernel + HOUSE fetch). WIRING native-loop refreshed (R257 post-place guard documented + 3 anchors,
  verified-against 6834860). Casebook 14<=15 (no fold). Gates/tokens audit R251..R259 CLEAN (only R250
  self-token). Re-red-team of R259 courtyard-PLACE headline: SURVIVES w/ refinement (rests on 4/4
  post-guard GROUNDED via resting_on_receptacle GT oracle; 'zero-config' is WORLD-config, place needed a
  general R257 guard). 2 acceptance(confirmed) + 1 experiments(review) rows banked.
frontier: PLACE CONFIRMED on house + courtyard (2 of 3 worlds); fetch grounds all 3 worlds zero-shot.
  AMBITION CRITIC (R260): 10-round PLATEAU — R250-R259 delivered 1 new confirmed cap (courtyard place)
  at 7 rounds+2 refutations. Non-gated frontier is now "place on the Nth existing world" (warehouse next)
  — a legit LAST datapoint but the same local hill. After warehouse-place CLOSES, declare non-gated
  frontier EXHAUSTED and HOLD at the gates. Genuinely-new bars (S4 3rd-embodiment BYO URDF, D182 grounder)
  GATED + UNANSWERED ~20 rounds — owner must unblock to escalate.
watch: Real-face place/fetch: bare vcli + NL, deepseek-v4-flash brain (provider per .env.example), local
  ollama gemma4:e4b eyes (repl_accept AUTO-routes via resolve_local_vlm_env — do NOT set the VLM url env),
  VECTOR_ROOM_TEMPLATE={courtyard|warehouse} (unset=HOUSE), repl_accept.py MODE={fetch|place|combo}; RUN via
  `.venv/bin/python` (bare `python` NOT on the systemd-scope PATH); memory-cap systemd-run --user --scope
  -p MemoryMax=24G; tear down `scripts/sim-teardown`; ~4-10min/run; Bash 120s -> BACKGROUND + poll the log.
  Guard: native_loop.py post-place guard (WIRING:80). LEDGER: redteam starts 'survived'; free-text<=280; append 1 line (E27).
next:
  1. [BUILD/VERIFY, NON-gated] WAREHOUSE PLACE transfer: VECTOR_ROOM_TEMPLATE=warehouse MODE=place
     '把绿色的瓶子放到架子上' on the bare face, N>=1. LAST non-gated place datapoint — confirms the PLACE
     composite generalizes to all 3 worlds (E56 nav-sub-goal fragile link). If it RANs, Hypothesis-Loop
     the nav-sub-goal (NOT perception — warehouse perception fixed R237/R238). Provisional -> promote next.
     THEN (per R260 critic): if it closes, non-gated frontier is EXHAUSTED -> phase: blocked at the gates.
  2. [SPINE, GATED] D182 world-owned NL->object grounder — removes witness-only fidelity; CEO gate.
  3. [FRONTIER/breadth, GATED] NEW 3rd embodiment via BYO URDF+manifest — S4 one-generic-driver (WIRING:53).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD.
  - SPINE (D182): actor-authored verify target — a world-owned NL->object grounder fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46). Object plug-in not pure config (per-object weld tuple + colour maps) -> S4.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (external: VLM-judge + BYO-N≥4 + PERCEPTION VLM OpenRouter/dashscope-402; local Ollama gemma4:e4b is the working seam) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R260): gate/token audit R251..R259 CLEAN. R209 schema-cap approval remains the last, audited clean.
last_review: R260
