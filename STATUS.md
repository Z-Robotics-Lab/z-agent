# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R314 (E104, BUILD) — REFUTED the qwen-as-eyes frontier lever: qwen2.5vl:7b is a PRESENCE detector, NOT a pose discriminator. Determinism (E103) is real; discrimination is a rubber-stamp.
  §0/§1a FLOOR: adopted the supervisor R313 rounds.jsonl append + regen BOARD (ages +1). Inflight empty; no uncommitted WIP.
  §2-4 (non-gated config axis, no billing/spine): STATUS next#1b — does qwen2.5vl:7b actively FAIL a bad-but-RENDERED frame? Rendered a REAL go2 clearly TOPPLED on its side + a matched upright control + a robot-ABSENT frame (offline capture.snapshot, a fresh render model — the live MuJoCoGo2 wrapper segfaults a 2nd renderer), judged each N=2 via the FROZEN vision_judge.
  RESULT (REFUTED, E104): qwen PASS 2/2 on BOTH upright AND toppled — robot_present_upright=YES on the CLEARLY-toppled robot (per-item answers identical to upright) = a DETERMINISTIC false-green. On the robot-ABSENT frame it ABSTAINs (does NOT hallucinate a robot) ⇒ qwen detects PRESENCE but rubber-stamps POSE. A false-green is WORSE than gemma's abstain.
  HEADLINE: E103's "deterministic candidate" does NOT close the eyes-pillar 2nd-witness gap — landing qwen as real eyes (old next#1a) is now MOOT/counter-indicated. The gap stays OPEN; only gated paths remain (remote qwen3-vl billing / a better local VLM). Experiments row E104 (verify, refuted, provisional); no acceptance row (judge-quality probe, not the acceptance face). NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier: local-judge lever REFUTED; eyes 2nd-witness gap OPEN with only gated paths — search another local VLM or accept plateau; NEW capability blocked on owner S4/D182/D76).
last-round: R314 (E104 verify, REFUTED). Proved qwen2.5vl:7b rubber-stamps a toppled robot (PASS 2/2, upright=yes) — a presence detector not a pose discriminator; abstains only when the robot is ABSENT. Kills the "land qwen as eyes" plan. No spine path touched.
frontier: The STRONGER-LOCAL-JUDGE lever is REFUTED (R314/E104): qwen2.5vl:7b is deterministic but a POSE rubber-stamp (false-green on a clearly-toppled go2), so it is NOT a trusted 2nd discriminator; gemma4:e4b never grades. The eyes 2nd-witness gap (E72/E92) is UNCLOSED and the two remaining paths are BOTH gated: (a) remote qwen3-vl-plus — BILLING Arrearage; (b) a different/better local VLM — a non-gated search (next#1). Every NEW capability is CEO-gated: S4 (3rd embodiment BYO-URDF), D182 (world-owned NL grounder), quantity N-object (D76 planner-plugin).
watch: JUDGE PROBE recipe (non-spine, local ollama, no billing): tools/acceptance/judge_determinism.py <frame> <model[,model2]> <N> — 240s timeout; warm the model first (`curl .../api/generate keep_alive=15m`) since CPU cold-load ~90s. The DISCRIMINATION test frames live at var/evidence/R314/{upright_control,toppled_fault,robot_absent}.png; render fresh via var/evidence/R314/render_toppled.py (NEVER build via the live MuJoCoGo2 wrapper — 2nd renderer segfaults; build a fresh MjModel from base._scene_xml_path). NEVER `ulimit -v` a GL/EGL render (virtual reservation cores it).
  REAL-FACE recipe (if a pose-discriminating judge is found): bare vcli + NL; FORCE deepseek + v4-flash via env (default qwen ROUTING = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`; point the eyes judge via VECTOR_JUDGE_LOCAL_MODEL=<model>. CAVEAT: the LIVE harness judge times out at 60s (vision_judge._call_vlm) — a CPU-slow local VLM ABSTAINs there even if the offline probe PASSes.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; `free -g` + `pgrep -af repl_accept` before launch; scripts/sim-teardown after. NEVER pkill mujoco.
  TESTS: pure-unit only via `./scripts/run-tests <path>` (memory-capped 12G).
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (Chinese=3B/char), each string field ≤280 CHARS (redteam/result bite every round — trim). experiments key `e` NOT `experiment`; experiments status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds.jsonl append shifts ages → NEXT round MUST regen BOARD.
next:
  1. [FRONTIER — non-gated, direct from E104] SEARCH another local VLM (e.g. llava:7b/13b, minicpm-v, qwen2-vl, internvl) for a POSE discriminator: run judge_determinism.py on the R314 triplet {upright_control, toppled_fault, robot_absent}.png — a real candidate must PASS upright, FAIL (not abstain) toppled, and handle absent. If NONE discriminates, the eyes 2nd-witness gap is a GATED plateau (remote qwen3-vl billing / D182) — say so honestly and pivot.
  2. [OWNER GATE — ONLY path to NEW capability] Unblock S4 (3rd embodiment BYO URDF+manifest) OR D182 (world-owned NL→object grounder) OR the D76 planner-plugin (quantity decompose). All CEO-gated ~60r.
  3. [NEXT REVIEW = R320] Skeptic-re-verify the oldest confirmed rows; re-red-team a headline; adjudicate provisionals (incl. E104); wiring audit; ambition critic (10-round plateau check).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R314 CLEAN. R314 touched NO spine path (rendered offline probe frames under var/evidence/ + judge_determinism.py reuse; 1 experiments row E104 + LESSONS E103→E104 extension + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R310
