# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R367 (E157, BUILD). HEADLINE: both levers SHUT (46th sim-block). Re-ran the landing doctor (`hard_channels_doctor` GREEN 4/4, refreshing the R352 15-round-stale re-confirm so G-323-1 stays a ~5-min owner merge). Then a genuine substrate re-scan OUTSIDE all saturated veins found a REAL Inv-1/Inv-3 gap in a DISTINCT vein: `playground/catalog.py` `_TABLETOP_OBJECTS` — the 6 arm-scene body names catalog.py:22 declares "MUST match so101_mujoco.xml bodies — the contract the sim-oracle predicates read ground truth against". Its ONLY guard was `tests/vcli/test_playground_real_arm.py::TestRealArmOracleContract` = `@pytest.mark.integration` (DESELECTED under `-m 'not integration'`) + a REAL headless MuJoCoArm sim (Inv-5 sim-gated). So the deterministic always-run gate had ZERO catalog↔MJCF guard: rename a body (mug→cup) OR edit the tuple ⇒ CI stays GREEN while the oracle grades a name the scene lost = false verdict on the acceptance face. Added `tests/unit/playground/test_catalog_mjcf_fidelity.py`: parses `scene_xml` via stdlib ElementTree (NO mujoco import, NO sim slot — verified `mujoco not in sys.modules`), INTROSPECTS `SCENARIOS` (not a hardcoded list, so a new preset scene is auto-guarded), asserts every `object_name` is a real `<body>`, + anti-vacuous (≥1 object-scenario, non-empty body parse, tabletop==6). 4/4 green in 0.08s. RED-TEAM (revert-clean, catalog+XML byte-unchanged after): (1) append bogus tuple name `cup` ⇒ 3 RED; (2) rename MJCF `mug`→`cup` ⇒ 3 RED (the exact drift the integration-only guard misses in the fast gate); control reverted ⇒ 4/4; `-m 'not integration'` runs my 4, deselects the old 3. Non-spine, non-mirror, non-gated (tests/ only).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked. This round's non-gated BUILD came from a genuine re-scan OUTSIDE the saturated veins — the arm-playground catalog↔MJCF set-membership contract is a DISTINCT vein from spine/manifest/config work and was UNGUARDED in the deterministic gate (only a sim-instantiating integration test). Now closed offline; do NOT re-force it — keep re-scanning ONLY for a REAL gap.)
last-round: R366 (E156 BUILD: frozen-guarded the embodiment-config morphology schema nested specs; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED across the FULL driver↔manifest↔bridge mirror for go2+g1 (E147-E151), vcli+acceptance verdict contracts (E152/E153), Inv-4 kernel-import purity via seam allowlist (E154), causation-baseline frozen (E155), embodiment-config morphology schema (E156) AND now the arm-playground catalog↔MJCF set-membership contract in the deterministic gate (E157) — spine + config-schema frozen + catalog-mirror coverage COMPLETE. The R312-R367 acceptance-face plateau is REAL + HARD-EXTERNAL (not a local hill): the two lever paths to a NEW confirmed capability are sim slot frees (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R367).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` to confirm a LIVE run not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline capability axis, (b) re-audit an already-audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED as a defect (mechanical age+1 drift, E102/E145), (g) force a manifest/bridge-mirror guard — SATURATED (E151), (h) re-force the Inv-4 import guard — allowlist-covered (E154), (i) re-force a SPINE frozen-dataclass guard — spine coverage COMPLETE (E152/E153/E155), (j) re-force the embodiment-config schema frozen guard — config coverage COMPLETE (E156), NOR (k) re-force the arm-playground catalog↔MJCF guard — object_names coverage COMPLETE (E157). But DO genuinely re-scan the substrate for a REAL unenforced invariant / missing drift-guard OUTSIDE all saturated veins before defaulting to HOLD (E152-E157 = six such found post-saturation, in progressively distinct veins). Next candidates if a gap is real: a load-bearing const with a comment-pinned mirror not yet ast-guarded; a hardcoded enumeration elsewhere that should introspect; a genuine test-coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R367: sibling isaac warehouse_nav.py LIVE, pid 3569755 — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R367 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (manifest mirror E151, spine frozen E152/E153/E155, Inv-4 import E154, config-schema frozen E156, catalog↔MJCF E157): a comment-pinned const mirror not yet ast-guarded, a hardcoded enumeration that should introspect, or a real test-coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap exists AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, re-investigate the preflight nag (E144) or BOARD age drift (E145).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E157 re-red-teamed E150): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 50 rounds (R318..R367). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0. FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated: needs a real driver+MJCF, NOT a clean offline win) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer, planner-free contract) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-10 CLEAN (git log R357..R366 grep GATE-APPROVED/CEO-APPROVED = 0 hits). R367 touched ONLY tests/unit/playground/test_catalog_mjcf_fidelity.py (new guard) + docs/LESSONS.md + ledger/BOARD + STATUS + events — no spine/manifest-gated path. No self-approval.
last_review: R360
