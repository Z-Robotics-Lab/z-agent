# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R383 (E172, BUILD). HEADLINE: found a REAL latent capability-gate authority bug on the North-Star plug-and-play path, from a collision of two ACCEPTED rulings. D72 (S5a) built `resolve_capability_profile` and ruled `has_arm` an ENRICHMENT flag reconciled runtime-OR-declared, justified as "behavior-inert — NO gate reads it." D175 (g1 2nd embodiment) THEN made `has_arm` a LIVE manipulation gate (`native_loop._build_motor_tools` withholds pick/place/... when `not has_arm`), silently invalidating D72's premise. Consequence (probe + 4 new tests, tests/vcli/test_declared_arm_gate_tension.py 4/4, caps suite 20/20): a plug-and-play manifest declaring `has_arm:true` with NO runtime `_arm` bound resolves has_arm=True → is offered all 9 manipulation skills it CANNOT execute ("No arm connected") — the exact false-reach the D175 gate exists to prevent. LATENT (both shipped manifests declare has_arm:false; go2+Piper binds `_arm` at runtime so runtime wins = accepted path unaffected). The GATED flags has_base/camera are runtime-authoritative for exactly this reason; has_arm became a gate without being reclassified. FIX (make has_arm runtime-authoritative, drop the declared-OR for has_arm only) touches accepted ruling D72 ⇒ CEO gate G-383-1, NOT self-crossed. This round: banked E172, queued G-383-1, de-rotted the capability_profile docstring (factual: has_arm IS now read by D175's gate), pinned the tension with a GREEN regression test, Casebook Case 19. Both levers still SHUT (61st sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R383 found a real North-Star latent bug — capability-gate authority tension D72↔D175 — non-gated to DOCUMENT+GUARD, but the FIX is CEO-gated G-383-1).
last-round: R383 (E172 BUILD: has_arm declared-OR feeds D175's live manip gate → un-executable skills offered; latent; gated G-383-1).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar mirrors (E147-E158), verdict contracts (E152/E153), Inv-4 allowlist (E154), embodiment-config schema (E156), arm-catalog↔MJCF (E157), home-pose (E161), F821/bugbear (E163/E164), head-cam res+renderer FAMILY (E165-E170), Inv-3 plug-and-play NOT overfit (E171), capability-gate authority D72↔D175 tension found+gated (E172). Head-cam renderer vein CLOSED. E171+E172 are squarely on the North Star (offline 3rd-embodiment + capability-resolution honesty). R312-R383 acceptance-face plateau (71 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. A domain expert would still say the loop cannot land a NEW sim-face capability while externally blocked — honestly acknowledged.
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE not leak) for the sim slot, inbox+gates.jsonl for the owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g) re-force any SATURATED vein incl. the head-cam renderer FAMILY (E167/168/170), the offline embodiment-config path proven generic (E171), or the capability-gate authority now found+gated (E172 — do NOT re-cross into fixing has_arm without G-383-1). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E172 = seventeen found post-saturation; E172 shows even two ACCEPTED rulings can silently collide into a latent bug). Candidates if REAL: a comment-pinned const/param mirror incl. a stale DEFAULT ARG (E165); a shared-resource per-call arg silently ignored (E167); a hardcoded enum that should introspect; an untested except/fallback (E162); a flag an OLD ruling called "inert / no consumer" that a LATER ruling started consuming (E172). Verify the gap on disk FIRST.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim. (R383: sibling isaac warehouse_nav.py LIVE pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (R383 E172 = 979B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R383 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (see watch candidates). Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver or re-force a saturated guard.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383, NEW): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm an inert enrichment flag (no gate reads it); D175 then made it a LIVE manipulation gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered a manip surface it can't execute (E172, 4/4 tests). LATENT (go2+g1 declare has_arm:false; go2+Piper binds _arm=runtime wins ⇒ fix preserves accepted path; g1 unchanged). Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Red-team: fix is provably behavior-preserving for ALL shipped bodies; only changes the un-shipped declared-true-no-runtime case from "offer un-executable" to "withhold" (= the gate's stated purpose). Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, substrate rot-free E121/E147-E172 re-red-teamed E150/E159/E169): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 65 rounds (R318..R383). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-review CLEAN (R380 §7) — `git log --grep=GATE-APPROVED\|CEO-APPROVED` since R370 = 0 real approvals. R383 touched ONLY a NEW test file + capability_profile.py DOCSTRING (no code semantics) + docs/LESSONS.md + STATUS.md + loop/ledger — no spine/manifest-gated path, no interface change, no production behavior.
last_review: R380
