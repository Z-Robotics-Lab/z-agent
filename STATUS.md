# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R319 (E110, BUILD) — the deterministic pose channel is now a COMPLETE PAIR. E109 gave ORIENTATION (quat-upright); the 4th R316 fault (sunk-through-floor) is orientation-upright and only root-z betrays it. R319 builds the HEIGHT companion `tools/acceptance/pose_height.py` (root_z vs stance_z=0.35). The pair (orient AND height) reads 4/4 EXACT R316 topologies; on sunk_floor(z0.07) orientation-alone reads STANDING (MISS) but height reads SUNK ⇒ the pair catches it, height the SOLE witness where minicpm SPLIT the frame by prompt framing (E108).
  §1a FLOOR: adopted supervisor R318 rounds.jsonl append (b830730, dur 729s) + regen BOARD (ages +1). Inflight file absent; no WIP; inbox G-183-12 already answered (gates.jsonl); provisionals check-exempt.
  §1b: E109(build) already carried final `confirmed` status. 67 acceptance provisionals all check-exempt (backfill/superseded/≤2r) → check.sh GREEN, none wedge.
  §2-4 (non-gated, no billing, NO spine path): built `tools/acceptance/pose_height.py` — `height_dev`=root_z-stance_z (signed m, 0 at stance); `classify` bands SUNK(<-tol)/NOMINAL/AIRBORNE(>+tol) at tol=0.12; sensor-floor rejects NaN/inf root_z + non-positive stance. TDD 13/13 (`tests/unit/test_pose_height.py`). stance_z=0.35 = the shipped go2 keyframe height (mujoco_go2.py:397 / :1273).
  E110 (build, CONFIRMED): the pair reads 4/4 EXACT R316 poses (var/evidence/R319/prove_pose_pair.py, sim-free — real keyframe quat via mju_mulQuat + the exact render-z from render_poses.py). sunk_floor is the decisive row: orient STANDING + height SUNK → pair FALLEN. A sibling loop may hold the one global sim slot (Inv-5) — this probe needs NO sim, so no contention.
  HEADLINE: pose = orientation(E109) + height(E110), a two-predicate deterministic channel that reads ALL 4 R316 fault topologies where every local VLM failed on ≥1. Honest scope (same as E109): an OFFLINE oracle on ground-truth quat/z, NOT the acceptance face → experiments row only, NO acceptance row. Landing the PAIR into the verify spine (acceptance/ + rubric) is honest-verify-spine semantics = CEO gate G-318-1. NO spine path touched this round.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier: the eyes-POSE lever is RESOLVED and now COMPLETE — orientation+height hard-channel pair (E109/E110), NOT a VLM; the pose question has fully left the eyes pillar. Every remaining acceptance-face advance is CEO-gated: G-318-1 (land the pose pair) · S4 · D182 · D76.)
last-round: R319 (E110 CONFIRMS the deterministic HEIGHT companion to pose_upright; the orient+height pair reads all 4 R316 topologies; new non-spine tools/acceptance/pose_height.py, TDD 13/13). No spine path touched.
frontier: eyes-POSE is SETTLED AND COMPLETE — pose → deterministic orient(E109)+height(E110) hard-channel pair; VLM eyes → object identity/placement (confirmed vlm-judge). The non-gated pose frontier is now EXHAUSTED (all 4 R316 topologies read deterministically). Remaining acceptance-face growth is ALL CEO-gated: land the pose pair (G-318-1); S4 (3rd embodiment BYO-URDF); D182 (world-owned NL→object grounder, also fixes ordinal/witness fidelity); D76 planner-plugin (quantity N-object). R320 is a REVIEW round — the honest read: R312-R319 (8 rounds) were ALL eyes-pillar probing with NO acceptance-face advance; the face is GATE-BLOCKED, not idea-blocked.
watch: pose PAIR (non-spine, pure math, no sim):
  `python tools/acceptance/pose_upright.py <w> <x> <y> <z> [--thresh 0.5]` (orientation: STANDING/FALLEN);
  `python tools/acceptance/pose_height.py <root_z> [--stance-z 0.35] [--tol 0.12]` (height: SUNK/NOMINAL/AIRBORNE).
  Units `tests/unit/test_pose_upright.py` (11) + `tests/unit/test_pose_height.py` (13). Pair demo (sim-free): `.venv/bin/python var/evidence/R319/prove_pose_pair.py` — reads keyframe quat 1,0,0,0 + exact render-z, prints the 4-topology table.
  SIM-SAFETY (Inv-5, R318): a SIBLING loop can hold the one global sim slot; `MuJoCoGo2.connect(room=True)` launches the full CMU nav stack. BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco'` NOT just `mujoco` — WAIT if live, NEVER teardown a sim you did not launch.
  POSE-PROBE recipe (VLM, refuted for pose): tools/acceptance/pose_probe.py [--framing neutral|fault] <model> <N> <frame..>; render via var/evidence/R317/render_multiview.py. NEVER a 2nd MuJoCoGo2 renderer (segfault). Local VLM cold-load ~90s.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; scripts/sim-teardown after. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD (E27/E81/R316-R319): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (R319 E110 trimmed redteam 1157→1094B). experiments key `e` NOT `experiment`; experiments status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds append shifts ages → regen BOARD.
next:
  1. [NEXT REVIEW = R320] Skeptic-re-verify the 2 oldest confirmed BOARD rows + 1 random on the REAL face; adjudicate provisionals (incl. E109/E110); re-red-team a headline; wiring audit (>25r-old cards); memory dedupe; gate/token audit. AMBITION CRITIC: R312-R319 were 8 straight eyes-pillar probe rounds with ZERO acceptance-face advance — say so honestly; the face is GATE-BLOCKED not idea-blocked; propose the owner unblock ONE gate (G-318-1 is the cheapest, most-proven).
  2. [OWNER GATE — the ONLY path landing pose eyes into the acceptance face] G-318-1 (wire the pose PAIR — orient+height — as the pose verify-channel in acceptance/ + rubric = honest-verify-spine/moat). Owner call owed. Also still queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] the deterministic pose channel is now COMPLETE (all 4 R316 topologies); the honest next non-gated probe is OUTSIDE pose — e.g. a deterministic PLACEMENT-distance channel (object-in-region) to cut vlm-judge reliance on placement (D168 place-oracle territory, currently gated). Weigh vs simply pushing the owner to unblock a gate.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-318-1 SPINE/MOAT (pose verify-channel): E109+E110 prove a deterministic pose PAIR (orient `tools/acceptance/pose_upright.py` + height `tools/acceptance/pose_height.py`) reads go2 pose 4/4 across all R316 topologies where every local VLM failed on ≥1. Landing it as a verify channel edits acceptance/ + the rubric (ADR-002 moat, checksum-pinned) = honest-verify-spine semantics. Per Invariant 1 this is STRICTLY better than a VLM (reads ground truth the actor cannot author). Owner call owed. <- next#2. (R318/E109, R319/E110)
  - G-315-1 SPINE (VLM pose rubric): MOOT FOR POSE — E109/E110 show pose belongs to the hard-channel PAIR (G-318-1), not a reworded VLM rubric item. KEEP only as: if the owner still wants a VLM 2nd-witness for pose, E105/E107/E108 say no local VLM (single or multi-view) robustly reads side-lying. Superseded-in-spirit by G-318-1. (R315/E106..R319/E110)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R319 CLEAN. R319 touched NO spine path (added tools/acceptance/pose_height.py + tests/unit/test_pose_height.py + var/evidence/R319/ demo (gitignored); 1 experiments row E110 + LESSONS E109-line extension + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ or config/rubric edit).
last_review: R310
