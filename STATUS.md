# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R323 (E114, BUILD). HEADLINE: the hard-channel QUARTET is now PACKAGED FOR LANDING as ONE unit — `tools/acceptance/hard_channels.py` binds all four proven cores (upright E109 / nominal_height E110 / near E112 / facing_target E113) as GT-reading, fail-safe-to-False bool predicates in the exact `go2_sim_oracle.build_verify_namespace` shape (`make_verify_namespace` + `verify_signatures` + a unified CLI dispatcher). It adds NO new geometry — it imports the tested cores verbatim and binds them to the real base-GT seam, turning four scattered scripts + FOUR separate spine gates into ONE reviewable ready-to-land unit. RED-TEAM CORRECTION (materially improved the design): a first read wrongly deferred `upright` to an interface gate ("base API exposes only yaw"); red-teaming `get_odometry` found the base ALREADY returns the full root quaternion (`Odometry` qw..qz), so ALL FOUR channels land on the EXISTING base API (get_position/get_heading/get_odometry) with NO interface change. Fake-base TDD 27/27; the four cores unchanged (72/72). Landing = merge into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric = a SINGLE CEO gate G-323-1 (consolidates G-318-1 + relational-near + G-322-1). Identity ("which object is the target") stays the VLM/D182 grounder's job.
  §1 ORIENT: inflight absent; no WIP; inbox clean. No experiments provisionals (enum has none). 67 acceptance provisionals all check-exempt → check.sh GREEN. SIM-BLOCKED A 5TH TIME: a sibling loop holds the one sim slot (localPlanner PID 3535348 + laser_mapping PID 3535356, parented to a live `ros2 launch system_isaac_sim.launch.py` under loop/run.sh PID 50965, 32min old) → Inv-5 + NEVER-KILL-INFRA: NOT touched, launched NO competing sim ⇒ next#1 (real-face stale-row re-run) stays blocked, as R320/R321/R322.
  §4 REAL-VERIFY: pure-wiring non-spine module ⇒ acceptance is the fake-base conformance proof + real CLI dispatcher smoke (as E109-E113 were verified sim-free). eyes: self-read. 27/27 fake-base (all 4 predicates: correct verdict, caller-owned tol/radius/cone, fail-safe-False on no/disconnected base + bad args); cores 72/72 unchanged; CLI routes upright/height/near/facing + errors loud on unknown. REDTEAM survives as-if-not-mine: additive + fail-safe-False (never a false PASS), reads unforgeable base pose (Inv-1 STRICTER not looser); fake base mirrors the real hardware/base.py ABC + mujoco go2/g1 impls; proves READY-to-land, NOT landed (the merge is the gate, queued G-323-1); experiments row only, NO acceptance row. NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE-blocked: every acceptance-face advance is CEO-gated — G-323-1 (the CONSOLIDATED quartet landing) · S4 · D182 · D76)
last-round: R323 (E114 BUILD: hard_channels.py packages the quartet as agent-bound verify-namespace predicates, 27/27 fake-base + cores 72/72; red-team on get_odometry found the root quat → all 4 land on the existing base API, no interface change, ONE gate G-323-1 not four. Sim-blocked a 5th time; no spine touched).
frontier: the offline hard-channel well is now DRY and PACKAGED — the quartet is built, proven, and bound to the real GT seam, one merge from the acceptance face. The 11-round acceptance-face plateau (R312-R323) is REAL and STRUCTURAL: the face is GATE-blocked, not idea-blocked. There is exactly ONE honest lever left: the OWNER approves G-323-1 (now a single low-risk diff: import + 4 predicate registrations + rubric — no interface change), converting the whole proven quartet into acceptance-face verify-channels in one call. Building a 5th offline oracle would be pure treadmill — do NOT.
watch: quartet PACKAGED (non-spine, pure wiring over tested cores, no sim):
  `.venv/bin/python tools/acceptance/hard_channels.py {upright <w x y z>|height <z>|near <ax ay> --to <bx by>|facing --at <rx ry> --to <tx ty> --yaw <rad>}` — unified CLI over the four cores.
  `hard_channels.make_verify_namespace(agent)` → {upright, nominal_height, near, facing_target}: GT-reading fail-safe-False bool predicates (reads _base.get_position/get_heading/get_odometry); `verify_signatures()` → the DecomposeVocab.verify_fn_signatures entries. Test: `./scripts/run-tests tests/unit/test_hard_channels.py` (27). Cores: test_pose_upright(11)+pose_height(13)+place_near(18)+heading_facing(30).
  LANDING SHAPE (for the gate round): merge make_verify_namespace into `RobotWorld.build_verify_namespace` (robot.py), add each name to `evidence_classifier._PREDICATE_ORACLES` + `verify_signatures()` to the vocab, reword rubric — WIRING verify-spine §. All on the EXISTING base API; identity stays VLM/D182.
  SIM-SAFETY (Inv-5, R318/R320-R323): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco'` (NOT just `mujoco`); WAIT if live, NEVER teardown a sim you did not launch.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board: `.venv/bin/python loop/board.py`.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R323 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: merge hard_channels.make_verify_namespace into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric (one low-risk diff, no interface change). Consolidates G-318-1 · relational-near · G-322-1. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] the offline hard-channel well is DRY and PACKAGED — do NOT build a 5th oracle (treadmill). Weigh instead: a small non-spine ergonomic (e.g. a hard_channels doctor that dry-runs make_verify_namespace against a fake agent), OR honestly hold and push the owner to unblock G-323-1. Do NOT reopen any eyes-VLM pose/heading probe (refuted E105-E108).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing) <- next#2: merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114-packaged 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind to the EXISTING base API (get_position/get_heading/get_odometry quat) ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. One low-risk diff. Owner call owed. SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1 (kept for history). (R323/E114)
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, now CONSOLIDATED under G-323-1 (land the packaged quartet in one merge). Kept for provenance (R318/E109, R319/E110, R320/E111, R321/E112, R322/E113).
  - G-315-1 SPINE (VLM pose rubric): MOOT — pose belongs to the hard-channel quartet (G-323-1). (R315/E106..R320/E111)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py; CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — the IDENTITY half the quartet's geometry channels leave to the VLM (E46, E112/E113). <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R323 CLEAN. R323 (build) touched NO spine path (added tools/acceptance/hard_channels.py + tests/unit/test_hard_channels.py, both non-spine pure-wiring/import over tested cores; appended experiments E114 + gates G-323-1 queued; extended 1 LESSONS saga line; STATUS; NO acceptance/ or rubric or interface edit).
last_review: R320
