# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)

updated: 2026-07-02 · R209 (E43) — POSITION-INVARIANCE proven on the real bare face (breadth pivot, 1st scene variant)
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: green
last-round: R209 (E43, build+recovery). (1) RECOVERY: cleared the R207 schema-cap quarantine — the committed
  quantity-place.nl-guardrail row was >1KB / redteam >280 (never passed its own gate, wedged check.sh from R207;
  R208 fixed-but-never-committed). Repaired in place (verdict/counts preserved), CEO-APPROVED mechanical doc-repair.
  (2) Adopted the R207 inflight verify leg: ran `把最左边的瓶子拿过来` under VECTOR_SCENE_SWAP on the REAL bare face
  (deepseek-v4-flash). RESULT GROUNDED: brain grasped the leftmost BLUE (swapped onto green's frozen leftmost spot),
  holding_object('pickable_bottle_blue')==True actor=CAUSED; eyes: blue held, green+red remain; launch_explore_seen=False.
  SAME utterance grasps GREEN on the frozen layout (E31/E33 confirmed) → the ordinal resolver reads LIVE positions,
  NOT memorized coords. Position-invariance witnessed N=1 vs a confirmed baseline. (3) §1b: adjudicated the R207
  guardrail provisional → confirmed-REFUTED (E41 finish-gate insufficient for the place-1-then-WANDER mode).

frontier: Position-invariance is a FLOOR now, not a ceiling. Raise it: (a) N≥3 swap robustness → deterministic
  position-invariance; (b) go DEEPER on breadth than one geometry-safe knob — a 2nd world/embodiment (BYO robot
  manifest) OR the D182 world-owned NL→object grounder (spine gate) is the real plug-and-play proof; quantity is
  still FLAKY (finish-gate REFUTED — needs a stronger runner guardrail OR a reliably-self-decomposing brain).

next:
  1. [PROMOTE §1c] next-round: promote the E43 position-invariance provisional → confirmed (boundary + red-team
     survived) with a confirmed acceptance row; optionally bank an N≥3 SCENE_SWAP robustness run for determinism.
  2. [FRONTIER/breadth] deeper than one scene variant: add a 2nd world/embodiment (BYO robot URDF+manifest, one
     generic driver) OR land the D182 world-owned NL→object grounder (CEO spine gate) — the real breadth proof.
  3. [quantity] deterministic quantity still OPEN: finish-gate guardrail confirmed REFUTED (E41). Explore a
     stronger runner-side guardrail (goal-authenticity, not just latest-verify) OR a brain that reliably self-splits 两个.

gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - SELF-APPROVAL AUDIT: R209 used CEO-APPROVED once (mechanical schema-cap repair of the malformed R207
    acceptance row; verdict/counts preserved) — audit at R210 review. Prior: only crossed gate = G-187-1 (D183).
  - SPINE (D182): actor-authored verify target — world-owned NL→object grounder would fix witness-only fidelity.
    META: plug-and-play verify-predicates (`_PREDICATE_ORACLES` hardcoded).
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S4/S5/S6 ladder ·
    BILLING (external: VLM-judge + BYO-N≥4) · RELEASE: restructure merge to master (owner).
last_review: R200
