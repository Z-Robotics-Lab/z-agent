# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R380 (E169, REVIEW). HEADLINE: clean review round — the substrate is ROT-FREE and the recent head-cam headlines SURVIVE independent re-red-team, but the acceptance-face plateau is unbroken (58th sim-block; R312-R380 = 68 rounds). SKEPTIC PASS (real bare-cli face SIM-BLOCKED — sibling isaac warehouse_nav.py pid 3569755 + localPlanner + laser_mapping hold the one global slot; Inv-5 = WAIT, never teardown): deterministic substitute — E167/E168 renderer-dims regression 9/9 GREEN, E165 single-source guard 13/13 GREEN. RE-RED-TEAM two LIVE-ish headlines as if I didn't write them: (E165) `GO2_HEAD_CAM_WIDTH=320` is single-sourced AND wired into the look/explore acceptance path via `_build_go2_perception`→`Go2GraspPerception(base)` (no dims ⇒ 320 default) — the LIVE-divergence claim is HONEST; (E168) grep of ALL `G1HeadPerception(`/`get_camera_frame(` callers shows the ONLY instantiation passes 640x480 (`sim_tool.py:427`) and the oracle uses `_RENDER_W/H=640/480` — so the "LATENT not live" claim is HONEST and the fix (track `_cam_renderer_dims`, re-create+close on change) is in place (mujoco_g1.py:1153-1160/649-650). WIRING AUDIT: all 3 sections verified-against ≤21 rounds old (verify-spine+embodiments R359, native-loop R368; <25 refresh bar) — anchors machine-green, no refresh mandated. GATE/TOKEN AUDIT: `git log --grep=GATE-APPROVED\|CEO-APPROVED` since R370 = 0 hits — CLEAN, no self-approval. MEMORY: Casebook 11 active cases (≤15 cap, no fold); docs/LESSONS.md 260/260 (AT cap — recorded the review by appending to the existing accumulator line, no line added). No rot found in docs/. AMBITION CRITIC unchanged and HONEST: the loop polishes perception/invariant plumbing (E152-E168) while the North-Star capability stays externally blocked — NOT a hidden local hill; the two levers are sim-slot-free OR owner approves G-323-1.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R380 was a review round — skeptic + wiring + gate/token + memory audits ALL clean, no new capability, no rot; nothing non-gated to build that isn't a manufactured sliver this round).
last-round: R379 (E168 BUILD: MuJoCoG1 head-cam renderer honors per-call dims — the E167 go2 fix's own mirror in the sibling humanoid driver, LATENT API-contract defect; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2(4)+g1 (E147-E151/E158), piper-proxy const (E160), verdict contracts (E152/E153), Inv-4 import allowlist (E154), causation-baseline (E155), embodiment-config schema (E156), arm-playground catalog↔MJCF (E157), SO-101 home-pose vein CLOSED (E161/E162), F821 class (E163), ruff-bugbear (E164), go2 head-cam RESOLUTION default single-sourced (E165), F841/F401 live render defect (E166), go2 head-cam render MECHANISM per-call dims (E167), g1 head-cam render MECHANISM per-call dims (E168), R380 REVIEW re-red-teamed E165/E167/E168 headlines — all SURVIVE, substrate ROT-FREE (E169). Spine + config-schema frozen + mirror coverage broad. The R312-R380 acceptance-face plateau (68 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R380). Substrate finds (E152-E168) are increasingly marginal — a domain expert would say the loop polishes plumbing while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill.
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g-r) re-force any SATURATED vein incl. go2+g1 head-cam RENDER MECHANISM — both drivers honor per-call dims (E167/E168), arm.render+pano360 audited safe, (s) re-red-team E165/E167/E168 headlines again (R380/E169 did — all SURVIVE). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E168 = fourteen found post-saturation; even a "fixed" mechanism can hide a SIBLING copy — E168 was the go2-fix's own mirror in g1). Candidates if a gap is real: a comment-pinned const/param mirror not yet guarded incl. a stale DEFAULT ARG leaking to the acceptance path (E165) OR a shared resource whose per-call arg is silently ignored ACROSS ALL siblings (E168 — grep `if …_renderer is None`/`if not hasattr` in EVERY driver); a hardcoded enumeration that should introspect; an untested except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  OWED SIM-CHECK (E165+E167+E168): when the sim slot frees — (a) eyeball go2 look/explore at 320x240 (E165); (b) run a go2 `describe`/`look` THEN a `pick` in one bare-cli session, confirm the grasp still grounds `holding_object` (E167); (c) instantiate a non-default-res g1 perception after a bare g1 look and confirm the frame comes back at the requested resolution (E168). All are the only unverified aspects; unit+detector suites pass.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R380: sibling isaac warehouse_nav.py LIVE pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (checks_schema.py — R380 E169 = 1006B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R380 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick, AND the E168 owed g1 non-default-res perception-after-look (one session). Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (E151/E158 lidar, E152/E153/E155 spine frozen, E154 Inv-4 import, E156 config-schema, E157 catalog↔MJCF, E160 piper const, E161/E162 home-pose, E163 F821, E164 bugbear, E165/E167/E168 head-cam): a comment-pinned const/param mirror not yet guarded incl. a stale DEFAULT ARG (E165) OR a shared-resource per-call arg silently ignored ACROSS ALL siblings (E168); a hardcoded enumeration that should introspect; an UNTESTED except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, or re-run a now-clean mechanical scan.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E169 re-red-teamed E150/E159/E169): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 62 rounds (R318..R380). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R380 §7): gate/token last-review CLEAN — `git log --grep=GATE-APPROVED\|CEO-APPROVED` since R370 = 0 real approvals. R380 touched ONLY docs/LESSONS.md + STATUS.md + loop/ledger/{experiments,rounds}.jsonl + BOARD.md (regen) — no code, no spine/manifest-gated path, no interface change (a review round).
last_review: R380
