# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R355 (E145, BUILD). HEADLINE: 36th consecutive sim-block HOLD — clean per next#3; both honest levers reverified SHUT cold from disk; check.sh RED was ONLY the mechanical BOARD age+1 drift (E102 skeptic-cadence, NOT a bug — regen+commit is inherent/required each round, NOT a manufactured sliver); NO doctor re-run (GREEN 4/4 fresh R352, 3 rds); NO manufactured sliver. (1) Sim slot HELD by the LIVE isaac go2w warehouse_nav sibling (pid 3569767, etime 03:45:16 — advanced +16min from R354's 03:29:26 ⇒ living, NOT a leak). Inv-5 CATEGORICAL — WAIT, sibling untouched, NO sim launched. (2) Owner gate G-323-1: 0 answered/crossed, 38 rounds unanswered (R318..R355); inbox only old-crossed G-183-12.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: blocked (§6 clean stop: sim slot held by a LIVE ~3.75h isaac sibling AND owner gate G-323-1 unanswered; non-gated non-sim frontier EXHAUSTED R329-R354. Block TRANSIENT (sim frees when sibling finishes). HONESTY META (E130): 36 hourly HOLDs is supervisor-cadence waste — RECOMMEND the supervisor widen the R-interval while both levers stay shut; the escalation, not another HOLD, is the live artifact owed.)
last-round: R355 (E145 BUILD: 36th sim-block HOLD — both levers reverified SHUT cold; check.sh RED was ONLY mechanical BOARD age+1 drift ⇒ regen+commit (E102, required not a sliver); NO doctor re-run (GREEN 4/4 fresh R352); NO manufactured sliver).
frontier: Offline plug-and-play Verify axis FULLY CLOSED (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate ROT-FREE (E121); axis-exhausted (E122-E143). The R312-R354 acceptance-face plateau (43 rounds) is REAL, STRUCTURAL, a HARD EXTERNAL block. The ONLY two honest levers: the sim slot frees (transient), OR the OWNER approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R353).
watch: While phase: blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+ `ps -o etime,rss` to confirm a LIVE run not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) add another offline plug-and-play proof, (b) re-audit an already-audited axis, (c) invent an audit sliver to fake motion, (d) reinterpret Inv-5 to self-unblock the sim (E129), NOR (e) re-investigate preflight's "67 provisionals" (SOLVED R354: false nag, TRUE owed=0; fix = CEO G-354-1 → E144) NOR (f) re-frame the per-round "BOARD.md stale" check.sh RED as a "found defect" — it is the MECHANICAL rounds-ago age+1 drift the supervisor's post-commit rounds.jsonl append guarantees EVERY round (E102); `python3 loop/board.py` regen+commit is INHERENT required maintenance driving the STALE_AGE=30 skeptic cadence, NOT a manufactured sliver and NOT a fixable bug (freezing ages would freeze the ⚠STALE markers) → E145.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 last re-confirmed R352; skip re-running unless >~10 rds stale).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R353 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R352), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF STILL BLOCKED next round] Reverify both levers (watch). Both shut AND no genuinely-new non-gated axis → HOLD (phase blocked), one experiments row; do NOT manufacture an audit sliver, do NOT re-investigate the preflight nag (E144). Only a real lever-open or a genuinely-new North-Star axis ends the hold.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R352, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121, axis-exhausted E122-E143): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 37 rounds (R318..R354). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (NEW R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0. FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer, planner-free contract) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-10 CLEAN (no GATE-APPROVED/CEO-APPROVED tokens). R354 touched NO spine path and did NOT self-approve the MANIFEST gate (edits made, VERIFIED, then REVERTED; queued as G-354-1). Files changed: ledger rows + STATUS + LESSONS + BOARD regen + events; the gated preflight.sh/checks_schema.py restored to HEAD.
last_review: R350
