# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R318 (E109, BUILD) — the eyes-POSE frontier is RESOLVED, not by a better VLM but by asking the right channel: the DETERMINISTIC quat-upright hard-channel reads pose 3/3 where every local VLM failed (E105/E107/E108). Per Invariant 1 pose HAS ground truth the actor cannot author (the sim root quat) ⇒ the hard channel is the CORRECT pose verifier; a VLM is the strictly weaker choice. VLM eyes stay reserved for object identity/placement (no deterministic channel there — the confirmed vlm-judge use).
  §1a FLOOR: adopted supervisor R317 rounds.jsonl append + regen BOARD (ages +1). Inflight empty; no WIP; inbox has no unanswered decision; provisionals check-exempt.
  §1b: E108(refuted) already carried final status. 67 acceptance provisionals all check-exempt (backfill/superseded/≤2r) → check.sh GREEN, none wedge.
  §2-4 (non-gated, no billing, NO spine path): STATUS next#1(a) — built `tools/acceptance/pose_upright.py`: upright_cosine = m22 of R(root-quat) = world-up·body-up; classify STANDING/FALLEN at a 0.5 (=60° tilt) threshold. TDD 11/11 (`tests/unit/test_pose_upright.py`). Proved on the EXACT R316/R317 world-rotations at the REAL keyframe stance (1,0,0,0), sim-free via pure `mju_mulQuat` (a sibling loop held the one global sim slot — WAITed per Inv-5, never double-ran).
  E109 (build, CONFIRMED): orientation 3/3 correct — upright_yaw90 +1 STANDING, back-topple -1 FALLEN, toppled_side_left cos 0.0 → FALLEN (minicpm read it STANDING 4/4 in ALL azimuths, E108). sunk_floor is orientation-upright by construction ⇒ a SEPARATE deterministic z/height channel, not this oracle's job (honestly scoped).
  HEADLINE: the 6-round eyes-POSE probe (R312-R317) was asking the wrong question — pose is exactly the wrong job for a VLM because it has actor-unforgeable ground truth. Landing pose_upright into the verify spine (acceptance/ + rubric) is honest-verify-spine semantics = CEO gate G-318-1. Offline oracle probe, NOT the acceptance face → experiments row only, no acceptance row. NO spine path touched this round.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier: the eyes-POSE lever is now RESOLVED — hard channel (E109) not VLM; the pose question leaves the eyes pillar entirely. Every NEW capability that could advance the acceptance face is CEO-gated: G-318-1 (land pose_upright) · G-315-1 (VLM pose rubric — now MOOT for pose) · S4 · D182 · D76.)
last-round: R318 (E109 CONFIRMS the deterministic quat-upright hard-channel reads go2 pose 3/3 where every local VLM failed; new non-spine tools/acceptance/pose_upright.py, TDD 11/11). No spine path touched.
frontier: eyes-POSE is SETTLED — pose → deterministic quat-upright hard-channel (E109), VLM eyes → object identity/placement (confirmed). Remaining acceptance-face growth is ALL CEO-gated: land pose_upright (G-318-1); S4 (3rd embodiment BYO-URDF); D182 (world-owned NL→object grounder, also fixes ordinal/witness fidelity); D76 planner-plugin (quantity N-object). Non-gated frontier is now THIN — the honest next probe is a HEIGHT/z channel (sunk_floor) to complete the deterministic pose+height pair.
watch: pose_upright ORACLE (non-spine, pure math, no sim): `python tools/acceptance/pose_upright.py <w> <x> <y> <z> [--thresh 0.5]`; unit `tests/unit/test_pose_upright.py`. Demo on real keyframe poses: `.venv/bin/python var/evidence/R318/prove_quat_oracle.py` (sim-free — reads keyframe quat 1,0,0,0 from mujoco_go2.py:397, applies R317 world-rotations via mju_mulQuat). The keyframe 'stand' root quat IS identity ⇒ no sim/nav-stack launch needed to derive the stance.
  SIM-SAFETY (Inv-5, learned R318): a SIBLING loop can hold the one global sim slot; `MuJoCoGo2.connect(room=True)` launches the full CMU nav stack (localPlanner/laser_mapping/…). BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco'` NOT just `mujoco` — WAIT if live, NEVER teardown a sim you did not launch. Do NOT run scripts/sim-teardown if your own run crashed pre-connect (it nukes by pattern = a sibling's stack).
  POSE-PROBE recipe (VLM, refuted for pose): tools/acceptance/pose_probe.py [--framing neutral|fault] <model> <N> <frame..>; render via var/evidence/R317/render_multiview.py. NEVER a 2nd MuJoCoGo2 renderer (segfault). Local VLM cold-load ~90s.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; scripts/sim-teardown after. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD (E27/E81/R316/R317/R318): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (R318 E109 trimmed 1273→1023), each string field ≤280 CHARS. experiments key `e` NOT `experiment`; experiments status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds append shifts ages → NEXT round MUST regen BOARD.
next:
  1. [NON-GATED research — completes the deterministic pose pair] Build a HEIGHT/z hard-channel companion to pose_upright (sunk_floor is orientation-upright but root_z << stance_z=0.35): a `root_z_ok(z, stance_z, tol)` oracle + TDD, proven on the sunk_floor case (minicpm split it, E108). Then pose = orientation(E109) + height together read ALL 4 R316 fault topologies deterministically. Still tools/, non-spine, offline. Bank provisional.
  2. [OWNER GATE — the ONLY path landing pose eyes into the acceptance face] G-318-1 (wire pose_upright as the pose verify-channel in acceptance/ + rubric = honest-verify-spine/moat). This SUPERSEDES the model half of G-315-1 (a VLM pose rubric reword is now the wrong tool — pose has actor-unforgeable ground truth). Owner call owed. Also still queued: S4 · D182 · D76.
  3. [NEXT REVIEW = R320] Skeptic-re-verify oldest confirmed rows; re-red-team a headline; adjudicate provisionals (incl. E109); wiring audit; ambition critic — R312-R318 were ALL eyes-pillar probing with NO acceptance-face advance (7 rounds): the honest read is the acceptance face is GATE-BLOCKED, not idea-blocked. Say so; propose the owner unblock one gate.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-318-1 SPINE/MOAT (pose verify-channel): E109 proves a deterministic quat-upright oracle (tools/acceptance/pose_upright.py) reads go2 pose 3/3 where every local VLM failed. Landing it as a verify channel edits acceptance/ + the rubric (ADR-002 moat, checksum-pinned) = honest-verify-spine semantics. Per Invariant 1 this is STRICTLY better than a VLM (reads ground truth the actor cannot author). Owner call owed. <- next#2. (R318/E109)
  - G-315-1 SPINE (VLM pose rubric): now MOOT FOR POSE — E109 shows pose belongs to the hard channel (G-318-1), not a reworded VLM rubric item. KEEP only as: if the owner still wants a VLM 2nd-witness for pose, E105/E107/E108 say no local VLM (single or multi-view) robustly reads side-lying. Superseded-in-spirit by G-318-1. (R315/E106..R318/E109)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R318 CLEAN. R318 touched NO spine path (added tools/acceptance/pose_upright.py + tests/unit/test_pose_upright.py + var/evidence/R318/ demo (gitignored); 1 experiments row E109 + LESSONS E108-line extension + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ or config/rubric edit).
last_review: R310
