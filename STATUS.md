# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R326 (E117, BUILD/doc-promotion). HEADLINE: the R325/E116 plug-and-play VERIFY finding is now PROMOTED into the ADOPTION doc — the misleading WIRING verify-spine "Seam for a new world" bullet, which told a BYO contributor that landing a verify predicate needs the gate-approved `_PREDICATE_ORACLES` edit, is CORRECTED. It now states the truth E116 proved and this round re-verified (guard 10/10): a BYO predicate GROUNDS zero-kernel-edit via `pred()==True` or `state()==const`; `_PREDICATE_ORACLES` is a first-party-only BARE-idiom affordance (why G-323-1 edits it), NOT a BYO requirement — moat intact. (reference.md was at its 280-line doc cap → left untouched; WIRING is the canonical seam home.) Doc-only, non-spine, no sim, no new guard (a 2nd adjudication guard is treadmill-banned).
  §1 ORIENT: inflight absent; no WIP; no experiments provisionals (E116 already confirmed). 67 acceptance provisionals all check-exempt → check.sh GREEN. inbox: no unanswered decisions. SIM-BLOCKED AN 8TH TIME: a sibling loop holds the one sim slot (isaac_sim launch PID 3564964 + localPlanner 3564979 + laser_mapping 3564987, live; +go2w RL train) → Inv-5 + NEVER-KILL-INFRA: NOT touched, launched NO competing sim ⇒ next#1 (real-face stale-row re-run) stays blocked, as R320-R325. Frontier bans a 5th oracle / 2nd guard / 2nd adjudication guard (treadmill) ⇒ PIVOTED to the honest non-treadmill lever: make the E116 contract DISCOVERABLE + CORRECT in the adoption docs (a real BYO contributor was being misled that a gate was needed).
  §4 REAL-VERIFY: doc-promotion round (§5b, next-round after E116's R325 formation) ⇒ acceptance is (a) re-running the EXISTING E116 guard on the REAL frozen classifier: `test_plug_and_play_boundary.py` 10/10 reproduces; (b) the corrected docs match the guard's pinned cases (`pred()==True`/`state()==1` GROUNDED; bare `pred()` RAN; `or True`/self-taut/absent RAN). eyes: self-read. experiments row only (E117), NO acceptance row (no sim face touched). NO spine path touched — WIRING/reference.md are docs; the frozen classifier/_PREDICATE_ORACLES are UNCHANGED.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE-blocked: every acceptance-face advance is CEO-gated — G-323-1 (the doctor-guarded quartet landing) · S4 · D182 · D76)
last-round: R326 (E117 BUILD: promoted E116 into the WIRING verify-spine seam bullet — corrected its misleading claim that a BYO verify predicate needs the gated `_PREDICATE_ORACLES` edit; re-verified guard 10/10. reference.md at its 280-cap → untouched. Sim-blocked 8th; no spine touched, no new guard).
frontier: the offline hard-channel well is DRY/PACKAGED/GUARDED (E109-E115), the plug-and-play Verify contract is proven at adjudication (E116) AND now DOCUMENTED for adopters (E117) — every non-gated axis is worked out. The 14-round acceptance-face plateau (R312-R326) is REAL and STRUCTURAL: the face is GATE-blocked, not idea-blocked. ONE honest lever left: the OWNER approves G-323-1 (import + 4 predicate registrations + `_PREDICATE_ORACLES` entries + rubric — one low-risk diff, doctor-confirmed no interface change). A 5th oracle / 2nd guard / 2nd adjudication guard = treadmill — do NOT. Further doc churn on E116 is also done — do NOT re-document.
watch: E116/E117 adjudication finding — non-spine, no sim:
  Zero-kernel-edit BYO verify idiom = write `pred()==True` / `state()==const` (NOT bare `pred()`, which needs the hardcoded `_PREDICATE_ORACLES`). Now documented: docs/WIRING.md verify-spine "Seam for a new world" bullet (E117). Guard: `./scripts/run-tests tests/vcli/test_plug_and_play_boundary.py` (10). Runtime verdict path = `trace_store.evidence_passed`→`classify_step_evidence`→`classify_verify_expr(expr, oracle_names)`; `oracle_names`=`verify_oracle_names(agent,engine)`=keys of ns merging `World.build_verify_namespace`.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. `hard_channels.py {upright|height|near|facing}`; `make_verify_namespace(agent)` → the 4 GT-reading fail-safe-False predicates. Run doctor BEFORE landing G-323-1.
  LANDING SHAPE (gate round): merge `make_verify_namespace` into `RobotWorld.build_verify_namespace` (robot.py:191 go2-base `ns.update`, beside at_position/facing), add each name to `evidence_classifier._PREDICATE_ORACLES` (bare-idiom grounding, per E116) + `verify_signatures()` to vocab, reword rubric — WIRING verify-spine §. All on EXISTING base API (get_position/get_heading/get_odometry quat); identity stays VLM/D182.
  SIM-SAFETY (Inv-5, R318/R320-R326): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (NOT just `mujoco`); WAIT if live, NEVER teardown a sim you did not launch.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (E117 row landed 991 B after 4 trims), each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board: `.venv/bin/python loop/board.py`.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R326 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (confirm READY-TO-LAND), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` (bare-idiom grounding, E116) + rubric (one low-risk diff, no interface change). Consolidates G-318-1 · relational-near · G-322-1. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] Every non-gated axis is now worked out (offline hard-channels E109-E115; plug-and-play Verify adjudication E116; adoption docs E117). Do NOT build a 5th oracle, a 2nd guard, a 2nd adjudication guard, NOR re-document E116 (treadmill). Honestly hold and push the owner to unblock G-323-1, OR pick a genuinely-new non-spine ergonomic ONLY if it advances the North Star. Do NOT reopen any eyes-VLM pose/heading probe (refuted E105-E108).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing, DOCTOR-GUARDED E115, bare-idiom rationale E116, adoption-doc'd E117) <- next#2: merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114-packaged 27/27 + cores 72/72; landing precondition E115-guarded) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind to the EXISTING base API (get_position/get_heading/get_odometry quat) ⇒ NO interface change (doctor confirms on the live tree); the `_PREDICATE_ORACLES` edit is REQUIRED for the bare-predicate idiom (E116), not an interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. One low-risk diff. Owner call owed. SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1 (kept for history). (R323/E114, R324/E115, R325/E116, R326/E117)
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - META (world-declared predicate-oracles): a proper seam letting a BYO world declare its bare-predicate oracles (so bare `pred()` grounds without editing `_PREDICATE_ORACLES`) is a SPINE/interface gate — NOT needed for the contract (E116: `pred()==True` is the zero-edit path, doc'd E117), queued as a nice-to-have. (R325/E116)
  - G-315-1 SPINE (VLM pose rubric): MOOT — pose belongs to the hard-channel quartet (G-323-1). (R315/E106..R320/E111)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py; CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — the IDENTITY half the quartet's geometry channels leave to the VLM (E46, E112/E113). <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R326 CLEAN. R326 (build/doc) touched NO spine path (corrected 1 WIRING verify-spine prose bullet + refreshed its verified-against a7d3f1a->3353a03; refined 1 LESSONS line; appended experiments E117; STATUS; NO acceptance/rubric/interface/classifier/_PREDICATE_ORACLES edit).
last_review: R320
