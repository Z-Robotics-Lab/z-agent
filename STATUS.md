# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R705 (E230, BUILD). HEADLINE: **fetch.nl-new-world-courtyard-purple (age~414 STALE, the OLDEST confirmed BOARD row) RE-GROUNDS on the live face — 4th STALE-row re-verify in the cadence (warehouse R702 / clutter R703 / courtyard R704 / courtyard-purple R705).** Gate `pgrep mujoco|vcli` CLEAR (only chronic match = go2w Isaac warehouse_nav sibling PID 3969555, DIFFERENT engine, does NOT trip the MuJoCo gate; alive throughout, Inv-8 untouched by scoped teardown); 38G avail + 7.5G VRAM free at launch. Ran the bare `vector-cli` courtyard PURPLE-BOX fetch in a **BACKGROUND job** (internal `timeout 900`, `.venv/bin/python`, ulimit -v 50G, deepseek-chat brain + gemma4:e4b eyes, VECTOR_ROOM_TEMPLATE=courtyard) per LESSONS L82: query=`紫色的盒子` → **verify holding_object('pickable_box_purple') actor=CAUSED (GT the actor cannot author), verified=True 1/1**, launch_explore_seen=False (in-process D163). vlm-judge witness=**PASS on ALL FOUR** (scene_rendered/robot_upright/body_intact/workspace_in_frame=yes). HUMAN LOOK var/evidence/R705/eyes_fetch.png: **purple BOX aloft in the gripper; blue/green/yellow cyl + red can distractors REMAIN** = colour AND geometry discrimination (box vs cylinders, not lucky-default); robot upright/intact; genuine open-sky sandstone COURTYARD. §1b/§1c: courtyard-purple confirmed→**re-confirmed** (supersedes R290 age~414 stale = clock-tick from appends, NOT a regression; R310/R702/R703/R704 precedent for re-verify of an already-confirmed capability). NOTE: first launch had a MISORDERED arg (tag→FETCH slot; sig is `<FETCH> <PLACE> <TAG> <MODE>`); caught in <90s from the [driver] echo, killed MY tree only (Isaac untouched), scoped teardown, relaunched clean. Sim torn down scoped (rosm nuke, 12 PIDs); Isaac sibling verified ALIVE post-teardown; host >30G avail throughout.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The acceptance face RUNS with a sibling Isaac sim present given headroom — confirmed across SIX independent rounds (R700-R705). Abundant non-gated work: ~25 remaining STALE BOARD rows to re-verify + breadth. Re-check the ACTUAL gate + `available` RAM/free VRAM each round; HOLD only if the gate is dirty OR headroom genuinely absent (available<~10G or free VRAM<~3G).
last-round: R705 (E230 verify confirmed). Re-verified fetch.nl-new-world-courtyard-purple (age~414→age0) in BACKGROUND with full vlm-judge PASS + human look. Self-approval audit: NO code/interface/spine/manifest edit ⇒ NOT a CEO gate, no token.
frontier: The 3-world go2 fetch + g1 cross-embodiment bars are the FLOOR. Real frontier: (1) keep re-verifying the ~25 STALE confirmed BOARD rows on the live face, oldest first; (2) CEO-gated capability rungs (G-323-1/S4/D182/D76) need owner action for NEW capability, NOT a face blocker. Meta-lesson STANDS: a HOLD read rotted 100+ rounds; the skeptic re-verify cadence catches it (E79/E90/E223-E230).
next:
  1. [NON-GATED, THE REAL BACKLOG] resume re-verifying STALE BOARD rows on the live face. Next oldest confirmed: fetch.nl-new-world-courtyard-red (age~414, target=pickable_can_red, utter e.g. `把红色的罐子拿过来`, VECTOR_ROOM_TEMPLATE=courtyard) / g1.navigation (age~412, g1_nav_accept.py) / g1.perception (age~410, g1_accept.py). BROAD pgrep + `available`/VRAM check FIRST; WAIT only if genuinely no headroom.
  2. [HARNESS RECIPE — LESSONS L82] repl_accept.py arg order is `<FETCH> <PLACE> <TAG> <MODE>` (tag is 3rd, NOT 1st) — verify the [driver] `FETCH='...'` echo in the first ~90s of the log before letting a background run proceed. Any bare-cli re-verify whose sim-build+grasp can exceed ~9 min MUST run in a BACKGROUND job with internal `timeout 900` + scoped teardown (host foreground tool cap = 10 min) and use `.venv/bin/python`. Courtyard purple-box fetch fit ~8 min end-to-end in background this round.
  3. [ADJUDICATE §1b] R705 banked E230 acceptance as re-confirmed directly (R310/R702/R703/R704 precedent); experiments E230 is provisional → adjudicate next round. Nothing else owed beyond the standing STALE-row cadence.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-323-1 hard-channel quartet (queued ~111 rounds) · G-383-1 has_arm · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old; true owed audited each round). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen ROUTING key arreared; qwen3-vl JUDGE arreared — deepseek-chat + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R700
