# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R305 (E94+E95, build) — STATUS next#1: PROMOTED courtyard-blue vlm-judge provisional → confirmed N=2 (re-ran bare-face across R304→R305 boundary, GROUNDED 1/1, judge PASS). next#2: skeptic re-confirmed the 2nd-oldest stale bar kernel.model-unavailable-surfacing (age~45→0). HEADLINE: eyes pillar now UNIFORM (vlm-judge) AND CONFIRMED (N≥2) across ALL non-house fetch/place bars — courtyard-blue was the last N=1 provisional. verified= rests on holding_object(blue) GT oracle (actor=CAUSED) + human LOOK, NEVER the non-determ judge (E72/E92 fail-closed); judge gemma4:e4b FIRED=PASS as a bonus datapoint. LOOK on verdict frame: blue cyl aloft in gripper (sandstone courtyard), red/yellow/purple/green remain on pick-table = discrimination.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~53r unanswered; non-gated NEW-capability frontier EXHAUSTED — loop treads on eyes-pillar upgrades + skeptic hygiene of stale bars while the gates stand). Concurrent SIBLING sim this round: Dockerized Isaac go2w nav (/workspace/go2w warehouse_nav.py, ~9GB) — NOT this repo's loop, NEVER-KILL-INFRA; my MuJoCo run hard-capped MemoryMax=24G serial, avail stayed ~40G → no host risk, proceeded.
last-round: R305 (E94+E95, build). §0/§1: BOOTSTRAP clean (preflight OK, no quarantine, inflight empty). §1a: adopted R304 supervisor rounds.jsonl append + regen BOARD (ages shifted, pure-append). §1b: (a) adjudicated the R304 courtyard-blue vlm-judge provisional → re-ran bare-face, GROUNDED N=2, banked confirmed superseding R304 (E94); (b) next#2 skeptic re-confirmed kernel.model-unavailable-surfacing 404-case on the bare REPL (clean 'model unavailable' line, zero credit), banked confirmed superseding R260 (E95). LOOKED at the courtyard-blue verdict frame.
frontier: Verify-pillar CLOSED at strength: eyes=vlm-judge FIRES + CONFIRMED (N≥2) across ALL non-house fetch/place bars (house + courtyard + warehouse, all colours, place too). RESIDUAL non-gated (local-hill skeptic hygiene): the genuinely-oldest stale bars remain quantity-place.nl (age~100, KNOWN brain-flaky GROUNDED-FLAKY 2/3, deepseek-chat self-read) and a few self-read house-fetch bars (category-only ~42, negated-distractor ~37, plain-colour ~45). NEW capability still needs owner S4/D182. gemma4:e4b judge PASS↔ABSTAIN↔FAIL non-determ (E72/E92) — a TRUSTED discriminator needs a stronger VLM (funded qwen3-vl / better local).
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select env vars (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): ROOM_TEMPLATE={warehouse|courtyard}, SCENE_CLUTTER=1, SCENE_SWAP=1.
  HARNESS: fetch launcher = var/evidence/R305/run_fetch.sh <room> <colour> <fetch_nl> (uses .venv/bin/python; systemd --scope has NO `python` on PATH). MODE=fetch → 1 fetch turn + eyes_frame; args = FETCH PLACE TAG fetch (place NL ignored but MUST be present; TAG must not be a valid mode word). eyes vlm-judge AUTO-routes to local ollama gemma4:e4b. Colour→noun: blue=蓝色的瓶子, purple=紫色的盒子(box), red=红色的罐子(can). Non-sim bar: tools/acceptance/model_unavailable_accept.py 404 (needs OPENROUTER_API_KEY; free pre-gen reject). RUN sim FOREGROUND, SERIAL one-at-a-time, under `systemd-run --user --scope -p MemoryMax=24G`; set VECTOR_EVIDENCE_DIR per run. Check `free -g` before launch. NEVER pkill mujoco; scripts/sim-teardown after. A ~9GB Isaac python3 (/isaac-sim, go2w) is a SIBLING project's sim — leave it.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps (checks_schema.py): line ≤1024 BYTES, each string field ≤280 chars (bit me 1× this round on kernel-row redteam — trim). experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); a supervisor rounds.jsonl append shifts BOARD ages → NEXT round MUST regen.
next:
  1. [SKEPTIC hygiene] Skeptic-verify/refute the genuinely-oldest stale bar quantity-place.nl (age~100, KNOWN brain place-decompose flaky, deepseek-chat self-read GROUNDED-FLAKY 2/3) on the real bare face — a stale green is worse than a known red; re-run or refute with supersedes.
  2. [SKEPTIC hygiene] The remaining stale self-read HOUSE fetch bars: fetch-place.nl-category-only (~42), fetch.nl-negated-distractor (~37), fetch.nl-plain-colour (~45) — skeptic re-verify / eyes-upgrade self-read→vlm-judge where the bar is vision-graded (Inv.1 stricter-only).
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~53r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R281..R305 CLEAN. R305 touched NO spine path (ledger + STATUS + BOARD regen + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
