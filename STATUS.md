# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R302 (E92, verify) — STATUS next#1: adjudicated BOTH R301/E91 place provisionals across the R301→R302 boundary. HEADLINE: PLACE is now vlm-judge-eyes GROUNDED-CONFIRMED on ALL 3 WORLDS (house E88/E89 + courtyard + warehouse). courtyard GROUNDED 2/2 reproduces (supersedes R301 prov→R259 self-read); warehouse CLEAN re-run GROUNDED 2/2 (the R301 RAN WAS a brain place-decompose flake E60/E70 — physical place always OK; supersedes R301 RAN, re-confirms R262 + upgrades eyes self-read→vlm-judge). CAVEAT (E72): the gemma4:e4b judge FIRED but returned FAIL(workspace_in_frame=no) on BOTH clean R302 frames a human trivially passes (vs R301 PASS/ABSTAIN) — NON-DETERMINISTIC, recorded-NOT-trusted; fail-closed so verified= stands on the GT resting_on_receptacle oracle + human LOOK, never the judge.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~50r unanswered; non-gated NEW-capability frontier EXHAUSTED again — place-transfer CLOSED on all 3 worlds now under vlm-judge eyes; loop treads on eyes-pillar upgrades of stale self-read bars + skeptic hygiene while the gates stand).
last-round: R302 (E92, verify). §0/§1a: BOOTSTRAP clean (preflight OK, no quarantine, inflight empty). Adopted R301's orphaned WIP (supervisor R301 rounds.jsonl append + BOARD regen, pure-append). §1b: adjudicated the two E91 place provisionals — both PROMOTED to confirmed via clean bare-face re-runs under vlm-judge (courtyard + warehouse, MODE=place, deepseek-v4-flash + local gemma4:e4b, in-process, launch_explore_seen=False). LOOKED at both verdict frames: green RESTING in each world's oak bin, red+purple remain on the pick table = discrimination.
frontier: Verify-pillar: eyes=vlm-judge FIRES on fetch (E69/E71) AND place (E88/E89 house · E92 courtyard+warehouse — now vlm-judge GROUNDED-CONFIRMED on ALL 3 worlds). Ordinal/relational NL fidelity still WITNESS-only (D182). RESIDUAL non-gated (local-hill): (a) upgrade the STALE self-read FETCH colour bars (warehouse blue/purple/red age~63, courtyard-blue) to vlm-judge eyes — the last self-read non-house fetch bars; (b) skeptic-verify the genuinely-oldest stale bars (quantity-place.nl age~97, kernel.model-unavailable age~43). NEW capability still needs owner S4/D182. gemma4:e4b judge stays PASS↔ABSTAIN↔FAIL non-determ (E72/E92) — a TRUSTED discriminator needs a stronger VLM (funded qwen3-vl / better local).
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select env vars (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): SCENE_CLUTTER=1, ROOM_TEMPLATE={warehouse|courtyard}, SCENE_SWAP=1 (swaps blue↔green only; yellow unmoved E90).
  HARNESS: launcher = var/evidence/R<N>/run_place_<world>.sh (uses .venv/bin/python — the systemd --scope has NO `python` on PATH). eyes vlm-judge AUTO-routes to local ollama gemma4:e4b (resolve_judge_env local-prefers it). MODE=place opens a FRESH session. repl_accept.py args = FETCH PLACE TAG place. RUN sim FOREGROUND under `systemd-run --user --scope -p MemoryMax=24G` (place run ~2-4min, fits deadline); pass ROUND_N + set VECTOR_EVIDENCE_DIR per run (>1 run/round else E54 overwrite). NEVER pkill mujoco → in-process sim dies with the python proc; pgrep -f "mujoco" MATCHES YOUR OWN shell cmd string (false-positive) — confirm with `ps`/`pgrep -af` before assuming a live sim.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→dumps→write-all (reorders → quarantine). Row caps ENFORCED by checks_schema.py: line ≤1024 BYTES (bit me 3× this round — trim redteam/note), each string field ≤280 chars. experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); a supervisor rounds.jsonl append shifts BOARD ages so the NEXT round MUST regen it.
next:
  1. [NON-gated eyes-pillar upgrade] Extend the self-read→vlm-judge upgrade to the last STALE self-read FETCH bars in a non-house world: warehouse blue/purple/red (age~63, MODE=fetch, ROOM_TEMPLATE=warehouse) — re-run under vlm-judge, LOOK, promote. Consistent stricter-only (Inv.1) with the place upgrade just landed; last local-hill fetch bars before the eyes-pillar is uniform.
  2. [NON-gated skeptic hygiene] Re-verify the genuinely-OLDEST stale confirmed rows on the real face (quantity-place.nl age~97 — KNOWN brain-flaky R270/R273, expect a repro-questioned/refuted finding; kernel.model-unavailable-surfacing age~43). A stale green is worse than a known red.
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~50r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R280..R302 CLEAN. R302 touched NO spine path (ledger + docs/LESSONS + STATUS + BOARD regen + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
