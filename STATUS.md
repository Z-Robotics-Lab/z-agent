# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R313 (E103, BUILD) — FIRST forward motion on the eyes-pillar frontier since E92: a STRONGER local VLM judge is DETERMINISTIC where gemma4:e4b is not.
  §0 QUARANTINE CLEARED (R312's post-check breach): trimmed acceptance.jsonl:176 redteam 359->274 chars + regen BOARD. Adopted R312 WIP (purple-box promoted confirmed; VECTOR_JUDGE_LOCAL_MODEL seam), 38/38 vlm_guard unit green.
  §2-4 FRONTIER (E103, non-gated config axis, no billing/spine): pulled qwen2.5vl:7b (ollama, 6GB); ran the FROZEN vision_judge via R312's VECTOR_JUDGE_LOCAL_MODEL seam N=5 on a real R311 fetch verdict frame + N=3 on a black frame.
  RESULT: qwen2.5vl:7b 5/5 identical PASS (4/4 rubric yes) on the purple-box fetch frame — DETERMINISTIC; gemma4:e4b 5/5 all-ABSTAIN on the SAME clear frame = never grades today. First LOCAL model that GRADES a real acceptance frame deterministically.
  RED-TEAM (survived, w/ caveat): NOT a rubber-stamp — qwen 3/3 ABSTAIN on a black frame (raw "completely black, impossible to determine", won't PASS). CAVEAT: it ABSTAINs (not FAILs) the black frame + truncates to 1 item, CPU-only ~90s/call → active-DOWNGRADE (FAIL a bad-but-RENDERED frame) UNPROVEN. Frontier ADVANCED, not CLOSED.
  HEADLINE: eyes-pillar had a TRUSTED-2nd-witness gap (E72/E92: gemma non-determ). qwen2.5vl:7b is a deterministic CANDIDATE — but landing it as REAL eyes (live bare-cli run) + proving it actively FAILs a bad frame is OWED. Experiments row E103 (research); no acceptance row (judge-quality probe, not the acceptance face). New CAPABILITY still CEO-gated (S4/D182/D76).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier: one deterministic-local-judge candidate found — real-face landing + active-FAIL proof owed; NEW capability blocked on owner S4/D182/D76).
last-round: R313 (E103 research). Cleared R312 quarantine; trialed qwen2.5vl:7b as a stronger local judge — DETERMINISTIC 5/5 PASS vs gemma 5/5 ABSTAIN on the same real fetch frame; NOT a rubber-stamp (3/3 ABSTAIN on black); active-FAIL + real-face landing owed. No spine path touched.
frontier: A DETERMINISTIC local vlm-judge candidate EXISTS (qwen2.5vl:7b, R313/E103) — the eyes-pillar's E72/E92 "trusted 2nd discriminator needs a stronger VLM" is now HALF-answered. TO CLOSE: (a) live bare-cli fetch with VECTOR_JUDGE_LOCAL_MODEL=qwen2.5vl:7b as the eyes, upgrading a real bar off the untrusted gemma judge; (b) prove qwen actively FAILs a bad-but-RENDERED frame (toppled robot / wrong object), not just abstains. Every NEW capability is CEO-gated: S4 (3rd embodiment BYO-URDF), D182 (world-owned NL grounder), quantity N-object (D76 planner-plugin).
watch: Real-face recipe: bare vcli + NL; FORCE deepseek + v4-flash via env (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. Point the eyes judge at the stronger local model via VECTOR_JUDGE_LOCAL_MODEL=qwen2.5vl:7b (R312 seam; local ollama, no billing).
  JUDGE PROBE: tools/acceptance/judge_determinism.py <frame> <model[,model2]> <N> — measures run-to-run witness stability on a fixed frame via the frozen vision_judge; local ollama only. Models slow on CPU (~90s/call, cold-load 77-93s) — WARM via `ollama run <m>` or a curl /api/generate before a batch; run DETACHED + poll; qwen2.5vl:7b & gemma4:e4b are pulled.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. A run ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; `free -g` + `pgrep -af repl_accept` before launch; scripts/sim-teardown after. NEVER pkill mujoco.
  TESTS: pure-unit only via `./scripts/run-tests <path>` (memory-capped 12G).
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (Chinese=3B/char), each string field ≤280 CHARS (redteam/result bite every round — trim; R312 quarantined on a 359-char redteam). experiments key `e` NOT `experiment`; experiments status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds.jsonl append shifts ages → NEXT round MUST regen BOARD.
next:
  1. [FRONTIER — non-gated, OWED] LAND qwen2.5vl:7b as REAL eyes: a live bare-cli fetch (deepseek-v4-flash) with VECTOR_JUDGE_LOCAL_MODEL=qwen2.5vl:7b, acceptance row eyes=vlm-judge — upgrade a bar off the untrusted gemma judge. AND prove qwen actively FAILs a bad-but-RENDERED frame (toppled-robot capture), not just abstains (E103 caveat). This is the only non-gated lever advancing the VERIFY pillar.
  2. [OWNER GATE — ONLY path to NEW capability] Unblock S4 (3rd embodiment BYO URDF+manifest) OR D182 (world-owned NL→object grounder) OR the D76 planner-plugin (quantity decompose). All CEO-gated ~60r.
  3. [NEXT REVIEW = R320] Skeptic-re-verify the oldest confirmed rows; re-red-team a headline (incl. this E103 qwen-determinism claim as if you didn't write it — re-run the probe); wiring audit; ambition critic.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R313 CLEAN. R313 touched NO spine path (added tools/acceptance/judge_determinism.py — a tools/ probe, NOT under vector_os_nano/acceptance/; 1 experiments row + LESSONS frontier line + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R310
