# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R712 (E235, BUILD). HEADLINE: **FIRST TWO-LEG ORCHESTRATION climb (frontier a) — a chained NL runs route THEN perceive in ONE turn, and the moat COMPOSES it with ZERO spine edit. `导航到坐标(11,3)，然后找前面的红色的东西` (g1, deepseek-v4-flash, bare vector-cli, launch_explore_seen=False) self-decomposed into nav→perceive with the CORRECT verifies on TWO independent unforgeable GT oracles — at_position (route) + detection_matches_gt (perceive) — and BOTH returned True. Verdict RAN 1/3 (HONEST RED, no fake verdict): the model navigated (missed default tol 0.30) then RE-navigated with tol=1.0 that PASSED verify but was already-at-baseline → actor_causation.py:290 grades that no-op retry UNCAUSED (moat fail-closed, correct); only the perceive leg GROUNDED. GAP = nav fail-then-loose-retry SPLIT, NOT the chain — orchestration is PROVEN sound end-to-end.** §1b: R711 tundra provisional RE-VERIFIED → confirmed (same recipe re-grounds fetch 1/1, N=2, eyes vlm-judge PASS, teardown clean). NON-GATED throughout (VerdictReport composes per-sub-goal; new harness tools/acceptance/g1_chain_accept.py only, no verdict.py/oracle/causation edit).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The loop now CLIMBS the ORCHESTRATION axis (multi-leg plan·route·verify), not just the WORLD axis. The acceptance face RUNS across the sibling Isaac sim given headroom. Re-check the ACTUAL gate (`pgrep mujoco|vcli`) + `available` RAM/free VRAM each round; HOLD only if the gate is dirty OR headroom genuinely absent (available<~10G or free VRAM<~3G).
last-round: R712 (E235 build, refuted-RED + E234 confirmed). Two-leg orchestration PROVEN sound (both GT oracles True) but RAN via a nav actor-causation split; tundra §1b promoted to confirmed.
frontier: The two-leg ORCHESTRATION chain is PROVEN composable by the moat with no spine edit; the last mile to a GROUNDED 2/2 is a CAUSED nav leg. NEXT: land the clean chain with a single-nav-reachable coord (~g1.navigation 9.5,4, red kept in view) so nav is CAUSED first-try = the FIRST multi-leg orchestration bar. THEN 3-leg (route→perceive→act) or same on go2; does an existing bar transfer into tundra? World axis (4 shells) is proven-by-repetition — a 5th swap is diminishing. Deep rungs (S4/D182/D76/G-323-1) stay CEO-gated.
next:
  1. [NON-GATED CLIMB — land the orchestration GROUNDED] re-run g1_chain_accept.py with a single-nav-reachable coord the g1 gait hits within DEFAULT tol in ONE nav (start ~9.5,4, a confirmed g1.navigation coord; verify red panel stays in head-cam view from there — red is at ~12.9,3.35, +x of spawn 10,3). Expect nav leg CAUSED → verified=True (2/2). Recipe: var/evidence/R712/run_g1chain.sh (edit CHAIN coord). Do NOT re-run (11,3)+retry (E235 do-not-retry). bg per L82.
  2. [NON-GATED FLOOR — interleave ≤1 per ~3 climbs] oldest confirmed STALE rows: place.nl-plain-colour (age~414, vlm-judge — WATCH gemma4:e4b abstain) / verify.eyes-place-judge-fires (age~414) / place.nl-new-world-courtyard+warehouse (age~410). BROAD pgrep + `available`/VRAM FIRST.
  3. [ADJUDICATE §1b next round] none owed — R712 banked E235 refuted (no adjudication debt) + tundra confirmed. Preflight's naive provisional counter over-reports (G-354-1); trust check.sh GREEN + BOARD.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-323-1 hard-channel quartet (queued ~120 rounds) · G-383-1 has_arm · G-354-1 preflight-counter · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. Consolidates G-315-1/G-318-1/G-322-1. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old — true owed audited each round). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen ROUTING+judge arreared; deepseek + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R710
