# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R702 (E226/E227, BUILD). HEADLINE: **STALE-row re-verify cadence RESUMES — the oldest confirmed BOARD row (fetch.nl-new-world-warehouse, age414) RE-GROUNDS on the live face, refreshed to age0.** Gate `pgrep -f 'mujoco|vcli'` CLEAR (same go2w Isaac sibling, different engine, does NOT trip the MuJoCo gate); 38G RAM avail + 7.5G VRAM free with it present. Ran the bare `vector-cli` warehouse green fetch FOREGROUND (deepseek-chat brain + gemma4:e4b eyes, ulimit -v 50G): **fetch_verified=True 1/1** (deterministic MOAT: holding_object('pickable_bottle_green') actor=CAUSED, unauthorable), launch_explore_seen=False (in-process path). vlm-judge witness=**PASS on ALL FOUR** fields incl `workspace_in_frame=yes`. Human LOOK var/evidence/R702/eyes_fetch.png: green cylinder aloft in gripper; red+magenta+yellow REMAIN on box = colour discrimination; robot upright/intact; warehouse world. §1b: fetch.nl-new-world-warehouse confirmed→**re-confirmed** (supersedes R287, age414 stale). Second re-verify (fetch.nl-scene-clutter, age414) attempted but **INCONCLUSIVE** — grasp in-flight (113 PGRASP, detections present) when the host's 10-min foreground tool cap cut it (no verdict); NOT a refutation. Both sims torn down scoped (12+12 PIDs); Isaac untouched (Inv-8); host stayed >30G avail.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The acceptance face RUNS with a sibling Isaac sim present given headroom — confirmed across THREE independent rounds (R700/R701/R702). Abundant non-gated work: the ~28 remaining STALE BOARD rows to re-verify + breadth. Re-check the ACTUAL gate + `available` RAM/free VRAM each round; HOLD only if the gate is dirty OR headroom genuinely absent (available<~10G or free VRAM<~3G).
last-round: R702 (E226 verify confirmed, E227 verify inconclusive). Re-verified the oldest STALE confirmed row (warehouse fetch, age414→age0) with full vlm-judge PASS. Clutter re-verify blocked by the 10-min foreground tool cap — retry via a background run. Self-approval audit: NO code/interface/spine/manifest edit ⇒ NOT a CEO gate, no token.
frontier: The 3-world go2 fetch + g1 cross-embodiment bars are the FLOOR. Real frontier: (1) keep re-verifying the ~28 STALE confirmed BOARD rows on the live face, oldest first; (2) CEO-gated capability rungs (G-323-1/S4/D182/D76) need owner action for NEW capability, NOT a face blocker. Meta-lesson STANDS: a HOLD read rotted 100+ rounds; the skeptic re-verify cadence catches it (E79/E90/E223-E226).
next:
  1. [NON-GATED, THE REAL BACKLOG] resume re-verifying STALE BOARD rows on the live face. Next oldest confirmed: fetch.nl-scene-clutter (age414, RETRY via BACKGROUND run — VECTOR_SCENE_CLUTTER=1, foreground 10-min tool cap cut E227) / fetch.nl-new-world-courtyard (age413) / g1.navigation (age409, g1_nav_accept.py) / g1.perception (age407, g1_accept.py) / E165 look-explore / E167 describe-then-pick. BROAD pgrep + `available`/VRAM check FIRST; WAIT only if genuinely no headroom.
  2. [HARNESS-CAP LESSON] any bare-cli re-verify whose sim-build+grasp can exceed ~9 min (2 sim-start attempts, clutter/harder grasp) MUST run in BACKGROUND with internal `timeout 900`, not foreground (host tool cap = 10 min, clamps a larger --timeout). Warehouse fetch fit foreground (~6 min); clutter did not (E227).
  3. [ADJUDICATE §1b] R702 banked NO provisionals (E226 re-confirmed directly per R310 precedent; E227 inconclusive, no promotion). Nothing owed next round beyond the standing STALE-row cadence.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-323-1 hard-channel quartet (queued ~110 rounds) · G-383-1 has_arm · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old; true owed audited each round). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen ROUTING key arreared; qwen3-vl JUDGE arreared — deepseek-chat + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R700
