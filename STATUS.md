# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R707 (E231, BUILD). HEADLINE: **fetch.nl-new-world-courtyard-red (age~416 STALE, the OLDEST confirmed BOARD row) RE-GROUNDS on the live face — 5th STALE-row re-verify in the cadence (warehouse R702 / clutter R703 / courtyard R704 / courtyard-purple R705 / courtyard-red R707).** This round ADOPTED R706's aborted E231 run (background job killed mid-FETCH at the round boundary, no verdict; no lingering sim). Gate `pgrep mujoco|vcli` CLEAR (only chronic match = go2w Isaac warehouse_nav sibling PID 3969555, DIFFERENT engine, does NOT trip the MuJoCo gate; ALIVE before+after scoped teardown, Inv-8 untouched); 38G avail + 7.3G VRAM free at launch. Ran the bare `vector-cli` courtyard RED-CAN fetch in a **BACKGROUND job** (internal `timeout 900`, `.venv/bin/python`, ulimit -v 50G, deepseek-v4-flash brain + gemma4:e4b eyes, VECTOR_ROOM_TEMPLATE=courtyard) per LESSONS L82: query=`红色的罐子` → **verify holding_object('pickable_can_red') actor=CAUSED (GT the actor cannot author=MOAT), verdict GROUNDED verified=True 1/1**, launch_explore_seen=False (in-process D163). NOTE: vlm-judge gemma4:e4b **ABSTAINed all 4 dims** this run → eyes DOWNGRADED vlm-judge→**human self-read** (moat authoritative, judge is only the secondary witness). HUMAN LOOK var/evidence/R707/eyes_fetch.png: **red can aloft in the gripper; purple box + yellow + green cyl distractors REMAIN** = colour discrimination (grabbed red, not a decoy); robot upright/intact; genuine open-sky sandstone COURTYARD. §1b: E231 acceptance+experiments banked **provisional** (adjudicate next round). Sim torn down scoped (rosm nuke, 12 residual PIDs); Isaac sibling verified ALIVE post-teardown; host >30G avail throughout.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The acceptance face RUNS with a sibling Isaac sim present given headroom — confirmed across SEVEN independent rounds (R700-R707). Abundant non-gated work: ~23 remaining STALE BOARD rows to re-verify + breadth. Re-check the ACTUAL gate + `available` RAM/free VRAM each round; HOLD only if the gate is dirty OR headroom genuinely absent (available<~10G or free VRAM<~3G).
last-round: R707 (E231 verify, provisional). Re-verified fetch.nl-new-world-courtyard-red (age~416→age0) in BACKGROUND; GT moat GROUNDED 1/1 + human LOOK PASS (judge abstained). Adopted R706's aborted E231 run. Self-approval audit: NO code/interface/spine/manifest edit ⇒ NOT a CEO gate, no token.
frontier: The 3-world go2 fetch + g1 cross-embodiment bars are the FLOOR. Real frontier: (1) keep re-verifying the ~23 STALE confirmed BOARD rows on the live face, oldest first; (2) CEO-gated capability rungs (G-323-1/S4/D182/D76) need owner action for NEW capability, NOT a face blocker. Meta-lesson STANDS: a HOLD read rotted 100+ rounds; the skeptic re-verify cadence catches it (E79/E90/E223-E231). NEW OBSERVATION: gemma4:e4b judge ABSTAIN rate rising — if it abstains on the next re-verify too, treat as a judge-health frontier item (Ollama model degrade vs prompt), not a per-run fluke.
next:
  1. [NON-GATED, THE REAL BACKLOG] resume re-verifying STALE BOARD rows on the live face. Next oldest confirmed: g1.navigation (age~412, g1_nav_accept.py, self-read) / g1.perception (age~410, g1_accept.py) / place.nl-new-world-courtyard (age~404) / place.nl-new-world-warehouse (age~404). BROAD pgrep + `available`/VRAM check FIRST; WAIT only if genuinely no headroom.
  2. [ADJUDICATE §1b] R707 banked E231 acceptance+experiments as PROVISIONAL → adjudicate next round: append `confirmed` (red-team survived + reproducible) or `refuted`/`superseded`. Nothing else owed beyond the standing STALE-row cadence.
  3. [HARNESS RECIPE — LESSONS L82] repl_accept.py arg order is `<FETCH> <PLACE> <TAG> <MODE>` (tag 3rd, NOT 1st) — verify the [driver] `FETCH='...'` echo in the first ~90s. Any bare-cli re-verify whose sim-build+grasp can exceed ~9 min MUST run in a BACKGROUND job with internal `timeout 900` + scoped teardown (host foreground tool cap = 10 min) and use `.venv/bin/python`. Courtyard red-can fetch fit ~6 min end-to-end in background this round.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-323-1 hard-channel quartet (queued ~113 rounds) · G-383-1 has_arm · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old; true owed audited each round). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen ROUTING key arreared; qwen3-vl JUDGE arreared — deepseek + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R700
