# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R709 (E232, BUILD). HEADLINE: **g1.navigation (age~412 STALE, the OLDEST confirmed BOARD row) RE-GROUNDS on the live bare-cli face — 6th STALE-row re-verify in the cadence (warehouse R702 / clutter R703 / courtyard R704 / purple R705 / red R707 / g1-nav R709).** This round FIRST cleared a quarantine breach (acceptance.jsonl:184 E231 redteam field 288→275 chars, BOARD regen; checks_schema OK) then ADOPTED R708's inflight E232 background job (started R708, still LIVE at cold-start ~500s into its 900s internal timeout). The job COMPLETED cleanly rc=0 (NOT a timeout): bare `vector-cli` REPL, g1 sim started by NL, two NAV legs to NON-MEMORIZED coords A=(9.0,3.5) B=(9.5,4.0) (inside the proven-clear box; R217 noted (11,3)/(11,4) obstacle-blocked). Raw REPL trace: legA `navigate(9,3.5)`→`verify at_position(9.0,3.5) ✓ (actor=CAUSED)`→`verdict GROUNDED verified=True`; legB `navigate(9.5,4.0)`→`verify at_position(9.5,4.0) ✓ (actor=CAUSED)`→GROUNDED verified=True. **N=2/2 GROUNDED**, GT pose oracle = the MOAT (actor cannot author its own pose). eyes=**self-read** (g1-nav renders no frame → "no verdict_*.png emitted", GT pose is the witness — honest, consistent with all prior g1.nav rows). launch_explore_seen=False (in-process D163). Red-team SURVIVED: tool calls carry the DEMANDED coords (not R216 9,3/10,4 nor R217 12,3) = non-memorized, 2 distinct coords → not a fixed-pair fluke. §1b: E232 acceptance+experiments banked **provisional** (adjudicate next round). Scoped teardown clean (NUKE killed 12 procs, residual none); 38G avail post-teardown, no sim/vcli procs left.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The acceptance face RUNS across a sibling Isaac sim given headroom — confirmed R700-R709. Abundant non-gated work: ~22 remaining STALE BOARD rows to re-verify + breadth. Re-check the ACTUAL gate (`pgrep mujoco|vcli`) + `available` RAM/free VRAM each round; HOLD only if the gate is dirty OR headroom genuinely absent (available<~10G or free VRAM<~3G).
last-round: R709 (E232 verify, provisional). Cleared quarantine (E231 redteam overlong; BOARD stale). Adopted+completed R708's inflight g1.navigation re-verify: N=2/2 GROUNDED on the bare face (age~412→0), non-memorized coords, GT moat. Self-approval audit: NO code/interface/spine/manifest edit ⇒ NOT a CEO gate, no token.
frontier: The 3-world go2 fetch + g1 cross-embodiment bars are the FLOOR. Real frontier: (1) keep re-verifying the ~22 STALE confirmed BOARD rows on the live face, oldest first (g1.navigation now age0); (2) CEO-gated capability rungs (G-323-1/S4/D182/D76) need owner action for NEW capability, NOT a face blocker. Meta-lesson STANDS: a HOLD read rotted 100+ rounds; the skeptic re-verify cadence catches it (E79/E90/E223-E232). JUDGE-HEALTH watch: gemma4:e4b abstained all 4 dims on R707 courtyard-red — g1-nav is self-read so no judge this round; watch the next perception re-verify (g1.perception uses vlm-judge) for a repeat abstain (Ollama model degrade vs prompt).
next:
  1. [NON-GATED, THE REAL BACKLOG] resume re-verifying STALE BOARD rows on the live face. Next oldest confirmed: g1.perception (age~410, g1_accept.py, vlm-judge — watch for gemma4:e4b abstain) / place.nl-new-world-courtyard (age~404) / place.nl-new-world-warehouse (age~404). BROAD pgrep + `available`/VRAM check FIRST; WAIT only if genuinely no headroom.
  2. [ADJUDICATE §1b] R709 banked E232 acceptance+experiments as PROVISIONAL → adjudicate next round: append `confirmed` (red-team survived + reproducible) or `refuted`/`superseded`. Nothing else owed beyond the standing STALE-row cadence.
  3. [HARNESS RECIPE — LESSONS L82] g1_nav_accept.py: pick TWO probe-verified REACHABLE non-memorized coords (E49: (11,3)/(11,4) obstacle-blocked; (9,3.5)/(9.5,4.0) both clear). Any bare-cli re-verify whose sim-build can exceed ~9 min MUST run BACKGROUND with internal `timeout 900` + scoped teardown (host foreground tool cap = 10 min) and `.venv/bin/python`. g1-nav fit ~15 min end-to-end this run (sim build slow); it ran fine as a bg job adopted across the round boundary.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-323-1 hard-channel quartet (queued ~114 rounds) · G-383-1 has_arm · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old — verified 0 fail-eligible this round; true owed audited each round). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen ROUTING key arreared; qwen3-vl JUDGE arreared — deepseek + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R700
