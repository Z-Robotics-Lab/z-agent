# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R298 (E89, build/verify) — adjudicated the E88 provisional AND ran the OWED next#2 HOUSE MODE=place eyes=vlm-judge repro: the judge FIRES on place again (witness=PASS) AND place GROUNDED clean verified=True 2/2 — R297's toppled-go2 was a one-off flake, not a place-locomotion regression.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~50r unanswered; R290 oldest-untested-bar backlog EXHAUSTED; loop hardening the verify-pillar + non-gated debug while the gates stand).
last-round: R298 (E89, build/verify). §0/§1a: BOOTSTRAP clean (no quarantine, inflight empty, no leaked sim). §1b: adjudicated the R297 E88 provisional. BUILD/VERIFY: ran next#1+#2 as ONE MODE=place run (bare vcli + NL, deepseek-v4-flash, eyes=vlm-judge gemma4:e4b, systemd MemoryMax=24G).
  RESULT (E89, confirmed): (a) verify.eyes-place-judge-fires REPRODUCES -> judge fires on the place verdict frame again (witness=PASS) -> PROMOTED confirmed (supersedes R297 provisional). The judge DISCRIMINATES: R297 FAIL on a toppled go2 vs R298 PASS on an upright/intact one. (b) HOUSE place GROUNDED clean: perception_grasp->holding_object(green)->mobile_place->resting_on_receptacle -> verdict verified=True 2/2, NO re-grasp thrash. R297's toppled-go2 was a ONE-OFF brain-wander flake (E60 family), did NOT reproduce N=1 clean. place.nl-plain-colour refreshed self-read 2/2 -> vlm-judge-witnessed. Red-team survived (fresh dir, live driver line, eyes==verdict md5 3bb9f2 settled, GT resting oracle unfakeable).
frontier: Verify-pillar SOLID: eyes=vlm-judge now fires + DISCRIMINATES (PASS/FAIL) on BOTH fetch (E69/E71) AND place (E88/E89) verdict frames. RESIDUAL non-gated: (a) ord-posinv SCENE_SWAP isolated debug (grasp-exec vs target-select); (b) place-breadth: warehouse/courtyard place bars are self-read STALE (age35/38) — refresh on the real face under the judge. NEW capability still needs owner S4/D182.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select env vars (default qwen = Arrearage -> PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): SCENE_CLUTTER=1, ROOM_TEMPLATE={warehouse|courtyard}, SCENE_SWAP=1.
  HARNESS: launcher = var/evidence/R<N>/run_place.sh (uses .venv/bin/python — the systemd --scope has NO `python` on PATH; a bare `python` dies 'command not found'). eyes vlm-judge AUTO-routes to local ollama gemma4:e4b (resolve_judge_env overrides stale supervisor VECTOR_JUDGE_MODEL). MODE=place opens a FRESH session (bottle on table). RUN sim BACKGROUND under `systemd-run --user --scope -p MemoryMax=24G`; pass ROUND_DEADLINE_EPOCH (E87 clamp) + ROUND_N (evidence auto-persists to var/evidence/R<N>). Bash-tool 120s cap kills foreground polls -> poll in a background command. NEVER pkill mujoco -> scripts/sim-teardown.
  LEDGER HAZARD (E27/E81/E87): append ONE preformatted line via `>>`; NEVER load-all->dumps->write-all (reorders -> quarantine). Row caps: <=1KB JSON bytes (a full redteam+note row runs ~880B — trim if the assert fires), each string <=280 PYTHON len. experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); the supervisor's rounds.jsonl append shifts BOARD ages so the NEXT round MUST regen it.
next:
  1. [NON-gated DEBUG, bounded] Root-cause the ISOLATED ord-posinv SCENE_SWAP regression (Hypothesis Loop, DEBUG.md): grasp-execution vs target-selection under swap vs the 8x-GROUNDED no-swap fetches.
  2. [NON-gated verify / place-breadth] Refresh the STALE self-read place bars on the real face under the judge: MODE=place ROOM_TEMPLATE=warehouse then courtyard, eyes=vlm-judge — does place ground + judge fire in the non-house worlds (place.nl-new-world-* are age35/38 self-read)?
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL->object grounder). CEO-gated ~50r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL->object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R280..R298 CLEAN. R298 touched NO spine path (verify-only run + ledger append + STATUS; NO code change; tools/acceptance/ is outside the SPINE regex vector_os_nano/acceptance/).
last_review: R290
