# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R384 (E173, BUILD). HEADLINE: found AND self-fixed a DISTINCT plug-and-play manipulation-gate false-reach on the North-Star BYO-skill path — a SIBLING of E172 via a different mechanism, but SELF-CROSSABLE (strictly stricter, no gate). D175's manip gate (`native_loop._build_motor_tools`) withholds pick/place/... from an armless body, but classified "is this a manip skill?" by a HARDCODED name denylist `_ARM_REQUIRING_SKILLS`. That list covers the SHIPPED skills yet fails OPEN for the North Star ("bring a skill — no kernel edit"): a probe (armless go2-manifest agent, has_arm:false CORRECTLY, + a novel `twist_valve` skill) showed the BYO skill IS offered → the exact false-reach D175 exists to prevent, distinct from E172 (the declared-arm-FLAG-authority bug, gated G-383-1). FIX (strictly-stricter UNION, Inv-1 sandbox-only-gets-stricter, non-spine `native_loop`+`skill_wrapper`, no interface/flag change ⇒ self-crossed not gated): withhold iff name-listed OR `SkillWrapperTool._requires_arm` (arm/gripper in the skill's OWN preconditions+effects; curated list retained for arm-silent domain cases scan/describe/wave). PROVEN zero shipped-behavior delta — the only armless-offered shipped skill `detect` is arm-silent — via enumeration + 5 new tests + E172 tension 4/4 + native unit 57 + skill_wrapper/caps 48, all GREEN. Single-sourced in skill_wrapper (Rule 11). LATENT (no shipped body+plugin ships it); LIMIT: unit-proof, NOT the bare-cli face (sim-blocked). Both levers still SHUT (62nd sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R384 hardened the D175 manip gate to be complete for plug-and-play BYO skills — a REAL North-Star fix, self-crossed, unit-proven).
last-round: R384 (E173 BUILD: hardcoded arm-skill denylist fails open for BYO skills → union with skill metadata; strictly stricter, self-crossed).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar mirrors (E147-E158), verdict contracts (E152/E153), Inv-4 allowlist (E154), embodiment-config schema (E156), arm-catalog↔MJCF (E157), home-pose (E161), F821/bugbear (E163/E164), head-cam res+renderer FAMILY (E165-E170), Inv-3 plug-and-play NOT overfit (E171), capability-gate authority D72↔D175 found+gated (E172), plug-and-play manip-gate classifier completed (E173). E171+E172+E173 are squarely on the North Star (offline 3rd-embodiment + capability-resolution honesty + BYO-skill gate completeness). R312-R384 acceptance-face plateau (72 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. A domain expert would still say the loop cannot land a NEW sim-face capability while externally blocked — honestly acknowledged.
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot, inbox+gates.jsonl for the owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g) re-force any SATURATED vein incl. head-cam renderer FAMILY (E167/168/170), offline embodiment-config (E171), capability-gate authority has_arm (E172 — do NOT re-cross into fixing has_arm without G-383-1), or the manip-gate classifier now completed (E173). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E173 = eighteen found post-saturation; E173 shows a hardcoded name-list gate fails open for plug-and-play). Candidates if REAL: a hardcoded enum/name-list a gate consults that should INTROSPECT the item's declared metadata (E173); a comment-pinned const/param mirror incl a stale DEFAULT ARG (E165); a shared-resource per-call arg silently ignored (E167); an untested except/fallback (E162); a flag an OLD ruling called "inert" a LATER ruling consumes (E172). Verify the gap on disk FIRST.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim. (R384: sibling isaac warehouse_nav.py LIVE pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (R384 E173 = 990B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R384 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (see watch candidates). Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver or re-force a saturated guard.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175. NOTE: E173 (R384) is the SIBLING classifier-completeness bug, self-crossed (strictly stricter, no D72/flag touch) — the two are independent.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, substrate rot-free E121/E147-E173 re-red-teamed E150/E159/E169): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 66 rounds (R318..R384). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-review CLEAN (R380 §7). R384 touched native_loop.py + skill_wrapper.py (NON-spine per check.sh SPINE regex) + a NEW test + docs/LESSONS.md + STATUS.md + ledger — strictly-stricter, behavior-preserving for all shipped bodies, no spine/manifest-gated path, no interface change, no flag-authority change (does NOT touch D72/has_arm resolution). Self-crossed under Inv-1.
last_review: R380
