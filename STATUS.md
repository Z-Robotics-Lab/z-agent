# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R350 (E140, REVIEW §7). HEADLINE: 31st sim-block, but a REAL review round — 1 concrete janitor defect FOUND+FIXED (BOARD.md was stale ⇒ check.sh RED ⇒ regenerated GREEN), all other axes CLEAN. Both honest levers reverified SHUT cold from disk: sim slot HELD by LIVE isaac go2w warehouse_nav sibling (pid 3569767 etime 03:00:38 RSS 8.9G — advanced +~6.8min from R349's 02:53:53 ⇒ living, policy stamped TODAY 2026-07-04_15-52-42 ⇒ genuinely active, NOT a leak; Inv-5 CATEGORICAL WAIT — sibling untouched, NO sim launched). Owner gate G-323-1 queued R323, 0 answered/crossed (33 rds unanswered R318..R350); inbox only old already-crossed G-183-12.
  §7 REVIEW verdicts: BOARD regenerated (real fix). WIRING oldest verified-against R325 = exactly 25rds (not >25) ⇒ none due; R329/R339 fresher. Casebook 12 full cases < 15 cap ⇒ no fold. Gate/token audit R340..R350 CLEAN (0 GATE/CEO-APPROVED). Pruned 12 var/evidence dirs <R330. Re-red-teamed the standing headline (sim-held / gate-unanswered) independently ⇒ holds. Skeptic REAL-face BOARD re-run still OWED (sim-blocked 31st) — logged as DEBT, not faked. Inv-5 + NEVER-KILL-INFRA intact.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: blocked (§6 clean stop: sim slot held by a LIVE ~3h isaac sibling — retrained TODAY, genuinely active — AND owner gate G-323-1 unanswered; non-gated non-sim frontier EXHAUSTED R329-R350, re-scanned this review. Block is TRANSIENT (sim frees when the sibling finishes). The loop parks and self-rechecks both levers each cold start; NOT dead. HONESTY META (E130): 31 near-identical hourly HOLDs is supervisor-cadence waste — the honest cadence is a LONGER park; the ESCALATION (G-323-1, 33 rds), not another HOLD, is the live artifact owed. RECOMMEND the supervisor widen R-interval while both levers stay shut.)
last-round: R350 (E140 REVIEW: 31st sim-block; 1 real fix — BOARD regenerated GREEN; wiring/casebook/gates all CLEAN; both levers reverified SHUT cold; sibling +6.8min policy-TODAY ⇒ living/transient; owner gate 33 rds unanswered; standing headline re-red-teamed ⇒ holds; 12 evidence dirs pruned).
frontier: Offline plug-and-play Verify axis FULLY CLOSED (seam E109-E115, classifier E116, docs E117, runtime E118, honest-audit E119); RECOVER built+re-confirmed (E120/E130); substrate ROT-FREE (E121); axis-exhausted (E122-E140, re-scanned R350). The R312-R350 acceptance-face plateau (39 rounds) is REAL, STRUCTURAL, a HARD EXTERNAL block. The ONLY two honest levers: the sim slot frees (transient), OR the OWNER approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R349).
watch: While phase: blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+ `ps -o etime,rss` to confirm a LIVE run not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) add another offline plug-and-play proof, (b) re-audit an already-audited axis (E119/E120/E121/E122-E140), (c) invent an audit sliver to fake motion, NOR (d) reinterpret Inv-5 to self-unblock the sim (a spine/safety CEO gate — E129). Do NOT reopen eyes-VLM pose/heading (refuted E105-E108).
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 last at R343).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 is CATEGORICAL (one sim at a time); free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  QUARANTINE DRILL: if preflight fails on `loop/.state/quarantine`, read it, fix the named breach (stale BOARD → `.venv/bin/python loop/board.py`), `rm loop/.state/quarantine`, re-run preflight. That is the round's FIRST job.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R350 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (confirm READY-TO-LAND, GREEN 4/4 at R343), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` (bare-idiom, E116) + rubric (one low-risk diff, no interface change). Consolidates G-318-1 · relational-near · G-322-1. Also queued: S4 · D182 · D76.
  3. [IF STILL BLOCKED next round] Reverify both levers (watch above). Both still shut AND no genuinely-new non-gated axis → HOLD again (phase blocked), one experiments row saying so; do NOT manufacture an audit sliver. Only a real lever-open or a genuinely-new North-Star axis ends the hold.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN R343, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121, axis-exhausted E122-E140): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114-packaged 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API (get_position/get_heading/get_odometry quat) ⇒ NO interface change; `_PREDICATE_ORACLES` edit REQUIRED for the bare-predicate idiom (E116); Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. One low-risk diff. OWNER CALL OWED — queued 33 rounds (R318..R350). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - META (world-declared predicate-oracles): a BYO world declaring its bare-predicate oracles is a SPINE/interface gate — NOT needed for the contract (E116 `pred()==True` is the zero-edit path, doc'd E117, runtime-proven E118). Nice-to-have. (R325/E116)
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — the IDENTITY half the quartet's geometry channels leave to the VLM (E46, E112/E113). <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared — perception VLM has a live local Ollama route) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R340..R350 CLEAN (no GATE-APPROVED/CEO-APPROVED tokens). R350 (review) touched NO spine path: BOARD regen (generated file) + experiments E140 + STATUS + events/rounds + evidence prune (gitignored); NO test/acceptance/rubric/interface/classifier/_PREDICATE_ORACLES/engine/code edit; sim never touched, sibling untouched.
last_review: R350
