# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R296 (E87, build) — §0/§1a cleared a quarantine + closed a HUNG R294 inflight, then built a round-deadline-aware verdict-expect clamp so a thrash can't orphan its sim into the next round.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~49r unanswered; R290 oldest-untested-bar backlog EXHAUSTED — g1.nav ✓R291, g1.perception ✓R292/R294, quantity-place REFUTED-flaky E72; loop now hardening harness operational-robustness + non-gated debug while the gates stand).
last-round: R296 (E87, build). BOOTSTRAP was RED: (a) quarantine = stale BOARD -> regenerated; (b) R294 quantity-place inflight had HUNG (harness+vcli stuck ~11min @4.5GB inside a 600s expect, log froze mid-QUANTITY-turn, no verdict) -> tore down the exact leaked PIDs (freed 5->9GB), banked E86 inconclusive, cleared inflight. ROOT CAUSE of the orphan: repl_accept's verdict-expect waited 600s x2 BLIND to ROUND_DEADLINE_EPOCH, so a thrash out-waits the round and leaks its sim into the next round's quarantine (exactly what broke R296's start).
  FIX (E87, non-gated tools/acceptance/): pure budget_timeout() clamps the 6 long verdict-expects to deadline-margin(120)/floor15; interactive (no deadline env) = byte-identical default. Unit 6/6 + REAL pexpect integration 2/2 (a never-grounding child returns at the 2s clamp not 600s, then close(force)). Red-team survived, narrowed: kills the ~11-20min blind-wait orphan; a fully-hung REPL tail can still run ~1-2min past deadline (bounded). Regr: tests/harness/test_vlm_guard.py::TestBudgetTimeout + test_expect_deadline_budget.py.
frontier: R290 backlog EXHAUSTED + quantity-place REFUTED (E72: deepseek-v4-flash place-decompose flaky run-to-run; do_not_retry_unless a decompose guardrail or stronger routing brain lands — a HANG guard like E87 is NOT that). Remaining NON-gated: (a) verify-pillar place-mode judge e2e (R273/E70 code landed, real MODE=place eyes=vlm-judge run OWED); (b) ord-posinv SCENE_SWAP isolated debug. NEW capability still needs owner S4/D182. eyes verdict-render off-centers workspace -> gate G-282-1.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select vars (default qwen = Arrearage -> PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): SCENE_CLUTTER=1, ROOM_TEMPLATE={warehouse|courtyard}, SCENE_SWAP=1.
  HARNESS: repl_accept verdict-expects are now ROUND-DEADLINE-AWARE (E87) — a thrash aborts within budget + tears the sim down; you no longer inherit a 600s orphan. RUN sim BACKGROUND under `systemd-run --user --scope -p MemoryMax=24G`; Bash-tool 120s timeout kills foreground polls -> read the evidence log. NEVER pkill mujoco -> scripts/sim-teardown. local ollama gemma4:e4b eyes AUTO-routed.
  LEDGER HAZARD (E27/E81/E87): append ONE preformatted line via `>>`; NEVER load-all->dumps->write-all (reorders -> quarantine). Row caps: <=1KB JSON bytes, each string <=280 PYTHON len (INCL redteam/result — assert fires => NOTHING appended, trim & re-run). experiments.status ∈ {confirmed,inconclusive,plateau,refuted} (NOT provisional — acceptance-only); confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); the supervisor's rounds.jsonl append shifts BOARD ages so the NEXT round MUST regen it.
next:
  1. [§1c ADJUDICATE] Did the E87 deadline-clamp HOLD? Confirm the NEXT round starts CLEAN (no sim-live warning, no orphan) — no re-quarantine from an orphaned harness. If a thrash still leaks, re-open (candidate: also bound the drain/wait_prompt tail).
  2. [NON-gated DEBUG, bounded] Root-cause the ISOLATED ord-posinv SCENE_SWAP regression (Hypothesis Loop, DEBUG.md): grasp-execution vs target-selection under swap vs the 8x-GROUNDED no-swap fetches. Best remaining non-gated capability-edge.
  3. [NON-gated verify-pillar] Run the DEFERRED real MODE=place eyes=vlm-judge run (R273/E70 reorder landed; e2e OWED) — now safe under the E87 clamp.
  4. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL->object grounder). CEO-gated ~49r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#4.
  - SPINE (D182): world-owned NL->object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#4.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R280..R296 CLEAN. R296 touched NO spine path (tools/acceptance/ is outside the SPINE regex vector_os_nano/acceptance/); rows pure-append.
last_review: R290
