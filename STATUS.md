# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R308 (E98, build) — §1a: inflight empty, no WIP; §1b: the naive "66 provisionals" preflight count is all supersedes-adjudicated (check.sh exempts). §2-4 next#1 SKEPTIC hygiene DONE: closed the LAST 2 STALE BOARD rows → BOARD now has ZERO ⚠ STALE table rows.
  (a) verify.eyes-vlm-judge-wired (age35): fresh v4-flash HOUSE fetch GROUNDED verified=True 1/1 (holding_object('pickable_bottle_green') actor=CAUSED), the vlm-judge FIRED witness=PASS(gemma4:e4b) all4 rubric yes → supersedes R272. LOOKED at eyes_fetch.png: green aloft in gripper, purple/red/yellow/blue REMAIN=discrimination.
  (b) quantity-place.nl-guardrail (age98, deepseek-chat REFUTED 0/1): SUPERSEDED on v4-flash as refuted-by-implication — E96/R306 already refuted quantity-place.nl 0/4 on v4-flash, so a finish-gate firing only on >=N-then-quit has NO precondition on the shipped brain. No fresh sim: E96 IS the v4-flash face run; re-running only reproduces the thrash → supersedes R209.
  HEADLINE: stale-bar hygiene backlog EXHAUSTED — the whole BOARD is now current-face + eyes=vlm-judge (or honestly refuted); every remaining NEW bar is CEO-gated (S4/D182).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~56r unanswered; non-gated NEW-capability frontier EXHAUSTED AND the stale-bar hygiene backlog is now also EXHAUSTED — the loop is at the R290-named plateau, treading on the gates). No sibling sim this round (Isaac go2w run PID 2817818 ended on its own before teardown; sim-teardown killed only my own leftover python3, Isaac NOT touched). My 1 MuJoCo fetch hard-capped MemoryMax=24G, avail stayed ≥33G → no host risk.
last-round: R308 (E98, build). Closed the last 2 STALE board rows (eyes-vlm-judge-wired refreshed GROUNDED+judge-PASS supersedes R272; quantity-place.nl-guardrail superseded on v4-flash via E96). Banked 2 acceptance rows + E98 experiments, regen BOARD (0 stale), extended LESSONS eyes-pillar line.
frontier: Verify-pillar eyes CLOSED at CONFIRMED strength on ALL single-object fetch+place bars across ALL 3 worlds; the vlm-judge is WIRED and FIRES (recorded-not-trusted, E72 gemma4 non-determ). ENTIRE board now current-face, 0 stale rows. N-OBJECT quantity-place remains openly RED (E96, 0/4 v4-flash): the brain cannot self-decompose "两个瓶子" into N grasp+place cycles — a REAL non-gated capability gap whose fix (decompose guardrail / stronger routing brain, E36/E72) OVERLAPS the gated D182 grounder. A TRUSTED 2nd vision discriminator still needs a stronger VLM (funded qwen3-vl / better local). NEW embodiment still needs owner S4/D182.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek + v4-flash via env (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`.
  HARNESS: house-fetch launcher = var/evidence/R308/run_house_fetch.sh <tag> <fetch_nl> (default HOUSE = NO VECTOR_ROOM_TEMPLATE; MODE=fetch; vlm-judge + perception auto-route to local gemma4:e4b). Non-house fetch = var/evidence/R305/run_fetch.sh <room> <colour> <fetch_nl>. A single fetch is ~5-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G) and POLL the log for "RESULT R308<tag>"; a foreground poll trips the 10-min Bash-tool ceiling. SERIAL one-at-a-time; `free -g` + `pgrep -x mjpython` (NOT `pgrep -f mujoco` — self-matches) before launch; scripts/sim-teardown after. NEVER pkill mujoco.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps (checks_schema.py): line ≤1024 BYTES (Chinese=3B/char — trims bit me 5× this round), each string field ≤280 chars. experiments row key `e` NOT `experiment` + needs `source∈{round,backfill}`; acceptance status∈{provisional,confirmed,refuted,superseded}; confirmed rows' redteam MUST start 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); a supervisor rounds.jsonl append shifts BOARD ages → NEXT round MUST regen.
next:
  1. [FRONTIER — the last non-gated build, overlaps D182] Quantity-place N-object decompose is openly RED (E96, v4-flash 0/4). A decompose guardrail (cf. R206 quantity guard / R257 post-place guard) making the brain emit N grasp+place cycles is the ONE genuine non-gated verify-pillar build left — weigh vs the gated D182 grounder before a multi-round build; note R310 is a review round.
  2. [REVIEW-PREP] R310 is the next review round (§7): skeptic re-verify the 2 oldest confirmed board rows (g1.perception age13 / g1.navigation age15 next up as they climb), re-red-team a headline, ambition/plateau critic. No stale rows remain to close.
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~56r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65; overlaps the E96 quantity-decompose gap. <- next#1/#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R282..R308 CLEAN. R308 touched NO spine path (banked 2 acceptance + 1 experiments row + STATUS + BOARD regen + LESSONS eyes-pillar line + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
