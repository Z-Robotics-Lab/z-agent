# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R309 (E99+E100, build) — §1a: inflight empty; adopted the supervisor R308 rounds.jsonl append (pure-append) + regen BOARD. §1b: naive "66 provisionals" preflight count all supersedes-adjudicated (check.sh exempts). §2-4: settled the quantity frontier + locked the guardrail deterministically.
  (E100 research) SETTLED: closing N-object quantity-place is CEO-GATED, NOT the "one non-gated build left" R308-STATUS named. Machinery sound (E38 seq 2/2), gap is the BRAIN not self-decomposing 两个 (v4-flash E96 0/4). Only fixes: (a) runner-side decomposition planner = D76 planner-plugin gate (native_loop is contractually planner-free, MUST NOT import goal_decomposer/executor); (b) per-model prompt fix = E23-uncreditable. So STOP re-attempting quantity as a build (R199..R308); hold at S4/D182+planner gate. Read-only (no sim; E96 do_not_retry honoured).
  (E99 build) LOCKED the finish-on-fail quantity guardrail deterministically: 2 tests in tests/unit/vcli/test_native_loop.py (FakeToolScriptBackend + real run_turn_native, NO sim) — FAIL verify->finish refused+nudged->run continues (4 calls not 2); fail-then-finish-forever TERMINATES bounded grading verified=False (never a forced green). Was only ever exercised by a flaky ~15min sim (E72/E96). 27/27 file green, 90M peak.
  HEADLINE: the loop is confirmed at the R290 plateau — every NEW capability (S4 3rd embodiment / D182 grounder / quantity planner) is CEO-gated ~57r. This round did the honest non-gated work: correct the frontier framing + regression-pin the moat behaviour the quantity ledger rests on. No sim run (deadline + thrash risk; the 2 age-31 STALE rows below belong to the R310 review skeptic pass).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-capability frontier EXHAUSTED and now CONFIRMED CEO-gated across the board — quantity too, per E100. The one build STATUS previously called non-gated is gated. Loop treads at the R290 plateau; only owner S4/D182/planner unblocks it). No sim this round.
last-round: R309 (E99 build + E100 research). Regression-pinned the finish-on-fail guardrail (2 deterministic tests, no sim); settled that quantity-place is CEO-gated not a non-gated build (corrects the R308 STATUS framing). Banked E99+E100 experiments rows, extended LESSONS quantity block, regen BOARD.
frontier: Verify-pillar eyes CLOSED (vlm-judge WIRED+FIRES, recorded-not-trusted). Every NEW capability is CEO-gated: S4 (3rd embodiment BYO-URDF), D182 (world-owned NL grounder), quantity N-object decompose (needs the D76 planner-plugin OR a stronger routing brain — E100). A TRUSTED 2nd vision discriminator still needs a stronger VLM (funded qwen3-vl / better local). NEW capability needs owner action.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek + v4-flash via env (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`.
  HARNESS: house-fetch launcher = var/evidence/R308/run_house_fetch.sh <tag> <fetch_nl>; non-house = var/evidence/R305/run_fetch.sh <room> <colour> <fetch_nl>. A single fetch ~5-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; `free -g` + `pgrep -x mjpython` before launch; scripts/sim-teardown after. NEVER pkill mujoco.
  TESTS: pure-unit only via `./scripts/run-tests <path>` (memory-capped 12G); the native_loop unit file is sim-free (0.18s, 90M).
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps (checks_schema.py): line ≤1024 BYTES (Chinese=3B/char — trims bit me on E99/E100), each string field ≤280 chars. experiments key `e` NOT `experiment` + `source∈{round,backfill}`. board.py: `.venv/bin/python loop/board.py` (NO redirect); a supervisor rounds.jsonl append shifts ages → NEXT round MUST regen (this round 2 rows ticked age30→31 STALE).
next:
  1. [REVIEW — R310 is the review round §7] Skeptic re-verify the 2 age-31 STALE confirmed BOARD rows on the REAL face: runner.degenerate-spin-guard (age31) + verify.eyes-vlm-judge-place (age31) — both ticked stale via the R308 age-shift (clock, not regression). Also re-verify the 2 OLDEST confirmed rows (g1.perception/g1.navigation climbing) + re-red-team a headline + ambition/plateau critic.
  2. [OWNER GATE — ONLY path to NEW capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder) OR the D76 planner-plugin (quantity decompose). All CEO-gated ~57r. Nothing non-gated advances the North Star until one is answered.
  3. [NON-GATED HYGIENE, if any surfaces] Only moat-hardening / regression-pinning of EXISTING behaviour (like E99) is non-gated; no new capability is. Do NOT re-run quantity as a build (E100 do_not_retry).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R282..R309 CLEAN. R309 touched NO spine path (added 2 unit tests under tests/ + 2 experiments rows + LESSONS quantity line + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ edit, NO oracle/verdict/cognitive edit).
last_review: R290
