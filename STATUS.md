# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R378 (E167, BUILD). HEADLINE: substrate re-scan OUTSIDE all saturated veins generalized the E165 head-cam-resolution find from the DEFAULT VALUE to the RENDER MECHANISM and caught a REAL latent acceptance-face grasp bug. `MuJoCoGo2.get_camera_frame`/`get_depth_frame` cached ONE renderer at the FIRST call's dims (`if not hasattr(self,"_cam_renderer")`) and IGNORED width/height on every later call — and that RGB renderer is SHARED by the look/explore/describe path (bare `base.get_camera_frame()` → 640x480 for the VLM) and the grasp path (`Go2GraspPerception` → `base.get_camera_frame(320,240)` so its RGB mask aligns pixel-for-pixel with 320x240 depth+intrinsics). So a `look`/`describe` BEFORE a `pick` (a normal bare-cli sequence) locked the renderer at 640x480, and grasp then got a 640x480 RGB against 320x240 depth → `front_object_mask` indices no longer mapped onto depth pixels → WRONG 3D grasp point (order-dependent latent). FIX: `_ensure_render(attr,w,h,depth=)` re-creates the cached renderer only when requested dims differ (best-effort `.close()` of the old GL ctx); each call renders at ITS resolution; same-dims still reuses one renderer. TDD: fake-`_get_mujoco` records every Renderer's construction dims (no real GL/sim slot) — pre-fix look→grasp RGB=(480,640) vs depth=(240,320) RED, post-fix both (240,320) + same-dims=1-renderer GREEN (5/5); E165 wiring guard 13/13; hw/sim unit 161/161. Verified DETERMINISTICALLY. `mujoco_go2.py` NOT spine ⇒ no gate. Both levers still SHUT (56th sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; a real non-gated find landed — the E165 resolution mirror was DEFAULT-only; the shared-renderer MECHANISM still let a look's 640x480 leak into a later grasp's 320x240 RGB → mask/depth misalignment; fixed by honoring per-call dims; TDD red→green, deterministic fake-mujoco, no sim slot touched, no manufactured sliver).
last-round: R377 (E166 BUILD: F841/F401 captured-then-dropped scan caught render_response dropping the code-block lang tag; wired rich Syntax; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar-offset mirror COMPLETE go2(4)+g1(E147-E151/E158), piper-proxy↔driver const (E160), verdict contracts (E152/E153), Inv-4 import allowlist (E154), causation-baseline (E155), embodiment-config schema (E156), arm-playground catalog↔MJCF (E157), SO-101 home-pose vein CLOSED (E161/E162), F821 class scanned (E163), ruff-bugbear swept (E164), go2 head-cam RESOLUTION default single-sourced+drift-guarded (E165), F841/F401 captured-then-dropped caught a live render defect (E166), head-cam render MECHANISM fixed — shared renderer now honors per-call dims (E167). Spine + config-schema frozen + mirror coverage broad. The R312-R378 acceptance-face plateau (66 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed capability are a sim-slot free (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R377). Substrate finds (E152-E167) are increasingly marginal — a domain expert would say the loop polishes perception/invariant plumbing while the North-Star capability stays externally blocked; honestly acknowledged, not a hidden local hill. (E162 dead-fallback + E163 dormant undefined-name + E164 live async closure + E165 stale-default + E166 dropped lang tag + E167 shared-renderer dim-cache are the substrate finds that were REAL live divergences, not just unguarded copies.)
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` = LIVE run not leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g-q) re-force any SATURATED vein: lidar-offset go2+g1+piper (E151/E158), Inv-4 import (E154), spine frozen-dataclass (E152/E153/E155), embodiment-config schema (E156), catalog↔MJCF (E157), piper const (E160), home-pose (E161/E162), F821 (E163), bugbear (E164), head-cam resolution DEFAULT — single-sourced GO2_HEAD_CAM_*+drift-guarded vs Go2ROS2Proxy (E165), F841/F401 dropped-value (E166), (r) re-force the go2 head-cam RENDER MECHANISM — `_ensure_render` now re-creates on dim change; renderers honor per-call dims + a fake-mujoco regression test pins it (E167). But DO genuinely re-scan for a REAL unenforced invariant OUTSIDE all saturated veins before HOLD (E152-E167 = thirteen found post-saturation; even a "SATURATED"/"CLOSED" vein can hide a missed copy or, per E167, a MECHANISM the value-only guard never covered — check the mechanism, not just the constant). Candidates if a gap is real: a load-bearing const/param comment-pinned mirror not yet guarded incl. a stale DEFAULT ARG leaking to the acceptance path (E165) OR a shared resource whose per-call parameter is silently ignored (E167 pattern — grep cache-once `if not hasattr`/`if.*is None:` builders shared across callers with different args); a hardcoded enumeration that should introspect; an untested except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  OWED SIM-CHECK (E165+E167): when the sim slot frees — (a) eyeball look/explore on the bare-cli go2 face at 320x240 to confirm the 640→320 grasp default did not degrade VLM detection (E165); (b) run a `describe`/`look` THEN a `pick` in one bare-cli session and confirm the grasp still grounds `holding_object` (E167 — proves the shared-renderer fix holds on the REAL renderer, not just the fake). Both are the only unverified aspects; unit+detector suites pass.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R378: sibling isaac warehouse_nav.py LIVE pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (checks_schema.py — R378 E167 = 1000B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R373 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore visual re-check AND the E167 owed describe-then-pick shared-renderer re-check (one session). Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (E151/E158 lidar, E152/E153/E155 spine frozen, E154 Inv-4 import, E156 config-schema, E157 catalog↔MJCF, E160 piper const, E161/E162 home-pose, E163 F821, E164 bugbear, E165 head-cam res default, E166 F841/F401 dropped-value, E167 shared-renderer dim-cache): a comment-pinned const/param mirror not yet guarded incl. a stale DEFAULT ARG leaking to the acceptance path (E165) OR a shared resource ignoring a per-call arg (E167 — grep cache-once `if not hasattr`/`if.*is None` builders reused by callers passing different args); a hardcoded enumeration that should introspect; an UNTESTED except/fallback branch (E162); an async-closure late-binding (E164); a real coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, or re-run a now-clean mechanical scan.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E167 re-red-teamed E150/E159): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 59 rounds (R318..R377). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0 (re-confirmed R370). FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R370 §7): gate/token last-review CLEAN — 0 real approvals. R378 touched ONLY vector_os_nano/hardware/sim/mujoco_go2.py (add `_ensure_render`; get_camera_frame/get_depth_frame call it) + tests/ + STATUS + ledger — no spine/manifest-gated path, no interface change (render-plumbing correctness fix).
last_review: R370
