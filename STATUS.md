# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R306 (E96, build) — §0 FIXED the quarantine breach (R305 ledger schema: E94/E95 experiments rows used 'experiment' key + missing 'source' → renamed to 'e'+source:round; acceptance:165 kernel redteam reworded to start 'survived'; E95 trimmed <1KB). §1b next#1 SKEPTIC: re-ran the OLDEST stale confirmed bar quantity-place.nl (age~100, R205 deepseek-chat self-read GROUNDED-FLAKY 2/3) on the CURRENT shipped bare face (deepseek-v4-flash) → REFUTED 0/4. Final resting_on_receptacle count verdict False(0/4); eyes vlm-judge=FAIL. LOOK eyes_quantity.png: arm holds ONE yellow can aloft, receptacle EMPTY, nothing placed. Superseded R205. HEADLINE: a stale green fell — the old "confirmed" rested entirely on a now-unused brain (deepseek-chat); N-object place-decompose (E36/E72 class) is flaky-to-broken on v4-flash. verified= rests on the GT resting_on_receptacle count oracle (0), never the non-determ judge (judge FAIL only corroborates).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~54r unanswered; non-gated NEW-capability frontier EXHAUSTED — loop treads on eyes-pillar upgrades + skeptic hygiene of stale bars while the gates stand). Concurrent SIBLING sim this round: Dockerized Isaac go2w nav (/isaac-sim warehouse_nav.py, ~9GB) — NOT this repo's loop, NEVER-KILL-INFRA; my MuJoCo run hard-capped MemoryMax=24G serial, avail stayed ≥33G → no host risk.
last-round: R306 (E96, build). §0: FIXED quarantine (3 R305 ledger schema violations), cleared it, preflight OK. §1a: inflight empty, no WIP to adopt. §1b next#1: skeptic-refuted quantity-place.nl on v4-flash (REFUTED 0/4, supersedes R205 confirmed). LOOKED at the verdict frame (arm holds 1 can, receptacle empty). Banked E96 refuted acceptance + experiments rows, regen BOARD, LESSONS line updated.
frontier: Verify-pillar CLOSED at strength on the SINGLE-object fetch/place bars: eyes=vlm-judge FIRES + CONFIRMED (N≥2) across ALL non-house fetch/place worlds. N-OBJECT quantity-place is now openly RED (E96): the brain cannot self-decompose "两个瓶子" into N grasp+place cycles on v4-flash (0/4) — a REAL non-gated capability gap, but its fix is a decompose guardrail/stronger routing brain (E36/E72), overlapping the gated D182 grounder. NEW embodiment still needs owner S4/D182. gemma4:e4b judge PASS↔ABSTAIN↔FAIL non-determ (E72/E92) — a TRUSTED discriminator needs a stronger VLM.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via env (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`.
  HARNESS: quantity launcher = var/evidence/R306/run_quantity.sh (MODE=quantity, utterance 把两个瓶子放到架子上; predicate = FINAL resting_on_receptacle count verdict, GT/self-read; eyes vlm-judge frame is a corroborating bonus). fetch launcher pattern = var/evidence/R305/run_fetch.sh <room> <colour> <fetch_nl>. RUN sim FOREGROUND but MODE=quantity is SLOW (2N+1 verdicts, up to 600s/expect) — it EXCEEDS the 10-min Bash-tool ceiling, so launch DETACHED (nohup timeout ... systemd-run --scope) and POLL the log, NOT a foreground `timeout` call. SERIAL one-at-a-time under `systemd-run --user --scope -p MemoryMax=24G`; set VECTOR_EVIDENCE_DIR per run; `free -g` before launch; scripts/sim-teardown after. NEVER pkill mujoco (a bare `pgrep -f mujoco` self-matches the running shell → false "SIM BUSY"; use `pgrep -x mjpython`). A ~9GB Isaac python3 (/isaac-sim, go2w) is a SIBLING project's sim — leave it.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps (checks_schema.py): line ≤1024 BYTES (Chinese = 3 bytes/char — bit me 3× this round; keep redteam/note terse), each string field ≤280 chars. experiments row key is `e` NOT `experiment`, and needs `source∈{round,backfill}`; acceptance status ∈ {provisional,confirmed,refuted,superseded}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); a supervisor rounds.jsonl append shifts BOARD ages → NEXT round MUST regen.
next:
  1. [SKEPTIC hygiene] The remaining stale self-read HOUSE fetch bars: fetch-place.nl-category-only (~43), fetch.nl-negated-distractor (~38), fetch.nl-plain-colour (~46) — skeptic re-verify / eyes-upgrade self-read→vlm-judge where the bar is vision-graded (Inv.1 stricter-only). These are FAST single-object fetches (~3-5min), fit the foreground Bash-tool ceiling.
  2. [FRONTIER — non-gated but overlaps D182] Quantity-place N-object decompose is now openly RED (E96). A decompose guardrail (cf. R206 quantity guard / R257 post-place guard) making the brain emit N grasp+place cycles is a genuine non-gated verify-pillar build — but weigh vs the gated D182 grounder before a multi-round build.
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~54r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65; overlaps the E96 quantity-decompose gap. <- next#2/#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R281..R306 CLEAN. R306 touched NO spine path (fixed ledger schema + STATUS + BOARD regen + LESSONS line + gitignored var/evidence; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
