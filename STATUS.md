# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R359 (E149, BUILD). HEADLINE: 3rd HOLD-reflex break — extended the manifest↔driver rot-guard to go2's lidar MOUNT OFFSET, the last asymmetric gap E148 left (g1 had test_g1_lidar_offset_matches_driver; go2 did NOT). go2's manifest pos:[0.3,0,0.2] AND the vnav bridge _SENSOR_X/_Z both NAME driver _LIDAR_OFFSET_X/_Z as source, yet those consts were METHOD-LOCAL to _update_lidar (unimportable) — a drift would silently make BOTH mirrors advertise a beam origin the driver no longer casts from. FIX: hoisted _LIDAR_OFFSET_X=0.3/_Z=0.2 to module scope (values byte-identical; _update_lidar still refs them; lidar-cast unchanged) + new guard test_go2_lidar_offset_matches_driver imports the const and asserts manifest pos mirrors it. 9/9 GREEN sim-free peak88M; embodiments dir 35/35 no-regression; DISCRIMINATION PROVEN (drifted driver _LIDAR_OFFSET_X 0.3→0.99 ⇒ RED, reverted clean). Sanctioned by R358's do_not_retry_unless (named this exact const-hoist). Both external levers STILL SHUT (verified cold): (1) sim slot HELD by LIVE isaac go2w warehouse_nav sibling (6 procs, pid3569767 etime 04:20:38) — Inv-5 CATEGORICAL, no 2nd sim; (2) owner gate G-323-1 unanswered 42 rds (R318..R359).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked, but R357+R358+R359 confirm the "non-gated frontier exhausted" read was TOO strong — THREE rounds of REAL substrate hardening found by re-scanning for UNENFORCED invariants / method-local consts that mirrors name but can't import. Keep doing that before HOLDing; it is real BUILD, distinct from the forbidden audit sliver. NOTE: the embodiment manifest↔driver drift-guard surface is now near-saturated — after this the next hardening likely lies OUTSIDE the manifest mirror.)
last-round: R358 (E148 BUILD: made manifest↔driver rot-guard symmetric across go2+g1, +3 guards, 8/8 green).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free now MACHINE-ENFORCED + SYMMETRIC for embodiment manifests across go2 AND g1 — incl. lidar MOUNT OFFSET as of E149 (E147+E148+E149, was only asserted at E121). The R312-R359 acceptance-face plateau is REAL + externally blocked. The two lever paths to a NEW confirmed acceptance capability: sim slot frees (transient), OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R359).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` to confirm a LIVE run not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove an already-closed offline capability axis, (b) re-audit an already-audited axis, (c) invent an audit/doc sliver to fake motion, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), NOR (f) re-frame the per-round "BOARD.md stale" check.sh RED as a defect (mechanical age+1 drift, E102/E145). But DO (R357/E147, R358/E148, R359/E149) genuinely re-scan the substrate for UNENFORCED invariants / missing drift-guards / method-local consts a mirror names-but-can't-import, before defaulting to HOLD — that is real non-gated BUILD, not a sliver. CAVEAT: the manifest↔driver mirror surface is now near-saturated; do not force a low-value guard just to avoid HOLD — a clean HOLD beats a manufactured sliver.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 last re-confirmed R352; skip re-running unless >~10 rds stale).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R353 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R352), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] first genuinely re-scan the substrate for a real non-gated hardening (unenforced invariant / drift-guard / test-coverage gap, à la E147/E148/E149) — ship it if found. The go2 lidar-offset hoist+guard is DONE (E149). Remaining candidate: the vnav bridge _SENSOR_X/_Y/_Z (scripts/go2_vnav_bridge.py) mirror the SAME driver _LIDAR_OFFSET consts by COMMENT only ("Must match mujoco_go2.py _LIDAR_OFFSET") — no test asserts it; a bridge↔driver guard would close the 3rd mirror IF the bridge module imports sim-free (it pulls ROS2 — verify importskip works before committing to it). The manifest-mirror surface is otherwise near-saturated. ONLY if no real hardening exists AND no lever opened → HOLD (one experiments row), do NOT manufacture an audit sliver, do NOT re-investigate the preflight nag (E144) or the BOARD age drift (E145).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R352, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147/E148/E149, axis-exhausted E122-E146): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 42 rounds (R318..R359). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0. FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer, planner-free contract) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-10 CLEAN (no GATE-APPROVED/CEO-APPROVED tokens). R359 touched NO spine/manifest-gated path (only tests/unit/embodiments/ + a behavior-preserving const-hoist in hardware/sim/mujoco_go2.py + ledger + STATUS + events). No self-approval.
last_review: R350
