# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R324 (E115, BUILD). HEADLINE: the quartet landing now has a STANDING GUARD — `tools/acceptance/hard_channels_doctor.py` turns R323's "no interface change" prose FINDING into a machine-checked precondition. The E114 packaging is unit-tested against a FAKE base, so it would stay GREEN even if the REAL seam drifted (get_odometry dropped / Odometry loses its quat / a landing name shadows an existing verify predicate) — the claim could rot UNNOTICED until the owner tried to land G-323-1. The doctor reads the REAL kernel modules read-only and asserts 4 landing preconditions (packaged_shape · base_api_surface · reference_seam_shape · landing_delta) + a fake-agent dry-run; CLI `--json` exits 0 iff READY-TO-LAND. PROVEN not a green-always stamp: deleting real `BaseProtocol.get_odometry` in-process flips `diagnose()` to False naming the missing method; restore recovers. TDD 12/12 (incl 4 failure-detection tests) + packaged+cores 99/99 regression. Non-spine (tools/), no sim.
  §1 ORIENT: inflight absent; no WIP; inbox clean. No experiments provisionals. 67 acceptance provisionals all check-exempt → check.sh GREEN. SIM-BLOCKED A 6TH TIME: a sibling loop holds the one sim slot (isaac_sim launch PID 3535332 + localPlanner 3535348 + laser_mapping 3535356, live) → Inv-5 + NEVER-KILL-INFRA: NOT touched, launched NO competing sim ⇒ next#1 (real-face stale-row re-run) stays blocked, as R320-R323.
  §4 REAL-VERIFY: non-spine guard over the tested packaging ⇒ acceptance is the doctor running against the REAL tree + the failure-detection tests (as E109-E114 were verified sim-free). eyes: self-read. Doctor CLI on the live tree = READY-TO-LAND (all 4 OK, exit 0); 12/12 unit incl injected-drift RED cases; packaged unit + 4 cores 99/99 unchanged. REDTEAM survives as-if-not-mine: NOT a green-always stamp — the pure check-fns return ok=False + name the offending token on injected drift, and `diagnose()` reads the LIVE ABC (hasattr/dataclasses.fields), PROVEN by an in-process delete of real get_odometry flipping overall to False (restore recovers). Non-spine, read-only imports, no sim/network; grades the LANDING not a 5th geometry (well is dry). experiments row only, NO acceptance row (no sim face touched). NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier GATE-blocked: every acceptance-face advance is CEO-gated — G-323-1 (the CONSOLIDATED quartet landing, now doctor-guarded) · S4 · D182 · D76)
last-round: R324 (E115 BUILD: hard_channels_doctor.py guards the quartet landing — reads the REAL kernel seam read-only, asserts 4 R323 landing preconditions, RED on injected drift; 12/12 + cores/pkg 99/99; live-read proven by in-process get_odometry delete flipping diagnose() False. Sim-blocked a 6th time; no spine touched).
frontier: the offline hard-channel well is DRY, PACKAGED (E114), and now GUARDED (E115) — the quartet is built, proven, agent-bound, and its landing precondition is machine-checked, one merge from the acceptance face. The 12-round acceptance-face plateau (R312-R324) is REAL and STRUCTURAL: the face is GATE-blocked, not idea-blocked. There is exactly ONE honest lever left: the OWNER approves G-323-1 (a single low-risk diff: import + 4 predicate registrations + rubric — no interface change, and the doctor now confirms that precondition holds before the merge). Building a 5th offline oracle would be pure treadmill — do NOT.
watch: quartet PACKAGED (E114) + GUARDED (E115) — both non-spine, no sim:
  `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` — landing-conformance doctor; exit 0 iff READY-TO-LAND. Test: `./scripts/run-tests tests/unit/test_hard_channels_doctor.py` (12). Run it BEFORE landing G-323-1 to confirm the seam still merges cleanly.
  `.venv/bin/python tools/acceptance/hard_channels.py {upright|height|near|facing} ...` — unified CLI over the four cores; `hard_channels.make_verify_namespace(agent)` → the 4 GT-reading fail-safe-False predicates. Tests: test_hard_channels.py (27) + cores test_pose_upright(11)/pose_height(13)/place_near(18)/heading_facing(30).
  LANDING SHAPE (for the gate round): merge make_verify_namespace into `RobotWorld.build_verify_namespace` (robot.py:184 go2-base ns.update, alongside at_position/facing), add each name to `evidence_classifier._PREDICATE_ORACLES` + `verify_signatures()` to the vocab, reword rubric — WIRING verify-spine §. All on the EXISTING base API (get_position/get_heading/get_odometry quat); identity stays VLM/D182. Run hard_channels_doctor FIRST.
  SIM-SAFETY (Inv-5, R318/R320-R324): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco'` (NOT just `mujoco`); WAIT if live, NEVER teardown a sim you did not launch.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (trim to fit — E115 row landed 999 B after 3 trims), each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board: `.venv/bin/python loop/board.py`.
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R324 skeptic debt: re-run the 2 oldest confirmed STALE BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (confirm READY-TO-LAND), then merge hard_channels.make_verify_namespace into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric (one low-risk diff, no interface change). Consolidates G-318-1 · relational-near · G-322-1. Also queued: S4 · D182 · D76.
  3. [NON-GATED, IF a build round is forced before the owner acts] the offline hard-channel well is DRY, PACKAGED, and now GUARDED — do NOT build a 5th oracle NOR a 2nd guard (treadmill). Honestly hold and push the owner to unblock G-323-1, OR pick a genuinely-new non-spine ergonomic ONLY if it advances the North Star (not busywork). Do NOT reopen any eyes-VLM pose/heading probe (refuted E105-E108).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing, now DOCTOR-GUARDED) <- next#2: merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114-packaged 27/27 + cores 72/72; landing precondition E115-guarded by hard_channels_doctor) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind to the EXISTING base API (get_position/get_heading/get_odometry quat) ⇒ NO interface change (doctor confirms on the live tree); Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. One low-risk diff. Owner call owed. SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1 (kept for history). (R323/E114, R324/E115)
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, now CONSOLIDATED under G-323-1 (land the packaged quartet in one merge). Kept for provenance (R318/E109, R319/E110, R320/E111, R321/E112, R322/E113).
  - G-315-1 SPINE (VLM pose rubric): MOOT — pose belongs to the hard-channel quartet (G-323-1). (R315/E106..R320/E111)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py; CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — the IDENTITY half the quartet's geometry channels leave to the VLM (E46, E112/E113). <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN (G-183-3) · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R324 CLEAN. R324 (build) touched NO spine path (added tools/acceptance/hard_channels_doctor.py + tests/unit/test_hard_channels_doctor.py, both non-spine pure-wiring/read-only imports over tested modules; appended experiments E115 + extended 1 LESSONS saga line; STATUS; NO acceptance/ or rubric or interface edit).
last_review: R320
