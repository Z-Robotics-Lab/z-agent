# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R667 (E190, BUILD). HEADLINE: **broke the HOLD reflex again with a REAL fresh non-gated fix — the NAV-GOAL boundary silently accepted NaN/±inf**. `navigate_to(x,y)`/`publish_goal(x,y)` is the pose-target command boundary for every mobile base (the sibling of E189's velocity boundary). It handed the goal to an EXTERNAL nav stack via bare `float(x)` with NO finiteness check: `Go2ROS2Proxy.navigate_to`→`_publish_goal_point` published `msg.point.x=float(x)` onto `/goal_point` (FAR planner); `NavStackClient._cmu_navigate` onto the waypoint topic and `_nav2_navigate` set `position.x=float(x)` on a NAV2 action goal. REACHABLE: `vcli/primitives/navigation.py:publish_goal` passes x/y straight through with NO guard (NL/CaP-X path) + `native_loop` navigate tool does `float(params["x"])` and `json.loads` ACCEPTS the `NaN`/`Infinity` tokens (`{"x":NaN}`→`float('nan')`). A poisoned goal ESCAPES onto ROS2 while staying invisible to the honest-verify spine (the robot may still be commanded toward it) — a security-floor violation ("reject NaN before acting"). Same class as E187 (loader) + E188 (arm move_joints) + E189 (base velocity), now the POSE-TARGET boundary. Fix = ONE pure `ensure_finite_nav_goal()` in `hardware/base.py` (Protocol-only, no hw/ROS2 import) wired at entry of all 4 `navigate_to` sinks (go2_ros2_proxy, mujoco_go2, mujoco_g1, core NavStackClient) + the `publish_goal` primitive; loud axis-scoped ValueError; ast-discovered wiring test = MIRROR-safe (a new sink can't skip it) + a behavioral tripwire (NaN never reaches nav_client). RED import-fail→GREEN 16/16; 216/216 hardware unit regress clean. Red-team ALL pass: publish_goal/native_loop pass x,y un-guarded + json NaN token ⇒ reachable; Go2ROS2Proxy+NavStackClient publish `float(x)` to external ROS2 unchecked ⇒ genuinely fail-OPEN (the mujoco vgraph drivers are fail-SAFE downstream — plan_path→unreachable OR the E189 set_velocity gate catches NaN waypoints — but gated too for mirror-safety); finite goals pass 216/216 ⇒ no false-reject; loud raise ⇒ strictly stricter. NON-spine (hardware/base.py + core/nav_client.py not in check.sh SPINE), no sig change ⇒ NOT a CEO gate, self-crossed like E187/E188/E189. Both levers RE-VERIFIED SHUT cold: (1) SIM — sibling isaac go2w warehouse_nav LIVE (pids 3751659/3751665/3751672 + nav stack 3751367/3751393/3751401) holds the ONE slot (Inv-5 WAIT); (2) OWNER — inbox only long-answered G-183-12; gates.jsonl ZERO answers for G-323-1 (queued 84 rounds)/G-383-1/G-354-1. (79th sim-block — but a REAL build, not a HOLD.)
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R667 landed a fresh non-gated external-input-validation hardening at the NAV-GOAL boundary; both levers still SHUT).
last-round: R667 (E190 build: NaN/inf silent-accept at the nav-goal boundary FIXED — `ensure_finite_nav_goal()` gate on all 4 navigate_to sinks + publish_goal, RED import-fail→GREEN 16/16, 216/216 regress, red-team clean, json-NaN-token reachability caught, LATENT/self-crossed non-spine).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED. SATURATED veins: name-list (E173-E175/E116/E118), in-kernel hardcoded-SET (E177), hardcoded-NAME exclusion (E178), numeric-threshold-in-verify (E180), tool-registry silent-overwrite (E181), vocab-derivation (E182), persona-swallow (E183), skill-registry-routing-swallow (E184); silent-swallow class grep-PROVABLY empty (E185). EXTERNAL-INPUT FINITENESS vein now spans FOUR boundaries: manifest loader (E187) + ARM actuator move_joints (E188) + BASE velocity set_velocity (E189) + NAV-GOAL navigate_to/publish_goal (E190). R312-R667 plateau REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. Highest-leverage unblock = owner acts on G-323-1 (queued 84 rounds).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — BROAD `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac|warehouse_nav'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot [narrow `isaac` alone MISSES the `python3`-named warehouse_nav], inbox+gates.jsonl for owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis (tools/verify/vocab/persona/skill-registry/loader/arm-actuator/base-velocity/nav-goal finiteness all scanned), (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144; authoritative owed=0), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145 — just `python3 loop/board.py`), (g) re-force any SATURATED vein. NEVER run `tests/hardware/sim/` under bare pytest (spawns MuJoCo, contends the ONE sim slot, can core-dump) — only `scripts/run-tests`. Before mining ANY "new" gap: grep ledger + docs/LESSONS.md FIRST — if E#-recorded/gated/benign, it is NOT a find.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check BROAD `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all; NEVER `echo "$EMPTY" >>` (an empty var appends a blank line ⇒ check.sh RED). Caps: line ≤1024 BYTES (R667 E190 line=975B), each string field ≤280 CHARS. status ∈ {confirmed,inconclusive,plateau,refuted} for experiments; key `e`. board: `python3 loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R667 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check BROAD `pgrep` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet; ALSO subsumes the moat _PREDICATE_ORACLES BYO-bare-predicate fix, E116/E118). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] the non-gated re-scan space is NARROW but R664-R667 proved not fully dry — E187 (loader) + E188 (arm) + E189 (base vel) + E190 (nav goal) all found fresh external-input-finiteness veins. Remaining candidate numeric sinks of the SAME class (validate untrusted numeric input, fail-OPEN not fail-safe): gripper width/force IF a set-width API appears, place/pose duration/tolerance args (move_joints `duration`, walk `duration` — CHECK FIRST: int()/time.sleep() already RAISE on NaN/inf = fail-safe = NOT a find). Before claiming ANY new gap, grep ledger+LESSONS to confirm non-duplication AND check it's fail-OPEN not fail-safe (a downstream loud check / int()-raise = not a find). ONLY a genuinely fresh, on-disk, reachable, non-duplicate fail-open → fix it; else → HOLD (one plateau experiments row). Do NOT manufacture an audit sliver.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367; fresh-axis-negative E180/E181/E182, fresh-axis-FIXED E183/E184/E187/E188/E189/E190): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. Its `_PREDICATE_ORACLES` edit ALSO closes the moat BYO-bare-predicate gap (E116/E118). OWNER CALL OWED — queued 84 rounds (R318..R667). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R390 §7): `git log --grep='GATE-APPROVED\|CEO-APPROVED'` since R380 = ZERO hits. R667 = a BUILD/fix: an OFFLINE non-spine self-crossed input-validation fix (E190 nav-goal finiteness gate in hardware/base.py + 4 navigate_to sinks + publish_goal; no interface signature change) — no interface/dep/hardware-contract/security-policy/spine/manifest edit ⇒ NOT a CEO gate, no token needed. Nothing self-crossed a gate.
last_review: R390
