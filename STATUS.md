# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R666 (E189, BUILD). HEADLINE: **broke the HOLD reflex again with a REAL fresh non-gated fix — the BASE-velocity command boundary silently accepted NaN/±inf**. `set_velocity(vx,vy,vyaw)` (the streaming cmd_vel actuator boundary) was fail-open on THREE drivers: `MuJoCoGo2` clamped with `np.clip` which CLAMPS ±inf but PROPAGATES NaN (`np.clip(nan,lo,hi)==nan`, verified) → NaN into `_cmd_vel`+`_cmd_motion`; `MuJoCoG1` did bare `float()` with NO clip/check → NaN AND inf into `self._cmd`+`_cmd_motion`; `Go2ROS2Proxy` republished `float()` onto `/cmd_vel_nav`. REACHABLE: `vcli/primitives/locomotion.py:80` passes vx/vy/vyaw straight to `base.set_velocity` with NO upstream finiteness guard (NL/BYO path) + the Nav2 bridge. A NaN poisons the `_cmd_motion` R2b actor-causation Inv-1 signal (`nan > MOTION_EPS` is False ⇒ a real motion registers as none) WHILE the command still reaches the actuator — a security-floor violation ("reject NaN before acting"). Same class as E187 (loader) + E188 (arm actuator), now the THIRD boundary (base velocity). Fix = ONE pure `ensure_finite_base_velocity()` in `hardware/base.py` (Protocol-only, no hw/ROS2 import) wired at entry of all 3 `set_velocity` impls; loud axis-scoped ValueError; ast-discovered wiring test = MIRROR-safe (a new driver can't skip it). RED import-fail→GREEN 14/14; 200/200 hardware unit regress clean. Red-team ALL pass: vcli locomotion primitive un-guarded ⇒ reachable NL/BYO; `np.clip(nan)=nan` ⇒ go2 NaN was genuinely fail-open; finite vels pass 200/200 incl walk/turn ⇒ no false-reject; loud raise ⇒ strictly stricter. NON-spine (hardware/ not in check.sh SPINE), no sig change ⇒ NOT a CEO gate, self-crossed like E187/E188. Both levers RE-VERIFIED SHUT cold: (1) SIM — sibling isaac go2w warehouse_nav LIVE (pids 3751659/3751665/3751672 + nav stack 3751367/3751393/3751401) holds the ONE slot (Inv-5 WAIT); (2) OWNER — inbox only long-answered G-183-12; gates.jsonl ZERO answers for G-323-1 (queued 83 rounds)/G-383-1/G-354-1. (78th sim-block — but a REAL build, not a HOLD.)
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R666 landed a fresh non-gated external-input-validation hardening at the BASE-velocity actuator boundary; both levers still SHUT).
last-round: R666 (E189 build: NaN/inf silent-accept at the base-velocity boundary FIXED — `ensure_finite_base_velocity()` gate on all 3 set_velocity impls, RED import-fail→GREEN 14/14, 200/200 regress, red-team clean, np.clip-passes-NaN caught, LATENT/self-crossed non-spine).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED. SATURATED veins: name-list (E173-E175/E116/E118), in-kernel hardcoded-SET (E177), hardcoded-NAME exclusion (E178), numeric-threshold-in-verify (E180), tool-registry silent-overwrite (E181), vocab-derivation (E182), persona-swallow (E183), skill-registry-routing-swallow (E184); silent-swallow class grep-PROVABLY empty (E185). EXTERNAL-INPUT FINITENESS vein now spans THREE command boundaries: manifest loader (E187 config.py) + ARM actuator (E188 move_joints) + BASE velocity (E189 set_velocity). R312-R666 plateau REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. Highest-leverage unblock = owner acts on G-323-1 (queued 83 rounds).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — BROAD `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac|warehouse_nav'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot [narrow `isaac` alone MISSES the `python3`-named warehouse_nav], inbox+gates.jsonl for owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis (tools/verify/vocab/persona/skill-registry/loader-finiteness/arm-actuator-finiteness/base-velocity-finiteness all scanned), (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144; authoritative owed=0), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145 — just `python3 loop/board.py`), (g) re-force any SATURATED vein. Before mining ANY "new" gap: grep ledger + docs/LESSONS.md FIRST — if E#-recorded/gated/benign, it is NOT a find.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check BROAD `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (R666 E189 line=967B), each string field ≤280 CHARS. status ∈ {confirmed,inconclusive,plateau,refuted} for experiments; key `e`. board: `python3 loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R666 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check BROAD `pgrep` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet; ALSO subsumes the moat _PREDICATE_ORACLES BYO-bare-predicate fix, E116/E118). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] the non-gated re-scan space is NARROW but R664/R665/R666 proved not fully dry — E187 (loader) + E188 (arm actuator) + E189 (base velocity) all found fresh external-input-finiteness veins. Remaining candidate numeric sinks of the SAME class (validate untrusted numeric input, fail-OPEN not fail-safe): gripper width/force from a BYO skill config, place/pose targets, duration/tolerance args. Before claiming ANY new gap, grep ledger+LESSONS to confirm non-duplication AND check it's fail-OPEN not fail-safe (a downstream loud check = not a find). ONLY a genuinely fresh, on-disk, reachable, non-duplicate fail-open → fix it; else → HOLD (one plateau experiments row). Do NOT manufacture an audit sliver.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367; fresh-axis-negative E180/E181/E182, fresh-axis-FIXED E183/E184/E187/E188/E189): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. Its `_PREDICATE_ORACLES` edit ALSO closes the moat BYO-bare-predicate gap (E116/E118). OWNER CALL OWED — queued 83 rounds (R318..R666). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R390 §7): `git log --grep='GATE-APPROVED\|CEO-APPROVED'` since R380 = ZERO hits. R666 = a BUILD/fix: an OFFLINE non-spine self-crossed input-validation fix (E189 base-velocity finiteness gate in hardware/base.py + 3 set_velocity impls; no interface signature change) — no interface/dep/hardware-contract/security-policy/spine/manifest edit ⇒ NOT a CEO gate, no token needed. Nothing self-crossed a gate.
last_review: R390
