# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R317 (E108, BUILD) — the MULTI-VIEW minicpm pose oracle that E107 proposed as the fix is REFUTED: a left-side topple reads STANDING in ALL 4 azimuths, so side-lying is a TOPOLOGY blind spot, not a camera-angle artifact. A minicpm ensemble is NOT a robust pose oracle.
  §1a FLOOR: adopted supervisor R316 rounds.jsonl append + regen BOARD (ages +1). Inflight empty; no WIP; inbox has no unanswered decision; provisionals check-exempt.
  §1b: E107(refuted) already carried final status. 67 acceptance provisionals all check-exempt (backfill/superseded/≤2r) → check.sh GREEN, none wedge.
  §2-4 (non-gated, no billing/spine): STATUS next#1 = test if a MULTI-VIEW minicpm vote rescues the E107 anisotropy. Rendered (offline capture.snapshot, fresh MjModel; NEVER a 2nd wrapper renderer) all 4 R316 poses from 4 azimuths (45/135/225/315) via a NEW tracked var/evidence/R317/render_multiview.py; probed minicpm-v N=3 neutral on all 16 frames (48 reads); majority-voted per pose. I LOOKED at the side-left + upright frames first.
  E108 (research, REFUTED): multi-view is NOT the fix. LEFT-side topple STANDING 3/3 in ALL 4 views → majority WRONG, though clearly on its side from az45/225/315. BACK-topple missed ONLY at az45 but LYING 3/3 at 135/225/315 → majority FALLEN (multi-view RESCUES a back-topple). Upright-yaw90 STANDING 4/4 (no false-FALLEN). Sunk UPRIGHT 3/4.
  HEADLINE: minicpm's side-lying miss is a TOPOLOGY blind spot in the MODEL, not a view artifact — a back-topple is view-anisotropic (ensemble helps) but a side-topple is invisible from every azimuth. So a minicpm 2nd-witness — single-frame OR multi-view ensemble — is NOT a robust pose oracle; eyes-pose needs a DIFFERENT model or the deterministic quat-upright hard-channel. Provisional; no acceptance row (offline judge-quality probe, not the acceptance face). NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier: eyes 2nd-witness — the local-VLM MODEL lever for POSE is now EXHAUSTED: qwen pose-blind (E105), minicpm side-lying-blind single-frame (E107) AND multi-view (E108). Every OTHER new capability CEO-gated S4/D182/D76.)
last-round: R317 (E108 REFUTES the multi-view minicpm pose oracle — side-lying is a topology blind spot, not a view artifact). New tracked render_multiview.py. No spine path touched.
frontier: eyes-pillar model lever for POSE is EXHAUSTED among local VLMs (qwen pose-blind E105; minicpm side-lying-blind single-frame E107 + multi-view E108). Robust pose-eyes now need EITHER a fundamentally different model (remote qwen3-vl = BILLING-gated, or an untried local pull) OR to concede pose to the deterministic quat-upright hard-channel (VLM eyes reserved for what the hard channel can't read — object identity/placement, already the confirmed vlm-judge use). All OTHER new capability CEO-gated: S4 (3rd embodiment BYO-URDF), D182 (NL grounder), quantity N-object (D76 planner-plugin).
watch: POSE-PROBE recipe (non-spine, local ollama, no billing): tools/acceptance/pose_probe.py [--framing neutral|fault] <model> <N> <frame..>; render fresh via var/evidence/R317/render_multiview.py (azimuth-varied) or R316/render_poses.py. NEVER a 2nd MuJoCoGo2 renderer (segfault); the render process cores at EGL teardown AFTER "done" (all PNGs already written — cosmetic, exit 139). NEVER `ulimit -v` a GL/EGL render. Local VLM cold-load ~90s; warm via curl keep_alive first. Frames: var/evidence/R317/{pose}_az{45,135,225,315}.png.
  REAL-FACE (only IF G-315-1 approved + rubric neutralized): bare vcli + NL; FORCE deepseek+v4-flash (default qwen ROUTING Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`; eyes via VECTOR_JUDGE_LOCAL_MODEL=minicpm-v:latest. CAVEAT: LIVE harness judge times out 60s (vision_judge._call_vlm) — a CPU-slow local VLM ABSTAINs there; AND E107/E108 say minicpm eyes are not a robust pose oracle anyway.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; `free -g` + `pgrep -af repl_accept` before launch; scripts/sim-teardown after. NEVER pkill mujoco. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD (E27/E81/R316/R317): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (Chinese=3B/char), each string field ≤280 CHARS (R317 first E108 result was 281 → trimmed). experiments key `e` NOT `experiment`; status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds append shifts ages → NEXT round MUST regen BOARD.
next:
  1. [NON-GATED, but the local model lever is exhausted] The eyes-POSE frontier via local minicpm is DEAD (E105/E107/E108). Two honest non-gated moves: (a) design-note whether POSE even needs VLM eyes — the sim root quat is a deterministic upright oracle the actor can't author-away on the SIM face; if so, VLM eyes stay reserved for object identity/placement (confirmed) and pose falls to the hard channel; OR (b) one untried local VLM pull (llava/moondream) probed on the R317 side-left frames — if it also misses, the local lever is CONCLUSIVELY exhausted. Bank provisional. Still non-spine (offline probe / design note, no rubric edit).
  2. [OWNER GATE — ONLY path to NEW capability] G-315-1 (rubric neutralization to land VLM eyes) — now with the E107+E108 caveat that NO local VLM (single or multi-view) is a robust pose oracle. OR S4 (3rd embodiment BYO URDF+manifest) OR D182 (world-owned NL→object grounder) OR D76 planner-plugin. All CEO-gated ~60r.
  3. [NEXT REVIEW = R320] Skeptic-re-verify oldest confirmed rows; re-red-team a headline; adjudicate provisionals (incl. E108); wiring audit; ambition critic (plateau check on the eyes-pillar — R312-R317 all eyes-probing, no acceptance-face advance).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-315-1 SPINE/MOAT (eyes rubric wording): minicpm-v (E106) discriminates SOME go2 pose under NEUTRAL framing, but E107+E108 show NO local VLM (single-frame or multi-view) robustly reads a side-lying topple. Landing VLM eyes as a real pose oracle needs BOTH a reworded neutral rubric item (ADR-002 moat change, checksum-pinned) AND a model that actually reads side-lying — the model half is currently unmet locally. Owner call owed. <- next#2. (R315/E106, R316/E107, R317/E108)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R317 CLEAN. R317 touched NO spine path (rendered azimuth-varied frames; added var/evidence/R317/render_multiview.py — evidence, gitignored; 1 experiments row E108 + LESSONS E107-bound extension + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ or config/rubric edit).
last_review: R310
