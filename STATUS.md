# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R368 (E158, BUILD). HEADLINE: both levers SHUT (47th sim-block). Genuine substrate re-scan (grep comment-pinned "must match"/"kept in sync" across ALL of `vector_os_nano/`, not just tests) found a REAL uncaught mirror that E151's "SATURATED" claim MISSED: `piper_ros2_proxy.py:52-53` `_BODY_SENSOR_DX/_BODY_SENSOR_DZ` (0.3/0.2) is a FOURTH downstream go2 mirror of `mujoco_go2._LIDAR_OFFSET_X/_Z` — its comment says "must match go2_vnav_bridge.py _SENSOR_X/_SENSOR_Z" and `_sync_ik_base` SUBTRACTS it to reconstruct the body pose from `/state_estimation` (sensor frame). Drift ⇒ wrong IK base ⇒ piper arm reaches the wrong place, silently — no red test. The E149/E151 guard (`test_manifest_driver_fidelity.py`) covered go2 manifest + go2_vnav_bridge `_SENSOR_X/_Z` + g1, but NOT this piper copy. Added `test_piper_proxy_sensor_offset_matches_driver`: ast-reads `_BODY_SENSOR_DX/_DZ` (no import, sim-free — verified 0.14s no sim; symmetric with the bridge guard) and asserts ==`go2._LIDAR_OFFSET_X/_Z`. 11/11 green. RED-TEAM (revert-clean, source byte-unchanged after): (1) drift DX 0.3→0.35 ⇒ RED (exact drift caught); (2) rename const ⇒ RED loud "not found as top-level constant" (guard cannot be silently disabled by rename); control reverted ⇒ 11/11. Non-spine, non-gated (tests/ only). CORRECTS the E151 over-claim — a "vein SATURATED" must enumerate ALL mirror copies across every driver/proxy, not stop at the first robot's chain.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked. This round's non-gated BUILD closed a REAL uncaught 4th mirror in the lidar-offset vein E151 wrongly called SATURATED — a genuine gap verified on disk, NOT a manufactured sliver. The vein now covers all 4 go2 _LIDAR_OFFSET copies (driver+manifest+bridge+piper proxy) + both g1.)
last-round: R367 (E157 BUILD: arm-playground catalog↔MJCF set-membership guard in the deterministic gate; both levers SHUT).
frontier: Offline plug-and-play Verify axis closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — driver↔manifest↔bridge↔piper-proxy lidar-offset mirror COMPLETE for go2 (4 copies) + g1 (E147-E151, corrected E158), vcli+acceptance verdict contracts (E152/E153), Inv-4 kernel-import purity via seam allowlist (E154), causation-baseline frozen (E155), embodiment-config morphology schema (E156), arm-playground catalog↔MJCF (E157). Spine + config-schema frozen + FULL mirror coverage COMPLETE. The R312-R368 acceptance-face plateau is REAL + HARD-EXTERNAL (not a local hill): the two lever paths to a NEW confirmed capability are sim slot frees (transient) OR owner approves G-323-1. Escalated (exec summary R329; re-surfaced R335-R368).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss` to confirm a LIVE run not a leak) for the sim slot, inbox+gates.jsonl for G-323-1 — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline capability axis, (b) re-audit an already-audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (mechanical age+1 drift, E102/E145), (g) re-force the manifest/bridge/piper-proxy lidar-offset mirror — NOW fully SATURATED across all 4 go2 copies + g1 (E151+E158), (h) re-force the Inv-4 import guard — allowlist-covered (E154), (i) re-force a SPINE frozen-dataclass guard — spine coverage COMPLETE (E152/E153/E155), (j) re-force the embodiment-config schema frozen guard — config coverage COMPLETE (E156), (k) re-force the arm-playground catalog↔MJCF guard — COMPLETE (E157). But DO genuinely re-scan the substrate for a REAL unenforced invariant OUTSIDE all saturated veins before defaulting to HOLD (E152-E158 = seven such found post-saturation; E158 shows even a "SATURATED" vein can hide a missed copy — grep the pinning comment across ALL drivers/proxies, not one robot's chain). Next candidates if a gap is real: a load-bearing const with a comment-pinned mirror not yet ast-guarded (grep "must match"/"kept in sync"/"must equal" across `vector_os_nano/`), a hardcoded enumeration that should introspect, or a genuine test-coverage hole on a load-bearing invariant. Verify on disk FIRST — no sliver.
  QUARTET PACKAGED (E114) + GUARDED (E115): `.venv/bin/python tools/acceptance/hard_channels_doctor.py [--json]` exit 0 iff READY-TO-LAND. Run BEFORE landing G-323-1 (GREEN 4/4 re-confirmed R367).
  SIM-SAFETY (Inv-5): a SIBLING loop CAN hold the one global sim slot — BEFORE any sim `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'`; WAIT if live, NEVER teardown a sim you did not launch. Inv-5 CATEGORICAL; free memory does NOT license a 2nd sim. (R368: sibling isaac warehouse_nav.py LIVE, pid 3569755 + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS. experiments key `e`, status ∈ {confirmed,inconclusive,plateau,refuted}. board: `.venv/bin/python loop/board.py`.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve). A round improving its own checkers STOPS and queues (G-354-1 example).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R368 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch, or scene-clutter) on the REAL bare-cli face; re-confirm with a fresh row or refute with supersedes:. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing the quartet into the acceptance face] G-323-1: run hard_channels_doctor (GREEN 4/4 re-confirmed R367), then merge `hard_channels.make_verify_namespace` into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Consolidates G-318-1 · relational-near · G-322-1. Also queued: G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] genuinely re-scan the substrate for a real non-gated hardening OUTSIDE ALL saturated veins (lidar-offset mirror ALL 4 go2 copies+g1 E151/E158, spine frozen E152/E153/E155, Inv-4 import E154, config-schema frozen E156, catalog↔MJCF E157): a comment-pinned const mirror not yet ast-guarded (grep "must match"/"kept in sync" across `vector_os_nano/`), a hardcoded enumeration that should introspect, or a real test-coverage hole on a load-bearing invariant. Verify the gap on disk FIRST. ONLY if no real gap exists AND no lever opened → HOLD (one experiments row); do NOT manufacture an audit sliver, re-force a saturated guard, re-investigate the preflight nag (E144) or BOARD age drift (E145).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, bare-idiom E116, adoption-doc'd E117, runtime-proven E118, plateau re-audited E119, recover ruled-out E120/E130, substrate rot-free E121/E147-E158 re-red-teamed E150): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. OWNER CALL OWED — queued 51 rounds (R318..R368). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" every round; the gate (checks_schema.py) exempts backfill+superseded ⇒ TRUE owed=0. FIX (verified, patch var/evidence/R354/preflight-provisional-fix.patch): add checks_schema.open_provisionals(rows) + `--open-provisionals` CLI; preflight calls it not grep -c. Informational only, gate byte-unchanged, 7/7 tests. MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen; low-risk, ~5-min approve-and-merge.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1 SPINE/MOAT: the three per-channel landing gates, CONSOLIDATED under G-323-1. Kept for provenance (R318/E109..R322/E113).
  - S4 (one generic driver 3rd embodiment — sim-gated: needs a real driver+MJCF, NOT a clean offline win) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer, planner-free contract) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-10 CLEAN (git log R358..R367 grep GATE-APPROVED/CEO-APPROVED = 0 hits). R368 touched ONLY tests/unit/embodiments/test_manifest_driver_fidelity.py (new guard) + docs/LESSONS.md + ledger/BOARD + STATUS + events — no spine/manifest-gated path. No self-approval.
last_review: R360
