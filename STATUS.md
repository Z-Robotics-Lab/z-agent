# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R392 (E181, BUILD→research/plateau). HEADLINE: fresh non-gated axis — the tool-registry silent last-wins OVERWRITE (`ToolRegistry.register` base.py:208) — tested NEGATIVE: benign by-design, no fail-open. Both levers RE-VERIFIED SHUT (70th sim-block, 81-round plateau); HOLD. (1) SIM: sibling isaac warehouse_nav pid 3569767 LIVE (ps etime 11h27m, growing = running sim NOT a leak) + localPlanner 3569488 + laser_mapping 3569496 hold the ONE sim slot; Inv-5 categorical ⇒ cannot launch. (2) OWNER: inbox only G-183-12 already answered+crossed a64149f; 5 gates queued unanswered. (3) FRESH-AXIS TEST (not a re-audit): `ToolRegistry.register` silently replaces by name (`self._tools[name]=inst`) — looked like a BYO-world-shadows-kernel-tool fail-open, but is BENIGN on 2 independent legs: (a) `/reload` (reload_tool.py:109) DEPENDS on last-wins idempotent-replace — fail-loud would break hot-reload; (b) the permission gate (`tool_execution.resolve_permission`) is name-keyed + deny-by-default + fail-closed, so a same-name shadow inherits the SAME gate ⇒ no privilege bypass. Grepped ledger+LESSONS first: no prior registry-collision E# (axis genuinely fresh, non-dup). (4) inflight empty; no new provisional owed (the "67" is the E144/G-354-1 false-nag). R312-R392 = 81-round acceptance-face plateau, REAL + hard-external.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R392 = fresh registry-collision axis tested = benign, HOLD).
last-round: R392 (E181 research/plateau: tool-registry silent-overwrite axis tested = benign by-design [/reload depends on it + gate is name-keyed fail-closed]; both levers re-verified SHUT; no fresh non-gated gap → HOLD).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar mirrors (E147-E158), verdict contracts (E152/E153), Inv-4 allowlist (E154), embodiment-config schema (E156), arm-catalog↔MJCF (E157), home-pose (E161), F821/bugbear (E163/E164), head-cam res+renderer FAMILY (E165-E170), Inv-3 plug-and-play NOT overfit (E171), capability-gate authority D72↔D175 (E172→G-383-1), name-list-fails-open vein: manip-gate (E173)/post-place (E174)/auto-home (E175, SURVIVES E179). SATURATED veins: name-list (arm E173-E175 + moat _PREDICATE_ORACLES E116/E118), in-kernel hardcoded-SET (E177), hardcoded-NAME exclusion (E178), numeric-threshold/magic-number (E180=verify-margins/guarded mirror consts), tool-registry silent-overwrite (E181=benign by-design). R312-R392 plateau (81 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. Honest domain-expert read: the loop cannot land a NEW sim-face capability while externally blocked, and the non-gated re-scan space is exhausted (every candidate is latent-BYO-inert, already-gated, or benign). Highest-leverage unblock = owner acts on G-323-1 (queued 73 rounds).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot, inbox+gates.jsonl for the owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144; authoritative owed=0), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145 — just regenerate `loop/board.py`), (g) re-force any SATURATED vein incl. head-cam renderer FAMILY (E167/168/170), offline embodiment-config (E171), capability-gate authority (E172→G-383-1), manip-gate (E173)/post-place (E174)/auto-home (E175, SURVIVED E179), moat `_PREDICATE_ORACLES` (E116/E118, G-323-1), in-kernel hardcoded-SET (E177), hardcoded-NAME exclusion (E178), numeric-threshold (E180), tool-registry silent-overwrite (E181). Before mining ANY "new" gap: grep ledger + docs/LESSONS.md FIRST (R387+R388's lesson) — if E#-recorded/gated/benign, it is NOT a find.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim. (R392: sibling isaac warehouse_nav pid 3569767 LIVE 11h27m holds the slot — SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (R392 E181 result=264). status ∈ {confirmed,inconclusive,plateau,refuted} for experiments; key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R390 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet; ALSO subsumes the moat _PREDICATE_ORACLES BYO-bare-predicate fix, E116/E118). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] the non-gated re-scan space is exhausted — every recent candidate is latent-BYO-inert, already-gated, or benign. Before claiming ANY new gap, grep ledger+LESSONS to confirm non-duplication/non-benign. ONLY if a genuinely fresh, on-disk, non-duplicate gap exists → fix it; else → HOLD (one plateau experiments row). Do NOT manufacture an audit sliver or re-file a documented finding.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, substrate rot-free E121/E147-E175 re-red-teamed E150/E159/E169/E179; fresh-axis-negative E180/E181): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. Its `_PREDICATE_ORACLES` edit ALSO closes the moat BYO-bare-predicate gap (E116/E118). OWNER CALL OWED — queued 73 rounds (R318..R391). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/ — KEPT despite >20-round age: live-gate dependency): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R390 §7): `git log --grep='GATE-APPROVED\|CEO-APPROVED'` since R380 = ZERO hits. R392 = a research/plateau HOLD: NO code/spine/test/manifest edit — ONLY ledger (E181 plateau) + STATUS + BOARD regen. Nothing self-crossed.
last_review: R390
