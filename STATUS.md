# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R387 (E176, BUILD→research). HEADLINE: an HONEST substrate re-scan with both levers shut found NO novel non-gated plug-and-play hardening — and, importantly, RED-TEAM caught me mid-round about to file a DUPLICATE gate (G-387-1) + redundant tests for a gap that is ALREADY DOCUMENTED. Scan results: (a) the remaining `native_loop._build_motor_tools` name checks (1162/1164 `navigate`/`detect`) are correct SAME-NAME DEDUP (which of two tools claiming one name wins), NOT fail-open capability gates — no metadata introspection applies; (b) the base/camera capability AXIS is inert — no base-less/camera-less SHIPPED body (go2+g1 both have base+camera), and shipped mobile skills (`mobile_pick`/`mobile_place`) carry NO structured base metadata (base need is a runtime guard) AND are already arm-gated ⇒ a base-axis gate would be a sliver; (c) silent `except` handlers in core/vcli are telemetry/notification callbacks, not control-safety swallows; (d) the verify sandbox (`GoalVerifier`) fails SAFE to False on unknown/malformed exprs. (e) The one apparent find — `evidence_classifier._PREDICATE_ORACLES` is a HARDCODED kernel list so a BYO bare bool-predicate call classifies RAN — turned out to be ALREADY E116/E118 (docs/LESSONS.md:144), already guarded by `tests/vcli/test_plug_and_play_boundary.py` (`test_byo_bare_predicate_idiom_is_the_zero_edit_boundary`), and already folded into G-323-1 (its `_PREDICATE_ORACLES` edit). So I REVERTED the redundant test additions + removed the mis-filed G-387-1 gate row + rewrote E176 to the honest plateau outcome — all before commit, so NO false audit trail entered history. NET: the moat `_PREDICATE_ORACLES` / bare-BYO-predicate path is SATURATED + already gated (do NOT re-scan). Both levers still SHUT (65th sim-block).
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R387 = an honest re-scan that found no novel non-gated gap and self-corrected a near-duplicate of E116/E118+G-323-1 before commit).
last-round: R387 (E176 research/plateau: re-scan found no novel non-gated hardening; base/camera axis inert, name-checks are dedup, verify fail-safe; the _PREDICATE_ORACLES apparent-find is already E116/E118/G-323-1 — reverted a near-duplicate gate+tests pre-commit).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED — lidar mirrors (E147-E158), verdict contracts (E152/E153), Inv-4 allowlist (E154), embodiment-config schema (E156), arm-catalog↔MJCF (E157), home-pose (E161), F821/bugbear (E163/E164), head-cam res+renderer FAMILY (E165-E170), Inv-3 plug-and-play NOT overfit (E171), capability-gate authority D72↔D175 found+gated (E172), plug-and-play name-list-fails-open vein: manip-gate (E173), post-place guard (E174), auto-home drop guard (E175). The name-list-fails vein (arm side E173-E175 gated/self-crossed; moat side _PREDICATE_ORACLES E116/E118 already gated under G-323-1) is now SATURATED on both faces. R312-R387 acceptance-face plateau (75 rounds) is REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. A domain expert would still say the loop cannot land a NEW sim-face capability while externally blocked — honestly acknowledged; the re-scan space OUTSIDE the gated queue is now largely exhausted (every recent find is either latent-BYO-inert or already-gated).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot, inbox+gates.jsonl for the owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis, (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145), (g) re-force any SATURATED vein incl. head-cam renderer FAMILY (E167/168/170), offline embodiment-config (E171), capability-gate authority has_arm (E172 — needs G-383-1), the manip-gate classifier (E173), the post-place guard classifier (E174), the auto-home drop guard (E175), OR — NEW THIS ROUND — the moat `_PREDICATE_ORACLES` / bare-BYO-predicate classifier boundary (E116/E118, already guarded by test_plug_and_play_boundary.py + folded into G-323-1; R387 re-derived it and REVERTED a near-duplicate gate/tests). The name-list-fails vein is SATURATED on BOTH faces (arm skills AND the moat). Before mining ANY "new" gap: grep the ledger + docs/LESSONS.md FIRST to confirm it is not already E#-recorded/gated (R387's lesson) — if it is, it is NOT a find. If a genuinely fresh class appears, verify the gap on disk AND its non-duplication BEFORE any edit.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim. (R387: sibling isaac warehouse_nav.py LIVE pid 3569767 RSS~8.9G etime 10.5h + localPlanner + laser_mapping — slot SHUT.)
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES, each string field ≤280 CHARS (R387 E176 = 886B; status ∈ {confirmed,inconclusive,plateau,refuted} for experiments). experiments key `e`. board: `.venv/bin/python loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R387 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac'` FIRST — WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet; ALSO subsumes the moat _PREDICATE_ORACLES BYO-bare-predicate fix, E116/E118). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] the non-gated re-scan space is now largely exhausted — every recent candidate is either latent-BYO-inert (base/camera axis) or already-gated (name-list vein both faces). Before claiming ANY new gap, grep ledger+LESSONS to confirm non-duplication (R387's lesson). ONLY if a genuinely fresh, on-disk, non-duplicate gap exists → fix it; else → HOLD (one plateau experiments row). Do NOT manufacture an audit sliver or re-file a documented finding.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367, substrate rot-free E121/E147-E175 re-red-teamed E150/E159/E169): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. Its `_PREDICATE_ORACLES` edit ALSO closes the moat BYO-bare-predicate gap (E116/E118). OWNER CALL OWED — queued 69 rounds (R318..R387). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token last-review CLEAN (R380 §7). R387 = a research/plateau round: NO code/spine/test edit committed (the mid-round test additions + G-387-1 gate row were REVERTED pre-commit as duplicates of E116/E118/G-323-1). ONLY ledger (E176 plateau) + STATUS + this note change. No interface/flag/spine/manifest touch. Nothing self-crossed.
last_review: R380
