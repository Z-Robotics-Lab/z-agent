# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R669 (E192, BUILD). HEADLINE: **CONSOLIDATED the E187-E191 sink-by-sink finiteness enumeration into ONE root gate at the untrusted plan-JSON boundary — defense-in-depth, honestly NOT a fresh fail-open**. All five prior rounds gated a downstream SINK (loader E187 · arm move_joints E188 · base set_velocity E189 · nav goal E190 · blocking walk_forward/turn E191) and every red-team cited the SAME reachability SOURCE: `goal_decomposer` parses `response.text` (untrusted LLM output) with bare `json.loads`, which (1) accepts the bareword tokens `NaN`/`Infinity`/`-Infinity` (parse_constant pass-through) AND (2) — the foot-gun NONE of E187-E191 named — coerces an OVERFLOW literal `1e999`→`inf` WITHOUT parse_constant ever firing. Fix = pure `_loads_finite()` in `vcli/cognitive/goal_decomposer.py` (sets a rejecting parse_constant AND recursively `math.isfinite`-scans parsed floats), raising `json.JSONDecodeError` ⇒ the existing except ⇒ safe single-step fallback; wired at BOTH `json.loads` sites + an ast source-mirror test (a new parse site can't skip it). RED (import-fail)→GREEN 8/8; 81/81 cognitive regress clean (incl. finite plans distance=2.0/angle=-90 round-trip ⇒ no false-reject). HONEST framing (survives red-team): this closes NO current fail-open — post-E191 every KNOWN numeric sink already gates NaN/inf; its value is the loop's OWN E154 root-fix>rotting-enumeration recipe (a FUTURE un-gated numeric primitive can't be reached by a non-finite plan value) + the overflow foot-gun + the security-floor "validate ALL external input at the boundary". NON-spine (`goal_decomposer.py` not in check.sh SPINE), no signature/dep/interface change ⇒ NOT a CEO gate, self-crossed like E187-E191. Both levers RE-VERIFIED SHUT cold: (1) SIM — sibling isaac go2w warehouse_nav LIVE (pids 3969261/555/561/568 + nav stack, RESTARTED across the round vs 3751* — an actively-cycling sibling) holds the ONE slot (Inv-5 WAIT, NEVER-KILL-INFRA — my over-reaching pytest that briefly ran tests/harness/ was killed BY me, sibling untouched); (2) OWNER — inbox only long-answered G-183-12; gates.jsonl ZERO answers for G-323-1 (queued 86 rounds)/G-383-1/G-354-1. (81st sim-block — a REAL build.)
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: build (acceptance FACE remains sim-blocked; R669 root-consolidated the E187-E191 finiteness vein at the untrusted plan-JSON source; both levers still SHUT).
last-round: R669 (E192 build: the untrusted plan-JSON parse SOURCE now rejects non-finite numbers — `_loads_finite()` closes both bareword-token AND overflow-literal foot-guns, wired at both goal_decomposer json.loads sites + ast mirror; RED→GREEN 8/8, 81 regress; defense-in-depth root-consolidation, honestly NOT a fresh fail-open — all sinks already gated E188-E191; NON-spine self-crossed).
frontier: Offline Verify closed (E109-E119); RECOVER built+re-confirmed (E120/E130); substrate rot-free MACHINE-ENFORCED. SATURATED veins: name-list, in-kernel hardcoded-SET (E177), hardcoded-NAME exclusion (E178), numeric-threshold-in-verify (E180), tool-registry silent-overwrite (E181), vocab-derivation (E182), persona-swallow (E183), skill-registry-routing-swallow (E184); silent-swallow class grep-PROVABLY empty (E185). EXTERNAL-INPUT FINITENESS vein now CLOSED at BOTH ends: five SINKS (loader E187 · arm E188 · base-vel E189 · nav-goal E190 · blocking-locomotion E191) AND the primary untrusted SOURCE (plan-JSON parse E192, the E154 root-consolidation catching the overflow-literal foot-gun the sink threat-models missed). R312-R669 plateau REAL + HARD-EXTERNAL: the ONLY two levers to a NEW confirmed CAPABILITY are a sim-slot free (transient) OR owner approves a gate. Highest-leverage unblock = owner acts on G-323-1 (queued 86 rounds).
watch: While the acceptance face stays sim-blocked, each round MUST (1) reverify both levers from disk — BROAD `pgrep -af 'localPlanner|laser_mapping|mujoco|isaac|warehouse_nav'` (+`ps -o etime,rss`=LIVE not leak) for the sim slot [narrow `isaac` alone MISSES the `python3`-named warehouse_nav; the sibling RESTARTS so PIDs rotate — match by CMD not PID], inbox+gates.jsonl for owner gates — and (2) the MOMENT a lever opens, execute next#1 (sim) or next#2 (owner). Do NOT (a) re-prove a closed offline axis, (b) re-audit an audited axis (tools/verify/vocab/persona/skill-registry/loader/arm/base-vel/nav-goal/blocking-locomotion/plan-JSON-source finiteness all scanned), (c) invent an audit/doc sliver, (d) reinterpret Inv-5 to self-unblock the sim (E129), (e) re-investigate preflight's "67 provisionals" (SOLVED R354→G-354-1/E144; authoritative owed=0), (f) re-frame the per-round "BOARD.md stale" check.sh RED (E102/E145 — just `python3 loop/board.py`), (g) re-force any SATURATED vein, (h) re-mine a 6th finiteness SINK — the vein is now root-gated at the source (E192) AND every known sink (E187-E191). NEVER run `tests/hardware/sim/`, `tests/unit/hardware/sim/`, OR `tests/harness/` under bare pytest (spawns MuJoCo, contends the ONE slot — verified R669 tests/harness spawns a sim) — only `scripts/run-tests`. Before mining ANY "new" gap: grep ledger + docs/LESSONS.md FIRST — if E#-recorded/gated/benign, it is NOT a find.
  SIM-SAFETY (Inv-5 CATEGORICAL): ONE sim at a time; check BROAD `pgrep`+`~/.claude/loops/*.lock`+`free -g` BEFORE launching; tear down ONLY sims YOU launched via `scripts/sim-teardown`; NEVER teardown a sim you did not launch; free memory does NOT license a 2nd sim.
  LEDGER HAZARD: append ONE preformatted line via `>>`; NEVER load-all→write-all; NEVER `echo "$EMPTY" >>` (an empty var appends a blank line ⇒ check.sh RED). Caps: line ≤1024 BYTES (R669 E192 line=936B), each string field ≤280 CHARS. status ∈ {confirmed,inconclusive,plateau,refuted} for experiments; key `e`. board: `python3 loop/board.py`.
  PYC RED-PROOF HAZARD (E160): a same-second mutate→revert can leave the GREEN re-confirm falsely RED (2nd-granularity .pyc mtime) — clear `__pycache__` before the confirm run.
  MANIFEST GATE: loop/{check.sh,checks_schema.py,preflight.sh,ROUND.md,run.sh,checks_anchors.py} + AGENTS/GOAL/RULES/scripts are hash-pinned in loop/MANIFEST.sha256 — editing ANY is a CEO gate (needs CEO-APPROVED + MANIFEST regen; NEVER self-approve).
next:
  1. [WHEN THE SIM SLOT IS FREE] finish the R320-R669 skeptic debt: re-run the 2 oldest confirmed BOARD rows (warehouse + courtyard fetch) on the REAL bare-cli face; ALSO the E165 owed 320x240 look/explore re-check, the E167 owed go2 describe-then-pick (+ E170 self-mask after an interleaved look), the E168 owed g1 non-default-res perception-after-look. Check BROAD `pgrep` FIRST (match by CMD, sibling PIDs rotate); WAIT if a sibling holds it.
  2. [OWNER GATE — the ONLY path landing a NEW capability into the acceptance face] G-323-1 (the consolidated quartet; ALSO subsumes the moat _PREDICATE_ORACLES BYO-bare-predicate fix, E116/E118). ALSO queued: G-383-1 (has_arm runtime-authoritative fix) · G-354-1 (preflight fix) · S4 · D182 · D76.
  3. [IF BOTH LEVERS STILL SHUT] the external-input-finiteness vein is now CLOSED at both the source (E192) AND every known sink (E187-E191) — do NOT re-mine a 6th sink. Remaining checked-benign (do NOT re-chase): navigation.py `wait_until_near(tolerance)` fail-SAFE (timeout deadline, no motion); perception.py `measure_distance(angle_rad)` fail-safe (`int(round(nan/inc))` RAISES); `openai_compat:446` tool-arg `json.loads` = a 2nd untrusted source but its sinks are gated ⇒ fail-safe, NOT a find. Genuinely-open only if a NEW numeric API with NO downstream gate appears (e.g. a gripper set-width/force). Before claiming ANY new gap, grep ledger+LESSONS to confirm non-duplication AND that it is fail-OPEN not fail-safe. Else → HOLD (one plateau experiments row). Do NOT manufacture an audit sliver.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-383-1 SPINE/HONEST-VERIFY (R383): make `resolve_capability_profile.has_arm` RUNTIME-AUTHORITATIVE (like has_base/camera), dropping the declared-OR for has_arm ONLY. WHY: D72 ruled has_arm inert; D175 made it a LIVE manip gate ⇒ a manifest declaring has_arm:true with no runtime _arm is offered un-executable manip (E172, 4/4 tests). LATENT. Touches accepted ruling D72 ⇒ CEO gate, NOT self-approved. Refs E172/D72/D175.
  - G-323-1 SPINE/MOAT (the CONSOLIDATED quartet landing — DOCTOR-GUARDED E115 & GREEN re-confirmed R367; fresh-axis-negative E180/E181/E182, fresh-axis-FIXED E183/E184/E187/E188/E189/E190/E191/E192): merge `hard_channels.make_verify_namespace` (upright/nominal_height/near/facing_target, E114 27/27 + cores 72/72) into RobotWorld.build_verify_namespace + add each to `_PREDICATE_ORACLES` + rubric. Red-team: all 4 bind the EXISTING base API ⇒ NO interface change; Inv-1 reads GT the actor cannot author; additive + fail-safe-False ⇒ strictly STRICTER. Its `_PREDICATE_ORACLES` edit ALSO closes the moat BYO-bare-predicate gap (E116/E118). OWNER CALL OWED — queued 86 rounds (R318..R669). SUPERSEDES-IN-SPIRIT G-318-1 · relational-near · G-322-1.
  - G-354-1 DOC-GOVERNANCE (R354): preflight.sh §1b counter false-nags "67 needing adjudication" (TRUE owed=0). FIX (patch var/evidence/R354/): checks_schema.open_provisionals + `--open-provisionals` CLI; MANIFEST-gated ⇒ needs CEO-APPROVED + MANIFEST regen.
  - G-318-1 · RELATIONAL near(a,b) · G-322-1: the three per-channel landing gates, CONSOLIDATED under G-323-1.
  - S4 (one generic driver 3rd embodiment — sim-gated; OFFLINE half guarded E171) · SPINE D182 (world-owned NL→object grounder) · PLANNER D76 (N-object decomposer) · D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · S5/S6 ladder · BILLING (qwen ROUTING key absent; qwen3-vl JUDGE arreared) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT (R390 §7): `git log --grep='GATE-APPROVED\|CEO-APPROVED'` since R380 = ZERO hits. R669 = a BUILD/fix: an OFFLINE non-spine self-crossed input-validation consolidation (E192 `_loads_finite()` in vcli/cognitive/goal_decomposer.py; no interface signature change) — no interface/dep/hardware-contract/security-policy/spine/manifest edit ⇒ NOT a CEO gate, no token needed. Nothing self-crossed a gate.
last_review: R390
