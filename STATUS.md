# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R299 (E90, debug) — ROOT-CAUSED the ord-posinv SCENE_SWAP "regression" (STATUS next#1): it is NOT grasp-execution rot. Skill-direct probe (swap + no-swap control) shows the resolver+grasp are CORRECT — they resolve 最左边的瓶子 → YELLOW and grasp it cleanly in BOTH configs. Yellow (y=3.11, added R211) is the true leftmost bottle; it was added AFTER the R209/R210 bar fixed leftmost=blue → STALE BAR, not code.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (NEW-robot capability still gated S4/D182 ~50r unanswered; loop hardening the verify-pillar + non-gated debug/hygiene while the gates stand).
last-round: R299 (E90, debug). §0/§1a: BOOTSTRAP clean (no quarantine, inflight empty; sibling /workspace/go2w Isaac sim is NOT the repo mujoco/vcli gate, E54). Adopted R298 rounds append + regen BOARD. §1b: no open provisional (R298's two already promoted). DEBUG (Hypothesis Loop, DEBUG.md): next#1 ord-posinv SCENE_SWAP.
  RESULT (E90, confirmed): scratchpad/ord_swap_probe.py skill-direct (LLM bypassed), VECTOR_SCENE_SWAP=1 then no-swap control. Both: `_resolve_ordinal_via_catalog(最左边的瓶子)` → pickable_bottle_yellow; PerceptionGraspSkill holds yellow CLEAN (weld_formed, grasp_world==yellow). H1/H3 REJECTED (grasp-exec + FOV fine), H4 PARTIAL (run1 empty=brain thrash), H2 REFRAMED (resolver picks yellow = CORRECT; the stale artifact is the bar's expected-blue). Larger-world-y ⇒ smaller image-cx ⇒ leftmost; yellow@3.11 > blue@3.0. Yellow is UNMOVED by the swap → the swap no longer even probes invariance. Red-team survived (no-swap control masks the swap; blue central/imageable yet yellow won on real cx; held-oracle is GT). Corrected LESSONS L7 (the "grasp rotted" read was wrong) + Casebook Case 17 + regression guard test_ordinal_resolve.py::test_wiring_leftmost_is_yellow_not_blue_after_R211_scene_growth (18/18 green). No spine/kernel touched.
frontier: Verify-pillar SOLID: eyes=vlm-judge fires + DISCRIMINATES on fetch (E69/E71) AND place (E88/E89). Ordinal/relational NL fidelity is still WITNESS-only (D182); ord-posinv retired as a stale probe. RESIDUAL non-gated: (a) refresh the STALE self-read place bars (warehouse age36 / courtyard age39) on the real face UNDER the judge (self-read→vlm-judge upgrade, stricter Inv.1); (b) optionally rebuild SCENE_SWAP to actually flip the ordinal answer (include yellow) if a live position-invariance probe is wanted. NEW capability still needs owner S4/D182.
watch: Real-face recipe: bare vcli + NL; FORCE deepseek provider + v4-flash via .env-select env vars (default qwen = Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`. World knobs (VECTOR_): SCENE_CLUTTER=1, ROOM_TEMPLATE={warehouse|courtyard}, SCENE_SWAP=1 (swaps blue↔green only; yellow unmoved).
  SKILL-DIRECT PROBE (fast, no LLM, ~35s): scratchpad/ord_swap_probe.py [query] — connects go2+piper, prints resolver output + holding_object for ALL 5 pickables; run under `systemd-run --user --scope -p MemoryMax=24G env … ./.venv/bin/python …`. Isolates target-SELECT vs grasp-EXEC without a REPL brain (E90 method; cf grasp_probe.py).
  HARNESS: launcher = var/evidence/R<N>/run_*.sh (uses .venv/bin/python — the systemd --scope has NO `python` on PATH). eyes vlm-judge AUTO-routes to local ollama gemma4:e4b. MODE=place opens a FRESH session. RUN sim BACKGROUND under `systemd-run --user --scope -p MemoryMax=24G`; pass ROUND_N (evidence → var/evidence/R<N>). NEVER pkill mujoco → scripts/sim-teardown.
  LEDGER HAZARD (E27/E81): append ONE preformatted line via `>>`; NEVER load-all→dumps→write-all (reorders → quarantine). Row caps: ≤1KB JSON bytes, each string ≤280 PYTHON len. experiments.status ∈ {confirmed,inconclusive,plateau,refuted}; confirmed rows' redteam MUST start with 'survived'. board.py WRITES BOARD.md (`.venv/bin/python loop/board.py`, NO redirect); the supervisor's rounds.jsonl append shifts BOARD ages so the NEXT round MUST regen it.
next:
  1. [NON-gated verify / place-breadth] Refresh the STALE self-read place bars on the real face UNDER the judge: MODE=place ROOM_TEMPLATE=warehouse then courtyard, eyes=vlm-judge — does place ground + judge fire in the non-house worlds (place.nl-new-world-* are age36/39 self-read)? Upgrades self-read→vlm-judge (stricter Inv.1).
  2. [NON-gated, optional] Rebuild the SCENE_SWAP scenario so the swap actually FLIPS the ordinal referent (E90: yellow is unmoved so the swap is masked) — either include yellow in the swap or restrict the ordinal probe to the swapped pair — only if a live position-invariance bar is wanted; else drop it (already superseded).
  3. [OWNER GATE — ONLY path to NEW ROBOT capability] Unblock S4 (3rd embodiment BYO URDF+manifest, one generic driver) OR D182 (world-owned NL→object grounder). CEO-gated ~50r.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → deliberate CEO review + real-face verify owed. Reverted R282 (was R281/E78). <- frontier.
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#3.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#3.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R280..R299 CLEAN. R299 touched NO spine path (DEBUG.md + tests/unit + docs/LESSONS + ledger + STATUS + gitignored scratchpad; NO code under vector_os_nano/, NO acceptance/ edit).
last_review: R290
