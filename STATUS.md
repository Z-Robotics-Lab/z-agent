# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-05 · R713 (E236, BUILD). HEADLINE: **FIRST MULTI-LEG ORCHESTRATION BAR LANDED — CLEAN 2/2 GROUNDED.** The chained NL `导航到坐标(12,3.5)，然后找前面的红色的东西` (g1, deepseek-v4-flash, bare vector-cli, launch_explore_seen=False) self-decomposed into nav→perceive in ONE turn and GROUNDED verified=True (2/2): leg1 `at_position(12.0,3.5,tol=0.5)` **actor=CAUSED FIRST-TRY** (default tol, NOT loosened; base walked spawn(10,3)→within0.5 of (12,3.5), 2.06m, transition EARNED) + leg2 `detection_matches_gt('红色的东西')==True` on the MuJoCo seg GT — BOTH unforgeable oracles in the SAME trace, ZERO spine edit (VerdictReport composes per-sub-goal). This CLOSES E235's RAN split: root cause = (11,3) is a 1m hop the gait overshoots past the DEFAULT 0.5m at_position tol; (12,3.5) at 2.06m settles within 0.5 AND keeps red 0.9m dead-ahead. Overrode STATUS hint (9.5,4) — geometrically behind-left of spawn, would turn the robot off red (perceive break). plan·route·verify now proven past single-skill fetch. Scoped teardown clean (12 nuked). NOTE: a concurrent SIBLING isaac-go2w run (pid 103510) was live — NOT mine, NOT killed (Inv.8); my teardown left it untouched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: ACTIVE. The loop CLIMBS the ORCHESTRATION axis (multi-leg plan·route·verify). Re-check the ACTUAL gate (`pgrep mujoco|vcli` — a SIBLING session may be live, see G-713-1) + `available` RAM/free VRAM each round; HOLD only if a sim/loop is genuinely running AND headroom absent (available<~10G or free VRAM<~3G).
last-round: R713 (E236 build, GROUNDED 2/2 provisional). First multi-leg orchestration bar LANDED clean; supersedes E235 refuted.
frontier: The FIRST multi-leg orchestration bar (2-leg nav→perceive) is GROUNDED clean 2/2. RAISE it: (b) a 3-leg chain route→perceive→ACT (act leg needs a moat-graded terminal predicate — use holding_object or an at_position waypoint act, NOT place: place-leg causation refuted E64); (c) does this 2-leg bar TRANSFER to go2 / into tundra (shell-fragile?); (d) a DATA-DEPENDENT chain where leg2's target is chosen by leg1's result (past mere sequencing). World axis (4 shells) proven-by-repetition — a 5th swap is diminishing. Deep rungs (S4/D182/D76/G-323-1) stay CEO-gated.
next:
  1. [ADJUDICATE §1b FIRST] E236 g1.orchestration.nav-then-perceive GROUNDED 2/2 provisional (R713) — red-team survived + reproducible → append `confirmed` row; supersedes R712 E235 refuted. This is the round's first job.
  2. [NON-GATED CLIMB — raise the orchestration bar] frontier (b): a 3-leg chain route→perceive→ACT on g1 with a moat-graded terminal act predicate (holding_object OR a 2nd at_position waypoint — NOT place, E64). Recipe: var/evidence/R713/run_g1chain.sh (edit CHAIN); use ≥2m confirmed-reachable coords keeping the perceive GT in the head-cam (LESSONS g1-chain RECIPE). bg per L82. OR frontier (c): does the 2-leg bar transfer to go2/tundra?
  3. [NON-GATED FLOOR — interleave ≤1 per ~3 climbs] oldest confirmed STALE rows: place.nl-plain-colour (age~415, vlm-judge — WATCH gemma4:e4b abstain) / verify.eyes-place-judge-fires (age~415) / place.nl-new-world-courtyard+warehouse (age~411). BROAD pgrep (incl. sibling isaac) + `available`/VRAM FIRST.
  4. [OWNER GATES — NEW capability only, NOT a face blocker] G-713-1 multi-session (sibling isaac-go2w loop live — coordinate scope?) · G-323-1 hard-channel quartet (~120 rounds) · G-383-1 has_arm · G-354-1 preflight-counter · S4 · D182 · D76.
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-713-1 MULTI-SESSION (R713): a concurrent SIBLING session is running an isaac-go2w BYO acceptance (pid 103510) in THIS repo/host. Not double-driving MY loop (preflight lock held), but two sims can share the 64G host. Per CLAUDE.md multi-session: claim scope in COORDINATION.md before horizontal-file edits; NEVER-KILL the sibling (Inv.8). OWNER: is this an intended parallel session? Refs Inv.5/Inv.8.
  - G-323-1 SPINE/MOAT (R323): merge hard_channels.make_verify_namespace (upright/nominal_height/near/facing_target, E114 27/27) into RobotWorld.build_verify_namespace + _PREDICATE_ORACLES + rubric. Additive, no interface change, strictly stricter; closes moat BYO-bare-predicate. OWNER CALL OWED. Refs E114/E116/D178.
  - G-383-1 SPINE/HONEST-VERIFY (R383): make resolve_capability_profile.has_arm RUNTIME-AUTHORITATIVE (drop declared-OR for has_arm). Touches D72. Refs E172/D72/D175.
  - G-354-1 DOC-GOVERNANCE (R354): preflight §1b counter over-counts provisionals (naive count; check.sh gates ONLY unadjudicated acceptance rows >2 rounds old). MANIFEST-gated. Fix in var/evidence/R354/.
  - S4 (3rd embodiment, offline half guarded E171) · D182 (world-owned NL→object grounder) · D76 (N-object planner) · S5/S6 ladder · BILLING (qwen arreared; deepseek + local gemma4:e4b is the working seam) · RELEASE: merge to master (owner).
last_review: R710
