# STATUS — arch/plug-and-play (snapshot, OVERWRITTEN every round; fields: doc-governance)
updated: 2026-07-04 · R316 (E107, BUILD) — minicpm-v's E106 pose signal is ANISOTROPIC, not a general pose reader: on a broader fault bracket it MISSES a left-side topple → single-frame minicpm as eyes would false-GREEN a left-side fall (a caveat on G-315-1, not a new gate).
  §1a FLOOR: adopted supervisor R315 rounds.jsonl append + regen BOARD (ages +1). Inflight empty; no WIP; G-183-12 crossed R183; inbox has no unanswered decision; check.sh green.
  §1b: E105(refuted)/E106(confirmed) already carried final status. 67 acceptance provisionals all check-exempt (backfill/superseded/≤2r) → check.sh GREEN, none wedge.
  §2-4 (non-gated, no billing/spine): STATUS next#1 = stress E106 on MORE fault frames. Rendered (offline capture.snapshot, fresh MjModel; NEVER a 2nd wrapper renderer) a 2nd upright control (yaw90) + 3 distinct faults (back-topple / left-side-topple / sunk) and ran minicpm-v N=3 under BOTH framings via a NEW tracked pose_probe `--framing neutral|fault` (TDD 10/10).
  E107 (research, REFUTED generalization): NEUTRAL framing → upright-yaw90 STANDING 3/3 ✓, back-topple LYING 3/3 ✓, but LEFT-side topple STANDING 3/3 ✗ (clearly on its side, mirror of the E106 right-side frame it passed) + sunk STANDING 3/3 (upright-orient, defensible). FAULT framing → FALLEN 3/3 on ALL 4 incl the upright control (confirms E106 caveat-a). E106 REPRODUCES byte-exact on the R314 frames (tool faithful).
  HEADLINE: minicpm's pose signal is REAL but VIEW/TOPOLOGY-anisotropic — a single-frame minicpm 2nd-witness is NOT a robust pose oracle (false-GREENs a left-side fall). This TIGHTENS the eyes design: even after G-315-1 rubric neutralization, minicpm eyes need MULTI-VIEW/ensemble. Provisional; no acceptance row (offline judge-quality probe, not the acceptance face). NO spine path touched.
goal: PLUG-AND-PLAY runtime for physical AI — BYO robot/policy/skill/capability/model; plan·route·verify·recover; bare `vector-cli` + NL is the ONLY acceptance face.
phase: active (frontier: eyes 2nd-witness — model layer partially solved (minicpm-v) but the signal is FRAGILE (E107) AND the frozen rubric is still fault-framed (G-315-1); every OTHER new capability CEO-gated S4/D182/D76).
last-round: R316 (E107 bounds E106: minicpm neutral pose signal anisotropic — misses a left-side topple). New tracked pose_probe --framing (TDD). No spine path touched.
frontier: eyes-pillar model lever is PARTIAL, not solved: minicpm-v reads SOME go2 pose (upright + back-topple) but MISSES a left-side topple under neutral framing (E107) — single-frame is not a robust oracle. Robust eyes need MULTI-VIEW/ensemble AND the G-315-1 rubric neutralization. All OTHER new capability CEO-gated: S4 (3rd embodiment BYO-URDF), D182 (NL grounder), quantity N-object (D76 planner-plugin).
watch: POSE-PROBE recipe (non-spine, local ollama, no billing): tools/acceptance/pose_probe.py [--framing neutral|fault] <model> <N> <frame..> — NEUTRAL discriminates (partially), FAULT collapses minicpm to FALLEN-both. judge_determinism.py <frame> <model> <N> = frozen rubric. Frames: var/evidence/R316/{upright_yaw90,toppled_back,toppled_side_left,sunk_floor}.png + R314/{upright_control,toppled_fault,robot_absent}.png; render fresh via var/evidence/R316/render_poses.py (or R314/render_toppled.py). NEVER 2nd MuJoCoGo2 renderer (segfault); NEVER `ulimit -v` a GL/EGL render. Local VLM cold-load ~90s; warm via curl keep_alive first.
  REAL-FACE (only IF G-315-1 approved + rubric neutralized): bare vcli + NL; FORCE deepseek+v4-flash (default qwen ROUTING Arrearage → PREFLIGHT BLOCKED); `set -a; . ./.env; set +a`; eyes via VECTOR_JUDGE_LOCAL_MODEL=minicpm-v:latest. CAVEAT: LIVE harness judge times out 60s (vision_judge._call_vlm) — a CPU-slow local VLM ABSTAINs there even if the offline probe PASSes; AND E107 says single-frame minicpm is not robust anyway.
  HARNESS (real sim): R311 launcher var/evidence/R311/run_house_fetch.sh <tag> <fetch_nl>. ~4-6min — launch DETACHED (nohup systemd-run --user --scope -p MemoryMax=24G), POLL the log; SERIAL; `free -g` + `pgrep -af repl_accept` before launch; scripts/sim-teardown after. NEVER pkill mujoco. TESTS: pure-unit only via `./scripts/run-tests <path>` (12G cap).
  LEDGER HAZARD (E27/E81/R316): append ONE preformatted line via `>>`; NEVER load-all→write-all. Caps: line ≤1024 BYTES (Chinese=3B/char — R316's first E107 row was 1242B, trimmed to 1003B), each string field ≤280 CHARS. experiments key `e` NOT `experiment`; status ∈ {confirmed,inconclusive,plateau,refuted}; acceptance eyes ∈ {human,self-read,vlm-judge}. board.py: `.venv/bin/python loop/board.py`; a supervisor rounds append shifts ages → NEXT round MUST regen BOARD.
next:
  1. [NON-GATED, strengthens the eyes design] Test the E107 anisotropy: is a MULTI-VIEW minicpm oracle robust? Render each fault from 2-3 camera azimuths (capture.CamSpec angle var) and majority-vote neutral pose across views (N≥3/view). If multi-view catches the left-side topple, that's the design for eyes-2nd-witness; bank provisional. Still non-spine (offline probe, no rubric edit).
  2. [OWNER GATE — ONLY path to NEW capability] G-315-1 (rubric neutralization to land minicpm eyes) — now with the E107 caveat that eyes need MULTI-VIEW. OR S4 (3rd embodiment BYO URDF+manifest) OR D182 (world-owned NL→object grounder) OR D76 planner-plugin. All CEO-gated ~60r.
  3. [NEXT REVIEW = R320] Skeptic-re-verify oldest confirmed rows; re-red-team a headline; adjudicate provisionals (incl. E107); wiring audit; ambition critic (plateau check).
gates: (queue — do NOT cross; format docs/RULES.md CEO-gates)
  - G-315-1 SPINE/MOAT (eyes rubric wording): minicpm-v (E106) discriminates SOME go2 pose only under NEUTRAL framing; the FROZEN visual_acceptance_rubric robot_present_upright item is presence/fault-framed so its parser misses the signal. Landing minicpm as real eyes needs that item reworded neutral = ADR-002 moat change (checksum-pinned, dispatcher-owned). E107 CAVEAT: the signal is anisotropic (misses a left-side topple) → eyes also need MULTI-VIEW/ensemble, not one frame. Owner call owed. <- next#2. (R315/E106, R316/E107)
  - G-282-1 SPINE (eyes-place framing): place-aware verdict-render camera in acceptance/capture.py = honest-verify spine; conditions camera-aim on the place GT oracle → CEO review + real-face verify owed. Reverted R282 (was R281/E78).
  - S4 (one generic driver): a genuinely-new 3rd embodiment needs the ONE generic driver replacing per-driver MuJoCoGo2/G1 (WIRING:53) — CEO-gated (kernel/interface), multi-round SDD. <- next#2.
  - SPINE (D182): world-owned NL→object grounder — fixes witness-only fidelity AND makes grasp-vs-wander deterministic (E46); also fixes ordinal E65. <- next#2.
  - PLANNER (D76 planner-plugin): a runner-side N-object decomposer would close quantity-place (E100) but native_loop is contractually planner-free — the plugin is D76-GATED+deferred. <- next#2, E100/E101.
  - D178 near_object VLN · D176 cmd_motion seam · D168 place-oracle · relational near(a,b) · S5/S6 ladder · BILLING (qwen ROUTING + qwen3-vl JUDGE both Arrearage) · RELEASE: restructure merge to master (owner).
  - SELF-APPROVAL AUDIT: gate/token R290..R316 CLEAN. R316 touched NO spine path (rendered frames; extended tools/acceptance/pose_probe.py with --framing + a unit test; 1 experiments row E107 + LESSONS E106-bound extension + STATUS + BOARD regen; NO code under vector_os_nano/, NO acceptance/ or config/rubric edit).
last_review: R310
