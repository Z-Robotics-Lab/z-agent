# Standing direction (never a task pointer — STATUS.md `next:` owns tasking)
<!-- northstar-anchor-begin (byte-identical to AGENTS.md; machine-checked) -->
An agent-orchestration runtime for physical AI: **plan · route · verify · recover**.
Orchestrate big models + small models + classical skills + atomic actions into one
cross-hardware/model/system whole — always the best tool per job; never re-implement
nav/manip. Plug-and-play: bring a robot (URDF+manifest), policy, skill, capability, or
verify-predicate — no kernel edits.
<!-- northstar-anchor-end -->
BIG GOAL (a FLOOR, not a ceiling): deliver the North Star through the ONLY acceptance
face — bare `vector-cli` + natural language, honest-verified, eyes on the sim. Once a
bar is met, RAISE it toward the ladder (docs/ARCHITECTURE.md + LESSONS `## Frontier`).
Robots are the end; sim is the means.
This file NEVER carries a current task — that rots. STATUS.md `next:` is authoritative.
